[
  {
    "objectID": "L05/Day5_Lab.html",
    "href": "L05/Day5_Lab.html",
    "title": "Final Project: Architecture Integration Capstone",
    "section": "",
    "text": "All five modules integrated: Kafka ingestion → Bronze/Silver/Gold Lakehouse → Quality Gate → RAG serving → Orchestration DAG with lineage.\n\n!pip install pandas chromadb sentence-transformers loguru\n\n\nimport osimport jsonimport uuidimport randomfrom datetime import datetime, timedeltafrom collections import defaultdictimport pandas as pdimport chromadbfrom chromadb.utils.embedding_functions import SentenceTransformerEmbeddingFunctionfrom sentence_transformers import SentenceTransformerfrom loguru import loggerlogger.add(\"capstone_run.log\", rotation=\"10 MB\")BRONZE_PATH = \"./lakehouse/bronze\"SILVER_PATH = \"./lakehouse/silver\"GOLD_PATH   = \"./lakehouse/gold\"\n\n\n# =====================================================================# MODULE 1 — INGESTION (Kafka simulation)\n\n\n# =====================================================================class MockKafka:    \"\"\"    Simulates Kafka's topic/partition/offset model.    In real Kafka:      - Topics are split into partitions (parallel ordered logs).      - Producers assign events to partitions (round-robin or key-hash).      - Consumer groups share partitions — each partition to one member.      - Offsets are committed so consumers can resume after failure.    \"\"\"    def __init__(self):        self.topics  = defaultdict(list)        self.offsets = defaultdict(int)    def produce(self, topic: str, event: dict):        self.topics[topic].append({            \"offset\":     self.offsets[topic],            \"event_time\": datetime.utcnow().isoformat(),            \"payload\":    event,        })        self.offsets[topic] += 1    def consume_all(self, topic: str) -&gt; list[dict]:        return self.topics.pop(topic, [])def module1_ingest(kafka: MockKafka) -&gt; int:    \"\"\"    Produces synthetic e-commerce order events to the orders_raw topic.    Replace this function with a real Kafka producer for your project.    \"\"\"    logger.info(\"[MODULE 1] Ingestion — producing events to Kafka...\")    products  = [\"Laptop\", \"Mouse\", \"Keyboard\", \"Monitor\", \"Headset\", \"Webcam\"]    countries = [\"SA\", \"AE\", \"EG\", \"KW\", \"BH\", \"QA\"]    for i in range(25):        kafka.produce(\"orders_raw\", {            \"order_id\":    f\"ORD-{1000 + i:04d}\",            \"customer_id\": f\"CUST-{random.randint(1, 10):03d}\",            \"product\":     random.choice(products),            \"amount\":      round(random.uniform(50.0, 2000.0), 2),            \"country\":     random.choice(countries),            \"landed_at\":   datetime.utcnow().isoformat(),        })    count = kafka.offsets[\"orders_raw\"]    logger.success(f\"[MODULE 1] {count} events published to orders_raw\")    return count\n\n\n# =====================================================================# MODULE 2 — STORAGE (Bronze → Silver → Gold)\n\n\n# =====================================================================def module2_storage(kafka: MockKafka) -&gt; dict[str, str]:    \"\"\"    Implements the Medallion Architecture (Delta Lakehouse pattern):      Bronze — Raw append: every field, no cleaning, with ingestion timestamp.               In production: Spark Structured Streaming writes directly here               using .format('delta').mode('append').      Silver — Cleansed: nulls dropped, invalid rows filtered, types enforced.               In production: a dbt model with enforced: true transforms Bronze.      Gold   — Aggregated: business-level metrics ready for BI and AI serving.               In production: scheduled Spark batch job or dbt mart model.    \"\"\"    logger.info(\"[MODULE 2] Storage — Bronze → Silver → Gold pipeline...\")    events   = kafka.consume_all(\"orders_raw\")    payloads = [e[\"payload\"] for e in events]    for path in [BRONZE_PATH, SILVER_PATH, GOLD_PATH]:        os.makedirs(path, exist_ok=True)    # Bronze: raw, no transformation    bronze_df   = pd.DataFrame(payloads)    bronze_file = f\"{BRONZE_PATH}/orders_{int(datetime.utcnow().timestamp())}.parquet\"    bronze_df.to_parquet(bronze_file, index=False)    logger.info(f\"[MODULE 2] Bronze: {len(bronze_df)} rows → {bronze_file}\")    # Silver: cleansed    silver_df = bronze_df.dropna()    silver_df = silver_df[silver_df[\"amount\"] &gt; 0].copy()    silver_df = silver_df[silver_df[\"country\"].str.match(r\"^[A-Z]{2}$\", na=False)]    silver_file = f\"{SILVER_PATH}/orders_clean.parquet\"    silver_df.to_parquet(silver_file, index=False)    dropped = len(bronze_df) - len(silver_df)    logger.info(f\"[MODULE 2] Silver: {len(silver_df)} rows ({dropped} dropped in cleansing)\")    # Gold: revenue by country    gold_df = (        silver_df        .groupby(\"country\")        .agg(            total_orders   = (\"order_id\", \"count\"),            total_revenue  = (\"amount\",   \"sum\"),            avg_order_value= (\"amount\",   \"mean\"),        )        .reset_index()        .sort_values(\"total_revenue\", ascending=False)    )    gold_df[\"avg_order_value\"] = gold_df[\"avg_order_value\"].round(2)    gold_file = f\"{GOLD_PATH}/revenue_by_country.parquet\"    gold_df.to_parquet(gold_file, index=False)    logger.success(\"[MODULE 2] Gold layer ready\")    return {\"bronze\": bronze_file, \"silver\": silver_file, \"gold\": gold_file}\n\n\n# =====================================================================# MODULE 3 — QUALITY GATE (DAMA + Great Expectations-style)\n\n\n# =====================================================================def module3_quality(silver_path: str) -&gt; bool:    \"\"\"    Runs DAMA quality checks on the Silver layer before serving.    Returns True if all checks pass (pipeline continues), False otherwise.    In production: replace each check with a Great Expectations expectation:      expect_column_values_to_not_be_null()      expect_column_values_to_be_between()      expect_column_values_to_be_unique()      expect_column_values_to_match_regex()    \"\"\"    logger.info(\"[MODULE 3] Quality Gate — validating Silver layer...\")    df     = pd.read_parquet(silver_path)    passed = True    checks = [        (            \"Completeness\",            df.isnull().sum().sum() == 0,            f\"{df.isnull().sum().sum()} null values found\",        ),        (            \"Accuracy (amount &gt; 0)\",            (df[\"amount\"] &gt; 0).all(),            \"Negative or zero amounts detected\",        ),        (            \"Uniqueness (order_id)\",            df.duplicated(\"order_id\").sum() == 0,            f\"{df.duplicated('order_id').sum()} duplicate order IDs\",        ),        (            \"Validity (country ISO-3166)\",            df[\"country\"].str.match(r\"^[A-Z]{2}$\", na=False).all(),            \"Non-ISO country codes detected\",        ),        (            \"Timeliness (landed within 10 min)\",            (                pd.to_datetime(df[\"landed_at\"])                &gt;= datetime.utcnow() - timedelta(minutes=10)            ).all(),            \"Records landed outside the 10-minute SLA\",        ),    ]    for name, result, fail_detail in checks:        if result:            logger.success(f\"  [PASSED] {name}\")        else:            logger.warning(f\"  [FAILED] {name}: {fail_detail}\")            passed = False    return passed\n\n\n# =====================================================================# MODULE 4 — SERVING (RAG pipeline over Gold data)\n\n\n# =====================================================================def module4_rag_serve(gold_path: str):    \"\"\"    Indexes Gold layer records into ChromaDB and answers analytical queries    using Retrieval-Augmented Generation.    In production:      - Use a persistent Chroma client or Pinecone for the vector index.      - Call an actual LLM API (OpenRouter, OpenAI, Anthropic) for answers.      - Implement hybrid search (BM25 + vector) + cross-encoder reranking.    \"\"\"    logger.info(\"[MODULE 4] Serving — building RAG index over Gold data...\")    df = pd.read_parquet(gold_path)    # Convert each Gold row into a human-readable document    documents = [        (            f\"Country {row.country}: {int(row.total_orders)} orders, \"            f\"total revenue ${row.total_revenue:.2f}, \"            f\"average order value ${row.avg_order_value:.2f}.\"        )        for _, row in df.iterrows()    ]    ef = SentenceTransformerEmbeddingFunction(model_name=\"all-MiniLM-L6-v2\")    client = chromadb.Client()    try:        client.delete_collection(\"capstone_gold\")    except Exception:        pass    collection = client.create_collection(\"capstone_gold\", embedding_function=ef)    collection.add(        ids       = [f\"row_{i}\" for i in range(len(documents))],        documents = documents,    )    queries = [        \"Which country generated the most revenue?\",        \"What is the average order value in Saudi Arabia?\",        \"Which markets have the most orders?\",    ]    embed_model = SentenceTransformer(\"all-MiniLM-L6-v2\")    for query in queries:        results  = collection.query(query_texts=[query], n_results=2)        top_docs = results[\"documents\"][0]        print(f\"\\n  🔍 RAG Query: {query}\")        print(f\"  📄 Retrieved: {top_docs[0]}\")        print(f\"  🤖 [LLM would answer here using the retrieved context]\")    logger.success(\"[MODULE 4] RAG serving complete\")\n\n\n# =====================================================================# MODULE 5 — ORCHESTRATION (Airflow-style DAG)\n\n\n# =====================================================================def module5_orchestrate():    \"\"\"    Executes all five modules in topological (dependency) order.    In production: each function below becomes a PythonOperator in    an Airflow DAG with depends_on_past=False and retries=2.    DAG shape:      ingest → storage → quality_gate → serve (if passed)                                      → quarantine (if failed)    XCom equivalent: paths dict passes dataset locations between tasks.    Lineage events are the audit trail — one event per task transition.    \"\"\"    logger.info(\"[MODULE 5] DAG orchestrator starting...\")    run_id  = str(uuid.uuid4())[:8]    lineage = []    def emit(task: str, event: str, details: dict = None):        lineage.append({            \"runId\":  run_id,            \"task\":   task,            \"event\":  event,            \"ts\":     datetime.utcnow().isoformat(),            **(details or {}),        })    kafka = MockKafka()    # Task 1: Ingest    emit(\"ingest\", \"START\")    count = module1_ingest(kafka)    emit(\"ingest\", \"COMPLETE\", {\"events_produced\": count})    # Task 2: Storage    emit(\"storage\", \"START\")    paths = module2_storage(kafka)    emit(\"storage\", \"COMPLETE\", paths)    # Task 3: Quality gate (branching)    emit(\"quality_gate\", \"START\")    quality_ok = module3_quality(paths[\"silver\"])    if quality_ok:        emit(\"quality_gate\", \"COMPLETE\")        # Task 4: Serve (downstream task — only runs if quality passed)        emit(\"rag_serve\", \"START\")        module4_rag_serve(paths[\"gold\"])        emit(\"rag_serve\", \"COMPLETE\")    else:        emit(\"quality_gate\", \"FAIL\", {\"action\": \"quarantine — serve step skipped\"})        logger.error(\"[MODULE 5] Quality gate failed — downstream serve step skipped.\")    # Write lineage audit trail    os.makedirs(\"lineage_events\", exist_ok=True)    lineage_path = f\"lineage_events/run_{run_id}.json\"    with open(lineage_path, \"w\") as f:        json.dump(lineage, f, indent=2)    logger.info(f\"[MODULE 5] Lineage → {lineage_path}\")    return quality_ok\n\n\n# =====================================================================# ENTRY POINT\n\n\n# =====================================================================def main():    print(\"=\" * 65)    print(\"  Day 5 Capstone — Architecture Integration\")    print(\"=\" * 65)    success = module5_orchestrate()    print(\"\\n\" + \"=\" * 65)    if success:        print(\"✅ All 5 modules ran successfully end-to-end.\")        print(\"\")        print(\"   Output directories:\")        print(f\"   • ./lakehouse/bronze/  — raw ingested records\")        print(f\"   • ./lakehouse/silver/  — cleansed, validated records\")        print(f\"   • ./lakehouse/gold/    — revenue aggregation by country\")        print(f\"   • ./lineage_events/    — pipeline audit trail (JSON)\")    else:        print(\"⚠️  Pipeline completed but quality gate failed.\")        print(\"   Fix the upstream data issues and re-run.\")    print(\"=\" * 65)    print(\"\\nTeam extension checklist:\")    print(\"  □ Replace MockKafka → real kafka-python producer/consumer\")    print(\"  □ Replace pandas parquet writes → PySpark + Delta Lake\")    print(\"  □ Replace manual checks → Great Expectations checkpoints\")    print(\"  □ Replace chromadb.Client() → persistent Chroma or Pinecone\")    print(\"  □ Wrap module5_orchestrate() tasks in an Airflow DAG\")    print(\"  □ Add OpenLineage HTTP emitter pointing at a Marquez server\")    print(\"  □ Record a 10-minute demo covering all five modules\")\n\n\n\n\nmain()\n\n=================================================================\n  Day 5 Capstone — Architecture Integration\n=================================================================\n\n\n2026-07-31 13:07:57.347 | INFO     | __main__:module5_orchestrate:272 - [MODULE 5] DAG orchestrator starting...\n2026-07-31 13:07:57.355 | INFO     | __main__:module1_ingest:57 - [MODULE 1] Ingestion — producing events to Kafka...\n2026-07-31 13:07:57.355 | SUCCESS  | __main__:module1_ingest:73 - [MODULE 1] 25 events published to orders_raw\n2026-07-31 13:07:57.355 | INFO     | __main__:module2_storage:95 - [MODULE 2] Storage — Bronze → Silver → Gold pipeline...\n2026-07-31 13:07:57.373 | INFO     | __main__:module2_storage:107 - [MODULE 2] Bronze: 25 rows → ./lakehouse/bronze/orders_1785481677.parquet\n2026-07-31 13:07:57.376 | INFO     | __main__:module2_storage:116 - [MODULE 2] Silver: 25 rows (0 dropped in cleansing)\n2026-07-31 13:07:57.382 | SUCCESS  | __main__:module2_storage:133 - [MODULE 2] Gold layer ready\n2026-07-31 13:07:57.382 | INFO     | __main__:module3_quality:153 - [MODULE 3] Quality Gate — validating Silver layer...\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Completeness\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Accuracy (amount &gt; 0)\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Uniqueness (order_id)\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Validity (country ISO-3166)\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Timeliness (landed within 10 min)\n2026-07-31 13:07:57.408 | INFO     | __main__:module4_rag_serve:212 - [MODULE 4] Serving — building RAG index over Gold data...\nWarning: You are sending unauthenticated requests to the HF Hub. Please set a HF_TOKEN to enable higher rate limits and faster downloads.\n\n\n\n\n\nLoading weights:   0%|          | 0/103 [00:00&lt;?, ?it/s]\nLoading weights: 100%|██████████| 103/103 [00:00&lt;00:00, 10297.55it/s]\n\n\n\n\n\nLoading weights:   0%|          | 0/103 [00:00&lt;?, ?it/s]\nLoading weights: 100%|██████████| 103/103 [00:00&lt;00:00, 12899.00it/s]\n\n\n\n  🔍 RAG Query: Which country generated the most revenue?\n  📄 Retrieved: Country EG: 2 orders, total revenue $2338.31, average order value $1169.16.\n  🤖 [LLM would answer here using the retrieved context]\n\n  🔍 RAG Query: What is the average order value in Saudi Arabia?\n  📄 Retrieved: Country EG: 2 orders, total revenue $2338.31, average order value $1169.16.\n  🤖 [LLM would answer here using the retrieved context]\n\n  🔍 RAG Query: Which markets have the most orders?\n  📄 Retrieved: Country EG: 2 orders, total revenue $2338.31, average order value $1169.16.\n  🤖 [LLM would answer here using the retrieved context]\n\n\n2026-07-31 13:08:09.466 | SUCCESS  | __main__:module4_rag_serve:251 - [MODULE 4] RAG serving complete\n2026-07-31 13:08:09.467 | INFO     | __main__:module5_orchestrate:316 - [MODULE 5] Lineage → lineage_events/run_220340e2.json\n\n\n\n=================================================================\n✅ All 5 modules ran successfully end-to-end.\n\n   Output directories:\n   • ./lakehouse/bronze/  — raw ingested records\n   • ./lakehouse/silver/  — cleansed, validated records\n   • ./lakehouse/gold/    — revenue aggregation by country\n   • ./lineage_events/    — pipeline audit trail (JSON)\n=================================================================\n\nTeam extension checklist:\n  □ Replace MockKafka → real kafka-python producer/consumer\n  □ Replace pandas parquet writes → PySpark + Delta Lake\n  □ Replace manual checks → Great Expectations checkpoints\n  □ Replace chromadb.Client() → persistent Chroma or Pinecone\n  □ Wrap module5_orchestrate() tasks in an Airflow DAG\n  □ Add OpenLineage HTTP emitter pointing at a Marquez server\n  □ Record a 10-minute demo covering all five modules",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "Final Project: Architecture Integration Capstone"
    ]
  },
  {
    "objectID": "L05/Day5_Lab.html#run",
    "href": "L05/Day5_Lab.html#run",
    "title": "Final Project: Architecture Integration Capstone",
    "section": "",
    "text": "main()\n\n=================================================================\n  Day 5 Capstone — Architecture Integration\n=================================================================\n\n\n2026-07-31 13:07:57.347 | INFO     | __main__:module5_orchestrate:272 - [MODULE 5] DAG orchestrator starting...\n2026-07-31 13:07:57.355 | INFO     | __main__:module1_ingest:57 - [MODULE 1] Ingestion — producing events to Kafka...\n2026-07-31 13:07:57.355 | SUCCESS  | __main__:module1_ingest:73 - [MODULE 1] 25 events published to orders_raw\n2026-07-31 13:07:57.355 | INFO     | __main__:module2_storage:95 - [MODULE 2] Storage — Bronze → Silver → Gold pipeline...\n2026-07-31 13:07:57.373 | INFO     | __main__:module2_storage:107 - [MODULE 2] Bronze: 25 rows → ./lakehouse/bronze/orders_1785481677.parquet\n2026-07-31 13:07:57.376 | INFO     | __main__:module2_storage:116 - [MODULE 2] Silver: 25 rows (0 dropped in cleansing)\n2026-07-31 13:07:57.382 | SUCCESS  | __main__:module2_storage:133 - [MODULE 2] Gold layer ready\n2026-07-31 13:07:57.382 | INFO     | __main__:module3_quality:153 - [MODULE 3] Quality Gate — validating Silver layer...\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Completeness\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Accuracy (amount &gt; 0)\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Uniqueness (order_id)\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Validity (country ISO-3166)\n2026-07-31 13:07:57.408 | SUCCESS  | __main__:module3_quality:190 -   [PASSED] Timeliness (landed within 10 min)\n2026-07-31 13:07:57.408 | INFO     | __main__:module4_rag_serve:212 - [MODULE 4] Serving — building RAG index over Gold data...\nWarning: You are sending unauthenticated requests to the HF Hub. Please set a HF_TOKEN to enable higher rate limits and faster downloads.\n\n\n\n\n\nLoading weights:   0%|          | 0/103 [00:00&lt;?, ?it/s]\nLoading weights: 100%|██████████| 103/103 [00:00&lt;00:00, 10297.55it/s]\n\n\n\n\n\nLoading weights:   0%|          | 0/103 [00:00&lt;?, ?it/s]\nLoading weights: 100%|██████████| 103/103 [00:00&lt;00:00, 12899.00it/s]\n\n\n\n  🔍 RAG Query: Which country generated the most revenue?\n  📄 Retrieved: Country EG: 2 orders, total revenue $2338.31, average order value $1169.16.\n  🤖 [LLM would answer here using the retrieved context]\n\n  🔍 RAG Query: What is the average order value in Saudi Arabia?\n  📄 Retrieved: Country EG: 2 orders, total revenue $2338.31, average order value $1169.16.\n  🤖 [LLM would answer here using the retrieved context]\n\n  🔍 RAG Query: Which markets have the most orders?\n  📄 Retrieved: Country EG: 2 orders, total revenue $2338.31, average order value $1169.16.\n  🤖 [LLM would answer here using the retrieved context]\n\n\n2026-07-31 13:08:09.466 | SUCCESS  | __main__:module4_rag_serve:251 - [MODULE 4] RAG serving complete\n2026-07-31 13:08:09.467 | INFO     | __main__:module5_orchestrate:316 - [MODULE 5] Lineage → lineage_events/run_220340e2.json\n\n\n\n=================================================================\n✅ All 5 modules ran successfully end-to-end.\n\n   Output directories:\n   • ./lakehouse/bronze/  — raw ingested records\n   • ./lakehouse/silver/  — cleansed, validated records\n   • ./lakehouse/gold/    — revenue aggregation by country\n   • ./lineage_events/    — pipeline audit trail (JSON)\n=================================================================\n\nTeam extension checklist:\n  □ Replace MockKafka → real kafka-python producer/consumer\n  □ Replace pandas parquet writes → PySpark + Delta Lake\n  □ Replace manual checks → Great Expectations checkpoints\n  □ Replace chromadb.Client() → persistent Chroma or Pinecone\n  □ Wrap module5_orchestrate() tasks in an Airflow DAG\n  □ Add OpenLineage HTTP emitter pointing at a Marquez server\n  □ Record a 10-minute demo covering all five modules",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "Final Project: Architecture Integration Capstone"
    ]
  },
  {
    "objectID": "L03/Day3_Lab.html",
    "href": "L03/Day3_Lab.html",
    "title": "Lab: Vector Databases and Advanced RAG Engineering",
    "section": "",
    "text": "Full RAG pipeline: document chunking → ChromaDB (HNSW) → BM25 → RRF hybrid search → cross-encoder reranking → RAGAS-style evaluation.\n\n!pip install chromadb sentence-transformers rank-bm25 numpy\n\n\nimport reimport numpy as npimport chromadbfrom chromadb.utils.embedding_functions import SentenceTransformerEmbeddingFunctionfrom sentence_transformers import SentenceTransformer, CrossEncoderfrom rank_bm25 import BM25Okapi\n\n\n# =====================================================================# KNOWLEDGE BASE — Eight data-engineering documents\n\n\n# =====================================================================DOCUMENTS = [    {        \"id\":   \"doc_001\",        \"text\": (            \"Apache Kafka is a distributed event streaming platform. \"            \"It uses partitions and offsets to guarantee message ordering \"            \"within a partition. Consumer groups allow multiple consumers \"            \"to read the same topic in parallel — each partition goes to \"            \"exactly one group member. Adding consumers beyond the partition \"            \"count yields no additional throughput.\"        ),    },    {        \"id\":   \"doc_002\",        \"text\": (            \"Delta Lake is an open-source storage layer that brings ACID \"            \"transactions to Apache Spark. The transaction log (.delta_log) \"            \"records every commit as a JSON file. Readers replay the log from \"            \"version 0 to reconstruct the current table state. VACUUM removes \"            \"old Parquet files once they fall outside the retention window.\"        ),    },    {        \"id\":   \"doc_003\",        \"text\": (            \"Retrieval-Augmented Generation (RAG) combines information retrieval \"            \"with large language model generation. The retriever finds relevant \"            \"chunks from a vector database using semantic similarity. The LLM \"            \"then generates an answer grounded in those retrieved chunks, \"            \"significantly reducing hallucination compared to pure generation.\"        ),    },    {        \"id\":   \"doc_004\",        \"text\": (            \"HNSW (Hierarchical Navigable Small World) is the graph-based \"            \"approximate nearest-neighbour index used inside ChromaDB, Pinecone, \"            \"and Weaviate. The M parameter controls bi-directional links per node. \"            \"ef_construction sets the candidate list size at index build time. \"            \"ef (search) controls recall vs latency at query time — higher ef \"            \"means better recall but slower queries.\"        ),    },    {        \"id\":   \"doc_005\",        \"text\": (            \"Hybrid search combines vector semantic search with BM25 keyword \"            \"search. Reciprocal Rank Fusion (RRF) merges both result lists: \"            \"score = Σ 1/(k + rank_i), where k=60 is the standard constant. \"            \"RRF is parameter-free and consistently outperforms a weighted \"            \"linear combination. Most vector databases expose hybrid search natively.\"        ),    },    {        \"id\":   \"doc_006\",        \"text\": (            \"Cross-encoder reranking is a two-stage retrieval pattern. Stage 1: \"            \"the bi-encoder retrieves the top-50 candidates quickly using \"            \"independent query and document embeddings. Stage 2: the cross-encoder \"            \"scores each (query, document) pair jointly — far more accurate because \"            \"it sees the full interaction. The top-3 to top-5 reranked results \"            \"are passed to the LLM as context.\"        ),    },    {        \"id\":   \"doc_007\",        \"text\": (            \"RAG evaluation requires four metrics. Context Precision: are the \"            \"retrieved chunks actually relevant? Context Recall: were all needed \"            \"chunks retrieved? Answer Faithfulness: does the answer stay within \"            \"the retrieved context (no hallucination)? Answer Relevance: does \"            \"the answer address the user question? RAGAS is the standard \"            \"open-source framework that computes all four automatically.\"        ),    },    {        \"id\":   \"doc_008\",        \"text\": (            \"Data contracts define the schema and SLA between a data producer \"            \"and its consumers. Pydantic v2 enforces contracts in Python using \"            \"strict type checking (ConfigDict(strict=True)). dbt supports \"            \"enforced: true in model YAML to fail the build if a column type \"            \"or name drifts from the declared contract specification.\"        ),    },]\n\n\n# =====================================================================# STAGE 1 — CHUNKING (sentence-level with overlap)\n\n\n# =====================================================================def chunk_documents(docs: list[dict], chunk_size: int = 2) -&gt; list[dict]:    \"\"\"    Splits each document into overlapping sentence chunks.    chunk_size = 2 means each chunk contains 2 consecutive sentences.    Overlap of 1 sentence ensures context is not lost at chunk boundaries.    \"\"\"    all_chunks = []    for doc in docs:        sentences = re.split(r\"(?&lt;=[.!?])\\s+\", doc[\"text\"].strip())        for i in range(0, len(sentences), max(1, chunk_size - 1)):            chunk_text = \" \".join(sentences[i : i + chunk_size])            if not chunk_text.strip():                continue            all_chunks.append({                \"id\":     f\"{doc['id']}_chunk_{i:03d}\",                \"text\":   chunk_text,                \"doc_id\": doc[\"id\"],            })    return all_chunks\n\n\n# =====================================================================# STAGE 2 — VECTOR INDEX (ChromaDB + SentenceTransformer bi-encoder)\n\n\n# =====================================================================def build_vector_index(chunks: list[dict]) -&gt; chromadb.Collection:    \"\"\"    Embeds all chunks with all-MiniLM-L6-v2 and stores them in ChromaDB.    ChromaDB uses HNSW internally — the same index type as Pinecone/Weaviate.    \"\"\"    print(\"\\n📦 Building ChromaDB vector index...\")    ef = SentenceTransformerEmbeddingFunction(model_name=\"all-MiniLM-L6-v2\")    client     = chromadb.Client()    collection = client.get_or_create_collection(        \"rag_lab_knowledge_base\", embedding_function=ef    )    collection.add(        ids       = [c[\"id\"]   for c in chunks],        documents = [c[\"text\"] for c in chunks],        metadatas = [{\"doc_id\": c[\"doc_id\"]} for c in chunks],    )    print(f\"   ✅ {len(chunks)} chunks indexed (HNSW backend, all-MiniLM-L6-v2 embeddings)\")    return collection\n\n\n# =====================================================================# STAGE 3 — BM25 KEYWORD INDEX\n\n\n# =====================================================================class BM25Index:    \"\"\"    Classic TF-IDF keyword search using the BM25 ranking function.    Finds exact keyword matches that semantic search often misses    (e.g., product names, acronyms, version numbers like 'GPT-4').    \"\"\"    def __init__(self, chunks: list[dict]):        tokenised  = [c[\"text\"].lower().split() for c in chunks]        self.bm25  = BM25Okapi(tokenised)        self.chunks = chunks    def search(self, query: str, top_k: int = 10) -&gt; list[tuple[float, dict]]:        scores = self.bm25.get_scores(query.lower().split())        ranked = sorted(enumerate(scores), key=lambda x: x[1], reverse=True)        return [(score, self.chunks[idx]) for idx, score in ranked[:top_k]]\n\n\n# =====================================================================# STAGE 4 — HYBRID SEARCH WITH RECIPROCAL RANK FUSION\n\n\n# =====================================================================def reciprocal_rank_fusion(    vector_hits: list[dict],    bm25_hits:   list[tuple[float, dict]],    k:           int = 60,    top_k:       int = 6,) -&gt; list[dict]:    \"\"\"    RRF score = Σ 1/(k + rank_i) across both result lists.    k=60 is the empirically optimal constant — it prevents the top-rank    position from dominating and rewards documents that rank consistently    across both systems. No weights to tune: RRF is parameter-free.    \"\"\"    rrf_scores: dict[str, float] = {}    id_to_chunk: dict[str, dict] = {}    for rank, hit in enumerate(vector_hits):        cid = hit[\"id\"]        rrf_scores[cid] = rrf_scores.get(cid, 0.0) + 1.0 / (k + rank + 1)        id_to_chunk[cid] = {\"id\": cid, \"text\": hit[\"document\"]}    for rank, (_, chunk) in enumerate(bm25_hits):        cid = chunk[\"id\"]        rrf_scores[cid] = rrf_scores.get(cid, 0.0) + 1.0 / (k + rank + 1)        id_to_chunk[cid] = chunk    sorted_ids = sorted(rrf_scores, key=lambda x: rrf_scores[x], reverse=True)    return [id_to_chunk[cid] for cid in sorted_ids[:top_k]]\n\n\n# =====================================================================# STAGE 5 — CROSS-ENCODER RERANKING (Stage 2 precision gate)\n\n\n# =====================================================================def rerank(    query:       str,    candidates:  list[dict],    model_name:  str = \"cross-encoder/ms-marco-MiniLM-L-6-v2\",    top_k:       int = 3,) -&gt; list[dict]:    \"\"\"    Cross-encoder jointly encodes the (query, document) pair.    This is much more accurate than cosine similarity between    independent embeddings, because it can model the interaction    between query tokens and document tokens directly.    Cost: O(n) inference calls vs O(1) for bi-encoder similarity.    Strategy: retrieve top-50 fast with bi-encoder, rerank top-50    with cross-encoder, return top-5 to the LLM.    \"\"\"    print(f\"  🎯 Cross-encoder reranking {len(candidates)} candidates...\")    model  = CrossEncoder(model_name)    pairs  = [(query, c[\"text\"]) for c in candidates]    scores = model.predict(pairs)    ranked = sorted(zip(scores, candidates), key=lambda x: x[0], reverse=True)    return [doc for _, doc in ranked[:top_k]]\n\n\n# =====================================================================# STAGE 6 — RAG PROMPT CONSTRUCTION\n\n\n# =====================================================================def build_rag_prompt(query: str, context_docs: list[dict]) -&gt; str:    \"\"\"    Constructs the prompt sent to the LLM.    In production: replace the print statement with an actual API call    (OpenAI, Anthropic, or any OpenRouter-compatible model).    \"\"\"    context = \"\\n\\n\".join(        f\"[Source {i+1}]: {d['text']}\" for i, d in enumerate(context_docs)    )    return (        \"You are a data engineering expert. Answer the question strictly based\\n\"        \"on the provided context. Do not add information not in the context.\\n\\n\"        f\"CONTEXT:\\n{context}\\n\\n\"        f\"QUESTION: {query}\\n\\n\"        \"ANSWER:\"    )\n\n\n# =====================================================================# STAGE 7 — EVALUATION (simplified RAGAS-style metrics)\n\n\n# =====================================================================def evaluate(    query:          str,    retrieved_docs: list[dict],    embed_model:    SentenceTransformer,    relevance_threshold: float = 0.30,) -&gt; dict:    \"\"\"    Computes two simplified metrics using cosine similarity:    Context Precision = fraction of retrieved chunks above the                        relevance threshold (no noise in context).    Avg Similarity    = mean cosine score between query and chunks                        (proxy for Context Recall).    In production: use RAGAS which calls an LLM judge for all four metrics.    Alert if faithfulness &lt; 0.85 — it means the LLM is hallucinating.    \"\"\"    q_emb   = embed_model.encode(query, normalize_embeddings=True)    scores  = [        float(np.dot(q_emb, embed_model.encode(d[\"text\"], normalize_embeddings=True)))        for d in retrieved_docs    ]    relevant = sum(s &gt; relevance_threshold for s in scores)    return {        \"context_precision\": round(relevant / len(scores), 3),        \"avg_similarity\":    round(sum(scores) / len(scores), 3),        \"chunks_in_context\": len(retrieved_docs),    }\n\n\n# =====================================================================# MAIN — Full pipeline run\n\n\n# =====================================================================def main():    print(\"=\" * 65)    print(\"  Day 3 Lab — Vector Databases and Advanced RAG Engineering\")    print(\"=\" * 65)    # Stage 1: Chunk    chunks = chunk_documents(DOCUMENTS, chunk_size=2)    print(f\"\\n📄 {len(DOCUMENTS)} documents → {len(chunks)} chunks after splitting\")    # Stage 2: Build indexes    collection  = build_vector_index(chunks)    bm25_index  = BM25Index(chunks)    embed_model = SentenceTransformer(\"all-MiniLM-L6-v2\")    queries = [        \"How does Kafka guarantee message ordering across partitions?\",        \"What is Reciprocal Rank Fusion and why is it better than weighted merging?\",        \"How does cross-encoder reranking improve over bi-encoder retrieval?\",        \"What metrics does RAGAS use to evaluate a RAG pipeline?\",    ]    for query in queries:        print(f\"\\n{'=' * 65}\")        print(f\"QUERY: {query}\")        print(\"=\" * 65)        # Vector (semantic) search        vec_results = collection.query(query_texts=[query], n_results=6)        vec_hits = [            {\"id\": vid, \"document\": vdoc}            for vid, vdoc in zip(                vec_results[\"ids\"][0],                vec_results[\"documents\"][0],            )        ]        print(f\"\\n  🔍 Vector search:   {len(vec_hits)} candidates\")        # BM25 keyword search        bm25_hits = bm25_index.search(query, top_k=6)        print(f\"  🔑 BM25 search:     {len(bm25_hits)} candidates\")        # Hybrid RRF fusion        hybrid = reciprocal_rank_fusion(vec_hits, bm25_hits, top_k=6)        print(f\"  ⚡ RRF fusion:      {len(hybrid)} merged candidates\")        # Cross-encoder rerank        final_docs = rerank(query, hybrid, top_k=3)        # Build prompt        prompt = build_rag_prompt(query, final_docs)        print(\"\\n  📝 Top-3 chunks after reranking:\")        for i, doc in enumerate(final_docs, 1):            print(f\"    [{i}] {doc['text'][:110]}...\")        print(\"\\n  📋 Prompt sent to LLM (first 300 chars):\")        print(f\"    {prompt[:300]}...\")        print(\"\\n  [In production: call your LLM API here with the full prompt]\")        # Evaluate        metrics = evaluate(query, final_docs, embed_model)        print(f\"\\n  📊 Retrieval metrics: {metrics}\")        if metrics[\"context_precision\"] &lt; 0.5:            print(\"  ⚠️  Low precision — consider increasing ef or lowering chunk overlap\")        else:            print(\"  ✅  Retrieval quality looks good\")    print(\"\\n\" + \"=\" * 65)    print(\"Lab complete. Key takeaways:\")    print(\"  1. Vector search finds semantic matches — misses exact terms\")    print(\"  2. BM25 finds exact keyword matches — misses paraphrases\")    print(\"  3. RRF fuses both result lists without needing to tune weights\")    print(\"  4. Cross-encoder adds a high-precision second stage (at O(n) cost)\")    print(\"  5. Always measure: context precision, recall, faithfulness, relevance\")    print(\"=\" * 65)\n\n\n\n\nmain()\n\n=================================================================\n  Day 3 Lab - Vector Databases and Advanced RAG Engineering\n=================================================================\n\n8 documents -&gt; 32 chunks after splitting\n\nBuilding ChromaDB vector index...\n   32 chunks indexed (HNSW backend, all-MiniLM-L6-v2 embeddings)\n\n=================================================================\nQUERY: How does Kafka guarantee message ordering across partitions?\n=================================================================\n\n  Vector search:   6 candidates\n  BM25 search:     6 candidates\n  RRF fusion:      6 merged candidates\n  Cross-encoder reranking 6 candidates...\n\n  Top-3 chunks after reranking:\n    [1] Apache Kafka is a distributed event streaming platform. It uses partitions and offsets to guarantee message or...\n    [2] It uses partitions and offsets to guarantee message ordering within a partition. Consumer groups allow multipl...\n    [3] Consumer groups allow multiple consumers to read the same topic in parallel - each partition goes to exactly o...\n\n  Prompt sent to LLM (first 300 chars):\n    You are a data engineering expert. Answer the question strictly based\non the provided context. Do not add information not in the context.\n\nCONTEXT:\n[Source 1]: Apache Kafka is a distributed event streaming platform. It uses partitions and offsets to guarantee message ordering within a partition.\n\n[S...\n\n  [In production: call your LLM API here with the full prompt]\n\n  Retrieval metrics: {'context_precision': 1.0, 'avg_similarity': 0.557, 'chunks_in_context': 3}\n  Retrieval quality looks good\n\n=================================================================\nQUERY: What is Reciprocal Rank Fusion and why is it better than weighted merging?\n=================================================================\n\n  Vector search:   6 candidates\n  BM25 search:     6 candidates\n  RRF fusion:      6 merged candidates\n  Cross-encoder reranking 6 candidates...\n\n  Top-3 chunks after reranking:\n    [1] Reciprocal Rank Fusion (RRF) merges both result lists: score = sum 1/(k + rank_i), where k=60 is the standard ...\n    [2] Hybrid search combines vector semantic search with BM25 keyword search. Reciprocal Rank Fusion (RRF) merges bo...\n    [3] RRF is parameter-free and consistently outperforms a weighted linear combination. Most vector databases expose...\n\n  Prompt sent to LLM (first 300 chars):\n    You are a data engineering expert. Answer the question strictly based\non the provided context. Do not add information not in the context.\n\nCONTEXT:\n[Source 1]: Reciprocal Rank Fusion (RRF) merges both result lists: score = sum 1/(k + rank_i), where k=60 is the standard constant. RRF is parameter-fre...\n\n  [In production: call your LLM API here with the full prompt]\n\n  Retrieval metrics: {'context_precision': 0.667, 'avg_similarity': 0.474, 'chunks_in_context': 3}\n  Retrieval quality looks good\n\n=================================================================\nQUERY: How does cross-encoder reranking improve over bi-encoder retrieval?\n=================================================================\n\n  Vector search:   6 candidates\n  BM25 search:     6 candidates\n  RRF fusion:      6 merged candidates\n  Cross-encoder reranking 6 candidates...\n\n  Top-3 chunks after reranking:\n    [1] Cross-encoder reranking is a two-stage retrieval pattern. Stage 1: the bi-encoder retrieves the top-50 candida...\n    [2] Stage 2: the cross-encoder scores each (query, document) pair jointly - far more accurate because it sees the ...\n    [3] Stage 1: the bi-encoder retrieves the top-50 candidates quickly using independent query and document embedding...\n\n  Prompt sent to LLM (first 300 chars):\n    You are a data engineering expert. Answer the question strictly based\non the provided context. Do not add information not in the context.\n\nCONTEXT:\n[Source 1]: Cross-encoder reranking is a two-stage retrieval pattern. Stage 1: the bi-encoder retrieves the top-50 candidates quickly using independent ...\n\n  [In production: call your LLM API here with the full prompt]\n\n  Retrieval metrics: {'context_precision': 1.0, 'avg_similarity': 0.745, 'chunks_in_context': 3}\n  Retrieval quality looks good\n\n=================================================================\nQUERY: What metrics does RAGAS use to evaluate a RAG pipeline?\n=================================================================\n\n  Vector search:   6 candidates\n  BM25 search:     6 candidates\n  RRF fusion:      6 merged candidates\n  Cross-encoder reranking 6 candidates...\n\n  Top-3 chunks after reranking:\n    [1] RAG evaluation requires four metrics. Context Precision: are the retrieved chunks actually relevant?...\n    [2] RAGAS is the standard open-source framework that computes all four automatically....\n    [3] Answer Relevance: does the answer address the user question? RAGAS is the standard open-source framework that ...\n\n  Prompt sent to LLM (first 300 chars):\n    You are a data engineering expert. Answer the question strictly based\non the provided context. Do not add information not in the context.\n\nCONTEXT:\n[Source 1]: RAG evaluation requires four metrics. Context Precision: are the retrieved chunks actually relevant?\n\n[Source 2]: RAGAS is the standard open...\n\n  [In production: call your LLM API here with the full prompt]\n\n  Retrieval metrics: {'context_precision': 1.0, 'avg_similarity': 0.535, 'chunks_in_context': 3}\n  Retrieval quality looks good\n\n=================================================================\nLab complete. Key takeaways:\n  1. Vector search finds semantic matches - misses exact terms\n  2. BM25 finds exact keyword matches - misses paraphrases\n  3. RRF fuses both result lists without needing to tune weights\n  4. Cross-encoder adds a high-precision second stage (at O(n) cost)\n  5. Always measure: context precision, recall, faithfulness, relevance\n=================================================================",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Lab: Vector Databases and Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/Day3_Lab.html#run",
    "href": "L03/Day3_Lab.html#run",
    "title": "Lab: Vector Databases and Advanced RAG Engineering",
    "section": "",
    "text": "main()\n\n=================================================================\n  Day 3 Lab - Vector Databases and Advanced RAG Engineering\n=================================================================\n\n8 documents -&gt; 32 chunks after splitting\n\nBuilding ChromaDB vector index...\n   32 chunks indexed (HNSW backend, all-MiniLM-L6-v2 embeddings)\n\n=================================================================\nQUERY: How does Kafka guarantee message ordering across partitions?\n=================================================================\n\n  Vector search:   6 candidates\n  BM25 search:     6 candidates\n  RRF fusion:      6 merged candidates\n  Cross-encoder reranking 6 candidates...\n\n  Top-3 chunks after reranking:\n    [1] Apache Kafka is a distributed event streaming platform. It uses partitions and offsets to guarantee message or...\n    [2] It uses partitions and offsets to guarantee message ordering within a partition. Consumer groups allow multipl...\n    [3] Consumer groups allow multiple consumers to read the same topic in parallel - each partition goes to exactly o...\n\n  Prompt sent to LLM (first 300 chars):\n    You are a data engineering expert. Answer the question strictly based\non the provided context. Do not add information not in the context.\n\nCONTEXT:\n[Source 1]: Apache Kafka is a distributed event streaming platform. It uses partitions and offsets to guarantee message ordering within a partition.\n\n[S...\n\n  [In production: call your LLM API here with the full prompt]\n\n  Retrieval metrics: {'context_precision': 1.0, 'avg_similarity': 0.557, 'chunks_in_context': 3}\n  Retrieval quality looks good\n\n=================================================================\nQUERY: What is Reciprocal Rank Fusion and why is it better than weighted merging?\n=================================================================\n\n  Vector search:   6 candidates\n  BM25 search:     6 candidates\n  RRF fusion:      6 merged candidates\n  Cross-encoder reranking 6 candidates...\n\n  Top-3 chunks after reranking:\n    [1] Reciprocal Rank Fusion (RRF) merges both result lists: score = sum 1/(k + rank_i), where k=60 is the standard ...\n    [2] Hybrid search combines vector semantic search with BM25 keyword search. Reciprocal Rank Fusion (RRF) merges bo...\n    [3] RRF is parameter-free and consistently outperforms a weighted linear combination. Most vector databases expose...\n\n  Prompt sent to LLM (first 300 chars):\n    You are a data engineering expert. Answer the question strictly based\non the provided context. Do not add information not in the context.\n\nCONTEXT:\n[Source 1]: Reciprocal Rank Fusion (RRF) merges both result lists: score = sum 1/(k + rank_i), where k=60 is the standard constant. RRF is parameter-fre...\n\n  [In production: call your LLM API here with the full prompt]\n\n  Retrieval metrics: {'context_precision': 0.667, 'avg_similarity': 0.474, 'chunks_in_context': 3}\n  Retrieval quality looks good\n\n=================================================================\nQUERY: How does cross-encoder reranking improve over bi-encoder retrieval?\n=================================================================\n\n  Vector search:   6 candidates\n  BM25 search:     6 candidates\n  RRF fusion:      6 merged candidates\n  Cross-encoder reranking 6 candidates...\n\n  Top-3 chunks after reranking:\n    [1] Cross-encoder reranking is a two-stage retrieval pattern. Stage 1: the bi-encoder retrieves the top-50 candida...\n    [2] Stage 2: the cross-encoder scores each (query, document) pair jointly - far more accurate because it sees the ...\n    [3] Stage 1: the bi-encoder retrieves the top-50 candidates quickly using independent query and document embedding...\n\n  Prompt sent to LLM (first 300 chars):\n    You are a data engineering expert. Answer the question strictly based\non the provided context. Do not add information not in the context.\n\nCONTEXT:\n[Source 1]: Cross-encoder reranking is a two-stage retrieval pattern. Stage 1: the bi-encoder retrieves the top-50 candidates quickly using independent ...\n\n  [In production: call your LLM API here with the full prompt]\n\n  Retrieval metrics: {'context_precision': 1.0, 'avg_similarity': 0.745, 'chunks_in_context': 3}\n  Retrieval quality looks good\n\n=================================================================\nQUERY: What metrics does RAGAS use to evaluate a RAG pipeline?\n=================================================================\n\n  Vector search:   6 candidates\n  BM25 search:     6 candidates\n  RRF fusion:      6 merged candidates\n  Cross-encoder reranking 6 candidates...\n\n  Top-3 chunks after reranking:\n    [1] RAG evaluation requires four metrics. Context Precision: are the retrieved chunks actually relevant?...\n    [2] RAGAS is the standard open-source framework that computes all four automatically....\n    [3] Answer Relevance: does the answer address the user question? RAGAS is the standard open-source framework that ...\n\n  Prompt sent to LLM (first 300 chars):\n    You are a data engineering expert. Answer the question strictly based\non the provided context. Do not add information not in the context.\n\nCONTEXT:\n[Source 1]: RAG evaluation requires four metrics. Context Precision: are the retrieved chunks actually relevant?\n\n[Source 2]: RAGAS is the standard open...\n\n  [In production: call your LLM API here with the full prompt]\n\n  Retrieval metrics: {'context_precision': 1.0, 'avg_similarity': 0.535, 'chunks_in_context': 3}\n  Retrieval quality looks good\n\n=================================================================\nLab complete. Key takeaways:\n  1. Vector search finds semantic matches - misses exact terms\n  2. BM25 finds exact keyword matches - misses paraphrases\n  3. RRF fuses both result lists without needing to tune weights\n  4. Cross-encoder adds a high-precision second stage (at O(n) cost)\n  5. Always measure: context precision, recall, faithfulness, relevance\n=================================================================",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Lab: Vector Databases and Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L02/Day2_Lab.html",
    "href": "L02/Day2_Lab.html",
    "title": "Lab: Real-Time Streaming Pipeline with Kafka & Delta Lake",
    "section": "",
    "text": "This single notebook covers everything in today’s lab:\n\nMock Kafka streaming pipeline — producers, a stateful window processor (tumbling/sliding/watermark), and a Delta Lake ACID sink.\nReal Kafka round trip — one real kafka-python producer/consumer exchange with a Pydantic schema gate, run against a real local broker.\n\n\n!pip install pyspark==3.5.0 delta-spark==3.2.0\n\nRequirement already satisfied: pyspark==4.0.1 in /usr/local/lib/python3.12/dist-packages (4.0.1)\nRequirement already satisfied: delta-spark==4.3.0 in /usr/local/lib/python3.12/dist-packages (4.3.0)\nRequirement already satisfied: py4j==0.10.9.9 in /usr/local/lib/python3.12/dist-packages (from pyspark==4.0.1) (0.10.9.9)\nRequirement already satisfied: importlib_metadata&lt;=8.7.1,&gt;=1.0.0 in /usr/local/lib/python3.12/dist-packages (from delta-spark==4.3.0) (8.7.1)\nRequirement already satisfied: zipp&gt;=3.20 in /usr/local/lib/python3.12/dist-packages (from importlib_metadata&lt;=8.7.1,&gt;=1.0.0-&gt;delta-spark==4.3.0) (4.1.0)\n\n\n\n\n\nimport asyncio\nimport os\nimport random\nimport shutil\nfrom datetime import datetime, timedelta, timezone\nfrom collections import defaultdict\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.types import (\n    StructType, StructField, StringType, DoubleType, IntegerType\n)\nfrom delta import configure_spark_with_delta_pip\n\n\n# =====================================================================\n# 1. BACKBONE — Mock Kafka Broker (partitions + offsets)\n\n\n# =====================================================================\n\nclass MockKafkaBroker:\n    \"\"\"\n    Simulates Apache Kafka's core model.\n\n    In real Kafka:\n      - A topic is split into N partitions (ordered, immutable logs).\n      - Each message gets an offset — its address within the partition.\n      - A consumer group shares partitions; each partition goes to\n        exactly one member, so adding consumers beyond the partition\n        count yields no extra throughput.\n      - Committed offsets let consumers resume after a crash.\n    \"\"\"\n\n    def __init__(self):\n        self.topics  = defaultdict(asyncio.Queue)\n        self.offsets = defaultdict(int)\n\n    async def publish(self, topic: str, message: dict):\n        offset = self.offsets[topic]\n        self.offsets[topic] += 1\n        await self.topics[topic].put({\n            \"offset\":     offset,\n            \"event_time\": datetime.now(timezone.utc).isoformat(),\n            \"topic\":      topic,\n            \"payload\":    message,\n        })\n\n    async def consume(self, topic: str) -&gt; dict:\n        return await self.topics[topic].get()\n\n\ndef init_delta_spark() -&gt; SparkSession:\n    builder = (\n        SparkSession.builder\n        .appName(\"Day2_RealTimeStreamingPipeline\")\n        .master(\"local[*]\")\n        .config(\"spark.sql.extensions\",\n                \"io.delta.sql.DeltaSparkSessionExtension\")\n        .config(\"spark.sql.catalog.spark_catalog\",\n                \"org.apache.spark.sql.delta.catalog.DeltaCatalog\")\n        .config(\"spark.sql.warehouse.dir\", \"./data/warehouse\")\n    )\n    spark = configure_spark_with_delta_pip(builder).getOrCreate()\n    spark.sparkContext.setLogLevel(\"ERROR\")\n    return spark\n\n\n# =====================================================================\n# 2. INGESTION LAYER — Three concurrent IoT producers\n\n\n# =====================================================================\n\nasync def machine_producer(broker: MockKafkaBroker, machine_id: str):\n    \"\"\"\n    Simulates a factory sensor streaming temperature readings every 300ms.\n    12% chance of a spike anomaly to trigger downstream alerts.\n    \"\"\"\n    print(f\"  🏭 [PRODUCER] {machine_id} stream online.\")\n    while True:\n        is_spike = random.random() &lt; 0.12\n        temp = random.uniform(92.0, 118.0) if is_spike else random.uniform(58.0, 78.0)\n        await broker.publish(\"factory_sensors_raw\", {\n            \"machine_id\": machine_id,\n            \"temperature\": round(temp, 2),\n            \"event_ts\":   datetime.now(timezone.utc).isoformat(),\n        })\n        await asyncio.sleep(0.3)\n\n\n# =====================================================================\n# 3. COMPUTE LAYER — Stream processor with window types + watermark\n\n\n# =====================================================================\n\nclass StreamProcessor:\n    \"\"\"\n    Stateful stream processing engine — mirrors Flink / Spark Structured Streaming.\n\n    Three window types demonstrated:\n      TUMBLING   — fixed, non-overlapping 1-second buckets.\n                   Every event belongs to exactly one bucket.\n                   Use case: per-second event count, hourly totals.\n\n      SLIDING    — overlapping rolling window over the last 3 seconds.\n                   The same event can appear in multiple windows.\n                   Use case: rolling average temperature or CPU.\n\n      WATERMARK  — events older than WATERMARK_S seconds are dropped.\n                   This bounds state memory and lets the engine close\n                   windows cleanly. Larger watermark = more completeness\n                   but higher latency.\n    \"\"\"\n\n    WATERMARK_S      = 2\n    SLIDING_WINDOW_S = 3\n\n    def __init__(self, broker: MockKafkaBroker, threshold: float):\n        self.broker    = broker\n        self.threshold = threshold\n        self.sliding   = defaultdict(list)\n        self.tumbling  = defaultdict(list)\n        self.watermark = datetime.now(timezone.utc)\n\n    async def run(self):\n        print(f\"  🧠 [PROCESSOR] Window engine live. Threshold: {self.threshold}°C\")\n        while True:\n            event   = await self.broker.consume(\"factory_sensors_raw\")\n            payload = event[\"payload\"]\n            m_id    = payload[\"machine_id\"]\n            val     = payload[\"temperature\"]\n            now     = datetime.now(timezone.utc)\n\n            # Watermark — drop late events\n            if now &lt; self.watermark - timedelta(seconds=self.WATERMARK_S):\n                print(f\"  ⚠️  [WATERMARK] Late event from {m_id} dropped.\")\n                continue\n            self.watermark = max(self.watermark, now)\n\n            # Sliding window — rolling average\n            self.sliding[m_id].append((now, val))\n            cutoff = now - timedelta(seconds=self.SLIDING_WINDOW_S)\n            self.sliding[m_id] = [\n                (t, v) for t, v in self.sliding[m_id] if t &gt; cutoff\n            ]\n            rolling_avg = (\n                sum(v for _, v in self.sliding[m_id]) / len(self.sliding[m_id])\n            )\n\n            # Tumbling window — count events in this 1-second bucket\n            bucket = now.strftime(\"%H:%M:%S\")\n            self.tumbling[bucket].append(val)\n            bucket_count = len(self.tumbling[bucket])\n\n            # Threshold check\n            if val &gt; self.threshold:\n                await self.broker.publish(\"anomalies_sink\", {\n                    \"machine_id\":   m_id,\n                    \"value\":        val,\n                    \"rolling_avg\":  round(rolling_avg, 2),\n                    \"bucket\":       bucket,\n                    \"bucket_count\": bucket_count,\n                    \"status\":       \"CRITICAL\",\n                })\n\n\n# =====================================================================\n# 4. STORAGE SINK — ACID append to Delta Lake\n\n\n# =====================================================================\n\nasync def delta_sink(\n    broker: MockKafkaBroker,\n    spark: SparkSession,\n    delta_path: str,\n):\n    \"\"\"\n    Reads anomaly alerts and commits them to Delta Lake.\n    Delta's transaction log guarantees ACID — no partial writes,\n    no corruption even if two writers race on the same files.\n    \"\"\"\n    schema = StructType([\n        StructField(\"event_time\",   StringType(),  True),\n        StructField(\"machine_id\",   StringType(),  True),\n        StructField(\"value\",        DoubleType(),  True),\n        StructField(\"rolling_avg\",  DoubleType(),  True),\n        StructField(\"bucket\",       StringType(),  True),\n        StructField(\"bucket_count\", IntegerType(), True),\n        StructField(\"status\",       StringType(),  True),\n    ])\n    print(\"  💾 [SINK] Delta Lake writer active.\")\n    while True:\n        alert = await broker.consume(\"anomalies_sink\")\n        p = alert[\"payload\"]\n        row = [(\n            alert[\"event_time\"],\n            p[\"machine_id\"],\n            p[\"value\"],\n            p[\"rolling_avg\"],\n            p[\"bucket\"],\n            p[\"bucket_count\"],\n            p[\"status\"],\n        )]\n        spark.createDataFrame(row, schema).write \\\n             .format(\"delta\").mode(\"append\").save(delta_path)\n        print(f\"  🚨 [DELTA WRITE] {p['machine_id']} — \"\n              f\"{p['value']}°C | rolling avg {p['rolling_avg']}°C | \"\n              f\"bucket {p['bucket']} ({p['bucket_count']} events)\")\n\n\n# =====================================================================\n# 5. PIPELINE ORCHESTRATION\n\n\n# =====================================================================\n\nasync def main():\n    delta_path = \"./data/delta/anomalies\"\n\n    if os.path.exists(\"./data\"):\n        shutil.rmtree(\"./data\")\n\n    broker = MockKafkaBroker()\n    spark  = init_delta_spark()\n\n    print(\"\\n🚀 Real-Time Streaming Pipeline booting...\\n\")\n\n    processor = asyncio.create_task(\n        StreamProcessor(broker, threshold=85.0).run()\n    )\n    sink = asyncio.create_task(\n        delta_sink(broker, spark, delta_path)\n    )\n    producers = [\n        asyncio.create_task(machine_producer(broker, \"CNC_MILL_01\")),\n        asyncio.create_task(machine_producer(broker, \"ROBOTIC_ARM_02\")),\n        asyncio.create_task(machine_producer(broker, \"ASSEMBLY_LINE_03\")),\n    ]\n\n    print(\"  Streaming for 8 seconds...\\n\")\n    await asyncio.sleep(8.0)\n\n    for task in [processor, sink, *producers]:\n        task.cancel()\n\n    print(\"\\n📊 Final Delta Lake state (all committed anomalies):\")\n    if os.path.exists(delta_path):\n        spark.read.format(\"delta\").load(delta_path) \\\n             .orderBy(\"event_time\").show(truncate=False)\n    else:\n        print(\"  No anomalies recorded in this run — lower the threshold to see more.\")\n\n    spark.stop()\n    print(\"\\n🏁 Pipeline shut down cleanly.\")\n\n\n\n\n\nawait main()\n\n\n🚀 Real-Time Streaming Pipeline booting...\n\n  Streaming for 8 seconds...\n\n  🧠 [PROCESSOR] Window engine live. Threshold: 85.0°C\n  💾 [SINK] Delta Lake writer active.\n  🏭 [PRODUCER] CNC_MILL_01 stream online.\n  🏭 [PRODUCER] ROBOTIC_ARM_02 stream online.\n  🏭 [PRODUCER] ASSEMBLY_LINE_03 stream online.\n  🚨 [DELTA WRITE] CNC_MILL_01 — 113.9°C | rolling avg 113.9°C | bucket 21:32:12 (1 events)\n\n📊 Final Delta Lake state (all committed anomalies):\n+--------------------------------+-----------+-----+-----------+--------+------------+--------+\n|event_time                      |machine_id |value|rolling_avg|bucket  |bucket_count|status  |\n+--------------------------------+-----------+-----+-----------+--------+------------+--------+\n|2026-06-27T21:32:12.761585+00:00|CNC_MILL_01|113.9|113.9      |21:32:12|1           |CRITICAL|\n+--------------------------------+-----------+-----+-----------+--------+------------+--------+\n\n\n🏁 Pipeline shut down cleanly.\n\n\n\n\n\nPart 1’s MockKafkaBroker is kept because it makes windowing logic easy to read and step through, but it never touches the actual Kafka client library. This part does one real KafkaProducer -&gt; topic -&gt; KafkaConsumer round trip with a Pydantic schema gate that rejects a deliberately malformed message instead of crashing.\nNeeds a running Kafka broker. In Colab, run this in a cell first:\n!pip install kafka-python pydantic\n# Run a local single-broker Kafka in the background (Colab has Java preinstalled):\n!curl -sSOL https://downloads.apache.org/kafka/3.7.0/kafka_2.13-3.7.0.tgz && tar -xzf kafka_2.13-3.7.0.tgz\n!cd kafka_2.13-3.7.0 && bin/kafka-storage.sh format -t $(bin/kafka-storage.sh random-uuid) -c config/kraft/server.properties\n!cd kafka_2.13-3.7.0 && nohup bin/kafka-server-start.sh config/kraft/server.properties &gt; /tmp/kafka.log 2&gt;&1 &\nGive it ~15-20 seconds to finish starting before running the cells below.\n\n!pip install kafka-python pydantic\n\nRequirement already satisfied: kafka-python in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (3.0.9)\nRequirement already satisfied: pydantic in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (2.13.4)\nRequirement already satisfied: annotated-types&gt;=0.6.0 in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (from pydantic) (0.8.0)\nRequirement already satisfied: pydantic-core==2.46.4 in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (from pydantic) (2.46.4)\nRequirement already satisfied: typing-extensions&gt;=4.14.1 in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (from pydantic) (4.16.0)\nRequirement already satisfied: typing-inspection&gt;=0.4.2 in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (from pydantic) (0.4.2)\n\n[notice] A new release of pip is available: 25.0.1 -&gt; 26.2\n[notice] To update, run: python.exe -m pip install --upgrade pip\n\n\n\nimport json\nimport time\n\nfrom pydantic import BaseModel, ValidationError, field_validator\n\nTOPIC = \"factory_sensors_raw\"\nBOOTSTRAP_SERVERS = \"localhost:9092\"\n\n\nclass SensorReading(BaseModel):\n    \"\"\"Same shape the mock pipeline's producers emit — reused here as the schema gate.\"\"\"\n\n    machine_id: str\n    temperature: float\n    event_ts: str\n\n    @field_validator(\"temperature\")\n    @classmethod\n    def plausible_range(cls, v: float) -&gt; float:\n        if not (-40.0 &lt;= v &lt;= 200.0):\n            raise ValueError(f\"temperature {v} outside plausible sensor range\")\n        return v\n\n\ndef produce_sample_messages():\n    from kafka import KafkaProducer\n\n    producer = KafkaProducer(\n        bootstrap_servers=BOOTSTRAP_SERVERS,\n        value_serializer=lambda v: json.dumps(v).encode(\"utf-8\"),\n    )\n\n    good = [\n        {\"machine_id\": \"CNC_MILL_01\", \"temperature\": 71.4, \"event_ts\": \"2026-06-29T10:00:00Z\"},\n        {\"machine_id\": \"ROBOTIC_ARM_02\", \"temperature\": 68.9, \"event_ts\": \"2026-06-29T10:00:01Z\"},\n    ]\n    # A malformed message on purpose, to prove the consumer's schema gate catches it\n    # instead of crashing the pipeline.\n    bad = {\"machine_id\": \"ASSEMBLY_LINE_03\", \"temperature\": \"not_a_number\", \"event_ts\": \"2026-06-29T10:00:02Z\"}\n\n    for msg in good + [bad]:\n        producer.send(TOPIC, msg)\n        print(f\"  [PRODUCER] sent -&gt; {msg}\")\n    producer.flush()\n    producer.close()\n\n\ndef consume_and_validate(max_messages: int = 3, timeout_s: int = 10):\n    from kafka import KafkaConsumer\n\n    consumer = KafkaConsumer(\n        TOPIC,\n        bootstrap_servers=BOOTSTRAP_SERVERS,\n        auto_offset_reset=\"earliest\",\n        enable_auto_commit=True,\n        group_id=\"day2-schema-check\",\n        value_deserializer=lambda v: json.loads(v.decode(\"utf-8\")),\n        consumer_timeout_ms=timeout_s * 1000,\n    )\n\n    accepted, rejected = 0, 0\n    for record in consumer:\n        try:\n            reading = SensorReading.model_validate(record.value)\n            accepted += 1\n            print(f\"  [CONSUMER] accepted @offset {record.offset}: {reading}\")\n        except ValidationError as e:\n            rejected += 1\n            print(f\"  [CONSUMER] REJECTED @offset {record.offset}: {e.errors()[0]['msg']}\")\n        if accepted + rejected &gt;= max_messages:\n            break\n    consumer.close()\n    print(f\"\\n  Summary: {accepted} accepted, {rejected} rejected by the schema gate.\")\n\n\ndef main():\n    print(\"Real Kafka round trip (topic: {})\".format(TOPIC))\n    try:\n        produce_sample_messages()\n        time.sleep(1)\n        consume_and_validate()\n    except ImportError as e:\n        print(f\"\\nMissing dependency: {e}\")\n        print(\"Install it first: pip install -r requirements.txt\")\n    except Exception as e:\n        print(f\"\\nCould not reach Kafka at {BOOTSTRAP_SERVERS}: {e}\")\n        print(\"Start a local broker first (see the docker run command in this file's docstring),\")\n        print(\"then re-run this cell. Part 1 above (the mock-broker pipeline)\")\n        print(\"still works without Kafka running.\")\n\n\nmain()\n\nReal Kafka round trip (topic: factory_sensors_raw)\n\nCould not reach Kafka at localhost:9092: KafkaTimeoutError: Unable to bootstrap from localhost:9092\nStart a local broker first (see the docker run command in this file's docstring),\nthen re-run this cell. Part 1 above (the mock-broker pipeline)\nstill works without Kafka running.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Lab: Real-Time Streaming Pipeline with Kafka & Delta Lake"
    ]
  },
  {
    "objectID": "L02/Day2_Lab.html#part-1-mock-kafka-streaming-pipeline-windowing-delta-sink",
    "href": "L02/Day2_Lab.html#part-1-mock-kafka-streaming-pipeline-windowing-delta-sink",
    "title": "Lab: Real-Time Streaming Pipeline with Kafka & Delta Lake",
    "section": "",
    "text": "import asyncio\nimport os\nimport random\nimport shutil\nfrom datetime import datetime, timedelta, timezone\nfrom collections import defaultdict\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.types import (\n    StructType, StructField, StringType, DoubleType, IntegerType\n)\nfrom delta import configure_spark_with_delta_pip\n\n\n# =====================================================================\n# 1. BACKBONE — Mock Kafka Broker (partitions + offsets)\n\n\n# =====================================================================\n\nclass MockKafkaBroker:\n    \"\"\"\n    Simulates Apache Kafka's core model.\n\n    In real Kafka:\n      - A topic is split into N partitions (ordered, immutable logs).\n      - Each message gets an offset — its address within the partition.\n      - A consumer group shares partitions; each partition goes to\n        exactly one member, so adding consumers beyond the partition\n        count yields no extra throughput.\n      - Committed offsets let consumers resume after a crash.\n    \"\"\"\n\n    def __init__(self):\n        self.topics  = defaultdict(asyncio.Queue)\n        self.offsets = defaultdict(int)\n\n    async def publish(self, topic: str, message: dict):\n        offset = self.offsets[topic]\n        self.offsets[topic] += 1\n        await self.topics[topic].put({\n            \"offset\":     offset,\n            \"event_time\": datetime.now(timezone.utc).isoformat(),\n            \"topic\":      topic,\n            \"payload\":    message,\n        })\n\n    async def consume(self, topic: str) -&gt; dict:\n        return await self.topics[topic].get()\n\n\ndef init_delta_spark() -&gt; SparkSession:\n    builder = (\n        SparkSession.builder\n        .appName(\"Day2_RealTimeStreamingPipeline\")\n        .master(\"local[*]\")\n        .config(\"spark.sql.extensions\",\n                \"io.delta.sql.DeltaSparkSessionExtension\")\n        .config(\"spark.sql.catalog.spark_catalog\",\n                \"org.apache.spark.sql.delta.catalog.DeltaCatalog\")\n        .config(\"spark.sql.warehouse.dir\", \"./data/warehouse\")\n    )\n    spark = configure_spark_with_delta_pip(builder).getOrCreate()\n    spark.sparkContext.setLogLevel(\"ERROR\")\n    return spark\n\n\n# =====================================================================\n# 2. INGESTION LAYER — Three concurrent IoT producers\n\n\n# =====================================================================\n\nasync def machine_producer(broker: MockKafkaBroker, machine_id: str):\n    \"\"\"\n    Simulates a factory sensor streaming temperature readings every 300ms.\n    12% chance of a spike anomaly to trigger downstream alerts.\n    \"\"\"\n    print(f\"  🏭 [PRODUCER] {machine_id} stream online.\")\n    while True:\n        is_spike = random.random() &lt; 0.12\n        temp = random.uniform(92.0, 118.0) if is_spike else random.uniform(58.0, 78.0)\n        await broker.publish(\"factory_sensors_raw\", {\n            \"machine_id\": machine_id,\n            \"temperature\": round(temp, 2),\n            \"event_ts\":   datetime.now(timezone.utc).isoformat(),\n        })\n        await asyncio.sleep(0.3)\n\n\n# =====================================================================\n# 3. COMPUTE LAYER — Stream processor with window types + watermark\n\n\n# =====================================================================\n\nclass StreamProcessor:\n    \"\"\"\n    Stateful stream processing engine — mirrors Flink / Spark Structured Streaming.\n\n    Three window types demonstrated:\n      TUMBLING   — fixed, non-overlapping 1-second buckets.\n                   Every event belongs to exactly one bucket.\n                   Use case: per-second event count, hourly totals.\n\n      SLIDING    — overlapping rolling window over the last 3 seconds.\n                   The same event can appear in multiple windows.\n                   Use case: rolling average temperature or CPU.\n\n      WATERMARK  — events older than WATERMARK_S seconds are dropped.\n                   This bounds state memory and lets the engine close\n                   windows cleanly. Larger watermark = more completeness\n                   but higher latency.\n    \"\"\"\n\n    WATERMARK_S      = 2\n    SLIDING_WINDOW_S = 3\n\n    def __init__(self, broker: MockKafkaBroker, threshold: float):\n        self.broker    = broker\n        self.threshold = threshold\n        self.sliding   = defaultdict(list)\n        self.tumbling  = defaultdict(list)\n        self.watermark = datetime.now(timezone.utc)\n\n    async def run(self):\n        print(f\"  🧠 [PROCESSOR] Window engine live. Threshold: {self.threshold}°C\")\n        while True:\n            event   = await self.broker.consume(\"factory_sensors_raw\")\n            payload = event[\"payload\"]\n            m_id    = payload[\"machine_id\"]\n            val     = payload[\"temperature\"]\n            now     = datetime.now(timezone.utc)\n\n            # Watermark — drop late events\n            if now &lt; self.watermark - timedelta(seconds=self.WATERMARK_S):\n                print(f\"  ⚠️  [WATERMARK] Late event from {m_id} dropped.\")\n                continue\n            self.watermark = max(self.watermark, now)\n\n            # Sliding window — rolling average\n            self.sliding[m_id].append((now, val))\n            cutoff = now - timedelta(seconds=self.SLIDING_WINDOW_S)\n            self.sliding[m_id] = [\n                (t, v) for t, v in self.sliding[m_id] if t &gt; cutoff\n            ]\n            rolling_avg = (\n                sum(v for _, v in self.sliding[m_id]) / len(self.sliding[m_id])\n            )\n\n            # Tumbling window — count events in this 1-second bucket\n            bucket = now.strftime(\"%H:%M:%S\")\n            self.tumbling[bucket].append(val)\n            bucket_count = len(self.tumbling[bucket])\n\n            # Threshold check\n            if val &gt; self.threshold:\n                await self.broker.publish(\"anomalies_sink\", {\n                    \"machine_id\":   m_id,\n                    \"value\":        val,\n                    \"rolling_avg\":  round(rolling_avg, 2),\n                    \"bucket\":       bucket,\n                    \"bucket_count\": bucket_count,\n                    \"status\":       \"CRITICAL\",\n                })\n\n\n# =====================================================================\n# 4. STORAGE SINK — ACID append to Delta Lake\n\n\n# =====================================================================\n\nasync def delta_sink(\n    broker: MockKafkaBroker,\n    spark: SparkSession,\n    delta_path: str,\n):\n    \"\"\"\n    Reads anomaly alerts and commits them to Delta Lake.\n    Delta's transaction log guarantees ACID — no partial writes,\n    no corruption even if two writers race on the same files.\n    \"\"\"\n    schema = StructType([\n        StructField(\"event_time\",   StringType(),  True),\n        StructField(\"machine_id\",   StringType(),  True),\n        StructField(\"value\",        DoubleType(),  True),\n        StructField(\"rolling_avg\",  DoubleType(),  True),\n        StructField(\"bucket\",       StringType(),  True),\n        StructField(\"bucket_count\", IntegerType(), True),\n        StructField(\"status\",       StringType(),  True),\n    ])\n    print(\"  💾 [SINK] Delta Lake writer active.\")\n    while True:\n        alert = await broker.consume(\"anomalies_sink\")\n        p = alert[\"payload\"]\n        row = [(\n            alert[\"event_time\"],\n            p[\"machine_id\"],\n            p[\"value\"],\n            p[\"rolling_avg\"],\n            p[\"bucket\"],\n            p[\"bucket_count\"],\n            p[\"status\"],\n        )]\n        spark.createDataFrame(row, schema).write \\\n             .format(\"delta\").mode(\"append\").save(delta_path)\n        print(f\"  🚨 [DELTA WRITE] {p['machine_id']} — \"\n              f\"{p['value']}°C | rolling avg {p['rolling_avg']}°C | \"\n              f\"bucket {p['bucket']} ({p['bucket_count']} events)\")\n\n\n# =====================================================================\n# 5. PIPELINE ORCHESTRATION\n\n\n# =====================================================================\n\nasync def main():\n    delta_path = \"./data/delta/anomalies\"\n\n    if os.path.exists(\"./data\"):\n        shutil.rmtree(\"./data\")\n\n    broker = MockKafkaBroker()\n    spark  = init_delta_spark()\n\n    print(\"\\n🚀 Real-Time Streaming Pipeline booting...\\n\")\n\n    processor = asyncio.create_task(\n        StreamProcessor(broker, threshold=85.0).run()\n    )\n    sink = asyncio.create_task(\n        delta_sink(broker, spark, delta_path)\n    )\n    producers = [\n        asyncio.create_task(machine_producer(broker, \"CNC_MILL_01\")),\n        asyncio.create_task(machine_producer(broker, \"ROBOTIC_ARM_02\")),\n        asyncio.create_task(machine_producer(broker, \"ASSEMBLY_LINE_03\")),\n    ]\n\n    print(\"  Streaming for 8 seconds...\\n\")\n    await asyncio.sleep(8.0)\n\n    for task in [processor, sink, *producers]:\n        task.cancel()\n\n    print(\"\\n📊 Final Delta Lake state (all committed anomalies):\")\n    if os.path.exists(delta_path):\n        spark.read.format(\"delta\").load(delta_path) \\\n             .orderBy(\"event_time\").show(truncate=False)\n    else:\n        print(\"  No anomalies recorded in this run — lower the threshold to see more.\")\n\n    spark.stop()\n    print(\"\\n🏁 Pipeline shut down cleanly.\")",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Lab: Real-Time Streaming Pipeline with Kafka & Delta Lake"
    ]
  },
  {
    "objectID": "L02/Day2_Lab.html#run",
    "href": "L02/Day2_Lab.html#run",
    "title": "Lab: Real-Time Streaming Pipeline with Kafka & Delta Lake",
    "section": "",
    "text": "await main()\n\n\n🚀 Real-Time Streaming Pipeline booting...\n\n  Streaming for 8 seconds...\n\n  🧠 [PROCESSOR] Window engine live. Threshold: 85.0°C\n  💾 [SINK] Delta Lake writer active.\n  🏭 [PRODUCER] CNC_MILL_01 stream online.\n  🏭 [PRODUCER] ROBOTIC_ARM_02 stream online.\n  🏭 [PRODUCER] ASSEMBLY_LINE_03 stream online.\n  🚨 [DELTA WRITE] CNC_MILL_01 — 113.9°C | rolling avg 113.9°C | bucket 21:32:12 (1 events)\n\n📊 Final Delta Lake state (all committed anomalies):\n+--------------------------------+-----------+-----+-----------+--------+------------+--------+\n|event_time                      |machine_id |value|rolling_avg|bucket  |bucket_count|status  |\n+--------------------------------+-----------+-----+-----------+--------+------------+--------+\n|2026-06-27T21:32:12.761585+00:00|CNC_MILL_01|113.9|113.9      |21:32:12|1           |CRITICAL|\n+--------------------------------+-----------+-----+-----------+--------+------------+--------+\n\n\n🏁 Pipeline shut down cleanly.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Lab: Real-Time Streaming Pipeline with Kafka & Delta Lake"
    ]
  },
  {
    "objectID": "L02/Day2_Lab.html#part-2-real-kafka-round-trip",
    "href": "L02/Day2_Lab.html#part-2-real-kafka-round-trip",
    "title": "Lab: Real-Time Streaming Pipeline with Kafka & Delta Lake",
    "section": "",
    "text": "Part 1’s MockKafkaBroker is kept because it makes windowing logic easy to read and step through, but it never touches the actual Kafka client library. This part does one real KafkaProducer -&gt; topic -&gt; KafkaConsumer round trip with a Pydantic schema gate that rejects a deliberately malformed message instead of crashing.\nNeeds a running Kafka broker. In Colab, run this in a cell first:\n!pip install kafka-python pydantic\n# Run a local single-broker Kafka in the background (Colab has Java preinstalled):\n!curl -sSOL https://downloads.apache.org/kafka/3.7.0/kafka_2.13-3.7.0.tgz && tar -xzf kafka_2.13-3.7.0.tgz\n!cd kafka_2.13-3.7.0 && bin/kafka-storage.sh format -t $(bin/kafka-storage.sh random-uuid) -c config/kraft/server.properties\n!cd kafka_2.13-3.7.0 && nohup bin/kafka-server-start.sh config/kraft/server.properties &gt; /tmp/kafka.log 2&gt;&1 &\nGive it ~15-20 seconds to finish starting before running the cells below.\n\n!pip install kafka-python pydantic\n\nRequirement already satisfied: kafka-python in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (3.0.9)\nRequirement already satisfied: pydantic in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (2.13.4)\nRequirement already satisfied: annotated-types&gt;=0.6.0 in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (from pydantic) (0.8.0)\nRequirement already satisfied: pydantic-core==2.46.4 in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (from pydantic) (2.46.4)\nRequirement already satisfied: typing-extensions&gt;=4.14.1 in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (from pydantic) (4.16.0)\nRequirement already satisfied: typing-inspection&gt;=0.4.2 in c:\\users\\fdasg\\appdata\\local\\programs\\python\\python312\\lib\\site-packages (from pydantic) (0.4.2)\n\n[notice] A new release of pip is available: 25.0.1 -&gt; 26.2\n[notice] To update, run: python.exe -m pip install --upgrade pip\n\n\n\nimport json\nimport time\n\nfrom pydantic import BaseModel, ValidationError, field_validator\n\nTOPIC = \"factory_sensors_raw\"\nBOOTSTRAP_SERVERS = \"localhost:9092\"\n\n\nclass SensorReading(BaseModel):\n    \"\"\"Same shape the mock pipeline's producers emit — reused here as the schema gate.\"\"\"\n\n    machine_id: str\n    temperature: float\n    event_ts: str\n\n    @field_validator(\"temperature\")\n    @classmethod\n    def plausible_range(cls, v: float) -&gt; float:\n        if not (-40.0 &lt;= v &lt;= 200.0):\n            raise ValueError(f\"temperature {v} outside plausible sensor range\")\n        return v\n\n\ndef produce_sample_messages():\n    from kafka import KafkaProducer\n\n    producer = KafkaProducer(\n        bootstrap_servers=BOOTSTRAP_SERVERS,\n        value_serializer=lambda v: json.dumps(v).encode(\"utf-8\"),\n    )\n\n    good = [\n        {\"machine_id\": \"CNC_MILL_01\", \"temperature\": 71.4, \"event_ts\": \"2026-06-29T10:00:00Z\"},\n        {\"machine_id\": \"ROBOTIC_ARM_02\", \"temperature\": 68.9, \"event_ts\": \"2026-06-29T10:00:01Z\"},\n    ]\n    # A malformed message on purpose, to prove the consumer's schema gate catches it\n    # instead of crashing the pipeline.\n    bad = {\"machine_id\": \"ASSEMBLY_LINE_03\", \"temperature\": \"not_a_number\", \"event_ts\": \"2026-06-29T10:00:02Z\"}\n\n    for msg in good + [bad]:\n        producer.send(TOPIC, msg)\n        print(f\"  [PRODUCER] sent -&gt; {msg}\")\n    producer.flush()\n    producer.close()\n\n\ndef consume_and_validate(max_messages: int = 3, timeout_s: int = 10):\n    from kafka import KafkaConsumer\n\n    consumer = KafkaConsumer(\n        TOPIC,\n        bootstrap_servers=BOOTSTRAP_SERVERS,\n        auto_offset_reset=\"earliest\",\n        enable_auto_commit=True,\n        group_id=\"day2-schema-check\",\n        value_deserializer=lambda v: json.loads(v.decode(\"utf-8\")),\n        consumer_timeout_ms=timeout_s * 1000,\n    )\n\n    accepted, rejected = 0, 0\n    for record in consumer:\n        try:\n            reading = SensorReading.model_validate(record.value)\n            accepted += 1\n            print(f\"  [CONSUMER] accepted @offset {record.offset}: {reading}\")\n        except ValidationError as e:\n            rejected += 1\n            print(f\"  [CONSUMER] REJECTED @offset {record.offset}: {e.errors()[0]['msg']}\")\n        if accepted + rejected &gt;= max_messages:\n            break\n    consumer.close()\n    print(f\"\\n  Summary: {accepted} accepted, {rejected} rejected by the schema gate.\")\n\n\ndef main():\n    print(\"Real Kafka round trip (topic: {})\".format(TOPIC))\n    try:\n        produce_sample_messages()\n        time.sleep(1)\n        consume_and_validate()\n    except ImportError as e:\n        print(f\"\\nMissing dependency: {e}\")\n        print(\"Install it first: pip install -r requirements.txt\")\n    except Exception as e:\n        print(f\"\\nCould not reach Kafka at {BOOTSTRAP_SERVERS}: {e}\")\n        print(\"Start a local broker first (see the docker run command in this file's docstring),\")\n        print(\"then re-run this cell. Part 1 above (the mock-broker pipeline)\")\n        print(\"still works without Kafka running.\")\n\n\nmain()\n\nReal Kafka round trip (topic: factory_sensors_raw)\n\nCould not reach Kafka at localhost:9092: KafkaTimeoutError: Unable to bootstrap from localhost:9092\nStart a local broker first (see the docker run command in this file's docstring),\nthen re-run this cell. Part 1 above (the mock-broker pipeline)\nstill works without Kafka running.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Lab: Real-Time Streaming Pipeline with Kafka & Delta Lake"
    ]
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Modern Data Engineering for AI Systems",
    "section": "",
    "text": "Before a model can be trained, before a feature vector can be computed, before making any prediction — data must move. It must be collected, cleaned, transformed, validated, and delivered reliably. Data engineering is that discipline.\nThis intensive training program prepares Data Engineers, AI Engineers, and AI Practitioners to build modern, scalable, secure, and production-ready data architectures that support Artificial Intelligence systems, real-time data pipelines, vector databases, Retrieval-Augmented Generation (RAG), and intelligent analytics platforms.\nIt gives you a complete mental model of modern data engineering for AI systems: how pipelines are designed, where they break, what patterns experienced engineers use to make them reliable, and how the data layer connects to every other part of the ML system.",
    "crumbs": [
      "Home",
      "Modern Data Engineering for AI Systems"
    ]
  },
  {
    "objectID": "index.html#course-overview",
    "href": "index.html#course-overview",
    "title": "Modern Data Engineering for AI Systems",
    "section": "",
    "text": "Before a model can be trained, before a feature vector can be computed, before making any prediction — data must move. It must be collected, cleaned, transformed, validated, and delivered reliably. Data engineering is that discipline.\nThis intensive training program prepares Data Engineers, AI Engineers, and AI Practitioners to build modern, scalable, secure, and production-ready data architectures that support Artificial Intelligence systems, real-time data pipelines, vector databases, Retrieval-Augmented Generation (RAG), and intelligent analytics platforms.\nIt gives you a complete mental model of modern data engineering for AI systems: how pipelines are designed, where they break, what patterns experienced engineers use to make them reliable, and how the data layer connects to every other part of the ML system.",
    "crumbs": [
      "Home",
      "Modern Data Engineering for AI Systems"
    ]
  },
  {
    "objectID": "index.html#target-audience",
    "href": "index.html#target-audience",
    "title": "Modern Data Engineering for AI Systems",
    "section": "Target audience",
    "text": "Target audience\n\nData Engineers\nData Scientists interested in production AI pipelines\nSystems Architects\nTechnical Team Leads and AI Architects\nData Infrastructure Administrators",
    "crumbs": [
      "Home",
      "Modern Data Engineering for AI Systems"
    ]
  },
  {
    "objectID": "index.html#key-learning-outcomes",
    "href": "index.html#key-learning-outcomes",
    "title": "Modern Data Engineering for AI Systems",
    "section": "Key learning outcomes",
    "text": "Key learning outcomes\n\nDesign and implement Data Lakehouse architectures using Delta Lake.\nBuild robust real-time data pipelines for streaming applications.\nImplement vector databases and advanced RAG engineering for generative AI models.\nApply automated data governance, quality frameworks, and lineage tracking.\nIntegrate components into a scalable data infrastructure via a production-simulated capstone project.",
    "crumbs": [
      "Home",
      "Modern Data Engineering for AI Systems"
    ]
  },
  {
    "objectID": "index.html#training-methodology",
    "href": "index.html#training-methodology",
    "title": "Modern Data Engineering for AI Systems",
    "section": "Training methodology",
    "text": "Training methodology\n\n\n\nComponent\nShare\n\n\n\n\nTheory\n25% (1.5 hours)\n\n\nLabs\n50% (3 hours, directed)\n\n\nReview\n25% (1.5 hours, feedback/review)",
    "crumbs": [
      "Home",
      "Modern Data Engineering for AI Systems"
    ]
  },
  {
    "objectID": "index.html#technical-requirements",
    "href": "index.html#technical-requirements",
    "title": "Modern Data Engineering for AI Systems",
    "section": "Technical requirements",
    "text": "Technical requirements\n\nHardware: Intel i5/M1, 16GB RAM (minimum) — i7/M2, 32GB RAM (preferred)\nSoftware: Python 3.11+, Docker Desktop, VS Code, cloud access (preferred)",
    "crumbs": [
      "Home",
      "Modern Data Engineering for AI Systems"
    ]
  },
  {
    "objectID": "index.html#outline",
    "href": "index.html#outline",
    "title": "Modern Data Engineering for AI Systems",
    "section": "Outline",
    "text": "Outline\nDay 1 — Modern Data Architectures\n\nCourse intro & methodology\nWarehouses, lakes, and lakehouses; decoupling compute from storage; ELT over ETL; Delta Lake and ACID transactions\nLab: build a local mini-lakehouse with Delta Lake\n\nDay 2 — Real-Time Data Pipelines\n\nCourse intro & subtopics\nStreaming and event-driven architectures\nBuilding real-time pipelines: Kafka, windowing, exactly-once delivery\nLab: real-time streaming pipeline with Kafka & Delta Lake\n\nDay 3 — Vector Databases and Advanced RAG Engineering\n\nCourse intro & subtopics\nVector databases and embeddings\nAdvanced RAG engineering: chunking, hybrid search, reranking, evaluation\nLab: vector databases and advanced RAG engineering\nExercise: build a RAG pipeline from scratch\n\nDay 4 — Data Quality, Governance, and Lineage\n\nCourse intro & subtopics\nData quality: principles, validation, and contracts\nAutomated governance and enterprise policy\nLineage and data observability\nLab: data quality gates, Great Expectations, and OpenLineage on real retail data\n\nDay 5 — Architecture Integration and Final Project\n\nCourse intro & subtopics\nThe unified architecture\nThe final project\nFinal project: architecture integration capstone",
    "crumbs": [
      "Home",
      "Modern Data Engineering for AI Systems"
    ]
  },
  {
    "objectID": "L05/01_unified_architecture.html",
    "href": "L05/01_unified_architecture.html",
    "title": "The Unified Architecture",
    "section": "",
    "text": "Every architecture diagram you’ve seen this week showed one slice of the system in isolation. In production none of those slices run alone — a lakehouse with nothing feeding it is empty, a vector index with nothing governing its source data is a liability, and a quality gate that nobody wired into the pipeline is just a notebook cell. Enterprises that build AI platforms combine all of it into a single ecosystem that can, at scale:\n\nCollect data\nProcess data\nStore data\nGovern data\nRetrieve knowledge\nPower AI applications\nDeliver real-time intelligence\n\nThat list is the whole week, restated as one pipeline. The rest of this page is that pipeline made concrete.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Unified Architecture"
    ]
  },
  {
    "objectID": "L05/01_unified_architecture.html#from-five-separate-labs-to-one-platform",
    "href": "L05/01_unified_architecture.html#from-five-separate-labs-to-one-platform",
    "title": "The Unified Architecture",
    "section": "",
    "text": "Every architecture diagram you’ve seen this week showed one slice of the system in isolation. In production none of those slices run alone — a lakehouse with nothing feeding it is empty, a vector index with nothing governing its source data is a liability, and a quality gate that nobody wired into the pipeline is just a notebook cell. Enterprises that build AI platforms combine all of it into a single ecosystem that can, at scale:\n\nCollect data\nProcess data\nStore data\nGovern data\nRetrieve knowledge\nPower AI applications\nDeliver real-time intelligence\n\nThat list is the whole week, restated as one pipeline. The rest of this page is that pipeline made concrete.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Unified Architecture"
    ]
  },
  {
    "objectID": "L05/01_unified_architecture.html#the-reference-architecture",
    "href": "L05/01_unified_architecture.html#the-reference-architecture",
    "title": "The Unified Architecture",
    "section": "The reference architecture",
    "text": "The reference architecture\n\n\n\n\n\ngraph TD\n    A[\"Data Sources&lt;br&gt;APIs · IoT · ERP · CRM · Files\"] --&gt; B\n    B[\"Real-Time Data Pipelines&lt;br&gt;Kafka / Spark / Flink / Airflow\"] --&gt; C\n    C[\"Data Quality &amp; Validation Layer\"] --&gt; D\n    D[\"Governance &amp; Security Layer&lt;br&gt;Access · Policies · Compliance\"] --&gt; E\n    E[\"Modern Data Architecture&lt;br&gt;Lakehouse / Warehouse / Mesh\"] --&gt; F\n    F[\"Embedding &amp; Vectorization Layer\"] --&gt; G\n    G[\"Vector Database&lt;br&gt;ChromaDB / Pinecone / Weaviate\"] --&gt; H\n    H[\"Advanced RAG System&lt;br&gt;Retrieval + Re-ranking + LLMs\"] --&gt; I\n    I[\"AI Applications &amp; Agents\"]\n\n\n\n\n\n\nEvery box in that diagram is something you already built this week:\n\n\n\n\n\n\n\n\nLayer\nWhat it is\nWhere you built it\n\n\n\n\nReal-Time Data Pipelines\nStreaming ingestion with schema validation at the door\nDay 2\n\n\nData Quality & Validation\nAutomated checks before bad data reaches production\nDay 4\n\n\nGovernance & Security\nAccess policies, compliance, lineage tracking\nDay 4\n\n\nModern Data Architecture\nBronze/Silver/Gold lakehouse with ACID guarantees\nDay 1\n\n\nEmbedding & Vectorization / Vector DB\nChunking, embeddings, similarity search\nDay 3\n\n\nAdvanced RAG System\nRetrieval + reranking + LLM response generation\nDay 3\n\n\n\nThe architectural insight isn’t any single box — it’s the order and the gating. Data quality sits before governance, and governance sits before storage, because a system that governs or stores bad data has already failed; catching it earlier is strictly cheaper. RAG serving sits at the very end, downstream of everything else, because a RAG answer is only as trustworthy as the governed, quality-checked data it was indexed from — this is the direct, practical answer to Day 3’s question of why RAG systems hallucinate: they retrieve from whatever they were given, so if the retrieval corpus was never quality-gated, the model will confidently ground its answer in garbage.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Unified Architecture"
    ]
  },
  {
    "objectID": "L05/01_unified_architecture.html#how-the-components-work-together-step-by-step",
    "href": "L05/01_unified_architecture.html#how-the-components-work-together-step-by-step",
    "title": "The Unified Architecture",
    "section": "How the components work together, step by step",
    "text": "How the components work together, step by step\nWalking the diagram top to bottom as a single request’s journey:\nStep 1 — Data ingestion. Real-time pipelines collect events from APIs, IoT devices, operational databases (via CDC), documents, and other enterprise systems, and land them on a topic — this is Day 2’s Kafka producer/consumer model.\nStep 2 — Data quality validation. Automated checks validate schema correctness, missing fields, duplicate records, and invalid values before anything is allowed further downstream. Bad data is blocked here, not discovered later in a dashboard — this is Day 4’s quality gate.\nStep 3 — Governance enforcement. Governance systems apply access policies, enforce security boundaries, track lineage, and monitor compliance. This is also Day 4, and it’s why lineage exists: a governance rule with no lineage trail can’t tell you what a policy change will actually affect.\nStep 4 — Data storage. Validated, governed data lands in the lakehouse — bronze (raw), silver (cleansed), gold (aggregated) — using the Delta Lake ACID guarantees, MERGE, and schema enforcement from Day 1.\nStep 5 — Embedding generation. Documents and records are converted into vector embeddings using an embedding model (Day 3’s sentence-transformers or equivalent).\nStep 6 — Vector storage. Those embeddings are persisted in a vector database (Chroma, Pinecone, Weaviate) for similarity search.\nStep 7 — RAG retrieval. AI systems retrieve the relevant enterprise knowledge semantically, rather than relying on keyword search alone.\nStep 8 — AI response generation. An LLM generates a response grounded in the retrieved context — the payoff of every layer beneath it having done its job correctly.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Unified Architecture"
    ]
  },
  {
    "objectID": "L05/01_unified_architecture.html#recommended-technology-stack",
    "href": "L05/01_unified_architecture.html#recommended-technology-stack",
    "title": "The Unified Architecture",
    "section": "Recommended technology stack",
    "text": "Recommended technology stack\nThe slide deck’s stack-by-layer table is a reasonable default if you’re choosing tools for a real deployment (not everything on it is required for this course’s capstone — see The Final Project for what’s actually graded):\n\n\n\n\n\n\n\nLayer\nRecommended technologies\n\n\n\n\nData Pipelines\nKafka, Spark, Flink\n\n\nStorage\nDatabricks, Snowflake (or open Delta Lake / Iceberg on cloud object storage)\n\n\nQuality\nGreat Expectations\n\n\nGovernance\nCollibra, Purview\n\n\nEmbeddings\nSentence Transformers\n\n\nVector DB\nPinecone, ChromaDB\n\n\nRAG\nLangChain, LlamaIndex\n\n\nLLMs\nGPT, Qwen, Claude",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Unified Architecture"
    ]
  },
  {
    "objectID": "L05/01_unified_architecture.html#orchestrating-the-platform",
    "href": "L05/01_unified_architecture.html#orchestrating-the-platform",
    "title": "The Unified Architecture",
    "section": "Orchestrating the platform",
    "text": "Orchestrating the platform\nNone of the layers above wire themselves together. AI infrastructure orchestration is the automated coordination of compute, storage, networking, and AI services so the pipeline above runs reliably, efficiently, and at scale — the conductor making sure ingestion, quality gating, storage, embedding, and serving each run at the right time without stepping on each other.\n\nWorkflow orchestration: the DAG\nThe tool most directly relevant to this week’s labs is Apache Airflow, which models a pipeline as a DAG (Directed Acyclic Graph) — tasks as nodes, dependencies as edges, executed in topological order:\n\nOperators are the task types: PythonOperator, BashOperator, SparkSubmitOperator, S3KeySensor, HttpSensor.\nKey configuration: schedule_interval (cron expression), start_date, retries, retry_delay, catchup=False (don’t backfill every missed run since start_date).\nXCom is a lightweight key-value store for passing data between tasks — keep it small (row counts, file paths, IDs), never whole DataFrames.\nBranchPythonOperator returns the task_id to run next, enabling conditional pipeline paths — exactly the “if quality gate fails, quarantine instead of serving” branch the capstone’s Module 5 already models.\nBest practice: one DAG per pipeline, idempotent tasks (safe to re-run without duplicating side effects), and a sensor for every external dependency instead of a fixed sleep/wait.\n\n\n\nBeyond workflows: the other orchestration layers\nAt real enterprise scale, “orchestration” splits into more than just DAG scheduling. This is background context for how the tools you used this week get operated in production — not something the capstone requires you to stand up:\n\n\n\n\n\n\n\n\nOrchestration layer\nManages\nExample tools\n\n\n\n\nCompute\nGPU/CPU allocation across workloads\nKubernetes, Kueue, Volcano, Karpenter\n\n\nWorkflow\nMulti-step pipeline dependencies\nAirflow, Prefect, Dagster, Temporal\n\n\nModel\nEmbedding models, rerankers, LLMs in production\nvLLM, Triton, BentoML, Ray Serve\n\n\nVector DB\nIndex rebuilds, shard rebalancing, backups\nCoordinated with the data lake’s own backup schedule\n\n\nData\nMovement and quality across pipeline stages\nKafka, dbt, Great Expectations, dlt\n\n\n\nWithout this coordination, the failure modes are concrete: a GPU training job starves a vector DB of latency budget, an embedding job starts before its upstream chunks are ready, scaling requires slow manual provisioning, one pipeline failure takes down the whole system instead of retrying in isolation, and a version upgrade breaks consumers mid-pipeline instead of rolling out as a canary. Orchestration exists specifically to turn each of those into an automated, recoverable non-event.\n\n\nWhy AI infrastructure orchestration isn’t just DevOps with a new name\nManaging an AI architecture means discarding some traditional DevOps assumptions outright, not just adding a few extra tools:\n\n\n\n\n\n\n\n\nArchitectural metric\nTraditional software orchestration\nAI infrastructure orchestration\n\n\n\n\nPrimary resource tracked\nCPU core count and RAM utilization\nGPU/TPU VRAM, High-Bandwidth Memory (HBM), and interconnect speed (NVLink)\n\n\nScaling trigger\nWeb traffic or API request spikes\nContext size, token generation speed, batch sizes\n\n\nData dependencies\nStatic application code packages loaded from standard storage\nMulti-gigabyte model weight tensors and dynamic vector-embedding caches\n\n\n\nThe underlying reason: a traditional web service scales by adding identical, stateless replicas — more RAM and CPU cores solve most problems. An LLM-serving or embedding workload is neither identical nor stateless in the same sense — a single model’s weights can be tens of gigabytes, live entirely in GPU memory, and its actual throughput is bound by how fast tokens stream out, not by how many requests are queued. Orchestration tools built for the first world (classic Kubernetes autoscaling on CPU%) don’t know how to reason about the second — which is exactly why the GPU/model-aware tools in the table above (Kueue, Karpenter, vLLM, Ray Serve) exist as a separate layer rather than just “more Kubernetes.”",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Unified Architecture"
    ]
  },
  {
    "objectID": "L05/01_unified_architecture.html#why-integration-is-the-point",
    "href": "L05/01_unified_architecture.html#why-integration-is-the-point",
    "title": "The Unified Architecture",
    "section": "Why integration is the point",
    "text": "Why integration is the point\nA fully integrated platform is what moves an enterprise from an AI prototype to production: real-time streaming, governance compliance, automated data validation, and semantic RAG search working as one system instead of four disconnected demos. That combination is what delivers fast, safe, and reliable AI applications at scale — better AI accuracy because the RAG layer only ever sees governed, quality-checked data; fewer hallucinations because responses are grounded in a trusted corpus, not whatever happened to be lying around; and governance and compliance enforced automatically instead of depending on someone remembering a policy.\nThe final project is where you build exactly this, end to end, for a real (if intentionally small) dataset.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Unified Architecture"
    ]
  },
  {
    "objectID": "L04/03_lineage_and_observability.html",
    "href": "L04/03_lineage_and_observability.html",
    "title": "Lineage and Data Observability",
    "section": "",
    "text": "In the early days of data architecture, engineers relied on basic software monitoring: write a strict rule (“alert me if the server crashes, or if the ETL job throws a 500”). That works for infrastructure failures. But as data systems grew into massive streaming pipelines feeding live AI models and executive dashboards, a new failure mode appeared: silent data corruption — the pipeline runs perfectly, no code crashes, and the data inside it is simply wrong.\n\nData monitoring (the “what”) is reactive and rule-based. It checks infrastructure for known errors: is the database online? Did the Airflow DAG complete? Is latency under 200ms? It tells you when something broke.\nData observability (the “why”) is proactive and inferred. It analyzes the actual data flowing through the system to detect “unknown unknowns”: why did average purchase value suddenly drop 90% today? It helps you trace the root cause of an anomaly monitoring alone would never have flagged.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lineage and Data Observability"
    ]
  },
  {
    "objectID": "L04/03_lineage_and_observability.html#monitoring-tells-you-something-broke-observability-tells-you-why",
    "href": "L04/03_lineage_and_observability.html#monitoring-tells-you-something-broke-observability-tells-you-why",
    "title": "Lineage and Data Observability",
    "section": "",
    "text": "In the early days of data architecture, engineers relied on basic software monitoring: write a strict rule (“alert me if the server crashes, or if the ETL job throws a 500”). That works for infrastructure failures. But as data systems grew into massive streaming pipelines feeding live AI models and executive dashboards, a new failure mode appeared: silent data corruption — the pipeline runs perfectly, no code crashes, and the data inside it is simply wrong.\n\nData monitoring (the “what”) is reactive and rule-based. It checks infrastructure for known errors: is the database online? Did the Airflow DAG complete? Is latency under 200ms? It tells you when something broke.\nData observability (the “why”) is proactive and inferred. It analyzes the actual data flowing through the system to detect “unknown unknowns”: why did average purchase value suddenly drop 90% today? It helps you trace the root cause of an anomaly monitoring alone would never have flagged.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lineage and Data Observability"
    ]
  },
  {
    "objectID": "L04/03_lineage_and_observability.html#the-five-pillars-of-data-observability",
    "href": "L04/03_lineage_and_observability.html#the-five-pillars-of-data-observability",
    "title": "Lineage and Data Observability",
    "section": "The five pillars of data observability",
    "text": "The five pillars of data observability\nA production-grade observability system (Monte Carlo, Acceldata, Databricks) continuously evaluates data health across five dimensions. The first four are what you’d expect to check; the fifth — lineage — is different in kind: it’s the map that gives the other four their context.\n\n\n\n\n\ngraph TD\n    L[\"Lineage — the end-to-end map\"] --&gt; F[\"Freshness\"]\n    L --&gt; V[\"Volume\"]\n    L --&gt; S[\"Schema\"]\n    L --&gt; D[\"Distribution\"]\n\n\n\n\n\n\n1. Freshness (SLA timeliness). Is the data up to date? If a dashboard expects sales updates every 15 minutes but the underlying table hasn’t refreshed in 4 hours, the observability engine fires a freshness alert before an executive notices a stagnant chart.\n2. Volume (completeness check). Is the scale of incoming data normal? If an ingestion pipeline usually sees ~50,000 events/hour and a batch arrives with only 2,000 — or spikes to 500,000 — that’s flagged as a critical anomaly, usually pointing to an upstream API drop or a duplicate load.\n3. Schema (structural changes). Schema drift is the single biggest killer of production applications and RAG systems. If user_id gets renamed to customer_uuid, or a field’s type silently changes from integer to string, every downstream transformation can break instantly. Observability tools continuously diff schemas and alert the moment structure changes.\n4. Distribution (field-level health). This looks at the statistical profile of values inside a table — mean, median, standard deviation, null-rate. If customer_age normally has a 1% null rate and an upstream change suddenly pushes 75% of rows to NULL, the pipeline didn’t crash, but the data flowing through it is completely broken.\n5. Lineage (the map of dependencies). Lineage is the foundational layer that gives the other four pillars context: it builds an end-to-end trace of where data originates, how it transforms, and which dashboards or models consume it. Without it, freshness/volume/schema/distribution alerts tell you something is wrong upstream but not what downstream actually depends on the broken table.\nCommon tooling in this space includes Monte Carlo, Datadog, Splunk, Microsoft Fabric Monitoring, and IBM’s observability suite — all built around some combination of these five pillars.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lineage and Data Observability"
    ]
  },
  {
    "objectID": "L04/03_lineage_and_observability.html#openlineage-lineage-as-structured-events",
    "href": "L04/03_lineage_and_observability.html#openlineage-lineage-as-structured-events",
    "title": "Lineage and Data Observability",
    "section": "OpenLineage — lineage as structured events",
    "text": "OpenLineage — lineage as structured events\nOpenLineage is an open, vendor-neutral standard (a JSON schema published on GitHub) for emitting lineage as discrete events rather than a hand-maintained diagram.\n\nEvent types — START when a pipeline run begins, COMPLETE on success (with output stats), FAIL on error (with the cause).\nPayload shape — run (a UUID plus facets), job (namespace + name), inputs (datasets read), outputs (datasets written).\nMarquez is the reference open-source lineage server: it receives these events, stores the resulting graph, and exposes it via a REST API and UI. A pipeline can also just write the events to a local file — no server required — which is what today’s lab does.\n\nThe integration pattern is deliberately minimal: emit START before the first read, COMPLETE after the final write, and FAIL inside the except block. No changes to the pipeline’s actual transformation logic are needed. The payoff is impact analysis — the lineage graph shows every downstream job that reads a given dataset, which is exactly what you need before approving a risky schema change.\n# Minimal shape of an OpenLineage RunEvent\n{\n  \"eventType\": \"COMPLETE\",\n  \"eventTime\": \"2026-07-31T12:00:00Z\",\n  \"run\":    {\"runId\": \"…uuid…\"},\n  \"job\":    {\"namespace\": \"retail_pipeline\", \"name\": \"quality_gate\"},\n  \"inputs\":  [{\"namespace\": \"kaggle\", \"name\": \"online_retail_raw.csv\"}],\n  \"outputs\": [{\"namespace\": \"warehouse\", \"name\": \"online_retail_clean\"}]\n}",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lineage and Data Observability"
    ]
  },
  {
    "objectID": "L04/03_lineage_and_observability.html#lab-automated-data-quality-gates-on-real-e-commerce-data",
    "href": "L04/03_lineage_and_observability.html#lab-automated-data-quality-gates-on-real-e-commerce-data",
    "title": "Lineage and Data Observability",
    "section": "Lab: automated data quality gates on real e-commerce data",
    "text": "Lab: automated data quality gates on real e-commerce data\nThe Day 4 lab notebook builds a full quality-gate pipeline against the UCI Online Retail Dataset — 541,909 real transactions from a UK-based online retailer (2010–2011), pulled live from Kaggle. Nothing here is synthetic or planted: roughly 25% of CustomerIDs are genuinely missing, cancelled orders carry negative quantities, and formatting is inconsistent — exactly the kind of mess a real ingestion boundary has to handle.\nThe notebook walks through the whole chain from today’s lesson, in order:\n\nDownload and profile the raw CSV, surfacing quality problems before any rule is written.\nEnforce a RetailTransactionContract (Pydantic v2) at the pipeline boundary — rows with a missing customer ID, non-positive quantity, or malformed invoice number are rejected immediately.\nRun a hand-rolled DataQualityEngine implementing all six DAMA dimensions against the contract-validated rows.\nRun the same core checks again through a real Great Expectations 1.x checkpoint — the actual library the capstone rubric names — so you see both the hand-rolled concept and the production tool agree.\nRoute the batch to production_warehouse/ or quarantine_zone/ based on the DAMA verdict.\nEmit both a hand-rolled OpenLineage-style JSON trail and real openlineage-python START/COMPLETE/FAIL events, written to a local file transport (no Marquez server required).\n\n\nWhat to look for in the output\n\nThe Pydantic contract alone rejects 144,025 of 541,909 rows (26.6%) — the large majority for a missing CustomerID, the rest for non-positive quantities (cancellations) or zero prices.\nThe DAMA engine, run on the already contract-clean 397,884 rows, still fails — on uniqueness (5,192 exact duplicate rows) and validity (1,838 StockCodes that don’t match the expected 5-digit format). This is the point: a data contract catches record-level violations, but it does not catch duplicates or catalog-format drift — that’s exactly what a broader quality engine (or Great Expectations) is for.\nThe real Great Expectations checkpoint reports success=True on the same contract-validated rows — because its four expectations (not_be_null, two be_between range checks, and the invoice regex) restate the same rules the Pydantic contract already enforced. It’s a useful confirmation that the two layers agree, not an independent catch.\nBecause the DAMA verdict is FAILED, the lineage emitters both record a START → FAIL sequence, and the batch is routed to quarantine_zone/ rather than production_warehouse/ — the quality gate actually holding the line, not just logging a warning.\n\n\n\n\n\n\n\nNote\n\n\n\nRunning this notebook end-to-end in Colab requires a free Kaggle account and API key (Step 2 — Kaggle Authentication), supplied either as Colab Secrets or an uploaded kaggle.json. The dataset itself (carrie1/ecommerce-data, matching the UCI Online Retail Dataset) is public, so some environments can fetch it without credentials — but Colab’s kagglehub calls are written to expect the credentials from Step 2 regardless, so don’t skip that setup.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lineage and Data Observability"
    ]
  },
  {
    "objectID": "L04/01_data_quality_principles.html",
    "href": "L04/01_data_quality_principles.html",
    "title": "Data Quality: Principles, Validation, and Contracts",
    "section": "",
    "text": "Computer science has one law that no amount of model size fixes: garbage in, garbage out. You can deploy the most advanced, billion-parameter LLM in the world, but if you feed it corrupted, incomplete, or outdated data, it will confidently produce nonsense — just very fluent nonsense. This is why production-grade data teams don’t treat data quality as a one-time cleaning pass; they build validation frameworks that check every batch, every time, before it reaches a model or a dashboard.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Data Quality: Principles, Validation, and Contracts"
    ]
  },
  {
    "objectID": "L04/01_data_quality_principles.html#garbage-in-garbage-out",
    "href": "L04/01_data_quality_principles.html#garbage-in-garbage-out",
    "title": "Data Quality: Principles, Validation, and Contracts",
    "section": "",
    "text": "Computer science has one law that no amount of model size fixes: garbage in, garbage out. You can deploy the most advanced, billion-parameter LLM in the world, but if you feed it corrupted, incomplete, or outdated data, it will confidently produce nonsense — just very fluent nonsense. This is why production-grade data teams don’t treat data quality as a one-time cleaning pass; they build validation frameworks that check every batch, every time, before it reaches a model or a dashboard.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Data Quality: Principles, Validation, and Contracts"
    ]
  },
  {
    "objectID": "L04/01_data_quality_principles.html#the-six-dama-dimensions-of-data-quality",
    "href": "L04/01_data_quality_principles.html#the-six-dama-dimensions-of-data-quality",
    "title": "Data Quality: Principles, Validation, and Contracts",
    "section": "The six DAMA dimensions of data quality",
    "text": "The six DAMA dimensions of data quality\nWhen data engineers ask “is this dataset healthy enough to use?”, they don’t guess — they measure it against six standard dimensions, formalized by the DAMA (Data Management Association) data quality framework.\n\n\n\n\n\n\n\n\nDimension\nWhat it means\nThe real-world risk\n\n\n\n\nAccuracy\nData correctly represents real-world ground truth, free of typos or sensor error.\nA transposed digit shifts an invoice from $10,000 to $100,000.\n\n\nCompleteness\nNo critical gaps, missing structural values, or unexpected NULLs.\nAn AI computes average customer lifetime value while half the history rows are blank.\n\n\nConsistency\nThe same entity has the same value everywhere it appears.\nOne system stores \"USA\", another stores \"United States\" — the model treats them as two different countries.\n\n\nTimeliness / Freshness\nData is current and reflects the real state of operations.\nA fraud-detection model trains on three-year-old buying patterns and misses modern attack vectors.\n\n\nValidity\nValues conform to defined business rules, schemas, and ranges.\nA column tracking human age contains -5 or 999.\n\n\nUniqueness\nEvery record exists exactly once, no redundant duplicates.\nDuplicate customer rows skew training statistics, forcing a model to over-index on repeated behavior.\n\n\n\nIn short: quality principles describe what “good” looks like; validation techniques (next section) are how you actually check for it.\n\nFrom principle to query\nEach dimension above has a direct, checkable implementation:\n\nCompleteness — percentage of non-null values in a critical field. A missing customer_id breaks revenue attribution entirely.\nAccuracy — cross-referencing against an authoritative source (does the value match the CRM or ERP record?).\nConsistency — the same entity must carry the same value across every system it appears in (an address in the CRM must match the one in the warehouse).\nTimeliness — data must land within an agreed SLA window; late financial data can miss a settlement cutoff entirely.\nUniqueness — detected with a simple ratio check: COUNT(*) vs COUNT(DISTINCT id). Any gap between the two is duplicates.\nValidity — domain checks such as quantity &gt; 0 or country_code IN ISO 3166-1 alpha-2.\n\nThis is exactly the shape of engine you’ll build in today’s lab: six methods, one per dimension, each returning a pass/fail verdict plus a human-readable reason.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Data Quality: Principles, Validation, and Contracts"
    ]
  },
  {
    "objectID": "L04/01_data_quality_principles.html#validation-techniques",
    "href": "L04/01_data_quality_principles.html#validation-techniques",
    "title": "Data Quality: Principles, Validation, and Contracts",
    "section": "Validation techniques",
    "text": "Validation techniques\nThese are the concrete, reusable mechanisms that implement the dimensions above at the code level:\n\nSchema validation — does the record have the right fields, in the right types?\nType validation — is quantity actually numeric, not a string that happens to look numeric?\nRange validation — does price fall within a sane business range?\nNull / missing value checks — are required fields actually populated?\nDuplicate detection — does a natural or primary key repeat?\nPattern validation — does a field match an expected format, such as an email regex or an invoice-number pattern?\nConsistency validation — does the same entity agree with itself across tables or systems?\n\nIndividually these are simple checks. The engineering discipline is applying all of them, consistently, at the boundary of the pipeline — which is exactly what a data contract formalizes.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Data Quality: Principles, Validation, and Contracts"
    ]
  },
  {
    "objectID": "L04/01_data_quality_principles.html#data-contracts-formalizing-the-producerconsumer-agreement",
    "href": "L04/01_data_quality_principles.html#data-contracts-formalizing-the-producerconsumer-agreement",
    "title": "Data Quality: Principles, Validation, and Contracts",
    "section": "Data contracts — formalizing the producer–consumer agreement",
    "text": "Data contracts — formalizing the producer–consumer agreement\nA data contract is a machine-enforceable schema plus an SLA between the system that produces data and the system that consumes it. Instead of a producer team silently renaming a column and breaking every downstream job, the contract makes the agreement explicit and enforced in code.\nTwo common ways to implement one:\n\nPydantic v2, for validating records at the application/pipeline boundary:\nfrom pydantic import BaseModel, field_validator, ConfigDict\n\nclass OrderContract(BaseModel):\n    model_config = ConfigDict(strict=True)   # '123' stays a string, never silently becomes 123\n\n    order_id: str\n    quantity: float\n    email: str\n\n    @field_validator(\"email\")\n    @classmethod\n    def valid_email(cls, v: str) -&gt; str:\n        if \"@\" not in v:\n            raise ValueError(\"malformed email\")\n        return v\nstrict=True blocks silent type coercion — a string '123' is never quietly promoted to the integer 123. Custom @field_validator methods raise on anything else the schema alone can’t express, like a malformed email.\ndbt contracts, for enforcing shape at the warehouse/model level: setting enforced: true in a model’s YAML makes the build itself fail the moment a column’s type or name drifts from what’s declared — the disagreement is caught at build time, not by a confused analyst three weeks later.\n\n\nThe violation pipeline\nA contract is only useful if violations have a defined path:\nvalidate at ingest → quarantine invalid records → alert the producer → block downstream propagation\nNothing invalid is silently dropped, and nothing invalid is silently allowed through either — it’s set aside, visibly, with the producer notified. Breaking changes to a contract — removing a column, narrowing a type — are treated as major-version changes, with a migration window for consumers before the old shape is retired.\nThe lab notebook builds exactly this: a RetailTransactionContract Pydantic model that rejects any record with a missing customer ID, a non-positive quantity, or a malformed invoice number, before the DAMA quality engine ever sees it.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Data Quality: Principles, Validation, and Contracts"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html",
    "href": "L03/02_advanced_rag_engineering.html",
    "title": "Advanced RAG Engineering",
    "section": "",
    "text": "Ask a large language model something like “What were the key discussion points in our company’s board meeting yesterday?” and it will do one of three things: hallucinate a confident but false answer, admit it doesn’t know (because its training data has a cutoff date), or give a generic non-answer (“board meetings typically discuss strategy and finances…”). None of those are useful, and all three come from the same root causes: the model’s knowledge is static (frozen at training time), it has no memory of your private data (it never saw your internal documents), and it is prone to hallucination (when unsure, it guesses plausibly instead of admitting ignorance).\nRetrieval-Augmented Generation (RAG) retrieves relevant documents from an external knowledge source — usually a vector database — and injects them into the LLM’s prompt, so the model generates an answer grounded in real, retrieved data rather than in memory alone. It’s the difference between an open-book and a closed-book exam: without RAG, the model answers purely from memory and may hallucinate; with RAG, it looks the answer up first, then answers from what it found. Embeddings and similarity search — covered in Vector Databases and Embeddings — are the fuel and the engine; RAG is the complete vehicle built around them.\nEvery RAG pipeline runs in three phases: retrieve relevant documents using similarity search, augment the prompt with those documents, and generate an answer grounded in that context.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#why-rag-exists",
    "href": "L03/02_advanced_rag_engineering.html#why-rag-exists",
    "title": "Advanced RAG Engineering",
    "section": "",
    "text": "Ask a large language model something like “What were the key discussion points in our company’s board meeting yesterday?” and it will do one of three things: hallucinate a confident but false answer, admit it doesn’t know (because its training data has a cutoff date), or give a generic non-answer (“board meetings typically discuss strategy and finances…”). None of those are useful, and all three come from the same root causes: the model’s knowledge is static (frozen at training time), it has no memory of your private data (it never saw your internal documents), and it is prone to hallucination (when unsure, it guesses plausibly instead of admitting ignorance).\nRetrieval-Augmented Generation (RAG) retrieves relevant documents from an external knowledge source — usually a vector database — and injects them into the LLM’s prompt, so the model generates an answer grounded in real, retrieved data rather than in memory alone. It’s the difference between an open-book and a closed-book exam: without RAG, the model answers purely from memory and may hallucinate; with RAG, it looks the answer up first, then answers from what it found. Embeddings and similarity search — covered in Vector Databases and Embeddings — are the fuel and the engine; RAG is the complete vehicle built around them.\nEvery RAG pipeline runs in three phases: retrieve relevant documents using similarity search, augment the prompt with those documents, and generate an answer grounded in that context.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#the-complete-rag-architecture",
    "href": "L03/02_advanced_rag_engineering.html#the-complete-rag-architecture",
    "title": "Advanced RAG Engineering",
    "section": "The complete RAG architecture",
    "text": "The complete RAG architecture\nThe full pipeline splits into two steps that run at very different times: indexing, done once offline, and querying, done every time a user asks something.\n\n\n\n\n\ngraph TD\n    A[\"Documents&lt;br&gt;(PDFs, wikis, emails, code)\"] --&gt; B[\"Chunking&lt;br&gt;(split into passages)\"]\n    B --&gt; C[\"Embedding&lt;br&gt;(chunk → vector)\"]\n    C --&gt; D[(\"Vector DB&lt;br&gt;(chunk + vector + metadata)\")]\n\n\n\n\n\n\n\nPhase one: indexing (preparation)\nThis is the offline step — building the “textbook” the system will later search:\n\n\n\n\n\n\n\n\nStep\nAction\nExample\n\n\n\n\n1. Load\nCollect all source documents\nCompany policies, product manuals, support tickets\n\n\n2. Chunk\nSplit into semantic units\nEach chunk = 512 tokens with 50-token overlap\n\n\n3. Embed\nGenerate a vector for each chunk\nchunk_vector = embedding_model(chunk_text)\n\n\n4. Store\nSave chunk + vector + metadata\n{id: 123, text: \"...\", vector: [0.1, 0.9, ...], source: \"HR_policy.pdf\"}\n\n\n\n\n\nPhase two: querying (inference)\nThis runs on every user question. For the query “What is our vacation policy?”: embed the query, run similarity search against the vector database to get the top-k nearest chunks (e.g., k=5), retrieve the actual text of those chunks, build a prompt containing system instructions plus the retrieved chunks plus the user’s question, send it to the LLM, and return the generated answer — e.g., “Our vacation policy provides 15 days per year…”",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#three-rag-architecture-patterns",
    "href": "L03/02_advanced_rag_engineering.html#three-rag-architecture-patterns",
    "title": "Advanced RAG Engineering",
    "section": "Three RAG architecture patterns",
    "text": "Three RAG architecture patterns\n\n\n\n\n\n\n\n\n\n\nPattern\nFlow\nPros\nCons\nUse case\n\n\n\n\n1. Naive RAG\nQuery → Retrieve → Augment → Generate → Answer\nEasy to build, works for simple Q&A\nNo quality checks; retrieves wrong chunks often\nPrototypes, internal demos\n\n\n2. Advanced RAG\nQuery → rewrite → Retrieve → Rerank → Filter → Augment → Generate\nMuch higher accuracy\nMore latency, more components to maintain\nProduction systems\n\n\n3. Modular / Agentic RAG\nSystem decides which retriever to call (vector DB, web search, SQL, API); can retrieve iteratively\nMost powerful — answers complex multi-hop questions\nComplex to build and debug\nMulti-hop, tool-using assistants\n\n\n\nAdvanced RAG adds two things around the naive loop: pre-retrieval processing (rewriting an ambiguous query, expanding it with synonyms) and post-retrieval processing (reranking chunks with a more expensive cross-encoder, filtering out low-quality ones). Modular/agentic RAG goes further still — the system can ask a follow-up question, retrieve again, and chain retrievals together, which is the only way to answer something like “What is the capital of the country where Einstein was born?” (a single retrieval can’t connect those two facts).",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#walking-through-a-real-query",
    "href": "L03/02_advanced_rag_engineering.html#walking-through-a-real-query",
    "title": "Advanced RAG Engineering",
    "section": "Walking through a real query",
    "text": "Walking through a real query\nUser asks: “How many vacation days do new hires get?”\n\nEmbed the query → [0.23, -0.56, 0.78, ...]\nSimilarity search returns the top 3 chunks: Chunk A (score 0.92, “New hires in their first year receive 10 vacation days”), Chunk B (score 0.88, “Employees with 1–5 years of service receive 15 days”), Chunk C (score 0.45, “Vacation policy was revised in January 2024”).\nReranking (optional): a cross-encoder re-evaluates all three and promotes Chunk A to rank 1, dropping Chunk C entirely as irrelevant noise.\nBuild the prompt: Context: New hires in their first year receive 10 vacation days. Question: How many vacation days do new hires get? Answer:\nLLM generation: “New hires receive 10 vacation days during their first year of employment.”\nReturn to user.\n\nNotice what happened: the LLM did not “know” this fact. It read it from the retrieved chunk and paraphrased it — that’s the entire point of RAG.\n\nWhere basic RAG breaks\n\n\n\n\n\n\n\n\nProblem\nExplanation\nMitigation\n\n\n\n\nLow recall\nRelevant chunks not retrieved\nImprove chunking, embedding model, or increase k\n\n\nLow precision\nIrrelevant chunks retrieved\nAdd reranking, metadata filtering\n\n\nContext window overflow\nRetrieved too much text\nCompress chunks, use a smaller k\n\n\nOutdated data\nVector DB hasn’t been updated\nImplement incremental indexing\n\n\nMulti-hop questions\nSingle retrieval can’t connect separate facts\nUse iterative or agentic RAG",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#chunking-strategies",
    "href": "L03/02_advanced_rag_engineering.html#chunking-strategies",
    "title": "Advanced RAG Engineering",
    "section": "Chunking strategies",
    "text": "Chunking strategies\nImagine splitting a 10-page medical paper into rigid 100-character chunks. One chunk ends with “The patient showed no signs of” and the next begins with “improvement after treatment.” A user asks “Did the patient improve?” and the system retrieves one chunk with no answer, or the other with missing context — either way, a wrong answer. Chunking is the art of breaking documents into pieces that are meaningful, self-contained, and searchable.\nGood chunking has three goals: semantic completeness (each chunk should make sense on its own — cutting a sentence in half fails this), appropriate size (small enough for efficient retrieval, large enough to hold context — 50-character chunks lose meaning, 5000-character chunks waste the context window), and contextual boundaries (respect the document’s natural structure — never split inside a table, code block, or list). The golden rule: a chunk should be a unit of meaning — a paragraph, a function — not an arbitrary slice of bytes.\n\n\n\n\n\n\n\n\n\nStrategy\nHow it works\nBest for\nAvoid for\n\n\n\n\nFixed-size\nSplits every N characters/tokens, regardless of content\nLogs, uniform text, unknown structure\nAny structured document — it breaks sentences and ideas\n\n\nSentence-based\nSplits on punctuation boundaries\nConversational text, support tickets\nVery long or complex sentences; abbreviations like “Dr. Smith” can fool it\n\n\nRecursive / hierarchical\nRecursively splits by decreasing boundary — document → section → paragraph → sentence — stopping once a chunk is within the target size\nBooks, manuals, legal documents with clear headings\nFlat, unstructured text with no real hierarchy\n\n\nSemantic\nEmbeds sliding windows of sentences; splits where similarity between adjacent windows drops sharply\nLarge, mixed-content documents — textbooks, research papers\nSmall or simple documents — computationally overkill\n\n\n\nRecursive chunking keeps a paragraph attached to its section, and a sentence to its paragraph, so the system can store parent-child relationships — but it requires actually parsing the document’s headings. Semantic chunking is the most content-aware of the four: it detects where meaning shifts by comparing the embeddings of consecutive sentence windows, splitting only where similarity drops — at the cost of running an embedding model during indexing, not just at query time.\n\nChunk overlap\nRegardless of strategy, chunks should almost always overlap — chunk 2 repeats the last few tokens of chunk 1:\nChunk 1: [A B C D E F G]\nChunk 2: [F G H I J K L]   ← overlaps by 2 tokens (F, G)\nChunk 3: [K L M N O P Q]   ← overlaps by 2 tokens (K, L)\nWithout overlap, “The patient showed no” ends one chunk and “signs of improvement” starts the next, and the LLM never sees the complete sentence. With overlap, both chunks contain the boundary. Recommended overlap is 10–20% of chunk size: 512-token chunks get 50–100 tokens of overlap; 256-token chunks get 25–50.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#hybrid-search-and-reciprocal-rank-fusion",
    "href": "L03/02_advanced_rag_engineering.html#hybrid-search-and-reciprocal-rank-fusion",
    "title": "Advanced RAG Engineering",
    "section": "Hybrid search and Reciprocal Rank Fusion",
    "text": "Hybrid search and Reciprocal Rank Fusion\nPure vector search misses exact keyword matches — a query for “GPT-4” can retrieve documents about “a language model” instead of the literal string. Pure BM25 (keyword search) misses semantic meaning entirely — “canine therapy” doesn’t match “dog treatment” at all if no words overlap.\nHybrid search runs both a vector ANN search and a BM25 keyword search in parallel, then merges the two result lists with Reciprocal Rank Fusion (RRF):\n\\[\\text{RRF score} = \\sum_{\\text{list}} \\frac{1}{k + \\text{rank}_i}, \\quad k = 60\\]\nRRF rewards a document that ranks highly in both lists, without needing any weights tuned by hand — and empirically it outperforms a weighted linear combination of the two scores in most benchmarks. Most modern vector databases (Weaviate, Elasticsearch, Qdrant) expose hybrid search natively.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#cross-encoder-reranking-the-second-precision-gate",
    "href": "L03/02_advanced_rag_engineering.html#cross-encoder-reranking-the-second-precision-gate",
    "title": "Advanced RAG Engineering",
    "section": "Cross-encoder reranking — the second precision gate",
    "text": "Cross-encoder reranking — the second precision gate\nA bi-encoder (the retrieval stage) embeds the query and each document independently — fast, but it can miss subtle relevance because query and document never actually interact during scoring. A cross-encoder (reranking stage) takes the (query, document) pair jointly and scores them together — far more accurate, because it can model the interaction between query tokens and document tokens directly, at the cost of one inference call per candidate (O(n), versus O(1) for bi-encoder similarity).\nThe standard workflow: retrieve the top 50 candidates cheaply with the bi-encoder, rerank those 50 with the cross-encoder, then pass only the top 3 to 5 to the LLM. Rerank only when retrieval returns more than ~10 candidates — it adds roughly 50–200ms of latency per query. Common models: ms-marco-MiniLM-L-6-v2 (fast), bge-reranker-large (more accurate), or a hosted option like the Cohere Rerank API.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#rag-evaluation-the-triad-you-must-measure",
    "href": "L03/02_advanced_rag_engineering.html#rag-evaluation-the-triad-you-must-measure",
    "title": "Advanced RAG Engineering",
    "section": "RAG evaluation — the triad you must measure",
    "text": "RAG evaluation — the triad you must measure\nYou cannot guess whether an architecture change improved retrieval — you have to measure it:\n\nContext Precision — are the retrieved chunks actually relevant? (no noise)\nContext Recall — did retrieval find all the chunks needed to answer? (completeness)\nAnswer Faithfulness — does the generated answer stick to the retrieved context, or does it add facts that weren’t there? (hallucination risk)\nAnswer Relevance — does the answer actually address what the user asked?\n\nRAGAS is the standard open-source framework: it computes all four metrics automatically using an LLM as judge. A useful alert rule: if faithfulness drops below 0.85, the retrieval or the prompt has regressed and needs attention.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#building-production-ready-rag-pipelines",
    "href": "L03/02_advanced_rag_engineering.html#building-production-ready-rag-pipelines",
    "title": "Advanced RAG Engineering",
    "section": "Building production-ready RAG pipelines",
    "text": "Building production-ready RAG pipelines\nTurning a working prototype into something stable and accurate at scale means implementing five layers:\n[Ingestion Layer] → [Hybrid Retrieval] → [Two-Stage Reranking] → [Data Sync / CDC] → [Evaluation Triad]\n\nThe ingestion layer. Production data is rarely clean markdown — it’s multi-column PDFs, scanned documents, and messy financial tables. Modern pipelines use vision-based layout parsers (Docling, Unstructured) to convert these into clean semantic trees that preserve table structure. Every chunk should also be stamped with metadata — file_id, security_clearance_level, creation_date, document_version — you can’t just inject raw text.\nHybrid retrieval. Relying only on vector search is a classic prototype mistake — it struggles with exact product serial numbers, code symbols, or jargon. Combine dense (vector) retrieval with sparse (BM25) retrieval via RRF, as covered above.\nTwo-stage reranking, to mitigate “lost-in-the-middle”: dumping the top 30 raw chunks into an LLM overwhelms it and it starts ignoring information buried in the middle of the prompt. Stage one fetches the top 50 candidates cheaply; stage two reranks with a cross-encoder and keeps only the best 3–5.\nKeeping data fresh — incremental sync and CDC. A production knowledge base isn’t static; a nightly full wipe-and-reindex doesn’t scale.\n\n\n\n\n\n\n\n\nSync strategy\nHow it works\nWhen to use it\n\n\n\n\nIncremental updates\nMonitors folder timestamps or commit histories, re-indexes only new/modified documents\nStandard corporate files and wikis\n\n\nChange Data Capture (CDC)\nHooks directly into a live database to mirror inserts/updates/deletes into the vector store instantly\nHighly dynamic data — live inventory, ticketing\n\n\n\nContinuous evaluation. Score every architectural change against the RAG triad (context relevance, groundedness, answer relevance) — you cannot tell whether a change helped without measuring it.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/02_advanced_rag_engineering.html#lab-building-an-advanced-rag-system",
    "href": "L03/02_advanced_rag_engineering.html#lab-building-an-advanced-rag-system",
    "title": "Advanced RAG Engineering",
    "section": "Lab: building an advanced RAG system",
    "text": "Lab: building an advanced RAG system\nThe Day 3 lab notebook implements this entire pipeline end to end over a small built-in knowledge base of data-engineering documents (Kafka, Delta Lake, RAG, HNSW, hybrid search): document chunking, a ChromaDB vector index (HNSW backend), BM25 keyword search, RRF hybrid fusion, cross-encoder reranking, RAG prompt construction, and RAGAS-style precision and similarity evaluation — all on a real, executed run using local, free components (no hosted vector DB, no paid embeddings API, no GPU).\nWhat to look for in the output:\n\nCandidate counts moving through each stage — vector search, BM25 search, RRF fusion — for four different queries against the same eight-document knowledge base.\nThe cross-encoder reranking step narrowing the fused candidates down to the top 3, with each surviving chunk’s text printed.\nThe final RAG prompt assembled from the top reranked chunks, plus the retrieval metrics (context_precision, avg_similarity) and a pass/fail quality read-out, printed for every query.\n\n\n\n\n\n\n\nNote\n\n\n\nThe practice exercise covers the same six stages as the lab, but its task cells contain deliberate ??? placeholders for students to fill in — it will not run as delivered, and that is intentional, not a bug. It’s kept as a separate notebook from the worked lab rather than merged into it, since merging would either break the lab’s run-as-delivered state or defeat the exercise’s purpose. Complete the ??? blanks yourself, then run it top to bottom to check your work against the inline test-cell assertions.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/00_intro.html",
    "href": "L03/00_intro.html",
    "title": "Day 3 — Vector Databases and Advanced RAG Engineering",
    "section": "",
    "text": "For two days we’ve focused on getting data to rest somewhere reliable — a lakehouse table, a stream of validated events. Today we ask a different question: once an AI system needs to reason over that data, how does it find the handful of facts relevant to one specific question, out of millions of documents, in milliseconds?\nIf you’ve used ChatGPT, Gemini, or any modern AI assistant and wondered how it instantly locates the exact sentence or concept you’re asking about, the answer is a combination of two ideas: embeddings, which turn meaning into geometry, and vector databases, which search that geometry at scale. Together they solve the two things a raw large language model is genuinely bad at — it forgets (a fixed context window) and it hallucinates (it guesses plausibly when it doesn’t actually know). Retrieval-Augmented Generation (RAG) is the architecture that wires a vector database into an LLM so the model answers from retrieved, grounded facts instead of from memory alone.\nA basic RAG system just chops text into blocks and matches keywords — that’s enough for a demo, but it breaks down in production: wrong chunks get retrieved, exact terms get missed, and the model drowns in irrelevant context. Advanced RAG engineering is the set of techniques — hybrid search, reranking, smarter chunking and indexing, continuous evaluation — that closes that gap.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Day 3 — Vector Databases and Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L03/00_intro.html#vector-databases-and-advanced-rag-engineering-day-3",
    "href": "L03/00_intro.html#vector-databases-and-advanced-rag-engineering-day-3",
    "title": "Day 3 — Vector Databases and Advanced RAG Engineering",
    "section": "Vector Databases and Advanced RAG Engineering — Day 3",
    "text": "Vector Databases and Advanced RAG Engineering — Day 3\nSubtopics for today:\n\nIntroduction to vector databases and embeddings\nSemantic search and similarity search\nArchitecture of Retrieval-Augmented Generation (RAG)\nChunking strategies and document indexing\nBuilding production-ready RAG pipelines\nHands-on lab: building an advanced RAG system — chunking, hybrid search, reranking, and evaluation, over a small knowledge base\n\nRead the lessons — Vector Databases and Embeddings and Advanced RAG Engineering — then work through the lab notebook and the practice exercise.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Day 3 — Vector Databases and Advanced RAG Engineering"
    ]
  },
  {
    "objectID": "L02/01_streaming_and_event_driven_architectures.html",
    "href": "L02/01_streaming_and_event_driven_architectures.html",
    "title": "Streaming and Event-Driven Architectures",
    "section": "",
    "text": "Picture a busy online bookstore running on yesterday’s architecture: batch processing. Every click, search, and cart addition during the day gets written to a holding area and left untouched. Nothing happens to it until midnight, when an automated job bundles the entire day’s activity into one enormous file, ships it off to be processed, and — by 2 a.m. — the dashboards and inventory counts finally catch up to reality.\nThat works fine for a monthly financial audit. It fails badly the moment the business needs to react during the day. If a book goes viral on social media at 8 a.m. and sells out by 9, the inventory system won’t know it’s gone until the midnight batch runs — the store spends the rest of the day selling a book it doesn’t have. Batch processing has two structural flaws: insights are always stale (by definition, historical), and cramming 24 hours of computation into one nightly window strains the infrastructure right when it runs.\nStreaming replaces the midnight truck with a moving escalator. The instant a customer buys a book, that action becomes an individual, self-contained event — placed directly onto the escalator instead of a holding pen. Processing engines on the other side read, clean, and analyze each event within milliseconds of it happening. Data is never allowed to sit idle.\n\n\n\n\n\n\n\n\n\nBatch processing\nStreaming\n\n\n\n\nData unit\nA large, bounded file with a clear start and end\nAn individual, unbounded event\n\n\nLatency\nHours (the next scheduled run)\nMilliseconds\n\n\nCompute pattern\nOne heavy burst, usually overnight\nContinuous, spread evenly over time\n\n\nBest for\nHistorical reports, monthly audits, model retraining\nFraud detection, live personalization, operational alerting\n\n\nFailure mode\nA viral spike goes unnoticed for hours\nA single malformed event can be isolated without stopping the flow",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Streaming and Event-Driven Architectures"
    ]
  },
  {
    "objectID": "L02/01_streaming_and_event_driven_architectures.html#from-the-midnight-truck-to-the-moving-escalator",
    "href": "L02/01_streaming_and_event_driven_architectures.html#from-the-midnight-truck-to-the-moving-escalator",
    "title": "Streaming and Event-Driven Architectures",
    "section": "",
    "text": "Picture a busy online bookstore running on yesterday’s architecture: batch processing. Every click, search, and cart addition during the day gets written to a holding area and left untouched. Nothing happens to it until midnight, when an automated job bundles the entire day’s activity into one enormous file, ships it off to be processed, and — by 2 a.m. — the dashboards and inventory counts finally catch up to reality.\nThat works fine for a monthly financial audit. It fails badly the moment the business needs to react during the day. If a book goes viral on social media at 8 a.m. and sells out by 9, the inventory system won’t know it’s gone until the midnight batch runs — the store spends the rest of the day selling a book it doesn’t have. Batch processing has two structural flaws: insights are always stale (by definition, historical), and cramming 24 hours of computation into one nightly window strains the infrastructure right when it runs.\nStreaming replaces the midnight truck with a moving escalator. The instant a customer buys a book, that action becomes an individual, self-contained event — placed directly onto the escalator instead of a holding pen. Processing engines on the other side read, clean, and analyze each event within milliseconds of it happening. Data is never allowed to sit idle.\n\n\n\n\n\n\n\n\n\nBatch processing\nStreaming\n\n\n\n\nData unit\nA large, bounded file with a clear start and end\nAn individual, unbounded event\n\n\nLatency\nHours (the next scheduled run)\nMilliseconds\n\n\nCompute pattern\nOne heavy burst, usually overnight\nContinuous, spread evenly over time\n\n\nBest for\nHistorical reports, monthly audits, model retraining\nFraud detection, live personalization, operational alerting\n\n\nFailure mode\nA viral spike goes unnoticed for hours\nA single malformed event can be isolated without stopping the flow",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Streaming and Event-Driven Architectures"
    ]
  },
  {
    "objectID": "L02/01_streaming_and_event_driven_architectures.html#the-architecture-producers-broker-stream-processor",
    "href": "L02/01_streaming_and_event_driven_architectures.html#the-architecture-producers-broker-stream-processor",
    "title": "Streaming and Event-Driven Architectures",
    "section": "The architecture: producers, broker, stream processor",
    "text": "The architecture: producers, broker, stream processor\nMoving beyond batches means building an event-driven pipeline out of three layers that never talk to each other directly:\n\n\n\n\n\ngraph LR\n    P[\"Producers&lt;br&gt;apps, sensors, GPS trackers\"] --&gt; B[\"Streaming Broker&lt;br&gt;Kafka / Redpanda\"]\n    B --&gt; S[\"Stream Processor&lt;br&gt;Flink / Spark Structured Streaming\"]\n    S --&gt; D[\"Downstream sinks&lt;br&gt;Delta Lake, dashboards, AI models\"]\n\n\n\n\n\n\n\nProducers are whatever generates the live data — a mobile app tap, a factory sensor, a GPS ping. The instant something happens, the producer broadcasts it; it doesn’t wait for anyone to be listening.\nThe streaming broker is the central station built to absorb millions of simultaneous events without falling over. Tools like Apache Kafka or Redpanda act as an immutable, ordered logbook: events are caught in the exact order they arrived and held safely in line until a consumer reads them.\nThe stream processor — Apache Flink, or Spark Structured Streaming — sits actively over the moving feed and computes on the fly. It never asks “what was yesterday’s revenue?”; it continuously maintains the answer to “what is revenue as of this exact second?”\n\n\nWhy this matters\n\nRide-hailing (Uber/Careem). If ETA and fare calculation ran as a nightly batch, the app would be useless. Instead, your phone streams location continuously; the broker catches it, and a stream processor recomputes your ETA every second.\nFinancial fraud detection. A batch system discovers a stolen card hours after the fact. A streaming system consumes the swipe event, scores it for anomalies, and can freeze the account before the transaction even clears.\nInstant personalization. Streaming pipelines feed live clicks directly into recommendation models — click on three football clips and your next scroll already reflects it, instead of waiting for tomorrow’s batch job.\n\nIn short, moving beyond batches shifts infrastructure from a reactive state (analyzing history) to an active one (reacting to the present).",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Streaming and Event-Driven Architectures"
    ]
  },
  {
    "objectID": "L02/01_streaming_and_event_driven_architectures.html#event-driven-architectures-for-ai-systems",
    "href": "L02/01_streaming_and_event_driven_architectures.html#event-driven-architectures-for-ai-systems",
    "title": "Streaming and Event-Driven Architectures",
    "section": "Event-driven architectures for AI systems",
    "text": "Event-driven architectures for AI systems\nTraditional software mostly talks in synchronous request-response — standard REST APIs. Component A calls an AI service and sits completely idle, blocking a thread, until the model finishes computing an answer. That model worked fine when “the AI service” was a fast lookup. It breaks down once AI workloads mean heavy LLM inference, multi-agent orchestration, and continuous streaming data — request-response buckles under the combined weight of high latency and tight coupling between services.\nAn Event-Driven Architecture (EDA) flips the paradigm: instead of issuing direct commands, components publish and consume events — structured records stating that something happened — through a central broker. Nothing blocks. Producers fire an event and move on; consumers, including AI models, process it whenever they’re ready, and can scale independently based on how deep the backlog gets.\n\n\n\n\n\n\n\n\nDesign dimension\nTraditional request-response (REST)\nEvent-driven architecture (EDA)\n\n\n\n\nHandling LLM latency\nPoor — a connection stays open for seconds/minutes during inference, risking timeouts and wasted compute\nExcellent — the task is accepted instantly; inference happens asynchronously in the background\n\n\nSystem coupling\nTight — if the LLM provider goes down, the calling app crashes immediately\nLoose — the broker holds messages safely until the AI service is back\n\n\nResource scaling\nRigid — hard to scale compute dynamically for bursty query traffic\nElastic — a growing queue can trigger extra worker containers on demand\n\n\nExtensibility\nHard — a new feature means editing the core application\nEasy — hook a new consumer onto the existing broker topic, no other code touched\n\n\n\n\nCore architectural components\nAn event-driven AI system relies on four structural layers:\n\nEvent producers — applications, users, or databases that capture an action (a chat message, a file upload) and broadcast it as a structured event.\nThe event broker — the high-throughput backbone (Kafka, Redpanda) that receives, orders, and durably stores events without losing any.\nAI event consumers — independent workers that listen on specific topics, run AI processing (embeddings, LLM inference) on each event, and emit new event payloads downstream.\nDownstream storage sinks and output gateways — where processed AI output lands: a Delta Lake table, a vector database, or a live push back to a user interface.\n\nA minimal version of this pattern — a broker built from nothing more than asyncio.Queue per topic, a producer that never blocks on the consumer, and an AI-processing consumer reading off it — looks like this:\nclass MockEventBroker:\n    def __init__(self):\n        self.topics = {\n            \"raw_support_queries\": asyncio.Queue(),\n            \"processed_embeddings\": asyncio.Queue(),\n        }\n\n    async def publish(self, topic, message):\n        await self.topics[topic].put(message)\n\n    async def consume(self, topic):\n        return await self.topics[topic].get()\nThe producer calls publish() and immediately goes back to work — it never waits to see what the embedding or inference consumer does with the message. That decoupling is the entire point: swap the asyncio.Queue for a real Kafka topic and the architecture doesn’t change, only the durability and throughput guarantees do. The lab’s MockKafkaBroker (in Day2_Lab.ipynb) builds on exactly this idea, adding partitions and offsets to mirror real Kafka’s semantics.\nIn short: an event-driven architecture lets independent AI components — producers, embedding workers, inference agents — communicate by publishing and reacting to events instead of waiting idly on each other, which is what lets a production system absorb slow, unpredictable LLM latency without tipping over.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Streaming and Event-Driven Architectures"
    ]
  },
  {
    "objectID": "L01/01_lakehouse_architectures.html",
    "href": "L01/01_lakehouse_architectures.html",
    "title": "Warehouses, Lakes, and Lakehouses",
    "section": "",
    "text": "To understand where we are today, we have to look at how data storage evolved. Historically, organizations moved through three distinct eras.\n\n\n\n\n\ngraph LR\n    A[\"Enterprise Data Warehouse&lt;br&gt;1990s–2010s\"] --&gt; B[\"Data Lake&lt;br&gt;2010s–2020s\"]\n    B --&gt; C[\"Data Lakehouse&lt;br&gt;Present\"]\n\n\n\n\n\n\nThe Enterprise Data Warehouse (EDW) era (1990s–2010s). Companies stored clean, highly structured data in rigid SQL databases. It was excellent for generating basic business reports, but it was incredibly expensive to scale and completely incapable of handling unstructured data like audio, video, or massive JSON logs.\nThe Data Lake era (2010s–2020s). With the birth of Hadoop and cloud object storage (like Amazon S3), companies started dumping everything — raw, structured, and unstructured data — into a cheap storage pool. While scalable, these lakes lacked control. With no data validation or transactional safety, they quickly degraded into disorganized “data swamps.”\nThe Modern Data Lakehouse era (present). This is the current gold standard. A Lakehouse implements a specialized storage layer — open-source columnar frameworks like Delta Lake or Apache Iceberg — directly on top of cheap cloud storage. It gives you the best of both worlds: the ultra-low cost and flexibility of a Data Lake, combined with the strict reliability, schema enforcement, and ACID transactions of a traditional Data Warehouse.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Warehouses, Lakes, and Lakehouses"
    ]
  },
  {
    "objectID": "L01/01_lakehouse_architectures.html#the-architectural-shift-from-warehouses-to-lakehouses",
    "href": "L01/01_lakehouse_architectures.html#the-architectural-shift-from-warehouses-to-lakehouses",
    "title": "Warehouses, Lakes, and Lakehouses",
    "section": "",
    "text": "To understand where we are today, we have to look at how data storage evolved. Historically, organizations moved through three distinct eras.\n\n\n\n\n\ngraph LR\n    A[\"Enterprise Data Warehouse&lt;br&gt;1990s–2010s\"] --&gt; B[\"Data Lake&lt;br&gt;2010s–2020s\"]\n    B --&gt; C[\"Data Lakehouse&lt;br&gt;Present\"]\n\n\n\n\n\n\nThe Enterprise Data Warehouse (EDW) era (1990s–2010s). Companies stored clean, highly structured data in rigid SQL databases. It was excellent for generating basic business reports, but it was incredibly expensive to scale and completely incapable of handling unstructured data like audio, video, or massive JSON logs.\nThe Data Lake era (2010s–2020s). With the birth of Hadoop and cloud object storage (like Amazon S3), companies started dumping everything — raw, structured, and unstructured data — into a cheap storage pool. While scalable, these lakes lacked control. With no data validation or transactional safety, they quickly degraded into disorganized “data swamps.”\nThe Modern Data Lakehouse era (present). This is the current gold standard. A Lakehouse implements a specialized storage layer — open-source columnar frameworks like Delta Lake or Apache Iceberg — directly on top of cheap cloud storage. It gives you the best of both worlds: the ultra-low cost and flexibility of a Data Lake, combined with the strict reliability, schema enforcement, and ACID transactions of a traditional Data Warehouse.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Warehouses, Lakes, and Lakehouses"
    ]
  },
  {
    "objectID": "L01/01_lakehouse_architectures.html#core-architectural-principle-decoupling-compute-from-storage",
    "href": "L01/01_lakehouse_architectures.html#core-architectural-principle-decoupling-compute-from-storage",
    "title": "Warehouses, Lakes, and Lakehouses",
    "section": "Core architectural principle: decoupling compute from storage",
    "text": "Core architectural principle: decoupling compute from storage\nIn legacy systems, Compute (the processor that does the heavy lifting, calculating, and transforming) and Storage (the hard drives where the raw data sits) were physically bundled inside the same server box. If you ran out of processing power, you had to buy more storage. If you ran out of storage, you had to buy more processors. It was expensive and wasteful.\nA modern data architecture completely unties these two components so they can live in separate places and scale independently:\n\nThe storage layer. All your data — petabytes of text, numbers, images, video — is dumped into an ultra-cheap, infinitely expandable cloud storage “lake” (Amazon S3, Google Cloud Storage). It just sits there safely, requiring almost zero electricity or processing power just to hold the data, so it costs pennies.\nThe compute layer. Processing engines (Apache Spark, Databricks) live entirely separately. When a data scientist or an AI model needs to run a massive computation, a cluster of virtual computers instantly wakes up, hooks into the storage layer, pulls the exact data it needs, processes it in parallel, and delivers the answer.\n\n\nWhy this matters\n\nMassive cost savings. If your company collects data 24/7 but only runs heavy AI models or reports once a week, you don’t need thousands of expensive processors sitting idle all week. You pay a tiny fee to store the data, spin up 100 virtual computers for the 2 hours it takes to run your AI models, and then shut them down completely — you pay for compute by the second it actually runs.\nInfinite scalability. If a sudden business need requires analyzing 10 years of data by tomorrow morning, you don’t buy new hardware — you command 500 cloud computers to team up, attack the storage lake simultaneously, finish in minutes, and disappear.\nNo single point of failure. Because your data isn’t trapped inside the specific computer doing the work, a crashed compute server doesn’t lose or corrupt anything. The data is safe in the storage layer, and another compute server can instantly step in.\n\nIn short: decoupling compute from storage turns your data infrastructure from a rigid, expensive box into a flexible system where storage is your permanent memory bank, and compute is a powerful muscle you only flex — and pay for — exactly when you need it.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Warehouses, Lakes, and Lakehouses"
    ]
  },
  {
    "objectID": "L01/01_lakehouse_architectures.html#data-ingestion-the-revolution-of-elt-over-etl",
    "href": "L01/01_lakehouse_architectures.html#data-ingestion-the-revolution-of-elt-over-etl",
    "title": "Warehouses, Lakes, and Lakehouses",
    "section": "Data ingestion: the revolution of ELT over ETL",
    "text": "Data ingestion: the revolution of ELT over ETL\nBoth ETL and ELT move raw data from its sources (a mobile app, a website, a database) into a central storage system where data scientists and AI models can use it. The letters stand for:\n\nExtract — gathering the raw data from its source\nLoad — sending the data to its destination\nTransform — cleaning, formatting, and preparing the data\n\nThe revolution comes down to flipping the order of the last two steps.\nThe traditional way — ETL (Extract → Transform → Load). Because legacy database storage was expensive, engineers used a separate middle-tier server to clean, aggregate, and shrink the data before storing it, then pushed the pre-formatted, clean data into a rigid Data Warehouse.\nThe modern way — ELT (Extract → Load → Transform). Pull raw, messy data from all company sources and instantly dump it into a cloud data pool (Amazon S3, Google Cloud Storage, Snowflake) in its native, raw state — JSON, Parquet, CSV, no changes made. When analysts or AI models need the data, distributed compute engines (Apache Spark, dbt) transform and clean it inside the storage destination itself, on demand.\n\n\n\n\n\n\n\n\nFeature\nLegacy ETL\nModern ELT\n\n\n\n\nSpeed of ingestion\nSlow — data waits to be transformed before it can be saved\nUltra-fast — raw data is dumped into storage within seconds\n\n\nData preservation\nLossy — raw details are thrown away during transformation\nPreserved — 100% of raw historical data is saved forever\n\n\nFlexibility\nLow — changing the pipeline means redesigning the whole system\nHigh — a new format is just a new query against the raw data\n\n\nFailure recovery\nHard — a mid-pipeline failure can lose or strand data\nEasy — raw data stays safe in storage; fix the script and rerun\n\n\n\nIn short, the revolution of ELT over ETL is a shift from “clean it before you keep it” to “keep it all, and clean it whenever you need it.” By leveraging cheap cloud storage and massive compute, ELT ensures modern AI systems always have access to a rich, uninterrupted history of raw information.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Warehouses, Lakes, and Lakehouses"
    ]
  },
  {
    "objectID": "L01/01_lakehouse_architectures.html#acid-transactions",
    "href": "L01/01_lakehouse_architectures.html#acid-transactions",
    "title": "Warehouses, Lakes, and Lakehouses",
    "section": "ACID transactions",
    "text": "ACID transactions\nACID — Atomicity, Consistency, Isolation, Durability — is a fundamental set of four safety properties that guarantee database operations remain reliable and error-free, even during sudden crashes, power failures, or simultaneous user errors. A transaction is a sequence of multiple database actions treated as a single logical unit of work.\nWithout ACID guardrails, if a multi-step update hits a network glitch halfway through, the system can save a partial, broken file that permanently corrupts your records. ACID compliance protects the whole ecosystem from data corruption by treating data mutations with strict, absolute validation rules:\n\nAtomicity — an all-or-nothing rule. If a pipeline updating 100 rows fails on row 99, the entire operation is rolled back as if it never happened. No partial writes.\nConsistency — any data written strictly obeys predefined structural blueprints, schema enforcement, and database constraints before it’s committed to storage.\nIsolation — when multiple pipelines or users concurrently modify the same data lake, their transactions run independently without overlapping or cross-contaminating each other’s live files.\nDurability — once a transaction is successfully written, its changes are permanently recorded in non-volatile storage and transaction logs. The clean data is never lost, even after a catastrophic power failure.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Warehouses, Lakes, and Lakehouses"
    ]
  },
  {
    "objectID": "L01/01_lakehouse_architectures.html#delta-lake",
    "href": "L01/01_lakehouse_architectures.html#delta-lake",
    "title": "Warehouses, Lakes, and Lakehouses",
    "section": "Delta Lake",
    "text": "Delta Lake\nDelta Lake is an open-source storage management layer installed directly on top of a standard cloud data lake (Amazon S3, Google Cloud Storage) to turn it into a highly organized, reliable Data Lakehouse. Historically, data lakes let companies store massive volumes of raw files cheaply, but they lacked management controls and degraded into “data swamps” where simultaneous file updates could corrupt information. Delta Lake fixes this by wrapping raw files in a protective engine that brings database-style reliability, strict data quality controls, and fast SQL query performance to your existing cloud storage — all while keeping the lake’s ultra-low operational cost.\nThe engine achieves this through three core technical guardrails:\n\nACID compliance — an all-or-nothing gatekeeper. If a computer crashes mid-pipeline, a half-written file is safely rolled back instead of permanently corrupting the database.\nThe Delta Log — a hidden central ledger recording a perfect historical diary of every data mutation. This powers Time Travel, letting engineers instantly query or restore the dataset exactly as it looked at any point in the past.\nSchema enforcement — validates the structure of all incoming streams at the door, blocking malformed data before it can crash downstream ML models or dashboards.\n\nYou don’t choose between Delta Lake and ACID transactions — you use Delta Lake because it provides ACID transactions on top of cheap cloud object storage. It turns a chaotic, unsafe data lake into a reliable, enterprise-ready lakehouse.\n\n\n\n\n\n\n\n\n\nDelta Lake\nACID transactions\n\n\n\n\nWhat is it?\nAn open-source storage layer built on top of file systems\nA theoretical standard of data integrity and reliability\n\n\nImplementation\nYou install it, write code with it, and store files (Delta tables)\nA checklist of guarantees implemented by the storage engine\n\n\nMechanism\nParquet files + an immutable _delta_log file tracker\nWrite-ahead logs, locking mechanisms, isolation levels\n\n\nScope\nSpecific to the Big Data / Cloud Lakehouse ecosystem\nApplies globally to all databases, financial systems, storage engines\n\n\n\n\nThe Delta transaction log: how ACID actually works\n_delta_log/ is a hidden directory of JSON commit files at the root of every Delta table. Each commit is one atomic operation — add new Parquet files, remove old ones, never a partial write. The read protocol replays every commit from version 0 to reconstruct the current table state.\nWriters use optimistic concurrency control: they check for conflicting commits before finalizing a write. If two writers race on the same partition, one wins and one retries — no corruption, no data loss. This is how Delta guarantees ACID without a traditional database engine underneath it.\nAfter every 10 commits, Delta writes a checkpoint.parquet so readers don’t have to replay thousands of JSON files on startup — this is Delta Log compaction.\n\n\nMERGE / UPSERT — why overwrite is not enough\nA raw overwrite rewrites the entire table for one changed row: O(n) cost, no history, unsafe under concurrency. MERGE is the operation that makes atomic upserts possible:\nMERGE INTO target USING source ON condition\nWHEN MATCHED THEN UPDATE\nWHEN NOT MATCHED THEN INSERT\nDelta executes MERGE atomically: it scans only the affected files, writes new Parquet, and logs the operation. The typical use case is CDC (Change Data Capture) — applying inserts, updates, and deletes from a source system in one pass. Predicate pushdown lets Delta skip files whose min/max stats prove no matching rows exist, which is a massive speedup. Combine MERGE with timestamp columns for SCD Type 2 — full history without duplicates or missing records.\n\n\nOPTIMIZE and Z-ORDER BY — data skipping\nStreaming writes create thousands of tiny Parquet files — the small files problem — and each file read is a separate I/O operation that kills query scan time.\n\nOPTIMIZE table compacts small files into larger ones (default target ~1 GB) within each partition.\nZ-ORDER BY (col1, col2) co-locates related values in the same files using space-filling curves. Every file’s _delta_log entry records min/max stats per column, so a reader skips irrelevant files entirely — e.g. WHERE country = 'SA' reads under 5% of files if the table is Z-ordered by country.\n\nSchedule OPTIMIZE weekly or after bulk loads — running it too frequently has diminishing returns.\n\n\nVACUUM and Time Travel — storage management\nOld Parquet files stay on disk until explicitly cleaned; Delta just stops referencing them. That’s what makes Time Travel possible:\nSELECT * FROM table VERSION AS OF 5\nSELECT * FROM table TIMESTAMP AS OF '2024-01-01'\nVACUUM table RETAIN 168 HOURS deletes files older than the retention window (default 7 days). VACUUM is irreversible — deleted files cannot be restored from Delta. Never vacuum below 7 days if streaming readers are still active; they may reference older offsets. Longer retention means richer time travel history, at the cost of a higher object storage bill.\n\n\nSchema evolution — handling upstream changes\nBy default, Delta rejects writes with schema mismatches — this protects downstream queries from silent corruption. To opt in to schema evolution:\n\nspark.conf.set(\"spark.databricks.delta.schema.autoMerge.enabled\", \"true\")\nmergeSchema=True on write — adds new columns from the source, never drops existing ones.\noverwriteSchema=True — a full schema replacement, for intentional breaking changes only.\n\nSchema enforcement happens at write time, not read time — errors surface at the producer, not the consumer. Best practice: version schema changes, and notify consumers before adding NOT NULL columns or renaming fields.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Warehouses, Lakes, and Lakehouses"
    ]
  },
  {
    "objectID": "L01/01_lakehouse_architectures.html#lab-build-a-local-mini-lakehouse-using-delta-lake",
    "href": "L01/01_lakehouse_architectures.html#lab-build-a-local-mini-lakehouse-using-delta-lake",
    "title": "Warehouses, Lakes, and Lakehouses",
    "section": "Lab: build a local mini-lakehouse using Delta Lake",
    "text": "Lab: build a local mini-lakehouse using Delta Lake\nThe Day 1 lab notebook simulates a production environment using Apache Spark (via PySpark) and Delta Lake, demonstrating every concept above end to end: ACID transactions, schema enforcement, MERGE, OPTIMIZE/Z-ORDER, VACUUM, Time Travel, and schema evolution — on a real, executed run.\nWhat to look for in the output:\n\nThe Delta Lake engine automatically catches a malicious schema deviation and causes a controlled crash, demonstrating how it prevents your production Data Lake from degrading into a disorganized “data swamp.”\nThe history ledger maps out exact version numbers, timestamps, and operations (WRITE / APPEND), showing how easy it is to perform point-in-time rollbacks for auditing or ML reproducibility.\n\n\n\n\n\n\n\nNote\n\n\n\nRunning the Delta Lake part of this notebook locally on Windows requires a winutils.exe / HADOOP_HOME setup for PySpark — a standard PySpark-on-Windows limitation, not a bug in the notebook. It runs cleanly on Colab (Linux) or macOS/Linux with no extra setup.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Warehouses, Lakes, and Lakehouses"
    ]
  },
  {
    "objectID": "L01/00_intro.html",
    "href": "L01/00_intro.html",
    "title": "Day 1 — Foundations of Data Engineering for AI",
    "section": "",
    "text": "Before a model can be trained, before a feature vector can be computed, before making any prediction — data must move. It must be collected, cleaned, transformed, validated, and delivered reliably. Data engineering is that discipline.\nIf you look at the field of Artificial Intelligence and Data Science today, everyone talks about the glamorous side: training Large Language Models, running neural networks, and building autonomous agents. But here is the industry’s open secret: an AI model is only as smart as the data you feed it. Without data engineers building the invisible roads, pipes, and purification systems, data remains trapped, messy, and useless.\nThis course gives you a complete mental model of modern data engineering for AI systems — how pipelines are designed, where they break, what patterns experienced engineers use to make them reliable, and how the data layer connects to every other part of the ML system.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Day 1 — Foundations of Data Engineering for AI"
    ]
  },
  {
    "objectID": "L01/00_intro.html#modern-data-architectures-day-1",
    "href": "L01/00_intro.html#modern-data-architectures-day-1",
    "title": "Day 1 — Foundations of Data Engineering for AI",
    "section": "Modern Data Architectures — Day 1",
    "text": "Modern Data Architectures — Day 1\nToday we explore the Modern Data Engineering Ecosystem — the foundational infrastructure that powers all contemporary AI and analytical systems. We’ll look at how data engineering evolved from legacy on-premise systems into highly scalable, cloud-native pipelines built specifically to feed downstream AI and business-intelligence workloads.\nSubtopics for today:\n\nEvolution of Data Architectures — from Data Warehouses to Lakehouses\nCore building blocks — decoupling compute and storage\nData integration paradigms — ETL vs. ELT\nDelta Lake and ACID transactions\nHands-on lab: building a mini-lakehouse using Delta Lake\n\nRead the full Day 1 lesson, then work through the lab notebook.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Day 1 — Foundations of Data Engineering for AI"
    ]
  },
  {
    "objectID": "L02/00_intro.html",
    "href": "L02/00_intro.html",
    "title": "Day 2 — Real-Time Data Pipelines",
    "section": "",
    "text": "Yesterday we decoupled storage from compute and settled the ETL-vs-ELT question. Both of those still assumed a comfortable premise: data arrives, sits somewhere, and gets processed later. Today we break that premise.\nMost of the data an AI system actually cares about — a user’s click, a sensor reading, a credit-card swipe, a chat message arriving mid-conversation — is not useful “later.” A fraud model that flags a stolen card six hours after the purchase has already failed. A recommendation engine that updates your feed once a day feels broken compared to one that adapts on your very next scroll. Real-time data engineering is the discipline of treating data as a continuous, never-ending flow of individual events, and processing each one within milliseconds of it happening — not storing it to process in bulk tonight.\nThis matters even more once AI is in the loop. LLM inference is slow relative to a database query — a call can take seconds, not milliseconds — and multi-agent systems chain many of those calls together. A traditional synchronous request-response design blocks a whole thread on every one of them. Event-driven, streaming architectures are how production AI systems absorb that latency without falling over: producers publish events and move on, brokers hold the backlog safely, and consumers — including AI models — process at their own pace, scaling up or down independently.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Day 2 — Real-Time Data Pipelines"
    ]
  },
  {
    "objectID": "L02/00_intro.html#real-time-data-pipelines-day-2",
    "href": "L02/00_intro.html#real-time-data-pipelines-day-2",
    "title": "Day 2 — Real-Time Data Pipelines",
    "section": "Real-Time Data Pipelines — Day 2",
    "text": "Real-Time Data Pipelines — Day 2\nSubtopics for today:\n\nBatch vs. streaming — why “process it tonight” stops being good enough\nEvent-driven architectures for AI systems — producers, brokers, consumers\nKafka internals — partitions, offsets, and consumer groups\nStream processing patterns — tumbling/sliding/session windows, watermarks, and exactly-once delivery\nIngestion, transformation, and orchestration — the tools that wire it all together\nHands-on lab: a mock IoT streaming pipeline with windowed anomaly detection and a Delta Lake sink, plus a real Kafka producer/consumer round trip with a schema gate\n\nRead the lessons — Streaming and Event-Driven Architectures and Building Real-Time Pipelines — then work through the lab notebook.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Day 2 — Real-Time Data Pipelines"
    ]
  },
  {
    "objectID": "L02/02_building_real_time_pipelines.html",
    "href": "L02/02_building_real_time_pipelines.html",
    "title": "Building Real-Time Pipelines",
    "section": "",
    "text": "Building a production-grade streaming pipeline is a complete departure from batch engineering. Data is no longer a bounded file with a beginning and an end — it’s an unbounded dataset: a continuous, append-only series of events that never finishes arriving. Producers and consumers stay loosely coupled, talking only through the broker, never directly to each other. To design for that, engineers need to understand what actually happens inside the broker and the processor sitting on top of it.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Building Real-Time Pipelines"
    ]
  },
  {
    "objectID": "L02/02_building_real_time_pipelines.html#kafka-partitions-offsets-and-consumer-groups",
    "href": "L02/02_building_real_time_pipelines.html#kafka-partitions-offsets-and-consumer-groups",
    "title": "Building Real-Time Pipelines",
    "section": "Kafka: partitions, offsets, and consumer groups",
    "text": "Kafka: partitions, offsets, and consumer groups\nA Kafka topic isn’t a single log — it’s split into N ordered, immutable partitions, and every message is routed to exactly one of them. Within a partition, each message gets a monotonically increasing offset, which is simply its address: “message #4,213 in partition 2.”\nConsumers read as a consumer group: a set of workers that jointly consume a topic, with every partition assigned to exactly one member of the group at a time. That assignment is what sets the pipeline’s real throughput ceiling — adding consumers beyond the partition count gives zero extra parallelism, because there’s nothing left to hand them.\n\n\n\n\n\n\n\n\nConcept\nWhat it is\nWhy it matters\n\n\n\n\nPartition\nOne ordered, immutable log within a topic\nUnit of parallelism — more partitions, more concurrent consumers\n\n\nOffset\nA message’s position within its partition\nLets a consumer say exactly where it is\n\n\nConsumer group\nA set of consumers sharing a topic’s partitions\nEach partition goes to one member only\n\n\nOffset commit\nA consumer checkpoints its current position\nCrash recovery resumes from the last committed offset, not from zero\n\n\nRebalance\nPartition reassignment when a member joins or leaves\nKeeps the group’s load balanced automatically",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Building Real-Time Pipelines"
    ]
  },
  {
    "objectID": "L02/02_building_real_time_pipelines.html#window-types-tumbling-sliding-session",
    "href": "L02/02_building_real_time_pipelines.html#window-types-tumbling-sliding-session",
    "title": "Building Real-Time Pipelines",
    "section": "Window types: tumbling, sliding, session",
    "text": "Window types: tumbling, sliding, session\nA stream never “ends,” so aggregations (sums, averages, counts) need a boundary to close over. That boundary is a window, and the three common shapes solve different problems:\n\n\n\n\n\n\n\n\nWindow type\nShape\nUse case\n\n\n\n\nTumbling\nFixed-size, non-overlapping — every event belongs to exactly one window\nHourly sales totals — clean, non-overlapping reporting periods\n\n\nSliding\nFixed-size, overlapping — one event can appear in multiple windows\nRolling 5-minute average CPU load — overlap is needed to catch a spike right at a window boundary\n\n\nSession\nGap-based — the window closes after N seconds of inactivity; size varies per session\nClick-stream grouping — a user’s session ends after 30+ minutes idle\n\n\n\nThe lab’s StreamProcessor (in Day2_Lab.ipynb) implements both a tumbling window (one-second buckets, used for a per-second event count) and a sliding window (a rolling 3-second average temperature) side by side over the same event stream, so you can see both update independently as events arrive.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Building Real-Time Pipelines"
    ]
  },
  {
    "objectID": "L02/02_building_real_time_pipelines.html#watermarks-handling-late-arriving-data",
    "href": "L02/02_building_real_time_pipelines.html#watermarks-handling-late-arriving-data",
    "title": "Building Real-Time Pipelines",
    "section": "Watermarks: handling late-arriving data",
    "text": "Watermarks: handling late-arriving data\nNetwork delays mean an event timestamped T can physically arrive at the processor at T + 5 minutes. A watermark is a declared threshold that answers “how long am I willing to wait for a late event before I close the window and move on?” — in Spark Structured Streaming:\nstream.withWatermark(\"event_time\", \"5 minutes\")\nAnything that arrives after the watermark has already passed is either dropped or routed to a dead-letter side output. The trade-off is direct: a larger watermark buys more completeness at the cost of higher latency; a smaller one returns results faster but drops more late data. Set it from the observed p99 latency of the actual data source — not the theoretical worst case, which would make every window wait far longer than necessary.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Building Real-Time Pipelines"
    ]
  },
  {
    "objectID": "L02/02_building_real_time_pipelines.html#exactly-once-delivery-the-hard-problem",
    "href": "L02/02_building_real_time_pipelines.html#exactly-once-delivery-the-hard-problem",
    "title": "Building Real-Time Pipelines",
    "section": "Exactly-once delivery — the hard problem",
    "text": "Exactly-once delivery — the hard problem\nEvery streaming system has to pick a delivery guarantee, and the honest options trade off against each other:\n\n\n\n\n\n\n\n\nGuarantee\nBehavior\nCost\n\n\n\n\nAt-most-once\nFire and forget\nFastest, but silent data loss on failure — never acceptable for financial or audit data\n\n\nAt-least-once\nRetry on failure\nNo data loss, but duplicates are possible — the most common default\n\n\nExactly-once\nNo loss, no duplicates\nRequires coordination between producer, broker, and consumer\n\n\n\nKafka reaches exactly-once with enable.idempotence=true, the transactional producer API, and isolation.level=read_committed on the consumer side. Flink reaches it with two-phase-commit checkpointing, which only works if the sink itself supports transactions (Kafka, JDBC, Iceberg all do). Either way, exactly-once isn’t free — it adds roughly 20% throughput overhead — so pay for it only where a duplicate would cause real harm (a double-charged payment, a double-counted fraud alert), not for every topic in the system by default.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Building Real-Time Pipelines"
    ]
  },
  {
    "objectID": "L02/02_building_real_time_pipelines.html#ingestion-transformation-orchestration",
    "href": "L02/02_building_real_time_pipelines.html#ingestion-transformation-orchestration",
    "title": "Building Real-Time Pipelines",
    "section": "Ingestion, transformation, orchestration",
    "text": "Ingestion, transformation, orchestration\nEvery pipeline — batch or streaming — still breaks down into the same three stages; only the tools and latency budget change.\n\n\n\n\n\n\n\n\nStage\nPurpose\nTypical tools\n\n\n\n\nIngestion\nPull raw data from external systems into central storage\nApache Kafka, Debezium, Fivetran, AWS Kinesis\n\n\nTransformation\nClean, filter, and structure the raw data\nApache Spark, dbt, PySpark, Pandas\n\n\nOrchestration\nSchedule tasks, manage dependencies, handle errors across the whole system\nApache Airflow, Prefect, Dagster\n\n\n\nIngestion is the vacuum cleaner gathering raw material; transformation is the factory turning it into a usable product; orchestration is the conductor making sure every stage runs in the right order and recovers cleanly when something fails.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Building Real-Time Pipelines"
    ]
  },
  {
    "objectID": "L02/02_building_real_time_pipelines.html#lab-a-real-time-streaming-pipeline-with-kafka-and-delta-lake",
    "href": "L02/02_building_real_time_pipelines.html#lab-a-real-time-streaming-pipeline-with-kafka-and-delta-lake",
    "title": "Building Real-Time Pipelines",
    "section": "Lab: a real-time streaming pipeline with Kafka and Delta Lake",
    "text": "Lab: a real-time streaming pipeline with Kafka and Delta Lake\nThe Day 2 lab notebook puts every concept above into one runnable pipeline, in two parts.\nPart 1 — mock Kafka streaming pipeline. A MockKafkaBroker (built on asyncio.Queue, with partitions and offsets modeled explicitly) receives readings from three concurrent factory-sensor producers (CNC_MILL_01, ROBOTIC_ARM_02, ASSEMBLY_LINE_03). A StreamProcessor consumes them and applies all three concepts from this page at once: a tumbling window (per-second event counts), a sliding window (a 3-second rolling average), and a watermark (dropping anything older than 2 seconds). Anything over threshold gets published to an anomalies_sink topic, which a Delta Lake writer appends transactionally — the same ACID guarantees from Day 1, now on a live stream instead of a static file.\nPart 2 — a real Kafka round trip. The mock broker never touches the actual kafka-python client, and a real capstone pipeline needs schema validation at the edge. This part runs one real KafkaProducer → topic → KafkaConsumer round trip, gated by a Pydantic model (SensorReading) that rejects a deliberately malformed message (a temperature sent as the string \"not_a_number\") instead of letting it crash the consumer.\n\n\n\n\n\n\nNote\n\n\n\nRunning Part 1’s Delta sink locally on Windows requires a winutils.exe / HADOOP_HOME setup for PySpark — a standard PySpark-on-Windows limitation, not a bug in the notebook (see the same caveat on Day 1). It runs cleanly on Colab (Linux) or macOS/Linux with no extra setup. The notebook’s own saved output for Part 1 was captured from a real executed run on Colab.\n\n\n\n\n\n\n\n\nNote\n\n\n\nPart 2 needs a real, running Kafka broker on localhost:9092 — the notebook includes the commands to start a local single-broker Kafka directly in Colab (KRaft mode, no Zookeeper needed). The output saved in this notebook was captured from a real run in an environment with kafka-python and pydantic installed but no broker listening — which is exactly what you’ll see if you run this cell without first starting Kafka: the schema-gate code catches the failure and prints clear setup instructions instead of a raw stack trace, rather than crashing. To see the full accept/reject round trip (two valid readings accepted, one malformed reading rejected by SensorReading’s validator), start the broker first, per the notebook’s own instructions.",
    "crumbs": [
      "Home",
      "Day 2 — Real-Time Data Pipelines",
      "Building Real-Time Pipelines"
    ]
  },
  {
    "objectID": "L03/01_vector_databases_and_embeddings.html",
    "href": "L03/01_vector_databases_and_embeddings.html",
    "title": "Vector Databases and Embeddings",
    "section": "",
    "text": "An embedding is a numerical representation of data — text, an image, a sound clip — as a list of numbers called a vector. It’s an AI technique that converts a word, a sentence, or a whole document into a long string of numbers, and you can think of those numbers as coordinates on a massive, multidimensional map.\nImagine a map of the world: Cairo and Alexandria sit close together because they’re physically close. An embedding model does the same thing, but for meaning instead of geography. The words “King” and “Queen” land at nearly the same coordinates because they both represent royalty. “I’m feeling down” and “I am sad” sit right next to each other even though the two sentences don’t share a single word. Concretely, “King” might embed as [0.12, -0.45, 0.78, ..., 0.33] (384 numbers long, for a typical small embedding model) and “Queen” as [0.14, -0.43, 0.77, ..., 0.31] — the vectors are close because the meanings are close. Semantic similarity equals distance proximity.\nEmbeddings capture relationships, not just definitions. The classic word2vec example: in embedding space, \"Paris\" - \"France\" + \"Italy\" lands very close to \"Rome\". In short: an embedding turns a human concept into a mathematical location.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Vector Databases and Embeddings"
    ]
  },
  {
    "objectID": "L03/01_vector_databases_and_embeddings.html#what-is-an-embedding",
    "href": "L03/01_vector_databases_and_embeddings.html#what-is-an-embedding",
    "title": "Vector Databases and Embeddings",
    "section": "",
    "text": "An embedding is a numerical representation of data — text, an image, a sound clip — as a list of numbers called a vector. It’s an AI technique that converts a word, a sentence, or a whole document into a long string of numbers, and you can think of those numbers as coordinates on a massive, multidimensional map.\nImagine a map of the world: Cairo and Alexandria sit close together because they’re physically close. An embedding model does the same thing, but for meaning instead of geography. The words “King” and “Queen” land at nearly the same coordinates because they both represent royalty. “I’m feeling down” and “I am sad” sit right next to each other even though the two sentences don’t share a single word. Concretely, “King” might embed as [0.12, -0.45, 0.78, ..., 0.33] (384 numbers long, for a typical small embedding model) and “Queen” as [0.14, -0.43, 0.77, ..., 0.31] — the vectors are close because the meanings are close. Semantic similarity equals distance proximity.\nEmbeddings capture relationships, not just definitions. The classic word2vec example: in embedding space, \"Paris\" - \"France\" + \"Italy\" lands very close to \"Rome\". In short: an embedding turns a human concept into a mathematical location.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Vector Databases and Embeddings"
    ]
  },
  {
    "objectID": "L03/01_vector_databases_and_embeddings.html#what-is-a-vector-database",
    "href": "L03/01_vector_databases_and_embeddings.html#what-is-a-vector-database",
    "title": "Vector Databases and Embeddings",
    "section": "What is a vector database?",
    "text": "What is a vector database?\nA traditional database — a spreadsheet, a SQL table — is built for exact matches: look up a specific order ID, a specific phone number. Ask it to “find documents about a happy puppy” and it has to scan every row of text looking for those literal words, missing anything phrased as “a joyful young dog.” That’s slow, and it’s blind to meaning.\nA vector database is built differently. It stores the embeddings themselves — those long coordinate strings — and when you ask it a question, it doesn’t read the text at all. It converts your question into a coordinate, drops that point onto the map, and asks: what are the closest items surrounding this point? This is called similarity search or nearest neighbor search, and because it relies on geometry rather than reading text line by line, a vector database can search billions of documents in milliseconds.\n\n\n\n\n\ngraph LR\n    Q[\"Your question\"] --&gt; E[\"Embedding model\"]\n    E --&gt; V[\"Query vector\"]\n    V --&gt; DB[(\"Vector database&lt;br&gt;(indexed embeddings)\")]\n    DB --&gt; R[\"Nearest-neighbor matches\"]\n\n\n\n\n\n\nPurpose-built, a vector database exists to do three things: store millions or billions of embedding vectors, index them with algorithms like HNSW (Hierarchical Navigable Small World) so search stays fast at scale, and answer a query by embedding it with the same model used on the stored documents, then finding its nearest neighbors.\n\nHow the search actually runs, in three steps\n\nIndexing (preparation). Before any query arrives, the database builds a graph or clustering structure over every stored vector, so it never has to brute-force scan the whole collection later. Common index types: HNSW (a layered graph) and IVF (clusters vectors into buckets, like a phonebook divided by city).\nQuerying (search). Your text query is embedded with the same model used at indexing time, then the system asks which stored vectors are closest to that query vector, using a distance metric.\nRetrieval (result). The database returns the top-k most similar items — say, the top 5 chunks — which are then handed to the LLM in a RAG system to generate an answer.\n\n\n\nDistance metrics\n\n\n\n\n\n\n\n\nMetric\nWhat it measures\nBest for\n\n\n\n\nCosine similarity\nAngle between vectors (ignores magnitude)\nText embeddings, where document length shouldn’t dominate\n\n\nEuclidean distance\nStraight-line distance\nLow-dimensional, numeric data\n\n\nDot product\nMagnitude × direction\nWhen vector length matters (e.g., recommendation scores)\n\n\n\nCosine similarity is the default for text: it ranges from −1 (opposite meaning) to +1 (identical meaning), and two vectors pointing the same direction score as highly similar even if one document is much longer than the other.\n\n\nWhy this matters\nWithout vector databases and embeddings, LLMs have two structural flaws: they forget (a fixed context window can’t hold a company’s entire knowledge base) and they hallucinate (when unsure, they guess plausibly rather than admit ignorance). A vector database gives an LLM the equivalent of an open-book exam — instead of relying purely on what it memorized during training, it can look up the exact relevant passage first, then answer from that. This pairing is exactly what RAG (covered on the next page) is built on.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Vector Databases and Embeddings"
    ]
  },
  {
    "objectID": "L03/01_vector_databases_and_embeddings.html#document-indexing-two-different-layers",
    "href": "L03/01_vector_databases_and_embeddings.html#document-indexing-two-different-layers",
    "title": "Vector Databases and Embeddings",
    "section": "Document indexing: two different layers",
    "text": "Document indexing: two different layers\n“Indexing” gets used loosely, but in a vector database context it actually refers to two separate layers: how chunks are logically organized so the LLM gets the cleanest context, and how vectors are physically stored so the search itself is fast. Chunking (splitting documents into pieces) happens before indexing; indexing is what the database does with those chunks once it has them.\n\nLogical / data indexing strategies\nThese strategies decide how retrieved text connects back to its source, to maximize what the LLM actually sees:\n\nSingle-level (flat) indexing. The simplest style — every chunk is an independent vector, and the system fetches the top matches and passes them straight to the LLM.\nParent-child (hierarchical) indexing. Small child chunks (sentences) are indexed for precision search, but each is linked to a larger parent chunk (a paragraph). The system searches the precise child vectors but returns the more context-rich parent text to the LLM.\nHypothetical question indexing. An LLM pre-reads every document and generates several questions a user might plausibly ask about it. Those questions — not the raw text — are what gets embedded and indexed. A real user question then matches against a question that’s phrased the way users actually phrase things.\nSummary / document indexing. Instead of indexing raw paragraphs, an LLM writes a high-level summary of each whole document. The system searches summaries first to find the right document, then drills into that document’s specific chunks.\nKnowledge graph indexing. Extracts entities as nodes and relationships as edges, so the system can answer questions that require connecting facts scattered across separate documents — something flat chunk retrieval can’t do on its own.\n\n\n\nPhysical / algorithmic indexing strategies\nThese are the actual Approximate Nearest Neighbor (ANN) algorithms — trading a small amount of mathematical precision for a large gain in search speed:\n\n\n\n\n\n\n\n\n\n\n\nStrategy\nAccuracy\nSpeed\nMemory\nTraining required?\nUse case\n\n\n\n\nFlat (no index)\nExact\nVery slow — O(N)\nLow\nNo\nUnder 10K vectors\n\n\nIVF\nApproximate\nFast\nModerate\nYes (k-means)\n100K–10M vectors\n\n\nHNSW\nVery high\nVery fast\nHigh\nNo\nUp to millions, when accuracy matters\n\n\nPQ\nApproximate\nFast\nVery low\nYes (codebooks)\nMemory-constrained environments\n\n\nIVF+PQ\nGood\nVery fast\nVery low\nYes\nLarge-scale, 100M+ vectors\n\n\nScaNN\nVery high\nExtremely fast\nModerate\nYes\nPerformance-critical, Google-scale\n\n\n\n\nFlat brute-forces the exact distance to every vector — perfect accuracy, but only viable on tiny collections.\nIVF (Inverted File Index) clusters the vector space with k-means into “buckets” (Voronoi cells); a query only searches the bucket its own vector falls closest to, at the cost of some accuracy.\nHNSW builds a multi-layer graph, like an express subway line — the search takes big leaps across the top layer, then drops down layers to fine-tune the exact match. It’s what Pinecone, Weaviate, and ChromaDB use by default.\nPQ (Product Quantization) compresses each vector into small approximate sub-vector codes, shrinking memory by up to 95% — often paired with IVF as IVF-PQ for billion-scale collections.\nScaNN, Google’s index, optimizes anisotropic vector distances to favor vectors that align cleanly with the query’s direction, for hyper-scale production search.\n\nA simple decision path: under 10K vectors, use Flat (no training, exact). Need over 99% accuracy and have the RAM for it, use HNSW. Have over 10M vectors and limited RAM, use IVF+PQ. Operating at Google scale, consider ScaNN.\n\n\nTuning HNSW: precision vs. speed\nSince HNSW is the default in most production vector databases (Pinecone, Weaviate, ChromaDB), its three parameters are worth knowing by name:\n\nM — bi-directional links per node. Higher M means better recall but more memory (typical range: 16–64).\nef_construction — candidate list size while the index is being built. Higher means a better-quality index, but a slower build.\nef (search) — candidate list size at query time. Higher means better recall but slower search — this is the one you tune per query, at runtime.\n\nThe trade-off is concrete: ef=10 is fast but only around 85% recall; ef=200 is slow but around 99% recall. Rule of thumb: tune M and ef_construction once, at build time; tune ef per query’s latency budget, at runtime.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Vector Databases and Embeddings"
    ]
  },
  {
    "objectID": "L03/01_vector_databases_and_embeddings.html#semantic-search-and-similarity-search",
    "href": "L03/01_vector_databases_and_embeddings.html#semantic-search-and-similarity-search",
    "title": "Vector Databases and Embeddings",
    "section": "Semantic search and similarity search",
    "text": "Semantic search and similarity search\nKeyword search — the traditional approach — matches exact words or substrings: LIKE clauses, inverted indexes, boolean logic. Search for “canine therapy” on a site whose articles only say “dog healing,” and you get zero results, even though the content is exactly what you wanted. Keyword search is blind to meaning: it misses synonyms (“automobile” vs. “car”), misses context (is “Jaguar” an animal, a car, or an OS?), and breaks on polysemy — one word, many meanings.\nSemantic search understands the intent behind a query rather than its literal words. “Ways to calm a nervous dog” retrieves documents about “reducing canine anxiety” or “soothing an agitated pet” even though none of those exact words match. It handles synonyms, concept matching (“cold weather” ↔︎ “winter conditions”), and phrasing variation (“How do I…” ↔︎ “What is the method for…”). Semantic search is conceptual: it maps queries and documents into a shared meaning space.\nSimilarity search is the mathematical engine underneath semantic search — not a linguistic operation at all, but a geometric one: given a target vector, find every stored vector close to it by some distance metric. A useful non-text analogy: imagine a room of people, each with an age and a height. Ask “find people similar to Alice” and a keyword match fails outright (no one else is named Alice), but plotting age against height and measuring distance immediately shows that Bob (26, 162cm) is close to Alice (25, 160cm) while Carol (45, 170cm) is far. Vector databases do exactly this, just across hundreds of dimensions instead of two.\n\n\n\n\n\n\n\n\nFeature\nKeyword search (old school)\nSemantic / similarity search (modern)\n\n\n\n\nHow it searches\nScans literal strings of text characters\nConverts the query to a vector; finds nearest-neighbor vectors\n\n\nWhat it finds\nOnly documents containing the exact words\nDocuments using different words for the same concept\n\n\nFlaw\nMisses relevant documents that use different vocabulary\nNeeds more compute and a purpose-built vector database\n\n\n\n\nSummary\n\n\n\n\n\n\n\nConcept\nExplanation\n\n\n\n\nEmbedding\nA list of numbers (vector) that captures the meaning of data\n\n\nVector database\nA database optimized to search millions of embeddings by similarity, not exact match\n\n\nDistance metric\nA mathematical way to measure similarity (cosine, Euclidean, dot product)\n\n\nIndex (e.g., HNSW)\nA pre-built structure that speeds up nearest-neighbor search dramatically\n\n\nUse case\nPowering LLM retrieval (RAG), semantic search, recommendations\n\n\n\nThe next lesson builds on all of this to cover chunking, the full RAG architecture, and the production patterns that turn a prototype retrieval demo into something reliable enough to ship. The lab notebook puts an HNSW-backed ChromaDB index, embeddings, and similarity search to work end to end.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Vector Databases and Embeddings"
    ]
  },
  {
    "objectID": "L04/00_intro.html",
    "href": "L04/00_intro.html",
    "title": "Day 4 — Data Quality, Governance, and Lineage",
    "section": "",
    "text": "By Day 3 your pipelines move data — ingested, landed in bronze/silver/gold, served to a RAG index or a model. But moving data and moving trustworthy data are different problems. A pipeline can run green every single day, throw no exceptions, miss no SLA, and still be quietly wrong: a schema drifts, a source starts sending 25% nulls in a column nobody’s watching, a duplicate customer row skews a training set. Nothing crashes. The dashboard just starts lying, and the model just starts learning the wrong thing.\nToday is about closing that gap. We look at the three pillars that turn a pipeline that merely runs into one that can be trusted, audited, and debugged:\nNone of these three work in isolation. A validation rule without governance has no owner to alert when it fires. A governance policy without lineage can’t tell you what a schema change will actually break. Together they turn raw, ungoverned data into an audited, secure, reliable asset — the same asset an AI system needs in order to be trusted with real decisions.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Day 4 — Data Quality, Governance, and Lineage"
    ]
  },
  {
    "objectID": "L04/00_intro.html#subtopics-for-today",
    "href": "L04/00_intro.html#subtopics-for-today",
    "title": "Day 4 — Data Quality, Governance, and Lineage",
    "section": "Subtopics for today",
    "text": "Subtopics for today\n\nData quality principles, validation techniques, and data contracts\nAutomated data governance frameworks\nLineage and data observability\nEnterprise governance policies\nHands-on lab: automated data quality gates on a real, messy e-commerce dataset\n\nRead Data Quality: Principles, Validation, and Contracts, then Automated Governance and Enterprise Policy, then Lineage and Data Observability, and finish with the lab notebook.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Day 4 — Data Quality, Governance, and Lineage"
    ]
  },
  {
    "objectID": "L04/02_governance_frameworks.html",
    "href": "L04/02_governance_frameworks.html",
    "title": "Automated Governance and Enterprise Policy",
    "section": "",
    "text": "If data quality is about catching bad records, governance is about controlling who may touch data at all, and under what rules. Historically that meant a written policy document nobody re-read after onboarding. Increasingly, it means code: automated governance frameworks that enforce policy in real time as data moves through a pipeline, sitting underneath the human, legal enterprise governance policies that actually define what the rules are.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Automated Governance and Enterprise Policy"
    ]
  },
  {
    "objectID": "L04/02_governance_frameworks.html#from-static-rules-to-an-automated-control-plane",
    "href": "L04/02_governance_frameworks.html#from-static-rules-to-an-automated-control-plane",
    "title": "Automated Governance and Enterprise Policy",
    "section": "From static rules to an automated control plane",
    "text": "From static rules to an automated control plane\nAutomated data governance is the shift from static, written-down rules to dynamic, machine-learning-driven software that monitors, classifies, and protects data continuously as it flows. Built on platforms like Collibra, Alation, Informatica CLAIRE, or open-source engines like Egeria, a modern framework operates as a control plane across four layers.\n\n\n\n\n\ngraph TD\n    A[\"1. Automated Discovery & AI Classification\"] --&gt; B[\"2. Live Metadata Sync & Active Catalogs\"]\n    B --&gt; C[\"3. Dynamic Policy & Real-Time Access Control\"]\n    C --&gt; D[\"4. Automated Data Lineage — the paper trail\"]\n\n\n\n\n\n\n\n1. Automated discovery and AI classification\nYou cannot govern data you don’t know exists, and enterprises routinely have thousands of tables scattered across lakes, warehouses, and production databases. Machine learning models continuously scan storage environments, reading structural patterns, regex indicators, and value distributions instead of waiting for a human to label each column. The outcome: sensitive data — PII, credit-card numbers (PCI), medical data (HIPAA) — gets automatically flagged and tagged, with encryption or masking applied instantly.\n\n\n2. Live metadata synchronization and active catalogs\nMetadata — who owns a table, when it last changed, its schema — used to live in a hand-maintained data dictionary that was obsolete within weeks. Automated frameworks capture schema changes and update frequencies as they happen, building an always-accurate active catalog. If an engineer changes a column from an integer to a string, the catalog reflects that instantly, enterprise-wide.\n\n\n3. Dynamic policy and real-time access control\nTraditional security relies on static roles (“HR managers can see this table”). Modern frameworks shift to Attribute-Based Access Control (ABAC), enforced directly inside the data pipeline: the system reads an asset’s metadata tag and cross-references it against compliance rules on the fly. If an analyst queries a table with customer history, the policy engine can mask just the social_security_number column while leaving the rest of the row available — security and usability at the same time.\n\n\n4. Automated data lineage — the paper trail\nWhen an executive looks at a dashboard number and asks “where did this come from, and can I trust it?”, lineage is the answer. Automated lineage differs from the legacy approach in every dimension that matters:\n\n\n\n\n\n\n\n\nFeature\nLegacy lineage\nAutomated lineage\n\n\n\n\nMapping method\nDevelopers manually draw diagrams in Visio or map paths in spreadsheets.\nA parsing engine reads SQL query history, dbt transformations, and orchestrator logs automatically.\n\n\nAccuracy\nProne to human error and missing updates.\nAccurate up to the second.\n\n\nImpact analysis\nGuessing what breaks if a schema changes.\nInstant visual maps of every downstream dashboard or model that depends on a table.\n\n\n\nWe go deeper on the mechanics of lineage — including the concrete OpenLineage event format — in the next lesson.\nIn short: automated governance frameworks use automation and AI to manage quality, security, compliance, and monitoring across a modern data estate with far less manual overhead than the legacy, spreadsheet-driven approach.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Automated Governance and Enterprise Policy"
    ]
  },
  {
    "objectID": "L04/02_governance_frameworks.html#enterprise-governance-policies-the-human-layer",
    "href": "L04/02_governance_frameworks.html#enterprise-governance-policies-the-human-layer",
    "title": "Automated Governance and Enterprise Policy",
    "section": "Enterprise governance policies — the human layer",
    "text": "Enterprise governance policies — the human layer\nIf automated frameworks are the code enforcing rules in real time, enterprise governance policies are the actual laws, ethical guardrails, and corporate boundaries themselves — the strategic intent of the C-suite and legal department, defining how data must be handled, protected, and audited to survive in a regulated global economy. Enterprise governance policies more broadly are the formal rules covering how an organization manages operations, data, technology, security, employees, and compliance — ensuring the organization runs securely, efficiently, ethically, legally, and consistently.\n\nThe four cornerstones of enterprise governance policy\n1. Regulatory compliance policies — the legal mandates. Non-negotiable rules dictated by governments; violating them risks massive fines, lawsuits, and executive liability.\n\nData sovereignty — data belonging to a country’s citizens must be physically stored and processed within that country’s borders (e.g. European data staying in the EU).\nRegional mandates — GDPR (Europe, user deletion rights), CCPA/CPRA (California, consumer privacy), HIPAA (US) or MDR (Europe) for medical data.\n\n2. Data privacy and access control policies — the security perimeter. Access follows the Principle of Least Privilege (PoLP): an employee gets exactly the access their job requires, nothing more.\n\nRBAC/ABAC — strict mandates on who can view what, e.g. “only HR directors with Level 3 clearance may view unmasked salary data.”\nClassification schemes — every document or stream is labeled Public, Internal Only, Confidential, or Restricted.\n\n3. Information Lifecycle Management (ILM) policies. Storing every byte forever is expensive, so ILM policies govern how data ages:\n\n\n\n\n\n\n\n\nPolicy phase\nCorporate mandate\nAction taken\n\n\n\n\nActive retention\nKeep hot, transactional data available for live reporting.\nStored in high-speed warehouses (Snowflake, BigQuery) for up to 2 years.\n\n\nArchival rules\nRetain older, rarely-accessed but legally required data.\nMoved to cold storage (e.g. AWS Glacier) for 7–10 years.\n\n\nDefensible deletion\nDestroy data once its legal retention window expires.\nAutomated hard-deletion and cryptographic shredding.\n\n\n\n4. Responsible AI and generative AI governance policies. With LLMs and RAG systems now embedded in corporate workflows, companies have had to draft an entirely new policy layer: the “Shadow AI” blockade — employees are forbidden from pasting corporate code, proprietary designs, or customer PII into public consumer LLMs. A confidential spreadsheet uploaded to an external model to “make a quick chart” can be absorbed into that vendor’s training loop — a catastrophic, irreversible data breach.\n\n\nWhy this matters\nAutomated governance frameworks and enterprise governance policies are two halves of the same system: policy defines what is allowed, and the automated control plane makes that definition actually happen, continuously, without depending on every engineer remembering the rulebook. Neither one alone is sufficient — a policy with no enforcement is a suggestion, and enforcement code with no policy behind it has no legal or ethical grounding to enforce.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Automated Governance and Enterprise Policy"
    ]
  },
  {
    "objectID": "L05/00_intro.html",
    "href": "L05/00_intro.html",
    "title": "Day 5 — Architecture Integration and Final Project",
    "section": "",
    "text": "For four days you built the pieces one at a time: a lakehouse that stores data reliably (Day 1), a streaming layer that gets it there in real time (Day 2), a vector index and RAG pipeline that lets an AI system reason over it (Day 3), and a quality/governance/lineage layer that keeps the whole thing trustworthy and auditable (Day 4). Each of those days was deliberately self-contained — its own notebook, its own tools, its own narrow problem.\nToday has no new tool to learn. Day 5 is where those four pieces stop being four separate labs and become one system. The technical question changes from “how does Kafka work” or “how does a vector index work” to “how do these things hand data to each other, in what order, and what happens when one of them fails or flags bad data.” That’s a systems-design question, not a new-library question — which is exactly the skill a production AI platform actually demands, and exactly what the capstone grades.\nThe second half of today is the final project: a single starter notebook that already runs end-to-end using lightweight stand-ins for every one of the week’s real tools, and your team’s job — in teams of three — is to replace those stand-ins with the real Kafka, Delta Lake, Great Expectations, and vector-DB code you already built earlier in the week.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "Day 5 — Architecture Integration and Final Project"
    ]
  },
  {
    "objectID": "L05/00_intro.html#subtopics-for-today",
    "href": "L05/00_intro.html#subtopics-for-today",
    "title": "Day 5 — Architecture Integration and Final Project",
    "section": "Subtopics for today",
    "text": "Subtopics for today\n\nThe unified reference architecture — how ingestion, storage, quality, governance, and RAG serving connect into one platform\nThe recommended technology stack for each architectural layer\nAI infrastructure orchestration — coordinating compute, workflows, models, and data at platform scale (Airflow DAGs and beyond)\nThe final capstone project — structure, the team extension checklist, and the grading rubric\nHands-on lab: the Day 5 capstone starter — a runnable five-module pipeline your team extends into the real thing\n\nRead The Unified Architecture, then The Final Project, then work through the lab notebook — it is the starting point for your capstone submission, not just a demo to read.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "Day 5 — Architecture Integration and Final Project"
    ]
  },
  {
    "objectID": "L05/02_final_project.html",
    "href": "L05/02_final_project.html",
    "title": "The Final Project",
    "section": "",
    "text": "All participants complete this as their final project, in teams of three. Day5_Lab.ipynb is your starter template — a five-module pipeline that already runs end to end, on purpose, so you can verify your environment before extending it. Your team’s actual deliverable is to replace its lightweight stand-ins with the real tools from the week.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Final Project"
    ]
  },
  {
    "objectID": "L05/02_final_project.html#the-assignment",
    "href": "L05/02_final_project.html#the-assignment",
    "title": "The Final Project",
    "section": "",
    "text": "All participants complete this as their final project, in teams of three. Day5_Lab.ipynb is your starter template — a five-module pipeline that already runs end to end, on purpose, so you can verify your environment before extending it. Your team’s actual deliverable is to replace its lightweight stand-ins with the real tools from the week.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Final Project"
    ]
  },
  {
    "objectID": "L05/02_final_project.html#what-the-starter-already-does",
    "href": "L05/02_final_project.html#what-the-starter-already-does",
    "title": "The Final Project",
    "section": "What the starter already does",
    "text": "What the starter already does\nThe notebook wires together a DAG of five modules over a synthetic e-commerce order stream:\n\n\n\n\n\n\n\n\nModule\nStage\nWhat the starter uses\n\n\n\n\n1\nIngestion\nMockKafka — an in-memory producer/topic/offset simulator\n\n\n2\nStorage\nBronze / Silver / Gold Lakehouse, written with pandas + Parquet\n\n\n3\nQuality Gate\nFive hand-rolled DAMA-style checks (completeness, accuracy, uniqueness, validity, timeliness)\n\n\n4\nServing\nA RAG pipeline over the Gold layer, using an in-memory ChromaDB collection and sentence-transformers\n\n\n5\nOrchestration\nA DAG executor that runs modules 1–4 in dependency order and emits JSON lineage events\n\n\n\nIt’s deliberately built from lightweight stand-ins — MockKafka instead of a real broker, pandas/Parquet instead of Delta Lake, hand-rolled checks instead of Great Expectations — so it runs anywhere with no external services and you can confirm your environment works before touching real infrastructure.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Final Project"
    ]
  },
  {
    "objectID": "L05/02_final_project.html#whats-actually-graded-the-extension-checklist",
    "href": "L05/02_final_project.html#whats-actually-graded-the-extension-checklist",
    "title": "The Final Project",
    "section": "What’s actually graded: the extension checklist",
    "text": "What’s actually graded: the extension checklist\nYour team’s job is to replace each stand-in with the real library you already used earlier in the week. This checklist is also printed at the end of every run of the notebook:\n\n\n\nReplace\nWith\nReference implementation\n\n\n\n\nMockKafka\na real kafka-python producer/consumer\nDay 2’s Kafka lab\n\n\npandas Parquet writes\nPySpark + Delta Lake\nDay 1’s Delta Lake lab\n\n\nthe five manual checks\nGreat Expectations checkpoints\nDay 4’s quality gate lab\n\n\nchromadb.Client() (in-memory)\na persistent Chroma instance or Pinecone\nDay 3’s RAG lab\n\n\nmodule5_orchestrate()’s task calls\ntasks wrapped in a real Airflow DAG\nOrchestrating the platform\n\n\n— (new)\nan OpenLineage HTTP emitter pointing at a Marquez server\nDay 4’s lineage lab\n\n\n— (new)\na recorded 10-minute demo covering all five modules\n—\n\n\n\nIf your team gets stuck on any single item, that day’s lab notebook has a working reference call for the real library — the starter’s mock exists specifically so you can see the shape of what needs replacing before you go find the real API.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Final Project"
    ]
  },
  {
    "objectID": "L05/02_final_project.html#deliverables-and-rubric",
    "href": "L05/02_final_project.html#deliverables-and-rubric",
    "title": "The Final Project",
    "section": "Deliverables and rubric",
    "text": "Deliverables and rubric\n\n\n\n\n\n\n\n\nDeliverable\nRequirement\nPoints\n\n\n\n\n1. Ingestion layer\nKafka producer + consumer with schema validation\n20\n\n\n2. Delta Lakehouse\nBronze/Silver/Gold zones with MERGE and schema enforcement\n25\n\n\n3. RAG pipeline\nChunking, embedding, vector index, hybrid search with reranking\n25\n\n\n4. Orchestration\nAirflow DAG connecting all modules end-to-end\n15\n\n\n5. Quality gate\nGreat Expectations suite + OpenLineage events emitted\n15\n\n\nTotal\n\n100\n\n\n\nSubmission is a GitHub repository with a README documenting the architecture and how to run it.\n\n\n\n\n\n\nNote\n\n\n\nThis mirrors the course’s standing capstone rubric exactly (100 points, ingestion / lakehouse / RAG / orchestration / quality-and-lineage), because Day 5’s final project is the capstone submission for this course — not a separate practice exercise.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Final Project"
    ]
  },
  {
    "objectID": "L05/02_final_project.html#framing-your-submission",
    "href": "L05/02_final_project.html#framing-your-submission",
    "title": "The Final Project",
    "section": "Framing your submission",
    "text": "Framing your submission\nThe deck also sketches four illustrative real-world scenarios this same five-layer pattern maps onto. Your team isn’t required to build one of these specifically — the actual deliverable is the checklist above applied to the starter’s e-commerce order dataset — but if your README benefits from a concrete “who would use this and why,” picking one of these framings (and its named integrations, as a hint at what “production-grade” looks like for that domain) is a reasonable way to motivate the architecture you built, without changing what’s graded.\n\n\n\n\n\n\n\n\nIdea\nDescription\nKey integrations\n\n\n\n\n1. Real-time customer support intelligence\nIngest live support tickets, chat transcripts, and knowledge-base articles through streaming pipelines; quality-gate for low-quality responses and contradictions; auto-update vector indexes; give agents real-time answer suggestions with citation tracking.\nFlink streaming, Great Expectations validation, Qdrant, hybrid search, cross-encoder reranking, audit logging\n\n\n2. Healthcare clinical-trial matcher\nIngest clinical trial data from CT.gov, PubMed, and internal databases through validated pipelines; apply HIPAA governance (no PHI in the vector DB); let researchers query matching trials with evidence trails and contradiction alerts.\nSpark batch processing, FHIR validation, ClinicalBERT embeddings, Qdrant with HIPAA config, cross-encoder reranking, per-query audit trail\n\n\n3. Academic research aggregator with citation network\nIngest papers from arXiv, PubMed, and university repositories; build a real-time citation graph where each chunk knows its references; flag retracted papers; answer with citation networks via GraphRAG.\nArgo workflows, a graph + vector DB (Neo4j + Milvus), GraphRAG, citation-based reranking, retraction detection, license-compliance governance\n\n\n4. E-commerce real-time product Q&A\nIngest product catalogs, reviews, and support tickets via CDC from production databases; filter spam and detect sentiment; auto-update vector indexes within seconds of a product change; answer with live inventory awareness.\nDebezium (PostgreSQL CDC), Feast feature store, Qdrant, hybrid search + exact product-code match, LLM with tool use, customer PII governance",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Final Project"
    ]
  },
  {
    "objectID": "L05/02_final_project.html#setup-and-running",
    "href": "L05/02_final_project.html#setup-and-running",
    "title": "The Final Project",
    "section": "Setup and running",
    "text": "Setup and running\npip install -r requirements.txt\nOnly install the extension dependencies for the specific real tools your team is integrating (Kafka, Delta Lake, Great Expectations, Airflow, etc.) — you don’t need all of them at once to get started. Open Day5_Lab.ipynb and run all cells top to bottom; the first code cell installs the starter’s own dependencies via !pip install.\nOutput lands in:\n\n./lakehouse/bronze/ — raw ingested records\n./lakehouse/silver/ — cleansed, validated records\n./lakehouse/gold/ — revenue aggregation by country\n./lineage_events/ — the pipeline’s JSON audit trail\n\n\n\n\n\n\n\nNote\n\n\n\nModule 4’s SentenceTransformerEmbeddingFunction downloads the all-MiniLM-L6-v2 model from the Hugging Face Hub the first time it runs — this needs internet access on that first run (in Colab or locally); the model is cached afterward and subsequent runs are offline. On Windows, the Hugging Face cache also falls back to a non-symlinked copy of the model files instead of symlinks, which just uses a little more disk space — it does not affect correctness.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Final Project"
    ]
  },
  {
    "objectID": "L05/02_final_project.html#standing-project-requirements-every-sdaia-academy-capstone",
    "href": "L05/02_final_project.html#standing-project-requirements-every-sdaia-academy-capstone",
    "title": "The Final Project",
    "section": "Standing project requirements — every SDAIA Academy capstone",
    "text": "Standing project requirements — every SDAIA Academy capstone\nOn top of this course’s rubric, every project in every SDAIA Academy program must additionally satisfy the following, and it’s part of how the project is evaluated:\n\nEvery trainee must create and activate a GitHub account, if they don’t already have one.\nThe project must be uploaded to GitHub, kept documented, and continuously updated.\nThe repository must include:\n\nA clear, comprehensive project description\nA professional README — the project idea plus how to run/use it\nProper technical documentation\nGood Git version-control practices — meaningful, incremental commits; a sane repository structure; a .gitignore excluding secrets and generated files\nA statement of which training program the project was completed under (program name and cohort dates)\nA link to SDAIA Academy’s GitHub — https://github.com/SDAIAAcademy — where relevant\n\n\nEncouraged, though not scored: support outstanding Saudi projects on GitHub — star high-quality repositories, follow Saudi accounts and repositories, contribute to open source, engage via forks, pull requests, and issues, and share standout projects with the wider tech community.",
    "crumbs": [
      "Home",
      "Day 5 — Architecture Integration and Final Project",
      "The Final Project"
    ]
  },
  {
    "objectID": "L01/Day1_Lab.html",
    "href": "L01/Day1_Lab.html",
    "title": "Lab: Mini-Lakehouse with Delta Lake",
    "section": "",
    "text": "This single notebook covers everything in today’s lab:\n\nETL vs ELT, and Warehouse vs Lake vs Lakehouse — a runnable comparison of the three ingestion/storage patterns named in the course objectives.\nMini-Lakehouse with Delta Lake — a hands-on PySpark + Delta Lake demo proving ACID transactions, schema enforcement, time travel, OPTIMIZE/Z-ORDER, and schema evolution.\n\n\n!pip install pyspark==3.5.0 delta-spark==3.2.0\n\n\n\nLoads the same raw e-commerce order batch three different ways so the contrast between the three patterns is visible in the output, not just described in words: ETL (clean-before-load, warehouse style), ELT (load-before-clean, lake style), and the Lakehouse pattern (raw Bronze landing + governed Silver transform) that Part 2 then builds on with Delta Lake.\n\nimport os\nimport shutil\n\nimport pandas as pd\n\nRAW_ORDERS = [\n    {\"order_id\": \"ORD001\", \"user_id\": \"User_A\", \"product\": \"Laptop\", \"price\": \"1200.00\", \"quantity\": \"1\"},\n    {\"order_id\": \"ORD002\", \"user_id\": \"User_B\", \"product\": \"Mouse\", \"price\": \"25.5\", \"quantity\": \"2\"},\n    {\"order_id\": \"ORD003\", \"user_id\": \"User_C\", \"product\": None, \"price\": \"75.00\", \"quantity\": \"1\"},  # bad row: missing product\n    {\"order_id\": \"ORD004\", \"user_id\": \"User_A\", \"product\": \"Headphones\", \"price\": \"not_a_number\", \"quantity\": \"1\"},  # bad row: corrupt price\n]\n\n\ndef clean_and_validate(rows):\n    \"\"\"Shared cleaning rule used by the ETL path up front, and by the ELT/Lakehouse paths later.\"\"\"\n    good, rejected = [], []\n    for row in rows:\n        if not row[\"product\"]:\n            rejected.append((row, \"missing product\"))\n            continue\n        try:\n            price = float(row[\"price\"])\n        except ValueError:\n            rejected.append((row, \"corrupt price\"))\n            continue\n        good.append({**row, \"price\": price, \"quantity\": int(row[\"quantity\"])})\n    return good, rejected\n\n\ndef etl_style_warehouse_load():\n    \"\"\"\n    ETL (\"Transform\" happens BEFORE \"Load\"):\n    Classic data-warehouse pattern. Bad rows are cleaned/rejected in-flight,\n    by the ingestion job itself, before anything ever lands in storage.\n    Storage only ever sees clean, schema-conforming rows.\n    \"\"\"\n    print(\"\\n=== ETL (transform-before-load)  -  Data Warehouse style ===\")\n    good, rejected = clean_and_validate(RAW_ORDERS)\n    print(f\"Rows accepted into the warehouse table: {len(good)}\")\n    print(f\"Rows rejected before ever reaching storage: {len(rejected)}\")\n    for row, reason in rejected:\n        print(f\"  dropped {row['order_id']}: {reason}\")\n    warehouse_table = pd.DataFrame(good)\n    print(warehouse_table)\n    print(\"Trade-off: storage is always clean, but the transform logic is baked into the\")\n    print(\"ingestion job  -  changing a business rule means re-running ingestion from source.\")\n    return warehouse_table\n\n\ndef elt_style_lake_load():\n    \"\"\"\n    ELT (\"Load\" happens BEFORE \"Transform\"):\n    Classic data-lake pattern. Everything lands as-is, including bad rows  - \n    there is no schema or quality gate at write time. Transformation is a\n    separate, later step run against whatever was dumped.\n    \"\"\"\n    print(\"\\n=== ELT (load-before-transform)  -  Data Lake style ===\")\n    raw_table = pd.DataFrame(RAW_ORDERS)\n    print(f\"Rows landed in the raw lake zone (no gate at all): {len(raw_table)}\")\n    print(raw_table)\n    print(\"Transforming later, on read, against whatever is sitting in the lake...\")\n    good, rejected = clean_and_validate(RAW_ORDERS)\n    print(f\"Only discovered at transform time that {len(rejected)} row(s) were bad  - \")\n    print(\"they already consumed storage and could have been queried by someone else first.\")\n    print(\"Trade-off: ingestion is fast and never blocks on data quality, but the raw zone\")\n    print(\"can silently become a 'data swamp' if nothing ever comes back to clean it up.\")\n    return raw_table\n\n\ndef lakehouse_style_load(tmp_path=\"./data/etl_elt_demo\"):\n    \"\"\"\n    Lakehouse: gets ELT's speed (raw data lands immediately, nothing is\n    dropped at ingestion) AND ETL's reliability (a governed table format\n    enforces schema and keeps an auditable transaction log), by separating\n    \"raw landing\" from \"governed table\" into Bronze/Silver layers instead of\n    forcing that trade-off into ingestion time or read time.\n    \"\"\"\n    print(\"\\n=== Lakehouse (raw landing + governed transform) ===\")\n    if os.path.exists(tmp_path):\n        shutil.rmtree(tmp_path)\n    os.makedirs(tmp_path, exist_ok=True)\n\n    bronze = pd.DataFrame(RAW_ORDERS)\n    bronze_path = os.path.join(tmp_path, \"bronze_orders.parquet\")\n    bronze.to_parquet(bronze_path)\n    print(f\"Bronze (raw, everything lands as-is, {len(bronze)} rows) written to {bronze_path}\")\n\n    good, rejected = clean_and_validate(RAW_ORDERS)\n    silver = pd.DataFrame(good)\n    silver_path = os.path.join(tmp_path, \"silver_orders.parquet\")\n    silver.to_parquet(silver_path)\n    print(f\"Silver (governed, schema-checked, {len(silver)} rows, {len(rejected)} quarantined)\")\n    print(\"written to\", silver_path)\n    print(\"Bronze is never lost, so a bad Silver rule can always be re-run from Bronze  - \")\n    print(\"this is what Day 1's Delta Lake lab then adds ACID/versioning/time-travel on top of.\")\n    return bronze, silver\n\n\ndef main():\n    warehouse_table = etl_style_warehouse_load()\n    lake_table = elt_style_lake_load()\n    bronze, silver = lakehouse_style_load()\n\n    print(\"\\n=== Summary ===\")\n    print(f\"{'Pattern':&lt;12}{'Rows at rest':&lt;15}{'Quality gate timing':&lt;25}\")\n    print(f\"{'Warehouse':&lt;12}{len(warehouse_table):&lt;15}{'before load (ETL)':&lt;25}\")\n    print(f\"{'Lake':&lt;12}{len(lake_table):&lt;15}{'on read, later (ELT)':&lt;25}\")\n    print(f\"{'Lakehouse':&lt;12}{len(bronze)} raw / {len(silver)} governed   {'both: raw at load, governed after':&lt;25}\")\n\n\nmain()\n\n\n=== ETL (transform-before-load)  -  Data Warehouse style ===\nRows accepted into the warehouse table: 2\nRows rejected before ever reaching storage: 2\n  dropped ORD003: missing product\n  dropped ORD004: corrupt price\n  order_id user_id product   price  quantity\n0   ORD001  User_A  Laptop  1200.0         1\n1   ORD002  User_B   Mouse    25.5         2\nTrade-off: storage is always clean, but the transform logic is baked into the\ningestion job  -  changing a business rule means re-running ingestion from source.\n\n=== ELT (load-before-transform)  -  Data Lake style ===\nRows landed in the raw lake zone (no gate at all): 4\n  order_id user_id     product         price quantity\n0   ORD001  User_A      Laptop       1200.00        1\n1   ORD002  User_B       Mouse          25.5        2\n2   ORD003  User_C         NaN         75.00        1\n3   ORD004  User_A  Headphones  not_a_number        1\nTransforming later, on read, against whatever is sitting in the lake...\nOnly discovered at transform time that 2 row(s) were bad  - \nthey already consumed storage and could have been queried by someone else first.\nTrade-off: ingestion is fast and never blocks on data quality, but the raw zone\ncan silently become a 'data swamp' if nothing ever comes back to clean it up.\n\n=== Lakehouse (raw landing + governed transform) ===\nBronze (raw, everything lands as-is, 4 rows) written to ./data/etl_elt_demo\\bronze_orders.parquet\nSilver (governed, schema-checked, 2 rows, 2 quarantined)\nwritten to ./data/etl_elt_demo\\silver_orders.parquet\nBronze is never lost, so a bad Silver rule can always be re-run from Bronze  - \nthis is what Day 1's Delta Lake lab then adds ACID/versioning/time-travel on top of.\n\n=== Summary ===\nPattern     Rows at rest   Quality gate timing      \nWarehouse   2              before load (ETL)        \nLake        4              on read, later (ELT)     \nLakehouse   4 raw / 2 governed   both: raw at load, governed after\n\n\n\n\n\nEverything below is the annotated walkthrough of a local Delta Lake mini-lakehouse: ACID transactions, schema enforcement, time travel, OPTIMIZE/Z-ORDER, and schema evolution.\n\n!pip install delta-spark\n\n\nCollecting delta-spark\n\n  Downloading delta_spark-4.3.0-py3-none-any.whl.metadata (2.2 kB)\n\nRequirement already satisfied: pyspark&lt;=4.1.1,&gt;=4.0.1 in /usr/local/lib/python3.12/dist-packages (from delta-spark) (4.0.3)\n\nRequirement already satisfied: importlib_metadata&lt;=8.7.1,&gt;=1.0.0 in /usr/local/lib/python3.12/dist-packages (from delta-spark) (8.7.1)\n\nRequirement already satisfied: zipp&gt;=3.20 in /usr/local/lib/python3.12/dist-packages (from importlib_metadata&lt;=8.7.1,&gt;=1.0.0-&gt;delta-spark) (4.1.0)\n\nRequirement already satisfied: py4j&lt;0.10.9.10,&gt;=0.10.9.7 in /usr/local/lib/python3.12/dist-packages (from pyspark&lt;=4.1.1,&gt;=4.0.1-&gt;delta-spark) (0.10.9.9)\n\nDownloading delta_spark-4.3.0-py3-none-any.whl (53 kB)\n\n   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 0.0/53.9 kB ? eta -:--:--\n   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 53.9/53.9 kB 2.1 MB/s eta 0:00:00\n\nInstalling collected packages: delta-spark\n\nSuccessfully installed delta-spark-4.3.0\n\n\n\n\n\nimport glob\nimport json\nimport os\nimport shutil\n\nfrom delta import configure_spark_with_delta_pip\nfrom delta.tables import DeltaTable\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.types import (\n    DoubleType,\n    IntegerType,\n    StructField,\n    StructType,\n    StringType,\n)\n\n\n# ─────────────────────────────────────────────────────────────────────────────\n# SESSION SETUP\n# ─────────────────────────────────────────────────────────────────────────────\n\ndef create_spark_session() -&gt; SparkSession:\n    builder = (\n        SparkSession.builder\n        .appName(\"MiniLakehouse_Extended\")\n        .master(\"local[*]\")\n        .config(\"spark.sql.extensions\",\n                \"io.delta.sql.DeltaSparkSessionExtension\")\n        .config(\"spark.sql.catalog.spark_catalog\",\n                \"org.apache.spark.sql.delta.catalog.DeltaCatalog\")\n        .config(\"spark.sql.warehouse.dir\", \"./data/warehouse\")\n        # Enable auto-optimization (Delta Lake 2.x+)\n        .config(\"spark.databricks.delta.optimizeWrite.enabled\", \"true\")\n        .config(\"spark.databricks.delta.autoCompact.enabled\",   \"true\")\n    )\n    return configure_spark_with_delta_pip(builder).getOrCreate()\n\n\nORDER_SCHEMA = StructType([\n    StructField(\"order_id\",  StringType(),  nullable=False),\n    StructField(\"user_id\",   StringType(),  nullable=True),\n    StructField(\"product\",   StringType(),  nullable=True),\n    StructField(\"price\",     DoubleType(),  nullable=True),\n    StructField(\"quantity\",  IntegerType(), nullable=True),\n    StructField(\"region\",    StringType(),  nullable=True),\n])\n\n\ndef sep(label: str) -&gt; None:\n    print(f\"\\n{'=' * 65}\")\n    print(f\"  {label}\")\n    print(\"=\" * 65)\n\n\n# ─────────────────────────────────────────────────────────────────────────────\n# MAIN\n# ─────────────────────────────────────────────────────────────────────────────\n\ndef main() -&gt; None:\n    TABLE_PATH = \"./data/delta/orders\"\n\n    if os.path.exists(\"./data\"):\n        shutil.rmtree(\"./data\")\n\n    print(\"\\nBooting Spark session with Delta Lake engine...\")\n    spark = create_spark_session()\n    spark.sparkContext.setLogLevel(\"ERROR\")\n\n    # ── STEP 1: Initial Write ─────────────────────────────────────────────────\n    sep(\"STEP 1 — Initial Batch Ingestion (WRITE, partitioned by region)\")\n\n    batch_1 = [\n        (\"ORD001\", \"usr_alice\",  \"Laptop\",      1200.00, 1, \"MENA\"),\n        (\"ORD002\", \"usr_bob\",    \"Mouse\",          25.50, 2, \"EU\"),\n        (\"ORD003\", \"usr_carol\",  \"Keyboard\",       75.00, 1, \"US\"),\n        (\"ORD004\", \"usr_alice\",  \"Headphones\",    150.00, 1, \"MENA\"),\n        (\"ORD005\", \"usr_dave\",   \"Monitor\",       300.00, 2, \"EU\"),\n    ]\n    df1 = spark.createDataFrame(batch_1, ORDER_SCHEMA)\n    (df1.write\n        .format(\"delta\")\n        .mode(\"overwrite\")\n        .partitionBy(\"region\")     # Physical partitioning — Spark skips entire\n        .save(TABLE_PATH))         # partition directories during filtered queries.\n\n    print(\"Delta table written and partitioned by 'region'.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).show()\n\n    # ── STEP 2: ACID Append ───────────────────────────────────────────────────\n    sep(\"STEP 2 — ACID Append Transaction\")\n\n    batch_2 = [\n        (\"ORD006\", \"usr_eve\",   \"Tablet\",   200.00, 1, \"US\"),\n        (\"ORD007\", \"usr_bob\",   \"Webcam\",    80.00, 3, \"EU\"),\n    ]\n    df2 = spark.createDataFrame(batch_2, ORDER_SCHEMA)\n    df2.write.format(\"delta\").mode(\"append\").save(TABLE_PATH)\n\n    print(\"Appended 2 new rows atomically. All-or-nothing guarantee enforced.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).show()\n\n    # ── STEP 3: Schema Enforcement ────────────────────────────────────────────\n    sep(\"STEP 3 — Schema Enforcement (Reject Malformed Ingestion)\")\n    print(\"Attempting to write a row with an undeclared 'discount' column...\")\n\n    bad_schema = StructType([\n        StructField(\"order_id\",  StringType(),  True),\n        StructField(\"user_id\",   StringType(),  True),\n        StructField(\"product\",   StringType(),  True),\n        StructField(\"price\",     DoubleType(),  True),\n        StructField(\"quantity\",  IntegerType(), True),\n        StructField(\"region\",    StringType(),  True),\n        StructField(\"discount\",  DoubleType(),  True),  # Not in the registered schema\n    ])\n    df_bad = spark.createDataFrame(\n        [(\"ORD999\", \"usr_hacker\", \"Free Item\", 0.0, 99, \"MENA\", 100.0)],\n        bad_schema,\n    )\n    try:\n        df_bad.write.format(\"delta\").mode(\"append\").save(TABLE_PATH)\n    except Exception as e:\n        print(\"Schema violation caught by Delta Lake — write REJECTED.\")\n        print(f\"  Error: {str(e).splitlines()[0]}\")\n        print(\"\\nWhy this matters: without schema enforcement a single misconfigured\")\n        print(\"upstream pipeline can silently corrupt a production lakehouse table.\")\n\n    # ── STEP 4: MERGE / UPSERT ────────────────────────────────────────────────\n    sep(\"STEP 4 — MERGE / UPSERT (Atomic Update + Insert in One Transaction)\")\n    print(\"\"\"\nMERGE is the most powerful Delta operation — impossible in a raw data lake:\n  • If a matching record exists  → UPDATE specific columns\n  • If no matching record        → INSERT the new row\n  • All in one atomic transaction (either all commits or nothing commits)\n\nUse case: a nightly CDC (Change Data Capture) feed from a production database\nthat may contain both price corrections and new orders in the same payload.\n\"\"\")\n    upsert_data = [\n        (\"ORD002\", \"usr_bob\",    \"Mouse\",       29.99, 2, \"EU\"),   # exists → update price\n        (\"ORD010\", \"usr_frank\",  \"SSD Drive\",   95.00, 1, \"US\"),   # new    → insert\n    ]\n    df_upsert = spark.createDataFrame(upsert_data, ORDER_SCHEMA)\n\n    delta_table = DeltaTable.forPath(spark, TABLE_PATH)\n    (\n        delta_table.alias(\"target\")\n        .merge(\n            df_upsert.alias(\"updates\"),\n            \"target.order_id = updates.order_id\",\n        )\n        .whenMatchedUpdate(set={\n            \"price\":    \"updates.price\",\n            \"quantity\": \"updates.quantity\",\n        })\n        .whenNotMatchedInsertAll()\n        .execute()\n    )\n    print(\"MERGE complete. ORD002 price updated to 29.99; ORD010 inserted.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show()\n\n    # ── STEP 5: OPTIMIZE + Z-ORDER ────────────────────────────────────────────\n    sep(\"STEP 5 — OPTIMIZE + Z-ORDER BY (Data Skipping for Query Acceleration)\")\n    print(\"\"\"\nThe small-file problem:\n  Streaming micro-batches and per-partition writes create thousands of tiny\n  Parquet files. Each file read requires a separate I/O operation — at scale\n  this kills query performance.\n\nOPTIMIZE:\n  Merges small files into fewer, larger Parquet files, reducing metadata\n  overhead by up to 99%.\n\nZ-ORDER BY:\n  Physically co-locates rows with similar values in the same Parquet file.\n  Delta reads the _delta_log/ min/max statistics per file. If your query is\n  WHERE user_id = 'usr_alice', Spark skips all files that cannot contain that\n  user — called data skipping.\n\nBenchmark: Z-Ordering a 1 TB table on user_id typically reduces query time\nfrom 4 minutes → 8 seconds on filtered queries.\n\"\"\")\n    # spark.sql(f\"OPTIMIZE delta.`{TABLE_PATH}` ZORDER BY (user_id, region)\") # Original line causing error\n    delta_table.optimize().executeZOrderBy([\"user_id\"])\n    print(\"OPTIMIZE + ZORDER complete.\")\n\n    # ── STEP 6: Delta Transaction Log Inspection ──────────────────────────────\n    sep(\"STEP 6 — Inspecting the Delta Transaction Log (_delta_log/)\")\n    print(\"\"\"\nEvery Delta write appends a JSON file to _delta_log/.\nThis is the single source of truth for ACID compliance, Time Travel, and lineage.\nEach JSON file contains one or more entries:\n  commitInfo  → operation type, timestamp, user, operation parameters\n  add         → new Parquet file paths written in this commit\n  remove      → old Parquet file paths superseded by this commit\n  metaData    → schema changes (only present when schema mutates)\n\nAfter every 10 commits Delta writes a checkpoint.parquet to avoid replaying\nthousands of JSON files on startup — this is the Delta Log compaction process.\n\"\"\")\n    log_files = sorted(glob.glob(f\"{TABLE_PATH}/_delta_log/*.json\"))\n    for log_path in log_files[:4]:\n        print(f\"\\n--- {os.path.basename(log_path)} ---\")\n        with open(log_path, encoding=\"utf-8\") as fh:\n            for line in fh:\n                entry = json.loads(line)\n                if \"commitInfo\" in entry:\n                    info = entry[\"commitInfo\"]\n                    print(f\"  operation  : {info.get('operation', 'N/A')}\")\n                    print(f\"  timestamp  : {info.get('timestamp', 'N/A')}\")\n                    params = info.get(\"operationParameters\", {})\n                    if params:\n                        print(f\"  parameters : {params}\")\n                elif \"add\" in entry:\n                    print(f\"  add file   : {entry['add'].get('path', '')}\")\n                elif \"remove\" in entry:\n                    print(f\"  remove file: {entry['remove'].get('path', '')}\")\n\n    # ── STEP 7: Time Travel ───────────────────────────────────────────────────\n    sep(\"STEP 7 — Time Travel (Query Historical Versions)\")\n\n    print(\"Full audit trail from the Delta Transaction Log:\")\n    delta_table.history().select(\"version\", \"timestamp\", \"operation\").show(truncate=False)\n\n    print(\"Restoring state as of VERSION 0 (initial ingest only):\")\n    spark.read.format(\"delta\").option(\"versionAsOf\", 0).load(TABLE_PATH).show()\n\n    print(\"Restoring state as of VERSION 1 (after first ACID append):\")\n    spark.read.format(\"delta\").option(\"versionAsOf\", 1).load(TABLE_PATH).show()\n\n    # ── STEP 8: VACUUM ────────────────────────────────────────────────────────\n    sep(\"STEP 8 — VACUUM (Storage Retention Cleanup)\")\n    print(\"\"\"\nDelta retains all historical Parquet files to support Time Travel.\nVACUUM permanently deletes files older than the retention window.\n\nWARNING: After VACUUM, Time Travel beyond the retention window is impossible.\n\nProduction recommendation: retentionHours &gt;= 168 (7 days).\nUnder GDPR Article 17 (right to erasure), lower retention may be required —\nbut confirm with your legal team before reducing below 168 hours.\n\nSetting 0 hours here for demonstration only — NEVER do this in production.\n\"\"\")\n    spark.conf.set(\n        \"spark.databricks.delta.retentionDurationCheck.enabled\", \"false\"\n    )\n    delta_table.vacuum(retentionHours=0)\n    print(\"VACUUM complete. Orphaned files removed.\")\n\n    # ── STEP 9: Schema Evolution ──────────────────────────────────────────────\n    sep(\"STEP 9 — Schema Evolution (Safe Column Addition via mergeSchema)\")\n    print(\"\"\"\nScenario: a product team wants to add a 'discount' column to the orders feed.\n\nWithout Schema Evolution: the write would be REJECTED (Step 3 behaviour).\nWith mergeSchema=True:   the new column is added to the registered schema.\n                         Existing rows receive NULL for the new column.\n\nThis is the production-safe migration path:\n  1. Data contract teams agree on the new column definition.\n  2. Producer writes first batch with mergeSchema=True.\n  3. Delta propagates the schema change; all downstream readers automatically\n     see the new nullable column on their next read.\n\"\"\")\n    evolved_schema = StructType([\n        StructField(\"order_id\",  StringType(),  True),\n        StructField(\"user_id\",   StringType(),  True),\n        StructField(\"product\",   StringType(),  True),\n        StructField(\"price\",     DoubleType(),  True),\n        StructField(\"quantity\",  IntegerType(), True),\n        StructField(\"region\",    StringType(),  True),\n        StructField(\"discount\",  DoubleType(),  True),  # new column\n    ])\n    df_evolved = spark.createDataFrame(\n        [(\"ORD011\", \"usr_grace\", \"SmartWatch\", 250.00, 1, \"MENA\", 15.0)],\n        evolved_schema,\n    )\n    (df_evolved.write\n               .format(\"delta\")\n               .mode(\"append\")\n               .option(\"mergeSchema\", \"true\")\n               .save(TABLE_PATH))\n\n    print(\"New column 'discount' merged into existing schema without downtime.\")\n    print(\"Pre-evolution rows show NULL for 'discount' — backward compatible:\")\n    spark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show()\n\n    spark.stop()\n    print(\"\\nMini Lakehouse lab complete. All 9 steps executed successfully.\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\nBooting Spark session with Delta Lake engine...\n\n=================================================================\n  STEP 1 — Initial Batch Ingestion (WRITE, partitioned by region)\n=================================================================\nDelta table written and partitioned by 'region'.\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD002|  usr_bob|     Mouse|  25.5|       2|    EU|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n+--------+---------+----------+------+--------+------+\n\n\n=================================================================\n  STEP 2 — ACID Append Transaction\n=================================================================\nAppended 2 new rows atomically. All-or-nothing guarantee enforced.\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD002|  usr_bob|     Mouse|  25.5|       2|    EU|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n|  ORD007|  usr_bob|    Webcam|  80.0|       3|    EU|\n|  ORD006|  usr_eve|    Tablet| 200.0|       1|    US|\n+--------+---------+----------+------+--------+------+\n\n\n=================================================================\n  STEP 3 — Schema Enforcement (Reject Malformed Ingestion)\n=================================================================\nAttempting to write a row with an undeclared 'discount' column...\nSchema violation caught by Delta Lake — write REJECTED.\n  Error: [DELTA_METADATA_MISMATCH] A metadata mismatch was detected when writing to the Delta table.\n\nWhy this matters: without schema enforcement a single misconfigured\nupstream pipeline can silently corrupt a production lakehouse table.\n\n=================================================================\n  STEP 4 — MERGE / UPSERT (Atomic Update + Insert in One Transaction)\n=================================================================\n\nMERGE is the most powerful Delta operation — impossible in a raw data lake:\n  • If a matching record exists  → UPDATE specific columns\n  • If no matching record        → INSERT the new row\n  • All in one atomic transaction (either all commits or nothing commits)\n\nUse case: a nightly CDC (Change Data Capture) feed from a production database\nthat may contain both price corrections and new orders in the same payload.\n\nMERGE complete. ORD002 price updated to 29.99; ORD010 inserted.\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD002|  usr_bob|     Mouse| 29.99|       2|    EU|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n|  ORD006|  usr_eve|    Tablet| 200.0|       1|    US|\n|  ORD007|  usr_bob|    Webcam|  80.0|       3|    EU|\n|  ORD010|usr_frank| SSD Drive|  95.0|       1|    US|\n+--------+---------+----------+------+--------+------+\n\n\n=================================================================\n  STEP 5 — OPTIMIZE + Z-ORDER BY (Data Skipping for Query Acceleration)\n=================================================================\n\nThe small-file problem:\n  Streaming micro-batches and per-partition writes create thousands of tiny\n  Parquet files. Each file read requires a separate I/O operation — at scale\n  this kills query performance.\n\nOPTIMIZE:\n  Merges small files into fewer, larger Parquet files, reducing metadata\n  overhead by up to 99%.\n\nZ-ORDER BY:\n  Physically co-locates rows with similar values in the same Parquet file.\n  Delta reads the _delta_log/ min/max statistics per file. If your query is\n  WHERE user_id = 'usr_alice', Spark skips all files that cannot contain that\n  user — called data skipping.\n\nBenchmark: Z-Ordering a 1 TB table on user_id typically reduces query time\nfrom 4 minutes → 8 seconds on filtered queries.\n\nOPTIMIZE + ZORDER complete.\n\n=================================================================\n  STEP 6 — Inspecting the Delta Transaction Log (_delta_log/)\n=================================================================\n\nEvery Delta write appends a JSON file to _delta_log/.\nThis is the single source of truth for ACID compliance, Time Travel, and lineage.\nEach JSON file contains one or more entries:\n  commitInfo  → operation type, timestamp, user, operation parameters\n  add         → new Parquet file paths written in this commit\n  remove      → old Parquet file paths superseded by this commit\n  metaData    → schema changes (only present when schema mutates)\n\nAfter every 10 commits Delta writes a checkpoint.parquet to avoid replaying\nthousands of JSON files on startup — this is the Delta Log compaction process.\n\n\n--- 00000000000000000000.json ---\n  operation  : WRITE\n  timestamp  : 1782584625055\n  parameters : {'mode': 'Overwrite', 'partitionBy': '[\"region\"]'}\n  add file   : region=MENA/part-00000-4b8ce36e-f285-461d-abbf-7e8c3386120a.c000.snappy.parquet\n  add file   : region=EU/part-00001-82437434-e654-44e9-a2a7-ae28bfee2c2d.c000.snappy.parquet\n  add file   : region=US/part-00002-ebfabcb7-73db-424c-ad49-2f45037d7ba0.c000.snappy.parquet\n\n--- 00000000000000000001.json ---\n  operation  : WRITE\n  timestamp  : 1782584635165\n  parameters : {'mode': 'Append', 'partitionBy': '[]'}\n  add file   : region=EU/part-00000-c119c505-a5bb-4c3b-a2c7-5b9da73d0380.c000.snappy.parquet\n  add file   : region=US/part-00001-ff37bf89-d4ed-44c0-9629-0f6d09bfe257.c000.snappy.parquet\n\n--- 00000000000000000002.json ---\n  operation  : MERGE\n  timestamp  : 1782584644731\n  parameters : {'predicate': '[\"(order_id#9861 = order_id#9855)\"]', 'matchedPredicates': '[{\"actionType\":\"update\"}]', 'notMatchedPredicates': '[{\"actionType\":\"insert\"}]', 'notMatchedBySourcePredicates': '[]'}\n  add file   : region=EU/part-00000-ce1d0a99-f3d5-40ee-8916-21491735a60d.c000.snappy.parquet\n  add file   : region=US/part-00000-5c241594-0759-490c-beda-26aeb6be95d2.c000.snappy.parquet\n  remove file: region=EU/part-00001-82437434-e654-44e9-a2a7-ae28bfee2c2d.c000.snappy.parquet\n\n--- 00000000000000000003.json ---\n  operation  : OPTIMIZE\n  timestamp  : 1782584654016\n  parameters : {'predicate': '[]', 'zOrderBy': '[\"user_id\"]', 'clusterBy': '[]', 'auto': False}\n  add file   : region=MENA/part-00000-6bbd99d1-bddd-4909-bf1e-7d3d884c287e.c000.snappy.parquet\n  remove file: region=MENA/part-00000-4b8ce36e-f285-461d-abbf-7e8c3386120a.c000.snappy.parquet\n  add file   : region=EU/part-00000-f60c2c85-ece8-49fd-b4ce-ce3ef04efe55.c000.snappy.parquet\n  remove file: region=EU/part-00000-c119c505-a5bb-4c3b-a2c7-5b9da73d0380.c000.snappy.parquet\n  remove file: region=EU/part-00000-ce1d0a99-f3d5-40ee-8916-21491735a60d.c000.snappy.parquet\n  add file   : region=US/part-00000-9045252d-4b7a-489b-9181-dc56995a5068.c000.snappy.parquet\n  remove file: region=US/part-00001-ff37bf89-d4ed-44c0-9629-0f6d09bfe257.c000.snappy.parquet\n  remove file: region=US/part-00002-ebfabcb7-73db-424c-ad49-2f45037d7ba0.c000.snappy.parquet\n  remove file: region=US/part-00000-5c241594-0759-490c-beda-26aeb6be95d2.c000.snappy.parquet\n\n=================================================================\n  STEP 7 — Time Travel (Query Historical Versions)\n=================================================================\nFull audit trail from the Delta Transaction Log:\n+-------+-----------------------+---------+\n|version|timestamp              |operation|\n+-------+-----------------------+---------+\n|3      |2026-06-27 18:24:14.037|OPTIMIZE |\n|2      |2026-06-27 18:24:04.763|MERGE    |\n|1      |2026-06-27 18:23:55.183|WRITE    |\n|0      |2026-06-27 18:23:45.071|WRITE    |\n+-------+-----------------------+---------+\n\nRestoring state as of VERSION 0 (initial ingest only):\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD002|  usr_bob|     Mouse|  25.5|       2|    EU|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n+--------+---------+----------+------+--------+------+\n\nRestoring state as of VERSION 1 (after first ACID append):\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD002|  usr_bob|     Mouse|  25.5|       2|    EU|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n|  ORD007|  usr_bob|    Webcam|  80.0|       3|    EU|\n|  ORD006|  usr_eve|    Tablet| 200.0|       1|    US|\n+--------+---------+----------+------+--------+------+\n\n\n=================================================================\n  STEP 8 — VACUUM (Storage Retention Cleanup)\n=================================================================\n\nDelta retains all historical Parquet files to support Time Travel.\nVACUUM permanently deletes files older than the retention window.\n\nWARNING: After VACUUM, Time Travel beyond the retention window is impossible.\n\nProduction recommendation: retentionHours &gt;= 168 (7 days).\nUnder GDPR Article 17 (right to erasure), lower retention may be required —\nbut confirm with your legal team before reducing below 168 hours.\n\nSetting 0 hours here for demonstration only — NEVER do this in production.\n\nVACUUM complete. Orphaned files removed.\n\n=================================================================\n  STEP 9 — Schema Evolution (Safe Column Addition via mergeSchema)\n=================================================================\n\nScenario: a product team wants to add a 'discount' column to the orders feed.\n\nWithout Schema Evolution: the write would be REJECTED (Step 3 behaviour).\nWith mergeSchema=True:   the new column is added to the registered schema.\n                         Existing rows receive NULL for the new column.\n\nThis is the production-safe migration path:\n  1. Data contract teams agree on the new column definition.\n  2. Producer writes first batch with mergeSchema=True.\n  3. Delta propagates the schema change; all downstream readers automatically\n     see the new nullable column on their next read.\n\nNew column 'discount' merged into existing schema without downtime.\nPre-evolution rows show NULL for 'discount' — backward compatible:\n+--------+---------+----------+------+--------+------+--------+\n|order_id|  user_id|   product| price|quantity|region|discount|\n+--------+---------+----------+------+--------+------+--------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|    NULL|\n|  ORD002|  usr_bob|     Mouse| 29.99|       2|    EU|    NULL|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|    NULL|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|    NULL|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|    NULL|\n|  ORD006|  usr_eve|    Tablet| 200.0|       1|    US|    NULL|\n|  ORD007|  usr_bob|    Webcam|  80.0|       3|    EU|    NULL|\n|  ORD010|usr_frank| SSD Drive|  95.0|       1|    US|    NULL|\n|  ORD011|usr_grace|SmartWatch| 250.0|       1|  MENA|    15.0|\n+--------+---------+----------+------+--------+------+--------+\n\n\nMini Lakehouse lab complete. All 9 steps executed successfully.\n\n\nLet’s start by explaining the import statements and then move to the function definitions.\n\n\n\nimport glob, import json, import os, import shutil: These lines import standard Python modules for working with file paths (glob, os), JSON data (json), and high-level file operations like deleting directories (shutil).\nfrom delta import configure_spark_with_delta_pip: This imports a specific function from the delta library to simplify setting up a Spark session with Delta Lake capabilities.\nfrom delta.tables import DeltaTable: This imports the DeltaTable class, which is used to interact with Delta Lake tables programmatically.\nfrom pyspark.sql import SparkSession: Imports the main entry point for Spark functionality.\nfrom pyspark.sql.types import (...): Imports various data types (DoubleType, IntegerType, StructField, StructType, StringType) from PySpark’s SQL module, which are used to define the schema of DataFrames.\n\n\n\n\nThis section defines functions and schema that are crucial for setting up the Spark environment and defining the data structure.\n\n\nThis function is responsible for creating and configuring a SparkSession with Delta Lake extensions.\ndef create_spark_session() -&gt; SparkSession:\n    builder = (\n        SparkSession.builder\n        .appName(\"MiniLakehouse_Extended\")\n        .master(\"local[*]\")\n        .config(\"spark.sql.extensions\",\n                \"io.delta.sql.DeltaSparkSessionExtension\")\n        .config(\"spark.sql.catalog.spark_catalog\",\n                \"org.apache.spark.sql.delta.catalog.DeltaCatalog\")\n        .config(\"spark.sql.warehouse.dir\", \"./data/warehouse\")\n        .config(\"spark.databricks.delta.optimizeWrite.enabled\", \"true\")\n        .config(\"spark.databricks.delta.autoCompact.enabled\",   \"true\")\n    )\n    return configure_spark_with_delta_pip(builder).getOrCreate()\n\ndef create_spark_session() -&gt; SparkSession:: Defines a function named create_spark_session that returns a SparkSession object.\nbuilder = (...): Initializes a SparkSession.builder object to configure various Spark settings.\n.appName(\"MiniLakehouse_Extended\"): Sets the name of the Spark application.\n.master(\"local[*]\"): Configures Spark to run in local mode, using all available CPU cores.\n.config(\"spark.sql.extensions\", \"io.delta.sql.DeltaSparkSessionExtension\"): Enables Delta Lake SQL functionalities.\n.config(\"spark.sql.catalog.spark_catalog\", \"org.apache.spark.sql.delta.catalog.DeltaCatalog\"): Registers Delta Lake as the default catalog for Spark SQL, allowing it to manage Delta tables.\n.config(\"spark.sql.warehouse.dir\", \"./data/warehouse\"): Specifies the directory where Spark will store its warehouse data.\n.config(\"spark.databricks.delta.optimizeWrite.enabled\", \"true\"): Enables automatic optimization during write operations, which helps in merging small files.\n.config(\"spark.databricks.delta.autoCompact.enabled\", \"true\"): Enables automatic compaction of small files to improve read performance.\nreturn configure_spark_with_delta_pip(builder).getOrCreate(): Applies the Delta Lake configurations and then creates and returns the SparkSession.\n\n\n\n\nThis defines the structure of the data that will be stored in the Delta table.\nORDER_SCHEMA = StructType([\n    StructField(\"order_id\",  StringType(),  nullable=False),\n    StructField(\"user_id\",   StringType(),  nullable=True),\n    StructField(\"product\",   StringType(),  nullable=True),\n    StructField(\"price\",     DoubleType(),  nullable=True),\n    StructField(\"quantity\",  IntegerType(), nullable=True),\n    StructField(\"region\",    StringType(),  nullable=True),\n])\n\nORDER_SCHEMA = StructType([...]): Defines a StructType which represents the schema of a DataFrame. It’s a list of StructField objects.\nStructField(\"order_id\", StringType(), nullable=False): Defines a column named order_id of type StringType, which cannot be null.\nSimilarly, other StructField entries define user_id, product, price, quantity, and region with their respective types and nullability settings. This schema ensures data consistency in the Delta table.\n\n\n\n\nThis is a simple helper function for printing separators in the console output to make the different steps more readable.\ndef sep(label: str) -&gt; None:\n    print(f\"\\n{'=' * 65}\")\n    print(f\"  {label}\")\n    print(\"=\" * 65)\n\ndef sep(label: str) -&gt; None:: Defines a function named sep that takes a string label and returns nothing (None).\nprint(f\"\\n{'=' * 65}\"): Prints a line of 65 equal signs, followed by a newline, to create a visual separator.\nprint(f\"  {label}\"): Prints the provided label centered between the lines.\nprint(\"=\" * 65): Prints another line of 65 equal signs.\n\n\n\n\n\nThis is the core of the script, demonstrating various Delta Lake functionalities step by step.\ndef main() -&gt; None:\n    TABLE_PATH = \"./data/delta/orders\"\n\n    if os.path.exists(\"./data\"):\n        shutil.rmtree(\"./data\")\n\n    print(\"\\nBooting Spark session with Delta Lake engine...\")\n    spark = create_spark_session()\n    spark.sparkContext.setLogLevel(\"ERROR\")\n\ndef main() -&gt; None:: Defines the main function of the script.\nTABLE_PATH = \"./data/delta/orders\": Sets a variable TABLE_PATH to define the location where the Delta table will be stored.\nif os.path.exists(\"./data\"): shutil.rmtree(\"./data\"): Checks if a directory named data exists. If it does, it deletes the entire directory and its contents to ensure a clean start for the demonstration.\nprint(\"\\nBooting Spark session with Delta Lake engine...\"): Prints a message indicating that the Spark session is starting.\nspark = create_spark_session(): Calls the create_spark_session function (explained above) to initialize a SparkSession and assigns it to the spark variable.\nspark.sparkContext.setLogLevel(\"ERROR\"): Sets the logging level for Spark to ERROR, meaning only error messages will be displayed, reducing verbose output.\n\n\n\nThis step demonstrates how to create a new Delta table and write initial data to it, partitioned by the ‘region’ column.\n    sep(\"STEP 1 — Initial Batch Ingestion (WRITE, partitioned by region)\")\n\n    batch_1 = [\n        (\"ORD001\", \"usr_alice\",  \"Laptop\",      1200.00, 1, \"MENA\"),\n        (\"ORD002\", \"usr_bob\",    \"Mouse\",          25.50, 2, \"EU\"),\n        (\"ORD003\", \"usr_carol\",  \"Keyboard\",       75.00, 1, \"US\"),\n        (\"ORD004\", \"usr_alice\",  \"Headphones\",    150.00, 1, \"MENA\"),\n        (\"ORD005\", \"usr_dave\",   \"Monitor\",       300.00, 2, \"EU\"),\n    ]\n    df1 = spark.createDataFrame(batch_1, ORDER_SCHEMA)\n    (df1.write\n        .format(\"delta\")\n        .mode(\"overwrite\")\n        .partitionBy(\"region\")\n        .save(TABLE_PATH))\n\n    print(\"Delta table written and partitioned by 'region'.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).show()\n\nsep(...): Prints a separator with the step title.\nbatch_1 = [...]: Defines a list of tuples, where each tuple represents a row of data for the first batch.\ndf1 = spark.createDataFrame(batch_1, ORDER_SCHEMA): Creates a Spark DataFrame df1 from batch_1 using the predefined ORDER_SCHEMA.\n(df1.write.format(\"delta\").mode(\"overwrite\").partitionBy(\"region\").save(TABLE_PATH)): This chain of methods performs the write operation:\n\n.format(\"delta\"): Specifies that the data should be written in Delta Lake format.\n.mode(\"overwrite\"): If the table already exists at TABLE_PATH, it will be completely overwritten.\n.partitionBy(\"region\"): Organizes the data physically on disk into subdirectories based on the ‘region’ column’s values. This optimizes queries that filter by ‘region’.\n.save(TABLE_PATH): Writes the DataFrame to the specified TABLE_PATH.\n\nprint(...): Prints a confirmation message.\nspark.read.format(\"delta\").load(TABLE_PATH).show(): Reads the newly created Delta table and displays its contents.\n\n\n\n\nThis step demonstrates how to append new data to an existing Delta table, leveraging Delta Lake’s ACID (Atomicity, Consistency, Isolation, Durability) properties.\n    sep(\"STEP 2 — ACID Append Transaction\")\n\n    batch_2 = [\n        (\"ORD006\", \"usr_eve\",   \"Tablet\",   200.00, 1, \"US\"),\n        (\"ORD007\", \"usr_bob\",   \"Webcam\",    80.00, 3, \"EU\"),\n    ]\n    df2 = spark.createDataFrame(batch_2, ORDER_SCHEMA)\n    df2.write.format(\"delta\").mode(\"append\").save(TABLE_PATH)\n\n    print(\"Appended 2 new rows atomically. All-or-nothing guarantee enforced.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).show()\n\nsep(...): Prints a separator with the step title.\nbatch_2 = [...]: Defines a list of tuples for the second batch of data.\ndf2 = spark.createDataFrame(batch_2, ORDER_SCHEMA): Creates a Spark DataFrame df2 from batch_2 using the ORDER_SCHEMA.\ndf2.write.format(\"delta\").mode(\"append\").save(TABLE_PATH): Appends df2 to the existing Delta table at TABLE_PATH. mode(\"append\") ensures that new data is added without affecting existing data. This is an atomic operation, meaning it either fully succeeds or fully fails, preventing partial writes.\nprint(...): Prints a confirmation message about the atomic append.\nspark.read.format(\"delta\").load(TABLE_PATH).show(): Reads and displays the updated Delta table, showing the newly appended rows.\n\n\n\n\nThis step illustrates Delta Lake’s schema enforcement, which prevents writing data that doesn’t conform to the table’s defined schema, thus maintaining data quality.\n    sep(\"STEP 3 — Schema Enforcement (Reject Malformed Ingestion)\")\n    print(\"Attempting to write a row with an undeclared 'discount' column...\")\n\n    bad_schema = StructType([\n        StructField(\"order_id\",  StringType(),  True),\n        StructField(\"user_id\",   StringType(),  True),\n        StructField(\"product\",   StringType(),  True),\n        StructField(\"price\",     DoubleType(),  True),\n        StructField(\"quantity\",  IntegerType(), True),\n        StructField(\"region\",    StringType(),  True),\n        StructField(\"discount\",  DoubleType(),  True),  # Not in the registered schema\n    ])\n    df_bad = spark.createDataFrame(\n        [(\"ORD999\", \"usr_hacker\", \"Free Item\", 0.0, 99, \"MENA\", 100.0)],\n        bad_schema,\n    )\n    try:\n        df_bad.write.format(\"delta\").mode(\"append\").save(TABLE_PATH)\n    except Exception as e:\n        print(\"Schema violation caught by Delta Lake — write REJECTED.\")\n        print(f\"  Error: {str(e).splitlines()[0]}\")\n        print(\"\\nWhy this matters: without schema enforcement a single misconfigured\")\n        print(\"upstream pipeline can silently corrupt a production lakehouse table.\")\n\nsep(...): Prints a separator with the step title.\nprint(...): Explains the upcoming attempt to write data with a mismatched schema.\nbad_schema = StructType([...]): Defines a new schema that includes an additional column, discount, which is not present in the original ORDER_SCHEMA.\ndf_bad = spark.createDataFrame(...): Creates a DataFrame df_bad using this bad_schema.\ntry...except Exception as e:: This block attempts to write df_bad to the Delta table. Because of schema enforcement, this write operation is expected to fail.\n\ndf_bad.write.format(\"delta\").mode(\"append\").save(TABLE_PATH): Tries to append the DataFrame with the bad_schema.\nThe except block catches the expected exception (a schema violation) and prints messages explaining why the write was rejected and the importance of schema enforcement for data integrity.\n\n\n\n\n\nThis step showcases the powerful MERGE operation, which allows for atomic updates and inserts (upserts) in a single transaction, a capability often found in traditional data warehouses.\n    sep(\"STEP 4 — MERGE / UPSERT (Atomic Update + Insert in One Transaction)\")\n    print(\"\"\"\nMERGE is the most powerful Delta operation — impossible in a raw data lake:\n  • If a matching record exists  → UPDATE specific columns\n  • If no matching record        → INSERT the new row\n  • All in one atomic transaction (either all commits or nothing commits)\n\nUse case: a nightly CDC (Change Data Capture) feed from a production database\nthat may contain both price corrections and new orders in the same payload.\n\"\"\")\n    upsert_data = [\n        (\"ORD002\", \"usr_bob\",    \"Mouse\",       29.99, 2, \"EU\"),   # exists → update price\n        (\"ORD010\", \"usr_frank\",  \"SSD Drive\",   95.00, 1, \"US\"),   # new    → insert\n    ]\n    df_upsert = spark.createDataFrame(upsert_data, ORDER_SCHEMA)\n\n    delta_table = DeltaTable.forPath(spark, TABLE_PATH)\n    (\n        delta_table.alias(\"target\")\n        .merge(\n            df_upsert.alias(\"updates\"),\n            \"target.order_id = updates.order_id\",\n        )\n        .whenMatchedUpdate(set={\n            \"price\":    \"updates.price\",\n            \"quantity\": \"updates.quantity\",\n        })\n        .whenNotMatchedInsertAll()\n        .execute()\n    )\n    print(\"MERGE complete. ORD002 price updated to 29.99; ORD010 inserted.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show()\n\nsep(...): Prints a separator with the step title.\nprint(...): Provides a detailed explanation of what the MERGE operation does and its benefits.\nupsert_data = [...]: Defines data for the upsert operation. One record (ORD002) already exists in the table and will be updated; another (ORD010) is new and will be inserted.\ndf_upsert = spark.createDataFrame(upsert_data, ORDER_SCHEMA): Creates a DataFrame from the upsert data.\ndelta_table = DeltaTable.forPath(spark, TABLE_PATH): Creates a DeltaTable object, which provides an API for interacting with the Delta table.\ndelta_table.alias(\"target\").merge(...): Starts the merge operation. The existing table is aliased as target.\n\n.merge(df_upsert.alias(\"updates\"), \"target.order_id = updates.order_id\"): Merges the target table with the updates DataFrame (aliased from df_upsert), using order_id as the join key.\n.whenMatchedUpdate(set={...}): Specifies actions to take when a record in updates matches a record in target (based on order_id). Here, it updates the price and quantity columns.\n.whenNotMatchedInsertAll(): Specifies actions to take when a record in updates does not match any record in target. Here, it inserts all columns of the new record.\n.execute(): Executes the merge operation.\n\nprint(...): Prints a confirmation message about the merge results.\nspark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show(): Reads and displays the updated table, sorted by order_id to show the changes clearly.\n\n\n\n\nThis step demonstrates how to optimize the physical layout of the Delta table for faster query performance by consolidating small files and using Z-Ordering.\n    sep(\"STEP 5 — OPTIMIZE + Z-ORDER BY (Data Skipping for Query Acceleration)\")\n    print(\"\"\"\nThe small-file problem:\n  Streaming micro-batches and per-partition writes create thousands of tiny\n  Parquet files. Each file read requires a separate I/O operation — at scale\n  this kills query performance.\n\nOPTIMIZE:\n  Merges small files into fewer, larger Parquet files, reducing metadata\n  overhead by up to 99%.\n\nZ-ORDER BY:\n  Physically co-locates rows with similar values in the same Parquet file.\n  Delta reads the _delta_log/ min/max statistics per file. If your query is\n  WHERE user_id = 'usr_alice', Spark skips all files that cannot contain that\n  user — called data skipping.\n\nBenchmark: Z-Ordering a 1 TB table on user_id typically reduces query time\nfrom 4 minutes → 8 seconds on filtered queries.\n\"\"\")\n    delta_table.optimize().executeZOrderBy([\"user_id\"])\n    print(\"OPTIMIZE + ZORDER complete.\")\n\nsep(...): Prints a separator with the step title.\nprint(...): Explains the\n\nDelta Lake’s OPTIMIZE and Z-ORDER BY features.\n\ndelta_table.optimize().executeZOrderBy([\"user_id\"]): This is the core operation for optimization and Z-ordering.\n\ndelta_table.optimize(): Initiates the optimization process, which aims to coalesce small files into larger ones, reducing the number of files and improving read performance.\n.executeZOrderBy([\"user_id\"]): Applies Z-Ordering on the user_id column. Z-Ordering physically co-locates data records with similar user_id values within the same data files. This allows Delta Lake to use file-level statistics (min/max values) to quickly skip entire files that do not contain the user_id being queried, significantly speeding up filtered queries.\n\nprint(...): Prints a confirmation that the optimization and Z-Ordering are complete.\n\n\n\n\nThis step demonstrates how to directly inspect the transaction log (_delta_log/) of a Delta table, which records every change to the table and enables ACID properties and Time Travel.\n    sep(\"STEP 6 — Inspecting the Delta Transaction Log (_delta_log/)\")\n    print(\"\"\"\nEvery Delta write appends a JSON file to _delta_log/.\nThis is the single source of truth for ACID compliance, Time Travel, and lineage.\nEach JSON file contains one or more entries:\n  commitInfo  → operation type, timestamp, user, operation parameters\n  add         → new Parquet file paths written in this commit\n  remove      → old Parquet file paths superseded by this commit\n  metaData    → schema changes (only present when schema mutates)\n\nAfter every 10 commits Delta writes a checkpoint.parquet to avoid replaying\nthousands of JSON files on startup — this is the Delta Log compaction process.\n\"\"\")\n    log_files = sorted(glob.glob(f\"{TABLE_PATH}/_delta_log/*.json\"))\n    for log_path in log_files[:4]:\n        print(f\"\\n--- {os.path.basename(log_path)} ---\")\n        with open(log_path, encoding=\"utf-8\") as fh:\n            for line in fh:\n                entry = json.loads(line)\n                if \"commitInfo\" in entry:\n                    info = entry[\"commitInfo\"]\n                    print(f\"  operation  : {info.get('operation', 'N/A')}\")\n                    print(f\"  timestamp  : {info.get('timestamp', 'N/A')}\")\n                    params = info.get(\"operationParameters\", {})\n                    if params:\n                        print(f\"  parameters : {params}\")\n                elif \"add\" in entry:\n                    print(f\"  add file   : {entry['add'].get('path', '')}\")\n                elif \"remove\" in entry:\n                    print(f\"  remove file: {entry['remove'].get('path', '')}\")\n\nsep(...): Prints a separator with the step title.\nprint(...): Provides an explanation of the Delta Transaction Log, its structure, and its importance for ACID properties, Time Travel, and lineage.\nlog_files = sorted(glob.glob(f\"{TABLE_PATH}/_delta_log/*.json\")): Uses the glob module to find all JSON files within the _delta_log subdirectory of the Delta table and sorts them chronologically.\nfor log_path in log_files[:4]:: Iterates through the first four transaction log files to demonstrate their content.\nprint(f\"\\n--- {os.path.basename(log_path)} ---\"): Prints the filename of the current log file.\nwith open(log_path, encoding=\"utf-8\") as fh:: Opens each JSON log file for reading.\nfor line in fh:: Each line in a Delta transaction log file represents a single action (e.g., adding a file, removing a file, a commit info).\nentry = json.loads(line): Parses the JSON string from each line into a Python dictionary.\nif \"commitInfo\" in entry: ...: If the entry contains commitInfo, it extracts and prints details about the operation (e.g., type of operation, timestamp, parameters).\nelif \"add\" in entry: ...: If the entry contains add, it indicates a new data file was added and prints its path.\nelif \"remove\" in entry: ...: If the entry contains remove, it indicates an old data file was removed (e.g., due to an overwrite or update) and prints its path.\n\n\n\n\nThis step demonstrates Delta Lake’s Time Travel capability, allowing you to query previous versions of the table by specifying a version number or a timestamp.\n    sep(\"STEP 7 — Time Travel (Query Historical Versions)\")\n\n    print(\"Full audit trail from the Delta Transaction Log:\")\n    delta_table.history().select(\"version\", \"timestamp\", \"operation\").show(truncate=False)\n\n    print(\"Restoring state as of VERSION 0 (initial ingest only):\")\n    spark.read.format(\"delta\").option(\"versionAsOf\", 0).load(TABLE_PATH).show()\n\n    print(\"Restoring state as of VERSION 1 (after first ACID append):\")\n    spark.read.format(\"delta\").option(\"versionAsOf\", 1).load(TABLE_PATH).show()\n\nsep(...): Prints a separator with the step title.\nprint(\"Full audit trail...\"): Informs the user that the full history will be displayed.\ndelta_table.history().select(\"version\", \"timestamp\", \"operation\").show(truncate=False): Retrieves the full history of the Delta table, showing each version, its timestamp, and the operation that created it. truncate=False ensures that all column content is displayed.\nprint(\"Restoring state as of VERSION 0...\"): Indicates that the table is being queried as it appeared after the first commit (version 0).\nspark.read.format(\"delta\").option(\"versionAsOf\", 0).load(TABLE_PATH).show(): Reads the Delta table at a specific historical version (version 0) and displays its content. This effectively\n\nrestores the table to its state at version 0. * print(\"Restoring state as of VERSION 1...\"): Indicates that the table is being queried as it appeared after the second commit (version 1). * spark.read.format(\"delta\").option(\"versionAsOf\", 1).load(TABLE_PATH).show(): Reads the Delta table at a specific historical version (version 1) and displays its content. This shows the state of the table after the initial batch and the first append.\n\n\n\nThis step demonstrates how to use the VACUUM command to remove data files that are no longer referenced by the Delta table’s transaction log and are older than a specified retention period.\n    sep(\"STEP 8 — VACUUM (Storage Retention Cleanup)\")\n    print(\"\"\"\nDelta retains all historical Parquet files to support Time Travel.\nVACUUM permanently deletes files older than the retention window.\n\nWARNING: After VACUUM, Time Travel beyond the retention window is impossible.\n\nProduction recommendation: retentionHours &gt;= 168 (7 days).\nUnder GDPR Article 17 (right to erasure), lower retention may be required —\nbut confirm with your legal team before reducing below 168 hours.\n\nSetting 0 hours here for demonstration only — NEVER do this in production.\n\"\"\")\n    spark.conf.set(\n        \"spark.databricks.delta.retentionDurationCheck.enabled\", \"false\"\n    )\n    delta_table.vacuum(retentionHours=0)\n    print(\"VACUUM complete. Orphaned files removed.\")\n\nsep(...): Prints a separator with the step title.\nprint(...): Provides a detailed explanation of VACUUM, its purpose (storage cleanup), its implications (loss of time travel history), and important considerations for production environments regarding retention periods.\nspark.conf.set(\"spark.databricks.delta.retentionDurationCheck.enabled\", \"false\"): This configuration setting temporarily disables the check that prevents users from setting a very low retention duration (like 0 hours). This is done here only for demonstration purposes to allow immediate cleanup, but it’s strongly advised against in production.\ndelta_table.vacuum(retentionHours=0): Executes the VACUUM command on the delta_table. retentionHours=0 means that all files no longer part of the latest table state are immediately eligible for deletion, regardless of their age. This will permanently delete older data files, reducing storage costs but removing the ability to time travel to those specific versions.\n\n\n\n\nThis step demonstrates how Delta Lake’s schema evolution feature allows for safely adding new columns to a table without breaking existing data or pipelines.\n    sep(\"STEP 9 — Schema Evolution (Safe Column Addition via mergeSchema)\")\n    print(\"\"\"\nScenario: a product team wants to add a 'discount' column to the orders feed.\n\nWithout Schema Evolution: the write would be REJECTED (Step 3 behaviour).\nWith mergeSchema=True:   the new column is added to the registered schema.\n                         Existing rows receive NULL for the new column.\n\nThis is the production-safe migration path:\n  1. Data contract teams agree on the new column definition.\n  2. Producer writes first batch with mergeSchema=True.\n  3. Delta propagates the schema change; all downstream readers automatically\n     see the new nullable column on their next read.\n\"\"\")\n    evolved_schema = StructType([\n        StructField(\"order_id\",  StringType(),  True),\n        StructField(\"user_id\",   StringType(),  True),\n        StructField(\"product\",   StringType(),  True),\n        StructField(\"price\",     DoubleType(),  True),\n        StructField(\"quantity\",  IntegerType(), True),\n        StructField(\"region\",    StringType(),  True),\n        StructField(\"discount\",  DoubleType(),  True),  # new column\n    ])\n    df_evolved = spark.createDataFrame(\n        [(\"ORD011\", \"usr_grace\", \"SmartWatch\", 250.00, 1, \"MENA\", 15.0)],\n        evolved_schema,\n    )\n    (df_evolved.write\n               .format(\"delta\")\n               .mode(\"append\")\n               .option(\"mergeSchema\", \"true\")\n               .save(TABLE_PATH))\n\n    print(\"New column 'discount' merged into existing schema without downtime.\")\n    print(\"Pre-evolution rows show NULL for 'discount' — backward compatible:\")\n    spark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show()\n\nsep(...): Prints a separator with the step title.\nprint(...): Explains the concept of Schema Evolution, contrasting it with schema enforcement (from Step 3), and describes the benefits and typical workflow for safely adding new columns.\nevolved_schema = StructType([...]): Defines a new schema, evolved_schema, which includes the existing columns plus a new discount column.\ndf_evolved = spark.createDataFrame(...): Creates a DataFrame df_evolved using this new schema, including data for the new discount column for a new order.\n(df_evolved.write.format(\"delta\").mode(\"append\").option(\"mergeSchema\", \"true\").save(TABLE_PATH)): Appends the new DataFrame to the Delta table.\n\n.option(\"mergeSchema\", \"true\"): This crucial option enables schema evolution. When set to true, if the DataFrame being written has a schema that is a superset of the table’s current schema, Delta Lake will automatically evolve the table’s schema to include the new columns. Existing rows will have NULL values for the newly added columns.\n\nprint(...): Prints a confirmation message.\nspark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show(): Reads and displays the table again. This time, the discount column is present. Existing rows (from previous steps) will show NULL for discount, while the newly added row (ORD011) will have its discount value.\n\n\n\n\nThis block ensures the Spark session is properly terminated and confirms the completion of the lab.\n    spark.stop()\n    print(\"\\nMini Lakehouse lab complete. All 9 steps executed successfully.\")\n\n\nif __name__ == \"__main__\":\n    main()\n\nspark.stop(): Stops the SparkSession, releasing all its resources.\nprint(...): Prints a final confirmation message indicating that the entire lab has completed successfully.\nif __name__ == \"__main__\": main(): This standard Python construct ensures that the main() function is called only when the script is executed directly (not when it’s imported as a module). This starts the entire workflow defined in the main function.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Lab: Mini-Lakehouse with Delta Lake"
    ]
  },
  {
    "objectID": "L01/Day1_Lab.html#part-1-etl-vs-elt-and-warehouse-vs-lake-vs-lakehouse",
    "href": "L01/Day1_Lab.html#part-1-etl-vs-elt-and-warehouse-vs-lake-vs-lakehouse",
    "title": "Lab: Mini-Lakehouse with Delta Lake",
    "section": "",
    "text": "Loads the same raw e-commerce order batch three different ways so the contrast between the three patterns is visible in the output, not just described in words: ETL (clean-before-load, warehouse style), ELT (load-before-clean, lake style), and the Lakehouse pattern (raw Bronze landing + governed Silver transform) that Part 2 then builds on with Delta Lake.\n\nimport os\nimport shutil\n\nimport pandas as pd\n\nRAW_ORDERS = [\n    {\"order_id\": \"ORD001\", \"user_id\": \"User_A\", \"product\": \"Laptop\", \"price\": \"1200.00\", \"quantity\": \"1\"},\n    {\"order_id\": \"ORD002\", \"user_id\": \"User_B\", \"product\": \"Mouse\", \"price\": \"25.5\", \"quantity\": \"2\"},\n    {\"order_id\": \"ORD003\", \"user_id\": \"User_C\", \"product\": None, \"price\": \"75.00\", \"quantity\": \"1\"},  # bad row: missing product\n    {\"order_id\": \"ORD004\", \"user_id\": \"User_A\", \"product\": \"Headphones\", \"price\": \"not_a_number\", \"quantity\": \"1\"},  # bad row: corrupt price\n]\n\n\ndef clean_and_validate(rows):\n    \"\"\"Shared cleaning rule used by the ETL path up front, and by the ELT/Lakehouse paths later.\"\"\"\n    good, rejected = [], []\n    for row in rows:\n        if not row[\"product\"]:\n            rejected.append((row, \"missing product\"))\n            continue\n        try:\n            price = float(row[\"price\"])\n        except ValueError:\n            rejected.append((row, \"corrupt price\"))\n            continue\n        good.append({**row, \"price\": price, \"quantity\": int(row[\"quantity\"])})\n    return good, rejected\n\n\ndef etl_style_warehouse_load():\n    \"\"\"\n    ETL (\"Transform\" happens BEFORE \"Load\"):\n    Classic data-warehouse pattern. Bad rows are cleaned/rejected in-flight,\n    by the ingestion job itself, before anything ever lands in storage.\n    Storage only ever sees clean, schema-conforming rows.\n    \"\"\"\n    print(\"\\n=== ETL (transform-before-load)  -  Data Warehouse style ===\")\n    good, rejected = clean_and_validate(RAW_ORDERS)\n    print(f\"Rows accepted into the warehouse table: {len(good)}\")\n    print(f\"Rows rejected before ever reaching storage: {len(rejected)}\")\n    for row, reason in rejected:\n        print(f\"  dropped {row['order_id']}: {reason}\")\n    warehouse_table = pd.DataFrame(good)\n    print(warehouse_table)\n    print(\"Trade-off: storage is always clean, but the transform logic is baked into the\")\n    print(\"ingestion job  -  changing a business rule means re-running ingestion from source.\")\n    return warehouse_table\n\n\ndef elt_style_lake_load():\n    \"\"\"\n    ELT (\"Load\" happens BEFORE \"Transform\"):\n    Classic data-lake pattern. Everything lands as-is, including bad rows  - \n    there is no schema or quality gate at write time. Transformation is a\n    separate, later step run against whatever was dumped.\n    \"\"\"\n    print(\"\\n=== ELT (load-before-transform)  -  Data Lake style ===\")\n    raw_table = pd.DataFrame(RAW_ORDERS)\n    print(f\"Rows landed in the raw lake zone (no gate at all): {len(raw_table)}\")\n    print(raw_table)\n    print(\"Transforming later, on read, against whatever is sitting in the lake...\")\n    good, rejected = clean_and_validate(RAW_ORDERS)\n    print(f\"Only discovered at transform time that {len(rejected)} row(s) were bad  - \")\n    print(\"they already consumed storage and could have been queried by someone else first.\")\n    print(\"Trade-off: ingestion is fast and never blocks on data quality, but the raw zone\")\n    print(\"can silently become a 'data swamp' if nothing ever comes back to clean it up.\")\n    return raw_table\n\n\ndef lakehouse_style_load(tmp_path=\"./data/etl_elt_demo\"):\n    \"\"\"\n    Lakehouse: gets ELT's speed (raw data lands immediately, nothing is\n    dropped at ingestion) AND ETL's reliability (a governed table format\n    enforces schema and keeps an auditable transaction log), by separating\n    \"raw landing\" from \"governed table\" into Bronze/Silver layers instead of\n    forcing that trade-off into ingestion time or read time.\n    \"\"\"\n    print(\"\\n=== Lakehouse (raw landing + governed transform) ===\")\n    if os.path.exists(tmp_path):\n        shutil.rmtree(tmp_path)\n    os.makedirs(tmp_path, exist_ok=True)\n\n    bronze = pd.DataFrame(RAW_ORDERS)\n    bronze_path = os.path.join(tmp_path, \"bronze_orders.parquet\")\n    bronze.to_parquet(bronze_path)\n    print(f\"Bronze (raw, everything lands as-is, {len(bronze)} rows) written to {bronze_path}\")\n\n    good, rejected = clean_and_validate(RAW_ORDERS)\n    silver = pd.DataFrame(good)\n    silver_path = os.path.join(tmp_path, \"silver_orders.parquet\")\n    silver.to_parquet(silver_path)\n    print(f\"Silver (governed, schema-checked, {len(silver)} rows, {len(rejected)} quarantined)\")\n    print(\"written to\", silver_path)\n    print(\"Bronze is never lost, so a bad Silver rule can always be re-run from Bronze  - \")\n    print(\"this is what Day 1's Delta Lake lab then adds ACID/versioning/time-travel on top of.\")\n    return bronze, silver\n\n\ndef main():\n    warehouse_table = etl_style_warehouse_load()\n    lake_table = elt_style_lake_load()\n    bronze, silver = lakehouse_style_load()\n\n    print(\"\\n=== Summary ===\")\n    print(f\"{'Pattern':&lt;12}{'Rows at rest':&lt;15}{'Quality gate timing':&lt;25}\")\n    print(f\"{'Warehouse':&lt;12}{len(warehouse_table):&lt;15}{'before load (ETL)':&lt;25}\")\n    print(f\"{'Lake':&lt;12}{len(lake_table):&lt;15}{'on read, later (ELT)':&lt;25}\")\n    print(f\"{'Lakehouse':&lt;12}{len(bronze)} raw / {len(silver)} governed   {'both: raw at load, governed after':&lt;25}\")\n\n\nmain()\n\n\n=== ETL (transform-before-load)  -  Data Warehouse style ===\nRows accepted into the warehouse table: 2\nRows rejected before ever reaching storage: 2\n  dropped ORD003: missing product\n  dropped ORD004: corrupt price\n  order_id user_id product   price  quantity\n0   ORD001  User_A  Laptop  1200.0         1\n1   ORD002  User_B   Mouse    25.5         2\nTrade-off: storage is always clean, but the transform logic is baked into the\ningestion job  -  changing a business rule means re-running ingestion from source.\n\n=== ELT (load-before-transform)  -  Data Lake style ===\nRows landed in the raw lake zone (no gate at all): 4\n  order_id user_id     product         price quantity\n0   ORD001  User_A      Laptop       1200.00        1\n1   ORD002  User_B       Mouse          25.5        2\n2   ORD003  User_C         NaN         75.00        1\n3   ORD004  User_A  Headphones  not_a_number        1\nTransforming later, on read, against whatever is sitting in the lake...\nOnly discovered at transform time that 2 row(s) were bad  - \nthey already consumed storage and could have been queried by someone else first.\nTrade-off: ingestion is fast and never blocks on data quality, but the raw zone\ncan silently become a 'data swamp' if nothing ever comes back to clean it up.\n\n=== Lakehouse (raw landing + governed transform) ===\nBronze (raw, everything lands as-is, 4 rows) written to ./data/etl_elt_demo\\bronze_orders.parquet\nSilver (governed, schema-checked, 2 rows, 2 quarantined)\nwritten to ./data/etl_elt_demo\\silver_orders.parquet\nBronze is never lost, so a bad Silver rule can always be re-run from Bronze  - \nthis is what Day 1's Delta Lake lab then adds ACID/versioning/time-travel on top of.\n\n=== Summary ===\nPattern     Rows at rest   Quality gate timing      \nWarehouse   2              before load (ETL)        \nLake        4              on read, later (ELT)     \nLakehouse   4 raw / 2 governed   both: raw at load, governed after",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Lab: Mini-Lakehouse with Delta Lake"
    ]
  },
  {
    "objectID": "L01/Day1_Lab.html#part-2-mini-lakehouse-with-delta-lake",
    "href": "L01/Day1_Lab.html#part-2-mini-lakehouse-with-delta-lake",
    "title": "Lab: Mini-Lakehouse with Delta Lake",
    "section": "",
    "text": "Everything below is the annotated walkthrough of a local Delta Lake mini-lakehouse: ACID transactions, schema enforcement, time travel, OPTIMIZE/Z-ORDER, and schema evolution.\n\n!pip install delta-spark\n\n\nCollecting delta-spark\n\n  Downloading delta_spark-4.3.0-py3-none-any.whl.metadata (2.2 kB)\n\nRequirement already satisfied: pyspark&lt;=4.1.1,&gt;=4.0.1 in /usr/local/lib/python3.12/dist-packages (from delta-spark) (4.0.3)\n\nRequirement already satisfied: importlib_metadata&lt;=8.7.1,&gt;=1.0.0 in /usr/local/lib/python3.12/dist-packages (from delta-spark) (8.7.1)\n\nRequirement already satisfied: zipp&gt;=3.20 in /usr/local/lib/python3.12/dist-packages (from importlib_metadata&lt;=8.7.1,&gt;=1.0.0-&gt;delta-spark) (4.1.0)\n\nRequirement already satisfied: py4j&lt;0.10.9.10,&gt;=0.10.9.7 in /usr/local/lib/python3.12/dist-packages (from pyspark&lt;=4.1.1,&gt;=4.0.1-&gt;delta-spark) (0.10.9.9)\n\nDownloading delta_spark-4.3.0-py3-none-any.whl (53 kB)\n\n   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 0.0/53.9 kB ? eta -:--:--\n   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 53.9/53.9 kB 2.1 MB/s eta 0:00:00\n\nInstalling collected packages: delta-spark\n\nSuccessfully installed delta-spark-4.3.0\n\n\n\n\n\nimport glob\nimport json\nimport os\nimport shutil\n\nfrom delta import configure_spark_with_delta_pip\nfrom delta.tables import DeltaTable\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.types import (\n    DoubleType,\n    IntegerType,\n    StructField,\n    StructType,\n    StringType,\n)\n\n\n# ─────────────────────────────────────────────────────────────────────────────\n# SESSION SETUP\n# ─────────────────────────────────────────────────────────────────────────────\n\ndef create_spark_session() -&gt; SparkSession:\n    builder = (\n        SparkSession.builder\n        .appName(\"MiniLakehouse_Extended\")\n        .master(\"local[*]\")\n        .config(\"spark.sql.extensions\",\n                \"io.delta.sql.DeltaSparkSessionExtension\")\n        .config(\"spark.sql.catalog.spark_catalog\",\n                \"org.apache.spark.sql.delta.catalog.DeltaCatalog\")\n        .config(\"spark.sql.warehouse.dir\", \"./data/warehouse\")\n        # Enable auto-optimization (Delta Lake 2.x+)\n        .config(\"spark.databricks.delta.optimizeWrite.enabled\", \"true\")\n        .config(\"spark.databricks.delta.autoCompact.enabled\",   \"true\")\n    )\n    return configure_spark_with_delta_pip(builder).getOrCreate()\n\n\nORDER_SCHEMA = StructType([\n    StructField(\"order_id\",  StringType(),  nullable=False),\n    StructField(\"user_id\",   StringType(),  nullable=True),\n    StructField(\"product\",   StringType(),  nullable=True),\n    StructField(\"price\",     DoubleType(),  nullable=True),\n    StructField(\"quantity\",  IntegerType(), nullable=True),\n    StructField(\"region\",    StringType(),  nullable=True),\n])\n\n\ndef sep(label: str) -&gt; None:\n    print(f\"\\n{'=' * 65}\")\n    print(f\"  {label}\")\n    print(\"=\" * 65)\n\n\n# ─────────────────────────────────────────────────────────────────────────────\n# MAIN\n# ─────────────────────────────────────────────────────────────────────────────\n\ndef main() -&gt; None:\n    TABLE_PATH = \"./data/delta/orders\"\n\n    if os.path.exists(\"./data\"):\n        shutil.rmtree(\"./data\")\n\n    print(\"\\nBooting Spark session with Delta Lake engine...\")\n    spark = create_spark_session()\n    spark.sparkContext.setLogLevel(\"ERROR\")\n\n    # ── STEP 1: Initial Write ─────────────────────────────────────────────────\n    sep(\"STEP 1 — Initial Batch Ingestion (WRITE, partitioned by region)\")\n\n    batch_1 = [\n        (\"ORD001\", \"usr_alice\",  \"Laptop\",      1200.00, 1, \"MENA\"),\n        (\"ORD002\", \"usr_bob\",    \"Mouse\",          25.50, 2, \"EU\"),\n        (\"ORD003\", \"usr_carol\",  \"Keyboard\",       75.00, 1, \"US\"),\n        (\"ORD004\", \"usr_alice\",  \"Headphones\",    150.00, 1, \"MENA\"),\n        (\"ORD005\", \"usr_dave\",   \"Monitor\",       300.00, 2, \"EU\"),\n    ]\n    df1 = spark.createDataFrame(batch_1, ORDER_SCHEMA)\n    (df1.write\n        .format(\"delta\")\n        .mode(\"overwrite\")\n        .partitionBy(\"region\")     # Physical partitioning — Spark skips entire\n        .save(TABLE_PATH))         # partition directories during filtered queries.\n\n    print(\"Delta table written and partitioned by 'region'.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).show()\n\n    # ── STEP 2: ACID Append ───────────────────────────────────────────────────\n    sep(\"STEP 2 — ACID Append Transaction\")\n\n    batch_2 = [\n        (\"ORD006\", \"usr_eve\",   \"Tablet\",   200.00, 1, \"US\"),\n        (\"ORD007\", \"usr_bob\",   \"Webcam\",    80.00, 3, \"EU\"),\n    ]\n    df2 = spark.createDataFrame(batch_2, ORDER_SCHEMA)\n    df2.write.format(\"delta\").mode(\"append\").save(TABLE_PATH)\n\n    print(\"Appended 2 new rows atomically. All-or-nothing guarantee enforced.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).show()\n\n    # ── STEP 3: Schema Enforcement ────────────────────────────────────────────\n    sep(\"STEP 3 — Schema Enforcement (Reject Malformed Ingestion)\")\n    print(\"Attempting to write a row with an undeclared 'discount' column...\")\n\n    bad_schema = StructType([\n        StructField(\"order_id\",  StringType(),  True),\n        StructField(\"user_id\",   StringType(),  True),\n        StructField(\"product\",   StringType(),  True),\n        StructField(\"price\",     DoubleType(),  True),\n        StructField(\"quantity\",  IntegerType(), True),\n        StructField(\"region\",    StringType(),  True),\n        StructField(\"discount\",  DoubleType(),  True),  # Not in the registered schema\n    ])\n    df_bad = spark.createDataFrame(\n        [(\"ORD999\", \"usr_hacker\", \"Free Item\", 0.0, 99, \"MENA\", 100.0)],\n        bad_schema,\n    )\n    try:\n        df_bad.write.format(\"delta\").mode(\"append\").save(TABLE_PATH)\n    except Exception as e:\n        print(\"Schema violation caught by Delta Lake — write REJECTED.\")\n        print(f\"  Error: {str(e).splitlines()[0]}\")\n        print(\"\\nWhy this matters: without schema enforcement a single misconfigured\")\n        print(\"upstream pipeline can silently corrupt a production lakehouse table.\")\n\n    # ── STEP 4: MERGE / UPSERT ────────────────────────────────────────────────\n    sep(\"STEP 4 — MERGE / UPSERT (Atomic Update + Insert in One Transaction)\")\n    print(\"\"\"\nMERGE is the most powerful Delta operation — impossible in a raw data lake:\n  • If a matching record exists  → UPDATE specific columns\n  • If no matching record        → INSERT the new row\n  • All in one atomic transaction (either all commits or nothing commits)\n\nUse case: a nightly CDC (Change Data Capture) feed from a production database\nthat may contain both price corrections and new orders in the same payload.\n\"\"\")\n    upsert_data = [\n        (\"ORD002\", \"usr_bob\",    \"Mouse\",       29.99, 2, \"EU\"),   # exists → update price\n        (\"ORD010\", \"usr_frank\",  \"SSD Drive\",   95.00, 1, \"US\"),   # new    → insert\n    ]\n    df_upsert = spark.createDataFrame(upsert_data, ORDER_SCHEMA)\n\n    delta_table = DeltaTable.forPath(spark, TABLE_PATH)\n    (\n        delta_table.alias(\"target\")\n        .merge(\n            df_upsert.alias(\"updates\"),\n            \"target.order_id = updates.order_id\",\n        )\n        .whenMatchedUpdate(set={\n            \"price\":    \"updates.price\",\n            \"quantity\": \"updates.quantity\",\n        })\n        .whenNotMatchedInsertAll()\n        .execute()\n    )\n    print(\"MERGE complete. ORD002 price updated to 29.99; ORD010 inserted.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show()\n\n    # ── STEP 5: OPTIMIZE + Z-ORDER ────────────────────────────────────────────\n    sep(\"STEP 5 — OPTIMIZE + Z-ORDER BY (Data Skipping for Query Acceleration)\")\n    print(\"\"\"\nThe small-file problem:\n  Streaming micro-batches and per-partition writes create thousands of tiny\n  Parquet files. Each file read requires a separate I/O operation — at scale\n  this kills query performance.\n\nOPTIMIZE:\n  Merges small files into fewer, larger Parquet files, reducing metadata\n  overhead by up to 99%.\n\nZ-ORDER BY:\n  Physically co-locates rows with similar values in the same Parquet file.\n  Delta reads the _delta_log/ min/max statistics per file. If your query is\n  WHERE user_id = 'usr_alice', Spark skips all files that cannot contain that\n  user — called data skipping.\n\nBenchmark: Z-Ordering a 1 TB table on user_id typically reduces query time\nfrom 4 minutes → 8 seconds on filtered queries.\n\"\"\")\n    # spark.sql(f\"OPTIMIZE delta.`{TABLE_PATH}` ZORDER BY (user_id, region)\") # Original line causing error\n    delta_table.optimize().executeZOrderBy([\"user_id\"])\n    print(\"OPTIMIZE + ZORDER complete.\")\n\n    # ── STEP 6: Delta Transaction Log Inspection ──────────────────────────────\n    sep(\"STEP 6 — Inspecting the Delta Transaction Log (_delta_log/)\")\n    print(\"\"\"\nEvery Delta write appends a JSON file to _delta_log/.\nThis is the single source of truth for ACID compliance, Time Travel, and lineage.\nEach JSON file contains one or more entries:\n  commitInfo  → operation type, timestamp, user, operation parameters\n  add         → new Parquet file paths written in this commit\n  remove      → old Parquet file paths superseded by this commit\n  metaData    → schema changes (only present when schema mutates)\n\nAfter every 10 commits Delta writes a checkpoint.parquet to avoid replaying\nthousands of JSON files on startup — this is the Delta Log compaction process.\n\"\"\")\n    log_files = sorted(glob.glob(f\"{TABLE_PATH}/_delta_log/*.json\"))\n    for log_path in log_files[:4]:\n        print(f\"\\n--- {os.path.basename(log_path)} ---\")\n        with open(log_path, encoding=\"utf-8\") as fh:\n            for line in fh:\n                entry = json.loads(line)\n                if \"commitInfo\" in entry:\n                    info = entry[\"commitInfo\"]\n                    print(f\"  operation  : {info.get('operation', 'N/A')}\")\n                    print(f\"  timestamp  : {info.get('timestamp', 'N/A')}\")\n                    params = info.get(\"operationParameters\", {})\n                    if params:\n                        print(f\"  parameters : {params}\")\n                elif \"add\" in entry:\n                    print(f\"  add file   : {entry['add'].get('path', '')}\")\n                elif \"remove\" in entry:\n                    print(f\"  remove file: {entry['remove'].get('path', '')}\")\n\n    # ── STEP 7: Time Travel ───────────────────────────────────────────────────\n    sep(\"STEP 7 — Time Travel (Query Historical Versions)\")\n\n    print(\"Full audit trail from the Delta Transaction Log:\")\n    delta_table.history().select(\"version\", \"timestamp\", \"operation\").show(truncate=False)\n\n    print(\"Restoring state as of VERSION 0 (initial ingest only):\")\n    spark.read.format(\"delta\").option(\"versionAsOf\", 0).load(TABLE_PATH).show()\n\n    print(\"Restoring state as of VERSION 1 (after first ACID append):\")\n    spark.read.format(\"delta\").option(\"versionAsOf\", 1).load(TABLE_PATH).show()\n\n    # ── STEP 8: VACUUM ────────────────────────────────────────────────────────\n    sep(\"STEP 8 — VACUUM (Storage Retention Cleanup)\")\n    print(\"\"\"\nDelta retains all historical Parquet files to support Time Travel.\nVACUUM permanently deletes files older than the retention window.\n\nWARNING: After VACUUM, Time Travel beyond the retention window is impossible.\n\nProduction recommendation: retentionHours &gt;= 168 (7 days).\nUnder GDPR Article 17 (right to erasure), lower retention may be required —\nbut confirm with your legal team before reducing below 168 hours.\n\nSetting 0 hours here for demonstration only — NEVER do this in production.\n\"\"\")\n    spark.conf.set(\n        \"spark.databricks.delta.retentionDurationCheck.enabled\", \"false\"\n    )\n    delta_table.vacuum(retentionHours=0)\n    print(\"VACUUM complete. Orphaned files removed.\")\n\n    # ── STEP 9: Schema Evolution ──────────────────────────────────────────────\n    sep(\"STEP 9 — Schema Evolution (Safe Column Addition via mergeSchema)\")\n    print(\"\"\"\nScenario: a product team wants to add a 'discount' column to the orders feed.\n\nWithout Schema Evolution: the write would be REJECTED (Step 3 behaviour).\nWith mergeSchema=True:   the new column is added to the registered schema.\n                         Existing rows receive NULL for the new column.\n\nThis is the production-safe migration path:\n  1. Data contract teams agree on the new column definition.\n  2. Producer writes first batch with mergeSchema=True.\n  3. Delta propagates the schema change; all downstream readers automatically\n     see the new nullable column on their next read.\n\"\"\")\n    evolved_schema = StructType([\n        StructField(\"order_id\",  StringType(),  True),\n        StructField(\"user_id\",   StringType(),  True),\n        StructField(\"product\",   StringType(),  True),\n        StructField(\"price\",     DoubleType(),  True),\n        StructField(\"quantity\",  IntegerType(), True),\n        StructField(\"region\",    StringType(),  True),\n        StructField(\"discount\",  DoubleType(),  True),  # new column\n    ])\n    df_evolved = spark.createDataFrame(\n        [(\"ORD011\", \"usr_grace\", \"SmartWatch\", 250.00, 1, \"MENA\", 15.0)],\n        evolved_schema,\n    )\n    (df_evolved.write\n               .format(\"delta\")\n               .mode(\"append\")\n               .option(\"mergeSchema\", \"true\")\n               .save(TABLE_PATH))\n\n    print(\"New column 'discount' merged into existing schema without downtime.\")\n    print(\"Pre-evolution rows show NULL for 'discount' — backward compatible:\")\n    spark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show()\n\n    spark.stop()\n    print(\"\\nMini Lakehouse lab complete. All 9 steps executed successfully.\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\nBooting Spark session with Delta Lake engine...\n\n=================================================================\n  STEP 1 — Initial Batch Ingestion (WRITE, partitioned by region)\n=================================================================\nDelta table written and partitioned by 'region'.\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD002|  usr_bob|     Mouse|  25.5|       2|    EU|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n+--------+---------+----------+------+--------+------+\n\n\n=================================================================\n  STEP 2 — ACID Append Transaction\n=================================================================\nAppended 2 new rows atomically. All-or-nothing guarantee enforced.\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD002|  usr_bob|     Mouse|  25.5|       2|    EU|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n|  ORD007|  usr_bob|    Webcam|  80.0|       3|    EU|\n|  ORD006|  usr_eve|    Tablet| 200.0|       1|    US|\n+--------+---------+----------+------+--------+------+\n\n\n=================================================================\n  STEP 3 — Schema Enforcement (Reject Malformed Ingestion)\n=================================================================\nAttempting to write a row with an undeclared 'discount' column...\nSchema violation caught by Delta Lake — write REJECTED.\n  Error: [DELTA_METADATA_MISMATCH] A metadata mismatch was detected when writing to the Delta table.\n\nWhy this matters: without schema enforcement a single misconfigured\nupstream pipeline can silently corrupt a production lakehouse table.\n\n=================================================================\n  STEP 4 — MERGE / UPSERT (Atomic Update + Insert in One Transaction)\n=================================================================\n\nMERGE is the most powerful Delta operation — impossible in a raw data lake:\n  • If a matching record exists  → UPDATE specific columns\n  • If no matching record        → INSERT the new row\n  • All in one atomic transaction (either all commits or nothing commits)\n\nUse case: a nightly CDC (Change Data Capture) feed from a production database\nthat may contain both price corrections and new orders in the same payload.\n\nMERGE complete. ORD002 price updated to 29.99; ORD010 inserted.\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD002|  usr_bob|     Mouse| 29.99|       2|    EU|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n|  ORD006|  usr_eve|    Tablet| 200.0|       1|    US|\n|  ORD007|  usr_bob|    Webcam|  80.0|       3|    EU|\n|  ORD010|usr_frank| SSD Drive|  95.0|       1|    US|\n+--------+---------+----------+------+--------+------+\n\n\n=================================================================\n  STEP 5 — OPTIMIZE + Z-ORDER BY (Data Skipping for Query Acceleration)\n=================================================================\n\nThe small-file problem:\n  Streaming micro-batches and per-partition writes create thousands of tiny\n  Parquet files. Each file read requires a separate I/O operation — at scale\n  this kills query performance.\n\nOPTIMIZE:\n  Merges small files into fewer, larger Parquet files, reducing metadata\n  overhead by up to 99%.\n\nZ-ORDER BY:\n  Physically co-locates rows with similar values in the same Parquet file.\n  Delta reads the _delta_log/ min/max statistics per file. If your query is\n  WHERE user_id = 'usr_alice', Spark skips all files that cannot contain that\n  user — called data skipping.\n\nBenchmark: Z-Ordering a 1 TB table on user_id typically reduces query time\nfrom 4 minutes → 8 seconds on filtered queries.\n\nOPTIMIZE + ZORDER complete.\n\n=================================================================\n  STEP 6 — Inspecting the Delta Transaction Log (_delta_log/)\n=================================================================\n\nEvery Delta write appends a JSON file to _delta_log/.\nThis is the single source of truth for ACID compliance, Time Travel, and lineage.\nEach JSON file contains one or more entries:\n  commitInfo  → operation type, timestamp, user, operation parameters\n  add         → new Parquet file paths written in this commit\n  remove      → old Parquet file paths superseded by this commit\n  metaData    → schema changes (only present when schema mutates)\n\nAfter every 10 commits Delta writes a checkpoint.parquet to avoid replaying\nthousands of JSON files on startup — this is the Delta Log compaction process.\n\n\n--- 00000000000000000000.json ---\n  operation  : WRITE\n  timestamp  : 1782584625055\n  parameters : {'mode': 'Overwrite', 'partitionBy': '[\"region\"]'}\n  add file   : region=MENA/part-00000-4b8ce36e-f285-461d-abbf-7e8c3386120a.c000.snappy.parquet\n  add file   : region=EU/part-00001-82437434-e654-44e9-a2a7-ae28bfee2c2d.c000.snappy.parquet\n  add file   : region=US/part-00002-ebfabcb7-73db-424c-ad49-2f45037d7ba0.c000.snappy.parquet\n\n--- 00000000000000000001.json ---\n  operation  : WRITE\n  timestamp  : 1782584635165\n  parameters : {'mode': 'Append', 'partitionBy': '[]'}\n  add file   : region=EU/part-00000-c119c505-a5bb-4c3b-a2c7-5b9da73d0380.c000.snappy.parquet\n  add file   : region=US/part-00001-ff37bf89-d4ed-44c0-9629-0f6d09bfe257.c000.snappy.parquet\n\n--- 00000000000000000002.json ---\n  operation  : MERGE\n  timestamp  : 1782584644731\n  parameters : {'predicate': '[\"(order_id#9861 = order_id#9855)\"]', 'matchedPredicates': '[{\"actionType\":\"update\"}]', 'notMatchedPredicates': '[{\"actionType\":\"insert\"}]', 'notMatchedBySourcePredicates': '[]'}\n  add file   : region=EU/part-00000-ce1d0a99-f3d5-40ee-8916-21491735a60d.c000.snappy.parquet\n  add file   : region=US/part-00000-5c241594-0759-490c-beda-26aeb6be95d2.c000.snappy.parquet\n  remove file: region=EU/part-00001-82437434-e654-44e9-a2a7-ae28bfee2c2d.c000.snappy.parquet\n\n--- 00000000000000000003.json ---\n  operation  : OPTIMIZE\n  timestamp  : 1782584654016\n  parameters : {'predicate': '[]', 'zOrderBy': '[\"user_id\"]', 'clusterBy': '[]', 'auto': False}\n  add file   : region=MENA/part-00000-6bbd99d1-bddd-4909-bf1e-7d3d884c287e.c000.snappy.parquet\n  remove file: region=MENA/part-00000-4b8ce36e-f285-461d-abbf-7e8c3386120a.c000.snappy.parquet\n  add file   : region=EU/part-00000-f60c2c85-ece8-49fd-b4ce-ce3ef04efe55.c000.snappy.parquet\n  remove file: region=EU/part-00000-c119c505-a5bb-4c3b-a2c7-5b9da73d0380.c000.snappy.parquet\n  remove file: region=EU/part-00000-ce1d0a99-f3d5-40ee-8916-21491735a60d.c000.snappy.parquet\n  add file   : region=US/part-00000-9045252d-4b7a-489b-9181-dc56995a5068.c000.snappy.parquet\n  remove file: region=US/part-00001-ff37bf89-d4ed-44c0-9629-0f6d09bfe257.c000.snappy.parquet\n  remove file: region=US/part-00002-ebfabcb7-73db-424c-ad49-2f45037d7ba0.c000.snappy.parquet\n  remove file: region=US/part-00000-5c241594-0759-490c-beda-26aeb6be95d2.c000.snappy.parquet\n\n=================================================================\n  STEP 7 — Time Travel (Query Historical Versions)\n=================================================================\nFull audit trail from the Delta Transaction Log:\n+-------+-----------------------+---------+\n|version|timestamp              |operation|\n+-------+-----------------------+---------+\n|3      |2026-06-27 18:24:14.037|OPTIMIZE |\n|2      |2026-06-27 18:24:04.763|MERGE    |\n|1      |2026-06-27 18:23:55.183|WRITE    |\n|0      |2026-06-27 18:23:45.071|WRITE    |\n+-------+-----------------------+---------+\n\nRestoring state as of VERSION 0 (initial ingest only):\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD002|  usr_bob|     Mouse|  25.5|       2|    EU|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n+--------+---------+----------+------+--------+------+\n\nRestoring state as of VERSION 1 (after first ACID append):\n+--------+---------+----------+------+--------+------+\n|order_id|  user_id|   product| price|quantity|region|\n+--------+---------+----------+------+--------+------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|\n|  ORD002|  usr_bob|     Mouse|  25.5|       2|    EU|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|\n|  ORD007|  usr_bob|    Webcam|  80.0|       3|    EU|\n|  ORD006|  usr_eve|    Tablet| 200.0|       1|    US|\n+--------+---------+----------+------+--------+------+\n\n\n=================================================================\n  STEP 8 — VACUUM (Storage Retention Cleanup)\n=================================================================\n\nDelta retains all historical Parquet files to support Time Travel.\nVACUUM permanently deletes files older than the retention window.\n\nWARNING: After VACUUM, Time Travel beyond the retention window is impossible.\n\nProduction recommendation: retentionHours &gt;= 168 (7 days).\nUnder GDPR Article 17 (right to erasure), lower retention may be required —\nbut confirm with your legal team before reducing below 168 hours.\n\nSetting 0 hours here for demonstration only — NEVER do this in production.\n\nVACUUM complete. Orphaned files removed.\n\n=================================================================\n  STEP 9 — Schema Evolution (Safe Column Addition via mergeSchema)\n=================================================================\n\nScenario: a product team wants to add a 'discount' column to the orders feed.\n\nWithout Schema Evolution: the write would be REJECTED (Step 3 behaviour).\nWith mergeSchema=True:   the new column is added to the registered schema.\n                         Existing rows receive NULL for the new column.\n\nThis is the production-safe migration path:\n  1. Data contract teams agree on the new column definition.\n  2. Producer writes first batch with mergeSchema=True.\n  3. Delta propagates the schema change; all downstream readers automatically\n     see the new nullable column on their next read.\n\nNew column 'discount' merged into existing schema without downtime.\nPre-evolution rows show NULL for 'discount' — backward compatible:\n+--------+---------+----------+------+--------+------+--------+\n|order_id|  user_id|   product| price|quantity|region|discount|\n+--------+---------+----------+------+--------+------+--------+\n|  ORD001|usr_alice|    Laptop|1200.0|       1|  MENA|    NULL|\n|  ORD002|  usr_bob|     Mouse| 29.99|       2|    EU|    NULL|\n|  ORD003|usr_carol|  Keyboard|  75.0|       1|    US|    NULL|\n|  ORD004|usr_alice|Headphones| 150.0|       1|  MENA|    NULL|\n|  ORD005| usr_dave|   Monitor| 300.0|       2|    EU|    NULL|\n|  ORD006|  usr_eve|    Tablet| 200.0|       1|    US|    NULL|\n|  ORD007|  usr_bob|    Webcam|  80.0|       3|    EU|    NULL|\n|  ORD010|usr_frank| SSD Drive|  95.0|       1|    US|    NULL|\n|  ORD011|usr_grace|SmartWatch| 250.0|       1|  MENA|    15.0|\n+--------+---------+----------+------+--------+------+--------+\n\n\nMini Lakehouse lab complete. All 9 steps executed successfully.\n\n\nLet’s start by explaining the import statements and then move to the function definitions.\n\n\n\nimport glob, import json, import os, import shutil: These lines import standard Python modules for working with file paths (glob, os), JSON data (json), and high-level file operations like deleting directories (shutil).\nfrom delta import configure_spark_with_delta_pip: This imports a specific function from the delta library to simplify setting up a Spark session with Delta Lake capabilities.\nfrom delta.tables import DeltaTable: This imports the DeltaTable class, which is used to interact with Delta Lake tables programmatically.\nfrom pyspark.sql import SparkSession: Imports the main entry point for Spark functionality.\nfrom pyspark.sql.types import (...): Imports various data types (DoubleType, IntegerType, StructField, StructType, StringType) from PySpark’s SQL module, which are used to define the schema of DataFrames.\n\n\n\n\nThis section defines functions and schema that are crucial for setting up the Spark environment and defining the data structure.\n\n\nThis function is responsible for creating and configuring a SparkSession with Delta Lake extensions.\ndef create_spark_session() -&gt; SparkSession:\n    builder = (\n        SparkSession.builder\n        .appName(\"MiniLakehouse_Extended\")\n        .master(\"local[*]\")\n        .config(\"spark.sql.extensions\",\n                \"io.delta.sql.DeltaSparkSessionExtension\")\n        .config(\"spark.sql.catalog.spark_catalog\",\n                \"org.apache.spark.sql.delta.catalog.DeltaCatalog\")\n        .config(\"spark.sql.warehouse.dir\", \"./data/warehouse\")\n        .config(\"spark.databricks.delta.optimizeWrite.enabled\", \"true\")\n        .config(\"spark.databricks.delta.autoCompact.enabled\",   \"true\")\n    )\n    return configure_spark_with_delta_pip(builder).getOrCreate()\n\ndef create_spark_session() -&gt; SparkSession:: Defines a function named create_spark_session that returns a SparkSession object.\nbuilder = (...): Initializes a SparkSession.builder object to configure various Spark settings.\n.appName(\"MiniLakehouse_Extended\"): Sets the name of the Spark application.\n.master(\"local[*]\"): Configures Spark to run in local mode, using all available CPU cores.\n.config(\"spark.sql.extensions\", \"io.delta.sql.DeltaSparkSessionExtension\"): Enables Delta Lake SQL functionalities.\n.config(\"spark.sql.catalog.spark_catalog\", \"org.apache.spark.sql.delta.catalog.DeltaCatalog\"): Registers Delta Lake as the default catalog for Spark SQL, allowing it to manage Delta tables.\n.config(\"spark.sql.warehouse.dir\", \"./data/warehouse\"): Specifies the directory where Spark will store its warehouse data.\n.config(\"spark.databricks.delta.optimizeWrite.enabled\", \"true\"): Enables automatic optimization during write operations, which helps in merging small files.\n.config(\"spark.databricks.delta.autoCompact.enabled\", \"true\"): Enables automatic compaction of small files to improve read performance.\nreturn configure_spark_with_delta_pip(builder).getOrCreate(): Applies the Delta Lake configurations and then creates and returns the SparkSession.\n\n\n\n\nThis defines the structure of the data that will be stored in the Delta table.\nORDER_SCHEMA = StructType([\n    StructField(\"order_id\",  StringType(),  nullable=False),\n    StructField(\"user_id\",   StringType(),  nullable=True),\n    StructField(\"product\",   StringType(),  nullable=True),\n    StructField(\"price\",     DoubleType(),  nullable=True),\n    StructField(\"quantity\",  IntegerType(), nullable=True),\n    StructField(\"region\",    StringType(),  nullable=True),\n])\n\nORDER_SCHEMA = StructType([...]): Defines a StructType which represents the schema of a DataFrame. It’s a list of StructField objects.\nStructField(\"order_id\", StringType(), nullable=False): Defines a column named order_id of type StringType, which cannot be null.\nSimilarly, other StructField entries define user_id, product, price, quantity, and region with their respective types and nullability settings. This schema ensures data consistency in the Delta table.\n\n\n\n\nThis is a simple helper function for printing separators in the console output to make the different steps more readable.\ndef sep(label: str) -&gt; None:\n    print(f\"\\n{'=' * 65}\")\n    print(f\"  {label}\")\n    print(\"=\" * 65)\n\ndef sep(label: str) -&gt; None:: Defines a function named sep that takes a string label and returns nothing (None).\nprint(f\"\\n{'=' * 65}\"): Prints a line of 65 equal signs, followed by a newline, to create a visual separator.\nprint(f\"  {label}\"): Prints the provided label centered between the lines.\nprint(\"=\" * 65): Prints another line of 65 equal signs.\n\n\n\n\n\nThis is the core of the script, demonstrating various Delta Lake functionalities step by step.\ndef main() -&gt; None:\n    TABLE_PATH = \"./data/delta/orders\"\n\n    if os.path.exists(\"./data\"):\n        shutil.rmtree(\"./data\")\n\n    print(\"\\nBooting Spark session with Delta Lake engine...\")\n    spark = create_spark_session()\n    spark.sparkContext.setLogLevel(\"ERROR\")\n\ndef main() -&gt; None:: Defines the main function of the script.\nTABLE_PATH = \"./data/delta/orders\": Sets a variable TABLE_PATH to define the location where the Delta table will be stored.\nif os.path.exists(\"./data\"): shutil.rmtree(\"./data\"): Checks if a directory named data exists. If it does, it deletes the entire directory and its contents to ensure a clean start for the demonstration.\nprint(\"\\nBooting Spark session with Delta Lake engine...\"): Prints a message indicating that the Spark session is starting.\nspark = create_spark_session(): Calls the create_spark_session function (explained above) to initialize a SparkSession and assigns it to the spark variable.\nspark.sparkContext.setLogLevel(\"ERROR\"): Sets the logging level for Spark to ERROR, meaning only error messages will be displayed, reducing verbose output.\n\n\n\nThis step demonstrates how to create a new Delta table and write initial data to it, partitioned by the ‘region’ column.\n    sep(\"STEP 1 — Initial Batch Ingestion (WRITE, partitioned by region)\")\n\n    batch_1 = [\n        (\"ORD001\", \"usr_alice\",  \"Laptop\",      1200.00, 1, \"MENA\"),\n        (\"ORD002\", \"usr_bob\",    \"Mouse\",          25.50, 2, \"EU\"),\n        (\"ORD003\", \"usr_carol\",  \"Keyboard\",       75.00, 1, \"US\"),\n        (\"ORD004\", \"usr_alice\",  \"Headphones\",    150.00, 1, \"MENA\"),\n        (\"ORD005\", \"usr_dave\",   \"Monitor\",       300.00, 2, \"EU\"),\n    ]\n    df1 = spark.createDataFrame(batch_1, ORDER_SCHEMA)\n    (df1.write\n        .format(\"delta\")\n        .mode(\"overwrite\")\n        .partitionBy(\"region\")\n        .save(TABLE_PATH))\n\n    print(\"Delta table written and partitioned by 'region'.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).show()\n\nsep(...): Prints a separator with the step title.\nbatch_1 = [...]: Defines a list of tuples, where each tuple represents a row of data for the first batch.\ndf1 = spark.createDataFrame(batch_1, ORDER_SCHEMA): Creates a Spark DataFrame df1 from batch_1 using the predefined ORDER_SCHEMA.\n(df1.write.format(\"delta\").mode(\"overwrite\").partitionBy(\"region\").save(TABLE_PATH)): This chain of methods performs the write operation:\n\n.format(\"delta\"): Specifies that the data should be written in Delta Lake format.\n.mode(\"overwrite\"): If the table already exists at TABLE_PATH, it will be completely overwritten.\n.partitionBy(\"region\"): Organizes the data physically on disk into subdirectories based on the ‘region’ column’s values. This optimizes queries that filter by ‘region’.\n.save(TABLE_PATH): Writes the DataFrame to the specified TABLE_PATH.\n\nprint(...): Prints a confirmation message.\nspark.read.format(\"delta\").load(TABLE_PATH).show(): Reads the newly created Delta table and displays its contents.\n\n\n\n\nThis step demonstrates how to append new data to an existing Delta table, leveraging Delta Lake’s ACID (Atomicity, Consistency, Isolation, Durability) properties.\n    sep(\"STEP 2 — ACID Append Transaction\")\n\n    batch_2 = [\n        (\"ORD006\", \"usr_eve\",   \"Tablet\",   200.00, 1, \"US\"),\n        (\"ORD007\", \"usr_bob\",   \"Webcam\",    80.00, 3, \"EU\"),\n    ]\n    df2 = spark.createDataFrame(batch_2, ORDER_SCHEMA)\n    df2.write.format(\"delta\").mode(\"append\").save(TABLE_PATH)\n\n    print(\"Appended 2 new rows atomically. All-or-nothing guarantee enforced.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).show()\n\nsep(...): Prints a separator with the step title.\nbatch_2 = [...]: Defines a list of tuples for the second batch of data.\ndf2 = spark.createDataFrame(batch_2, ORDER_SCHEMA): Creates a Spark DataFrame df2 from batch_2 using the ORDER_SCHEMA.\ndf2.write.format(\"delta\").mode(\"append\").save(TABLE_PATH): Appends df2 to the existing Delta table at TABLE_PATH. mode(\"append\") ensures that new data is added without affecting existing data. This is an atomic operation, meaning it either fully succeeds or fully fails, preventing partial writes.\nprint(...): Prints a confirmation message about the atomic append.\nspark.read.format(\"delta\").load(TABLE_PATH).show(): Reads and displays the updated Delta table, showing the newly appended rows.\n\n\n\n\nThis step illustrates Delta Lake’s schema enforcement, which prevents writing data that doesn’t conform to the table’s defined schema, thus maintaining data quality.\n    sep(\"STEP 3 — Schema Enforcement (Reject Malformed Ingestion)\")\n    print(\"Attempting to write a row with an undeclared 'discount' column...\")\n\n    bad_schema = StructType([\n        StructField(\"order_id\",  StringType(),  True),\n        StructField(\"user_id\",   StringType(),  True),\n        StructField(\"product\",   StringType(),  True),\n        StructField(\"price\",     DoubleType(),  True),\n        StructField(\"quantity\",  IntegerType(), True),\n        StructField(\"region\",    StringType(),  True),\n        StructField(\"discount\",  DoubleType(),  True),  # Not in the registered schema\n    ])\n    df_bad = spark.createDataFrame(\n        [(\"ORD999\", \"usr_hacker\", \"Free Item\", 0.0, 99, \"MENA\", 100.0)],\n        bad_schema,\n    )\n    try:\n        df_bad.write.format(\"delta\").mode(\"append\").save(TABLE_PATH)\n    except Exception as e:\n        print(\"Schema violation caught by Delta Lake — write REJECTED.\")\n        print(f\"  Error: {str(e).splitlines()[0]}\")\n        print(\"\\nWhy this matters: without schema enforcement a single misconfigured\")\n        print(\"upstream pipeline can silently corrupt a production lakehouse table.\")\n\nsep(...): Prints a separator with the step title.\nprint(...): Explains the upcoming attempt to write data with a mismatched schema.\nbad_schema = StructType([...]): Defines a new schema that includes an additional column, discount, which is not present in the original ORDER_SCHEMA.\ndf_bad = spark.createDataFrame(...): Creates a DataFrame df_bad using this bad_schema.\ntry...except Exception as e:: This block attempts to write df_bad to the Delta table. Because of schema enforcement, this write operation is expected to fail.\n\ndf_bad.write.format(\"delta\").mode(\"append\").save(TABLE_PATH): Tries to append the DataFrame with the bad_schema.\nThe except block catches the expected exception (a schema violation) and prints messages explaining why the write was rejected and the importance of schema enforcement for data integrity.\n\n\n\n\n\nThis step showcases the powerful MERGE operation, which allows for atomic updates and inserts (upserts) in a single transaction, a capability often found in traditional data warehouses.\n    sep(\"STEP 4 — MERGE / UPSERT (Atomic Update + Insert in One Transaction)\")\n    print(\"\"\"\nMERGE is the most powerful Delta operation — impossible in a raw data lake:\n  • If a matching record exists  → UPDATE specific columns\n  • If no matching record        → INSERT the new row\n  • All in one atomic transaction (either all commits or nothing commits)\n\nUse case: a nightly CDC (Change Data Capture) feed from a production database\nthat may contain both price corrections and new orders in the same payload.\n\"\"\")\n    upsert_data = [\n        (\"ORD002\", \"usr_bob\",    \"Mouse\",       29.99, 2, \"EU\"),   # exists → update price\n        (\"ORD010\", \"usr_frank\",  \"SSD Drive\",   95.00, 1, \"US\"),   # new    → insert\n    ]\n    df_upsert = spark.createDataFrame(upsert_data, ORDER_SCHEMA)\n\n    delta_table = DeltaTable.forPath(spark, TABLE_PATH)\n    (\n        delta_table.alias(\"target\")\n        .merge(\n            df_upsert.alias(\"updates\"),\n            \"target.order_id = updates.order_id\",\n        )\n        .whenMatchedUpdate(set={\n            \"price\":    \"updates.price\",\n            \"quantity\": \"updates.quantity\",\n        })\n        .whenNotMatchedInsertAll()\n        .execute()\n    )\n    print(\"MERGE complete. ORD002 price updated to 29.99; ORD010 inserted.\")\n    spark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show()\n\nsep(...): Prints a separator with the step title.\nprint(...): Provides a detailed explanation of what the MERGE operation does and its benefits.\nupsert_data = [...]: Defines data for the upsert operation. One record (ORD002) already exists in the table and will be updated; another (ORD010) is new and will be inserted.\ndf_upsert = spark.createDataFrame(upsert_data, ORDER_SCHEMA): Creates a DataFrame from the upsert data.\ndelta_table = DeltaTable.forPath(spark, TABLE_PATH): Creates a DeltaTable object, which provides an API for interacting with the Delta table.\ndelta_table.alias(\"target\").merge(...): Starts the merge operation. The existing table is aliased as target.\n\n.merge(df_upsert.alias(\"updates\"), \"target.order_id = updates.order_id\"): Merges the target table with the updates DataFrame (aliased from df_upsert), using order_id as the join key.\n.whenMatchedUpdate(set={...}): Specifies actions to take when a record in updates matches a record in target (based on order_id). Here, it updates the price and quantity columns.\n.whenNotMatchedInsertAll(): Specifies actions to take when a record in updates does not match any record in target. Here, it inserts all columns of the new record.\n.execute(): Executes the merge operation.\n\nprint(...): Prints a confirmation message about the merge results.\nspark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show(): Reads and displays the updated table, sorted by order_id to show the changes clearly.\n\n\n\n\nThis step demonstrates how to optimize the physical layout of the Delta table for faster query performance by consolidating small files and using Z-Ordering.\n    sep(\"STEP 5 — OPTIMIZE + Z-ORDER BY (Data Skipping for Query Acceleration)\")\n    print(\"\"\"\nThe small-file problem:\n  Streaming micro-batches and per-partition writes create thousands of tiny\n  Parquet files. Each file read requires a separate I/O operation — at scale\n  this kills query performance.\n\nOPTIMIZE:\n  Merges small files into fewer, larger Parquet files, reducing metadata\n  overhead by up to 99%.\n\nZ-ORDER BY:\n  Physically co-locates rows with similar values in the same Parquet file.\n  Delta reads the _delta_log/ min/max statistics per file. If your query is\n  WHERE user_id = 'usr_alice', Spark skips all files that cannot contain that\n  user — called data skipping.\n\nBenchmark: Z-Ordering a 1 TB table on user_id typically reduces query time\nfrom 4 minutes → 8 seconds on filtered queries.\n\"\"\")\n    delta_table.optimize().executeZOrderBy([\"user_id\"])\n    print(\"OPTIMIZE + ZORDER complete.\")\n\nsep(...): Prints a separator with the step title.\nprint(...): Explains the\n\nDelta Lake’s OPTIMIZE and Z-ORDER BY features.\n\ndelta_table.optimize().executeZOrderBy([\"user_id\"]): This is the core operation for optimization and Z-ordering.\n\ndelta_table.optimize(): Initiates the optimization process, which aims to coalesce small files into larger ones, reducing the number of files and improving read performance.\n.executeZOrderBy([\"user_id\"]): Applies Z-Ordering on the user_id column. Z-Ordering physically co-locates data records with similar user_id values within the same data files. This allows Delta Lake to use file-level statistics (min/max values) to quickly skip entire files that do not contain the user_id being queried, significantly speeding up filtered queries.\n\nprint(...): Prints a confirmation that the optimization and Z-Ordering are complete.\n\n\n\n\nThis step demonstrates how to directly inspect the transaction log (_delta_log/) of a Delta table, which records every change to the table and enables ACID properties and Time Travel.\n    sep(\"STEP 6 — Inspecting the Delta Transaction Log (_delta_log/)\")\n    print(\"\"\"\nEvery Delta write appends a JSON file to _delta_log/.\nThis is the single source of truth for ACID compliance, Time Travel, and lineage.\nEach JSON file contains one or more entries:\n  commitInfo  → operation type, timestamp, user, operation parameters\n  add         → new Parquet file paths written in this commit\n  remove      → old Parquet file paths superseded by this commit\n  metaData    → schema changes (only present when schema mutates)\n\nAfter every 10 commits Delta writes a checkpoint.parquet to avoid replaying\nthousands of JSON files on startup — this is the Delta Log compaction process.\n\"\"\")\n    log_files = sorted(glob.glob(f\"{TABLE_PATH}/_delta_log/*.json\"))\n    for log_path in log_files[:4]:\n        print(f\"\\n--- {os.path.basename(log_path)} ---\")\n        with open(log_path, encoding=\"utf-8\") as fh:\n            for line in fh:\n                entry = json.loads(line)\n                if \"commitInfo\" in entry:\n                    info = entry[\"commitInfo\"]\n                    print(f\"  operation  : {info.get('operation', 'N/A')}\")\n                    print(f\"  timestamp  : {info.get('timestamp', 'N/A')}\")\n                    params = info.get(\"operationParameters\", {})\n                    if params:\n                        print(f\"  parameters : {params}\")\n                elif \"add\" in entry:\n                    print(f\"  add file   : {entry['add'].get('path', '')}\")\n                elif \"remove\" in entry:\n                    print(f\"  remove file: {entry['remove'].get('path', '')}\")\n\nsep(...): Prints a separator with the step title.\nprint(...): Provides an explanation of the Delta Transaction Log, its structure, and its importance for ACID properties, Time Travel, and lineage.\nlog_files = sorted(glob.glob(f\"{TABLE_PATH}/_delta_log/*.json\")): Uses the glob module to find all JSON files within the _delta_log subdirectory of the Delta table and sorts them chronologically.\nfor log_path in log_files[:4]:: Iterates through the first four transaction log files to demonstrate their content.\nprint(f\"\\n--- {os.path.basename(log_path)} ---\"): Prints the filename of the current log file.\nwith open(log_path, encoding=\"utf-8\") as fh:: Opens each JSON log file for reading.\nfor line in fh:: Each line in a Delta transaction log file represents a single action (e.g., adding a file, removing a file, a commit info).\nentry = json.loads(line): Parses the JSON string from each line into a Python dictionary.\nif \"commitInfo\" in entry: ...: If the entry contains commitInfo, it extracts and prints details about the operation (e.g., type of operation, timestamp, parameters).\nelif \"add\" in entry: ...: If the entry contains add, it indicates a new data file was added and prints its path.\nelif \"remove\" in entry: ...: If the entry contains remove, it indicates an old data file was removed (e.g., due to an overwrite or update) and prints its path.\n\n\n\n\nThis step demonstrates Delta Lake’s Time Travel capability, allowing you to query previous versions of the table by specifying a version number or a timestamp.\n    sep(\"STEP 7 — Time Travel (Query Historical Versions)\")\n\n    print(\"Full audit trail from the Delta Transaction Log:\")\n    delta_table.history().select(\"version\", \"timestamp\", \"operation\").show(truncate=False)\n\n    print(\"Restoring state as of VERSION 0 (initial ingest only):\")\n    spark.read.format(\"delta\").option(\"versionAsOf\", 0).load(TABLE_PATH).show()\n\n    print(\"Restoring state as of VERSION 1 (after first ACID append):\")\n    spark.read.format(\"delta\").option(\"versionAsOf\", 1).load(TABLE_PATH).show()\n\nsep(...): Prints a separator with the step title.\nprint(\"Full audit trail...\"): Informs the user that the full history will be displayed.\ndelta_table.history().select(\"version\", \"timestamp\", \"operation\").show(truncate=False): Retrieves the full history of the Delta table, showing each version, its timestamp, and the operation that created it. truncate=False ensures that all column content is displayed.\nprint(\"Restoring state as of VERSION 0...\"): Indicates that the table is being queried as it appeared after the first commit (version 0).\nspark.read.format(\"delta\").option(\"versionAsOf\", 0).load(TABLE_PATH).show(): Reads the Delta table at a specific historical version (version 0) and displays its content. This effectively\n\nrestores the table to its state at version 0. * print(\"Restoring state as of VERSION 1...\"): Indicates that the table is being queried as it appeared after the second commit (version 1). * spark.read.format(\"delta\").option(\"versionAsOf\", 1).load(TABLE_PATH).show(): Reads the Delta table at a specific historical version (version 1) and displays its content. This shows the state of the table after the initial batch and the first append.\n\n\n\nThis step demonstrates how to use the VACUUM command to remove data files that are no longer referenced by the Delta table’s transaction log and are older than a specified retention period.\n    sep(\"STEP 8 — VACUUM (Storage Retention Cleanup)\")\n    print(\"\"\"\nDelta retains all historical Parquet files to support Time Travel.\nVACUUM permanently deletes files older than the retention window.\n\nWARNING: After VACUUM, Time Travel beyond the retention window is impossible.\n\nProduction recommendation: retentionHours &gt;= 168 (7 days).\nUnder GDPR Article 17 (right to erasure), lower retention may be required —\nbut confirm with your legal team before reducing below 168 hours.\n\nSetting 0 hours here for demonstration only — NEVER do this in production.\n\"\"\")\n    spark.conf.set(\n        \"spark.databricks.delta.retentionDurationCheck.enabled\", \"false\"\n    )\n    delta_table.vacuum(retentionHours=0)\n    print(\"VACUUM complete. Orphaned files removed.\")\n\nsep(...): Prints a separator with the step title.\nprint(...): Provides a detailed explanation of VACUUM, its purpose (storage cleanup), its implications (loss of time travel history), and important considerations for production environments regarding retention periods.\nspark.conf.set(\"spark.databricks.delta.retentionDurationCheck.enabled\", \"false\"): This configuration setting temporarily disables the check that prevents users from setting a very low retention duration (like 0 hours). This is done here only for demonstration purposes to allow immediate cleanup, but it’s strongly advised against in production.\ndelta_table.vacuum(retentionHours=0): Executes the VACUUM command on the delta_table. retentionHours=0 means that all files no longer part of the latest table state are immediately eligible for deletion, regardless of their age. This will permanently delete older data files, reducing storage costs but removing the ability to time travel to those specific versions.\n\n\n\n\nThis step demonstrates how Delta Lake’s schema evolution feature allows for safely adding new columns to a table without breaking existing data or pipelines.\n    sep(\"STEP 9 — Schema Evolution (Safe Column Addition via mergeSchema)\")\n    print(\"\"\"\nScenario: a product team wants to add a 'discount' column to the orders feed.\n\nWithout Schema Evolution: the write would be REJECTED (Step 3 behaviour).\nWith mergeSchema=True:   the new column is added to the registered schema.\n                         Existing rows receive NULL for the new column.\n\nThis is the production-safe migration path:\n  1. Data contract teams agree on the new column definition.\n  2. Producer writes first batch with mergeSchema=True.\n  3. Delta propagates the schema change; all downstream readers automatically\n     see the new nullable column on their next read.\n\"\"\")\n    evolved_schema = StructType([\n        StructField(\"order_id\",  StringType(),  True),\n        StructField(\"user_id\",   StringType(),  True),\n        StructField(\"product\",   StringType(),  True),\n        StructField(\"price\",     DoubleType(),  True),\n        StructField(\"quantity\",  IntegerType(), True),\n        StructField(\"region\",    StringType(),  True),\n        StructField(\"discount\",  DoubleType(),  True),  # new column\n    ])\n    df_evolved = spark.createDataFrame(\n        [(\"ORD011\", \"usr_grace\", \"SmartWatch\", 250.00, 1, \"MENA\", 15.0)],\n        evolved_schema,\n    )\n    (df_evolved.write\n               .format(\"delta\")\n               .mode(\"append\")\n               .option(\"mergeSchema\", \"true\")\n               .save(TABLE_PATH))\n\n    print(\"New column 'discount' merged into existing schema without downtime.\")\n    print(\"Pre-evolution rows show NULL for 'discount' — backward compatible:\")\n    spark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show()\n\nsep(...): Prints a separator with the step title.\nprint(...): Explains the concept of Schema Evolution, contrasting it with schema enforcement (from Step 3), and describes the benefits and typical workflow for safely adding new columns.\nevolved_schema = StructType([...]): Defines a new schema, evolved_schema, which includes the existing columns plus a new discount column.\ndf_evolved = spark.createDataFrame(...): Creates a DataFrame df_evolved using this new schema, including data for the new discount column for a new order.\n(df_evolved.write.format(\"delta\").mode(\"append\").option(\"mergeSchema\", \"true\").save(TABLE_PATH)): Appends the new DataFrame to the Delta table.\n\n.option(\"mergeSchema\", \"true\"): This crucial option enables schema evolution. When set to true, if the DataFrame being written has a schema that is a superset of the table’s current schema, Delta Lake will automatically evolve the table’s schema to include the new columns. Existing rows will have NULL values for the newly added columns.\n\nprint(...): Prints a confirmation message.\nspark.read.format(\"delta\").load(TABLE_PATH).orderBy(\"order_id\").show(): Reads and displays the table again. This time, the discount column is present. Existing rows (from previous steps) will show NULL for discount, while the newly added row (ORD011) will have its discount value.\n\n\n\n\nThis block ensures the Spark session is properly terminated and confirms the completion of the lab.\n    spark.stop()\n    print(\"\\nMini Lakehouse lab complete. All 9 steps executed successfully.\")\n\n\nif __name__ == \"__main__\":\n    main()\n\nspark.stop(): Stops the SparkSession, releasing all its resources.\nprint(...): Prints a final confirmation message indicating that the entire lab has completed successfully.\nif __name__ == \"__main__\": main(): This standard Python construct ensures that the main() function is called only when the script is executed directly (not when it’s imported as a module). This starts the entire workflow defined in the main function.",
    "crumbs": [
      "Home",
      "Day 1 — Modern Data Architectures",
      "Lab: Mini-Lakehouse with Delta Lake"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html",
    "href": "L03/Day3_Exercise.html",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Estimated time: 60 minutes\n\n\nYou are building a Data Engineering Q&A Assistant for the SDAIA bootcamp. Students can ask it questions and it will retrieve the most relevant content from the course knowledge base and generate a grounded answer.\nYour job is to implement each stage of the pipeline. Six tasks, each building on the previous one.\n\n\n\nDocuments → Chunking → ChromaDB Vector Index ─┐\n                     → BM25 Keyword Index    ─┴─► RRF Fusion → Reranking → RAG Prompt → Evaluation\nDo not modify cells marked # ── PROVIDED ──. Fill in every # TODO comment. Run the test cell after each task to verify.\n\n\n\n\n!pip install chromadb sentence-transformers rank-bm25 numpy\n\n\n# ── PROVIDED ── imports (do not modify)import reimport numpy as npimport chromadbfrom chromadb.utils.embedding_functions import SentenceTransformerEmbeddingFunctionfrom sentence_transformers import SentenceTransformer, CrossEncoderfrom rank_bm25 import BM25Okapiprint(\"✅ All libraries imported successfully.\")\n\n\n\n\nThese are the documents your RAG system will index. Do not modify them.\n\n# ── PROVIDED ── knowledge base (do not modify)DOCUMENTS = [    {\"id\": \"d01\", \"text\": \"Apache Kafka stores messages in topics. Each topic is split into partitions — ordered, append-only logs. Every message inside a partition gets an offset, which is its permanent address. A consumer group shares partitions so that each partition is assigned to exactly one member at a time.\"},    {\"id\": \"d02\", \"text\": \"A tumbling window is a fixed-size, non-overlapping time bucket. Every event belongs to exactly one tumbling window. A sliding window overlaps — the same event can appear in multiple windows. A session window closes after a configurable gap of inactivity, so its size varies per user.\"},    {\"id\": \"d03\", \"text\": \"A watermark is a threshold that tells the stream processor how long to wait for late-arriving events before closing a window. Events arriving after the watermark has passed are dropped or sent to a dead-letter queue. A larger watermark increases completeness but raises end-to-end latency.\"},    {\"id\": \"d04\", \"text\": \"Exactly-once delivery requires coordination between the producer, broker, and consumer. At-most-once loses data on failure. At-least-once retries on failure but may produce duplicates. Exactly-once prevents both, at a cost of roughly 20 percent throughput overhead.\"},    {\"id\": \"d05\", \"text\": \"Delta Lake stores data in Parquet files and records every write operation in a transaction log called the delta_log. Each entry in the log is a JSON commit file. Readers replay the log from version zero to reconstruct the current table state, which is how Delta provides ACID guarantees without a traditional database engine.\"},    {\"id\": \"d06\", \"text\": \"MERGE INTO allows you to apply inserts, updates, and deletes atomically in a single operation. It is the correct way to implement CDC (Change Data Capture) into a Delta table. Delta executes MERGE by scanning only the affected Parquet files and writing new ones, never modifying existing files in place.\"},    {\"id\": \"d07\", \"text\": \"HNSW stands for Hierarchical Navigable Small World. It is a graph-based approximate nearest-neighbour index used inside ChromaDB, Pinecone, and Weaviate. The M parameter controls how many bidirectional links each node has. The ef parameter at query time controls how large the candidate list is during search, trading recall for latency.\"},    {\"id\": \"d08\", \"text\": \"Hybrid search runs vector semantic search and BM25 keyword search in parallel, then merges the two result lists. Vector search finds semantically similar documents even when exact words differ. BM25 finds documents that share exact terms with the query. Hybrid search outperforms either method alone on most benchmarks.\"},    {\"id\": \"d09\", \"text\": \"Reciprocal Rank Fusion scores a document as the sum of 1 divided by k plus its rank across all result lists, where k equals 60 by convention. This formula rewards documents that rank consistently high in both the vector list and the BM25 list, without requiring any weights to be tuned manually.\"},    {\"id\": \"d10\", \"text\": \"A cross-encoder takes a query and a document as a pair and scores them jointly. This is more accurate than comparing independent embeddings because the model can see the interaction between query tokens and document tokens. The standard two-stage pattern is: retrieve the top 50 candidates with a bi-encoder, then rerank them with a cross-encoder, then pass the top 3 to the LLM.\"},    {\"id\": \"d11\", \"text\": \"RAG evaluation measures four properties. Context precision is the fraction of retrieved chunks that are actually relevant. Context recall measures whether all needed chunks were retrieved. Answer faithfulness checks whether the generated answer stays within the retrieved context. Answer relevance checks whether the answer addresses the user question.\"},    {\"id\": \"d12\", \"text\": \"A data contract is a machine-enforceable agreement between a data producer and its consumers. It specifies the schema, field types, and SLA. Pydantic v2 enforces contracts in Python using strict type checking. Breaking changes such as removing a column or narrowing a type require a major version bump and a migration window for downstream consumers.\"},]print(f\"✅ Knowledge base loaded — {len(DOCUMENTS)} documents ready.\")\n\n\n\n\nSplit each document into overlapping sentence chunks.\nRules: - Each chunk contains chunk_size consecutive sentences (default = 2). - Adjacent chunks share 1 sentence (overlap), so context is not cut off at boundaries. - Example with chunk_size=2 and sentences [A, B, C, D]: - Chunk 0 → \"A B\" - Chunk 1 → \"B C\" ← shares B with chunk 0 - Chunk 2 → \"C D\" ← shares C with chunk 1\n\nHint: The step between chunk start positions is chunk_size - 1. Use re.split(r'(?&lt;=[.!?])\\s+', text) to split into sentences.\n\n\ndef chunk_documents(docs, chunk_size=2):    \"\"\"    Split each document into overlapping sentence chunks.    Args:        docs       : list of dicts with keys 'id' and 'text'        chunk_size : number of sentences per chunk    Returns:        list of dicts with keys 'id', 'text', 'doc_id'    \"\"\"    all_chunks = []    for doc in docs:        sentences = re.split(r'(?&lt;=[.!?])\\s+', doc[\"text\"].strip())        # TODO: iterate through sentences in steps of (chunk_size - 1)        #       build a chunk string by joining chunk_size consecutive sentences        #       append a dict with keys: id, text, doc_id        #       id format: f\"{doc['id']}_c{i:02d}\"        for i in range(0, ???, ???):            chunk_text = ???            if not chunk_text.strip():                continue            all_chunks.append({                \"id\":     ???,                \"text\":   ???,                \"doc_id\": ???,            })    return all_chunks\n\n\n# ── TEST 1 ── run this to check your workchunks = chunk_documents(DOCUMENTS, chunk_size=2)assert len(chunks) &gt; len(DOCUMENTS), \"You should have more chunks than documents after splitting.\"assert all(\"id\" in c and \"text\" in c and \"doc_id\" in c for c in chunks), \"Each chunk must have id, text, doc_id.\"assert any(\"_c01\" in c[\"id\"] for c in chunks), \"Chunk IDs should follow the format doc_id_c01, _c02, etc.\"print(f\"✅ Task 1 passed — {len(DOCUMENTS)} documents → {len(chunks)} chunks\")print(f\"   Sample chunk: [{chunks[0]['id']}] {chunks[0]['text'][:80]}...\")\n\n\n\n\nStore all chunks in a ChromaDB collection using SentenceTransformer embeddings.\nSteps: 1. Create an embedding function using SentenceTransformerEmbeddingFunction with model \"all-MiniLM-L6-v2\". 2. Create an in-memory ChromaDB client with chromadb.Client(). 3. Create a collection named \"bootcamp_kb\" with the embedding function. 4. Add all chunks — you need ids, documents (the text), and metadatas (the doc_id).\n\nHint: collection.add(ids=[...], documents=[...], metadatas=[...])\n\n\ndef build_vector_index(chunks):    \"\"\"    Embed all chunks and store them in a ChromaDB collection.    Returns:        ChromaDB collection object    \"\"\"    # TODO: create the SentenceTransformer embedding function    ef = ???    # TODO: create an in-memory ChromaDB client    client = ???    # TODO: create a collection named \"bootcamp_kb\" with the embedding function    collection = ???    # TODO: add all chunks to the collection    collection.add(        ids       = ???,        documents = ???,        metadatas = ???,    )    return collection\n\n\n# ── TEST 2 ── run this to check your workcollection = build_vector_index(chunks)count = collection.count()assert count == len(chunks), f\"Expected {len(chunks)} items in the collection, got {count}.\"results = collection.query(query_texts=[\"What is a watermark in streaming?\"], n_results=3)assert len(results[\"documents\"][0]) == 3, \"Query should return 3 results.\"print(f\"✅ Task 2 passed — {count} chunks indexed in ChromaDB\")print(f\"   Top result for 'watermark': {results['documents'][0][0][:80]}...\")\n\n\n\n\nImplement keyword-based search using the BM25 ranking algorithm.\nSteps: 1. Tokenize each chunk’s text: lowercase and split by whitespace. 2. Build a BM25Okapi index from the tokenized corpus. 3. Get BM25 scores for the query (also tokenized). 4. Return the top_k results sorted by score descending.\n\nHint: bm25.get_scores(query.lower().split()) returns a score for every chunk.\n\n\ndef keyword_search(chunks, query, top_k=8):    \"\"\"    Search chunks using BM25 keyword ranking.    Returns:        list of (score, chunk_dict) tuples, sorted by score descending    \"\"\"    # TODO: tokenize every chunk (lowercase split)    tokenized_corpus = ???    # TODO: build the BM25 index    bm25 = ???    # TODO: score the query against all chunks    scores = ???    # TODO: pair each score with its chunk, sort descending, return top_k    ranked = sorted(???, key=lambda x: x[0], reverse=True)    return ranked[:top_k]\n\n\n# ── TEST 3 ── run this to check your workbm25_results = keyword_search(chunks, \"What is a watermark in streaming?\", top_k=5)assert len(bm25_results) == 5, \"Should return exactly 5 results.\"assert all(isinstance(s, float) and isinstance(c, dict) for s, c in bm25_results), \\    \"Each result must be a (float, dict) tuple.\"assert bm25_results[0][0] &gt;= bm25_results[-1][0], \"Results must be sorted descending by score.\"print(f\"✅ Task 3 passed — BM25 keyword search working\")print(f\"   Top BM25 match (score {bm25_results[0][0]:.3f}): {bm25_results[0][1]['text'][:80]}...\")\n\n\n\n\nMerge the vector results and the BM25 results using RRF.\nThe RRF formula for a document is:\n\\[\\text{score} = \\sum_{\\text{list}} \\frac{1}{k + \\text{rank}}\\]\nWhere k = 60 (standard constant) and rank starts at 1 (not 0).\nSteps: 1. For each document in the vector hit list, add 1 / (k + rank) to its running score. 2. Do the same for the BM25 hit list. 3. Sort all documents by their combined RRF score, return top top_k.\n\nHint: Use a dict keyed by chunk id to accumulate scores across both lists.\n\n\ndef fuse_with_rrf(vector_hits, bm25_hits, k=60, top_k=5):    \"\"\"    Merge vector and BM25 results with Reciprocal Rank Fusion.    Args:        vector_hits : list of dicts from collection.query  (keys: 'id', 'document')        bm25_hits   : list of (score, chunk_dict) tuples from keyword_search    Returns:        list of top_k chunk dicts sorted by RRF score descending    \"\"\"    rrf_scores  = {}   # chunk_id → cumulative RRF score    id_to_chunk = {}   # chunk_id → chunk dict    # TODO: score vector hits — rank starts at 1    for rank, hit in enumerate(vector_hits, start=1):        cid = hit[\"id\"]        rrf_scores[cid]  = rrf_scores.get(cid, 0.0) + ???        id_to_chunk[cid] = {\"id\": cid, \"text\": hit[\"document\"]}    # TODO: score BM25 hits — rank starts at 1    for rank, (_, chunk) in enumerate(bm25_hits, start=1):        cid = chunk[\"id\"]        rrf_scores[cid]  = rrf_scores.get(cid, 0.0) + ???        id_to_chunk[cid] = chunk    # TODO: sort by RRF score descending and return top_k chunk dicts    sorted_ids = sorted(rrf_scores, key=lambda x: ???, reverse=True)    return [id_to_chunk[cid] for cid in sorted_ids[:top_k]]\n\n\n# ── TEST 4 ── run this to check your workquery = \"How does RRF merge vector and keyword search results?\"vec_raw  = collection.query(query_texts=[query], n_results=8)vec_hits = [{\"id\": i, \"document\": d}            for i, d in zip(vec_raw[\"ids\"][0], vec_raw[\"documents\"][0])]bm25_hits = keyword_search(chunks, query, top_k=8)hybrid = fuse_with_rrf(vec_hits, bm25_hits, top_k=5)assert len(hybrid) &lt;= 5, \"Should return at most 5 results.\"assert all(\"id\" in c and \"text\" in c for c in hybrid), \"Each result must have id and text.\"print(f\"✅ Task 4 passed — RRF fusion working ({len(hybrid)} results)\")print(f\"   Top hybrid result: {hybrid[0]['text'][:100]}...\")\n\n\n\n\nAdd a second-stage reranker that scores each (query, document) pair jointly.\nSteps: 1. Create a CrossEncoder using model \"cross-encoder/ms-marco-MiniLM-L-6-v2\". 2. Build a list of (query, doc_text) pairs — one per candidate. 3. Call model.predict(pairs) to get a relevance score per pair. 4. Sort candidates by score descending and return the top top_k.\n\nWhy? The cross-encoder sees both the query and document together, giving much more accurate relevance scores than cosine similarity between independent embeddings.\n\n\ndef rerank(query, candidates, top_k=3):    \"\"\"    Rerank candidates using a cross-encoder relevance model.    Args:        query      : the user's question (string)        candidates : list of chunk dicts (output of fuse_with_rrf)        top_k      : how many to return after reranking    Returns:        list of top_k chunk dicts sorted by cross-encoder score descending    \"\"\"    # TODO: load the cross-encoder model    model = ???    # TODO: build (query, document_text) pairs for every candidate    pairs = ???    # TODO: predict relevance scores for all pairs at once    scores = ???    # TODO: zip scores with candidates, sort descending, return top_k dicts    ranked = sorted(???, key=lambda x: x[0], reverse=True)    return [doc for _, doc in ranked[:top_k]]\n\n\n# ── TEST 5 ── run this to check your workquery      = \"What is the difference between tumbling and sliding windows?\"vec_raw    = collection.query(query_texts=[query], n_results=8)vec_hits   = [{\"id\": i, \"document\": d}               for i, d in zip(vec_raw[\"ids\"][0], vec_raw[\"documents\"][0])]bm25_hits  = keyword_search(chunks, query, top_k=8)hybrid     = fuse_with_rrf(vec_hits, bm25_hits, top_k=6)final_docs = rerank(query, hybrid, top_k=3)assert len(final_docs) == 3, \"Should return exactly 3 results after reranking.\"print(f\"✅ Task 5 passed — cross-encoder reranking working\")for i, doc in enumerate(final_docs, 1):    print(f\"  [{i}] {doc['text'][:100]}...\")\n\n\n\n\nPart A: Implement evaluate_precision() — a simplified version of RAGAS Context Precision.\nContext Precision = fraction of retrieved chunks whose cosine similarity with the query exceeds a threshold.\nPart B: Wire everything together in run_pipeline() and run it on three queries.\n\nHint for Part A: embed_model.encode(text, normalize_embeddings=True) returns a unit vector. Cosine similarity between two unit vectors = np.dot(vec_a, vec_b).\n\n\nembed_model = SentenceTransformer(\"all-MiniLM-L6-v2\")def evaluate_precision(query, retrieved_docs, threshold=0.30):    \"\"\"    Compute context precision: fraction of retrieved chunks    with cosine similarity &gt; threshold relative to the query.    Returns: float between 0.0 and 1.0    \"\"\"    query_emb = embed_model.encode(query, normalize_embeddings=True)    # TODO: for each doc in retrieved_docs, encode its text and compute cosine similarity    scores = []    for doc in retrieved_docs:        doc_emb = ???        similarity = ???        scores.append(similarity)    # TODO: compute the fraction of scores above the threshold    precision = ???    return round(precision, 3)def run_pipeline(query, top_k_retrieve=8, top_k_rerank=3):    \"\"\"    Full RAG pipeline: query → hybrid retrieval → rerank → prompt → evaluation.    \"\"\"    print(f\"\\nQUERY: {query}\")    print(\"-\" * 60)    # Stage 1: Vector search    vec_raw  = collection.query(query_texts=[query], n_results=top_k_retrieve)    vec_hits = [{\"id\": i, \"document\": d}                for i, d in zip(vec_raw[\"ids\"][0], vec_raw[\"documents\"][0])]    # Stage 2: BM25 keyword search    bm25_hits = keyword_search(chunks, query, top_k=top_k_retrieve)    # Stage 3: RRF fusion    hybrid = fuse_with_rrf(vec_hits, bm25_hits, top_k=top_k_retrieve)    # Stage 4: Cross-encoder rerank    final_docs = rerank(query, hybrid, top_k=top_k_rerank)    # Stage 5: Build RAG prompt (in production you would call an LLM here)    context = \"\\n\\n\".join(f\"[{i+1}] {d['text']}\" for i, d in enumerate(final_docs))    prompt  = (        f\"Answer based strictly on the context below.\\n\\n\"        f\"CONTEXT:\\n{context}\\n\\n\"        f\"QUESTION: {query}\\nANSWER:\"    )    # Stage 6: Evaluate    precision = evaluate_precision(query, final_docs)    print(f\"Top {top_k_rerank} chunks after reranking:\")    for i, doc in enumerate(final_docs, 1):        print(f\"  [{i}] {doc['text'][:90]}...\")    print(f\"\\nContext Precision: {precision}\")    if precision &lt; 0.5:        print(\"⚠️  Low precision — try adjusting chunk size or threshold.\")    else:        print(\"✅  Good retrieval quality.\")    print(f\"\\nPrompt (first 300 chars):\\n{prompt[:300]}...\")    return final_docs, precision\n\n\n# ── TEST 6 ── run the full pipeline on three queriesTEST_QUERIES = [    \"How does Kafka guarantee message ordering?\",    \"What is the difference between tumbling and sliding windows?\",    \"How does cross-encoder reranking improve RAG accuracy?\",]all_passed = Truefor q in TEST_QUERIES:    docs, prec = run_pipeline(q)    if len(docs) != 3 or not (0.0 &lt;= prec &lt;= 1.0):        all_passed = False        print(f\"❌ Issue with query: {q}\")if all_passed:    print(\"\\n\" + \"=\"*60)    print(\"✅ ALL TASKS COMPLETE — Full RAG pipeline working!\")    print(\"=\"*60)\n\n\n\n\nPick one of the following:\nA — Chunk size experiment Run run_pipeline() with chunk_size=1 and chunk_size=3 and compare the precision scores. Which produces better results for these queries, and why?\nB — Threshold sensitivity Change the threshold in evaluate_precision() from 0.30 to 0.50. How many of your retrieved chunks survive at the higher threshold? What does this tell you about retrieval quality?\nC — Remove the reranker Comment out the rerank step in run_pipeline() and pass hybrid directly to evaluate_precision(). Does precision go up or down? Explain why.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#scenario",
    "href": "L03/Day3_Exercise.html#scenario",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "You are building a Data Engineering Q&A Assistant for the SDAIA bootcamp. Students can ask it questions and it will retrieve the most relevant content from the course knowledge base and generate a grounded answer.\nYour job is to implement each stage of the pipeline. Six tasks, each building on the previous one.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#pipeline-you-will-build",
    "href": "L03/Day3_Exercise.html#pipeline-you-will-build",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Documents → Chunking → ChromaDB Vector Index ─┐\n                     → BM25 Keyword Index    ─┴─► RRF Fusion → Reranking → RAG Prompt → Evaluation\nDo not modify cells marked # ── PROVIDED ──. Fill in every # TODO comment. Run the test cell after each task to verify.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#setup",
    "href": "L03/Day3_Exercise.html#setup",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "!pip install chromadb sentence-transformers rank-bm25 numpy\n\n\n# ── PROVIDED ── imports (do not modify)import reimport numpy as npimport chromadbfrom chromadb.utils.embedding_functions import SentenceTransformerEmbeddingFunctionfrom sentence_transformers import SentenceTransformer, CrossEncoderfrom rank_bm25 import BM25Okapiprint(\"✅ All libraries imported successfully.\")",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#knowledge-base",
    "href": "L03/Day3_Exercise.html#knowledge-base",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "These are the documents your RAG system will index. Do not modify them.\n\n# ── PROVIDED ── knowledge base (do not modify)DOCUMENTS = [    {\"id\": \"d01\", \"text\": \"Apache Kafka stores messages in topics. Each topic is split into partitions — ordered, append-only logs. Every message inside a partition gets an offset, which is its permanent address. A consumer group shares partitions so that each partition is assigned to exactly one member at a time.\"},    {\"id\": \"d02\", \"text\": \"A tumbling window is a fixed-size, non-overlapping time bucket. Every event belongs to exactly one tumbling window. A sliding window overlaps — the same event can appear in multiple windows. A session window closes after a configurable gap of inactivity, so its size varies per user.\"},    {\"id\": \"d03\", \"text\": \"A watermark is a threshold that tells the stream processor how long to wait for late-arriving events before closing a window. Events arriving after the watermark has passed are dropped or sent to a dead-letter queue. A larger watermark increases completeness but raises end-to-end latency.\"},    {\"id\": \"d04\", \"text\": \"Exactly-once delivery requires coordination between the producer, broker, and consumer. At-most-once loses data on failure. At-least-once retries on failure but may produce duplicates. Exactly-once prevents both, at a cost of roughly 20 percent throughput overhead.\"},    {\"id\": \"d05\", \"text\": \"Delta Lake stores data in Parquet files and records every write operation in a transaction log called the delta_log. Each entry in the log is a JSON commit file. Readers replay the log from version zero to reconstruct the current table state, which is how Delta provides ACID guarantees without a traditional database engine.\"},    {\"id\": \"d06\", \"text\": \"MERGE INTO allows you to apply inserts, updates, and deletes atomically in a single operation. It is the correct way to implement CDC (Change Data Capture) into a Delta table. Delta executes MERGE by scanning only the affected Parquet files and writing new ones, never modifying existing files in place.\"},    {\"id\": \"d07\", \"text\": \"HNSW stands for Hierarchical Navigable Small World. It is a graph-based approximate nearest-neighbour index used inside ChromaDB, Pinecone, and Weaviate. The M parameter controls how many bidirectional links each node has. The ef parameter at query time controls how large the candidate list is during search, trading recall for latency.\"},    {\"id\": \"d08\", \"text\": \"Hybrid search runs vector semantic search and BM25 keyword search in parallel, then merges the two result lists. Vector search finds semantically similar documents even when exact words differ. BM25 finds documents that share exact terms with the query. Hybrid search outperforms either method alone on most benchmarks.\"},    {\"id\": \"d09\", \"text\": \"Reciprocal Rank Fusion scores a document as the sum of 1 divided by k plus its rank across all result lists, where k equals 60 by convention. This formula rewards documents that rank consistently high in both the vector list and the BM25 list, without requiring any weights to be tuned manually.\"},    {\"id\": \"d10\", \"text\": \"A cross-encoder takes a query and a document as a pair and scores them jointly. This is more accurate than comparing independent embeddings because the model can see the interaction between query tokens and document tokens. The standard two-stage pattern is: retrieve the top 50 candidates with a bi-encoder, then rerank them with a cross-encoder, then pass the top 3 to the LLM.\"},    {\"id\": \"d11\", \"text\": \"RAG evaluation measures four properties. Context precision is the fraction of retrieved chunks that are actually relevant. Context recall measures whether all needed chunks were retrieved. Answer faithfulness checks whether the generated answer stays within the retrieved context. Answer relevance checks whether the answer addresses the user question.\"},    {\"id\": \"d12\", \"text\": \"A data contract is a machine-enforceable agreement between a data producer and its consumers. It specifies the schema, field types, and SLA. Pydantic v2 enforces contracts in Python using strict type checking. Breaking changes such as removing a column or narrowing a type require a major version bump and a migration window for downstream consumers.\"},]print(f\"✅ Knowledge base loaded — {len(DOCUMENTS)} documents ready.\")",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#task-1-document-chunking-10-min",
    "href": "L03/Day3_Exercise.html#task-1-document-chunking-10-min",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Split each document into overlapping sentence chunks.\nRules: - Each chunk contains chunk_size consecutive sentences (default = 2). - Adjacent chunks share 1 sentence (overlap), so context is not cut off at boundaries. - Example with chunk_size=2 and sentences [A, B, C, D]: - Chunk 0 → \"A B\" - Chunk 1 → \"B C\" ← shares B with chunk 0 - Chunk 2 → \"C D\" ← shares C with chunk 1\n\nHint: The step between chunk start positions is chunk_size - 1. Use re.split(r'(?&lt;=[.!?])\\s+', text) to split into sentences.\n\n\ndef chunk_documents(docs, chunk_size=2):    \"\"\"    Split each document into overlapping sentence chunks.    Args:        docs       : list of dicts with keys 'id' and 'text'        chunk_size : number of sentences per chunk    Returns:        list of dicts with keys 'id', 'text', 'doc_id'    \"\"\"    all_chunks = []    for doc in docs:        sentences = re.split(r'(?&lt;=[.!?])\\s+', doc[\"text\"].strip())        # TODO: iterate through sentences in steps of (chunk_size - 1)        #       build a chunk string by joining chunk_size consecutive sentences        #       append a dict with keys: id, text, doc_id        #       id format: f\"{doc['id']}_c{i:02d}\"        for i in range(0, ???, ???):            chunk_text = ???            if not chunk_text.strip():                continue            all_chunks.append({                \"id\":     ???,                \"text\":   ???,                \"doc_id\": ???,            })    return all_chunks\n\n\n# ── TEST 1 ── run this to check your workchunks = chunk_documents(DOCUMENTS, chunk_size=2)assert len(chunks) &gt; len(DOCUMENTS), \"You should have more chunks than documents after splitting.\"assert all(\"id\" in c and \"text\" in c and \"doc_id\" in c for c in chunks), \"Each chunk must have id, text, doc_id.\"assert any(\"_c01\" in c[\"id\"] for c in chunks), \"Chunk IDs should follow the format doc_id_c01, _c02, etc.\"print(f\"✅ Task 1 passed — {len(DOCUMENTS)} documents → {len(chunks)} chunks\")print(f\"   Sample chunk: [{chunks[0]['id']}] {chunks[0]['text'][:80]}...\")",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#task-2-build-the-vector-index-10-min",
    "href": "L03/Day3_Exercise.html#task-2-build-the-vector-index-10-min",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Store all chunks in a ChromaDB collection using SentenceTransformer embeddings.\nSteps: 1. Create an embedding function using SentenceTransformerEmbeddingFunction with model \"all-MiniLM-L6-v2\". 2. Create an in-memory ChromaDB client with chromadb.Client(). 3. Create a collection named \"bootcamp_kb\" with the embedding function. 4. Add all chunks — you need ids, documents (the text), and metadatas (the doc_id).\n\nHint: collection.add(ids=[...], documents=[...], metadatas=[...])\n\n\ndef build_vector_index(chunks):    \"\"\"    Embed all chunks and store them in a ChromaDB collection.    Returns:        ChromaDB collection object    \"\"\"    # TODO: create the SentenceTransformer embedding function    ef = ???    # TODO: create an in-memory ChromaDB client    client = ???    # TODO: create a collection named \"bootcamp_kb\" with the embedding function    collection = ???    # TODO: add all chunks to the collection    collection.add(        ids       = ???,        documents = ???,        metadatas = ???,    )    return collection\n\n\n# ── TEST 2 ── run this to check your workcollection = build_vector_index(chunks)count = collection.count()assert count == len(chunks), f\"Expected {len(chunks)} items in the collection, got {count}.\"results = collection.query(query_texts=[\"What is a watermark in streaming?\"], n_results=3)assert len(results[\"documents\"][0]) == 3, \"Query should return 3 results.\"print(f\"✅ Task 2 passed — {count} chunks indexed in ChromaDB\")print(f\"   Top result for 'watermark': {results['documents'][0][0][:80]}...\")",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#task-3-bm25-keyword-search-10-min",
    "href": "L03/Day3_Exercise.html#task-3-bm25-keyword-search-10-min",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Implement keyword-based search using the BM25 ranking algorithm.\nSteps: 1. Tokenize each chunk’s text: lowercase and split by whitespace. 2. Build a BM25Okapi index from the tokenized corpus. 3. Get BM25 scores for the query (also tokenized). 4. Return the top_k results sorted by score descending.\n\nHint: bm25.get_scores(query.lower().split()) returns a score for every chunk.\n\n\ndef keyword_search(chunks, query, top_k=8):    \"\"\"    Search chunks using BM25 keyword ranking.    Returns:        list of (score, chunk_dict) tuples, sorted by score descending    \"\"\"    # TODO: tokenize every chunk (lowercase split)    tokenized_corpus = ???    # TODO: build the BM25 index    bm25 = ???    # TODO: score the query against all chunks    scores = ???    # TODO: pair each score with its chunk, sort descending, return top_k    ranked = sorted(???, key=lambda x: x[0], reverse=True)    return ranked[:top_k]\n\n\n# ── TEST 3 ── run this to check your workbm25_results = keyword_search(chunks, \"What is a watermark in streaming?\", top_k=5)assert len(bm25_results) == 5, \"Should return exactly 5 results.\"assert all(isinstance(s, float) and isinstance(c, dict) for s, c in bm25_results), \\    \"Each result must be a (float, dict) tuple.\"assert bm25_results[0][0] &gt;= bm25_results[-1][0], \"Results must be sorted descending by score.\"print(f\"✅ Task 3 passed — BM25 keyword search working\")print(f\"   Top BM25 match (score {bm25_results[0][0]:.3f}): {bm25_results[0][1]['text'][:80]}...\")",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#task-4-hybrid-search-with-reciprocal-rank-fusion-10-min",
    "href": "L03/Day3_Exercise.html#task-4-hybrid-search-with-reciprocal-rank-fusion-10-min",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Merge the vector results and the BM25 results using RRF.\nThe RRF formula for a document is:\n\\[\\text{score} = \\sum_{\\text{list}} \\frac{1}{k + \\text{rank}}\\]\nWhere k = 60 (standard constant) and rank starts at 1 (not 0).\nSteps: 1. For each document in the vector hit list, add 1 / (k + rank) to its running score. 2. Do the same for the BM25 hit list. 3. Sort all documents by their combined RRF score, return top top_k.\n\nHint: Use a dict keyed by chunk id to accumulate scores across both lists.\n\n\ndef fuse_with_rrf(vector_hits, bm25_hits, k=60, top_k=5):    \"\"\"    Merge vector and BM25 results with Reciprocal Rank Fusion.    Args:        vector_hits : list of dicts from collection.query  (keys: 'id', 'document')        bm25_hits   : list of (score, chunk_dict) tuples from keyword_search    Returns:        list of top_k chunk dicts sorted by RRF score descending    \"\"\"    rrf_scores  = {}   # chunk_id → cumulative RRF score    id_to_chunk = {}   # chunk_id → chunk dict    # TODO: score vector hits — rank starts at 1    for rank, hit in enumerate(vector_hits, start=1):        cid = hit[\"id\"]        rrf_scores[cid]  = rrf_scores.get(cid, 0.0) + ???        id_to_chunk[cid] = {\"id\": cid, \"text\": hit[\"document\"]}    # TODO: score BM25 hits — rank starts at 1    for rank, (_, chunk) in enumerate(bm25_hits, start=1):        cid = chunk[\"id\"]        rrf_scores[cid]  = rrf_scores.get(cid, 0.0) + ???        id_to_chunk[cid] = chunk    # TODO: sort by RRF score descending and return top_k chunk dicts    sorted_ids = sorted(rrf_scores, key=lambda x: ???, reverse=True)    return [id_to_chunk[cid] for cid in sorted_ids[:top_k]]\n\n\n# ── TEST 4 ── run this to check your workquery = \"How does RRF merge vector and keyword search results?\"vec_raw  = collection.query(query_texts=[query], n_results=8)vec_hits = [{\"id\": i, \"document\": d}            for i, d in zip(vec_raw[\"ids\"][0], vec_raw[\"documents\"][0])]bm25_hits = keyword_search(chunks, query, top_k=8)hybrid = fuse_with_rrf(vec_hits, bm25_hits, top_k=5)assert len(hybrid) &lt;= 5, \"Should return at most 5 results.\"assert all(\"id\" in c and \"text\" in c for c in hybrid), \"Each result must have id and text.\"print(f\"✅ Task 4 passed — RRF fusion working ({len(hybrid)} results)\")print(f\"   Top hybrid result: {hybrid[0]['text'][:100]}...\")",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#task-5-cross-encoder-reranking-10-min",
    "href": "L03/Day3_Exercise.html#task-5-cross-encoder-reranking-10-min",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Add a second-stage reranker that scores each (query, document) pair jointly.\nSteps: 1. Create a CrossEncoder using model \"cross-encoder/ms-marco-MiniLM-L-6-v2\". 2. Build a list of (query, doc_text) pairs — one per candidate. 3. Call model.predict(pairs) to get a relevance score per pair. 4. Sort candidates by score descending and return the top top_k.\n\nWhy? The cross-encoder sees both the query and document together, giving much more accurate relevance scores than cosine similarity between independent embeddings.\n\n\ndef rerank(query, candidates, top_k=3):    \"\"\"    Rerank candidates using a cross-encoder relevance model.    Args:        query      : the user's question (string)        candidates : list of chunk dicts (output of fuse_with_rrf)        top_k      : how many to return after reranking    Returns:        list of top_k chunk dicts sorted by cross-encoder score descending    \"\"\"    # TODO: load the cross-encoder model    model = ???    # TODO: build (query, document_text) pairs for every candidate    pairs = ???    # TODO: predict relevance scores for all pairs at once    scores = ???    # TODO: zip scores with candidates, sort descending, return top_k dicts    ranked = sorted(???, key=lambda x: x[0], reverse=True)    return [doc for _, doc in ranked[:top_k]]\n\n\n# ── TEST 5 ── run this to check your workquery      = \"What is the difference between tumbling and sliding windows?\"vec_raw    = collection.query(query_texts=[query], n_results=8)vec_hits   = [{\"id\": i, \"document\": d}               for i, d in zip(vec_raw[\"ids\"][0], vec_raw[\"documents\"][0])]bm25_hits  = keyword_search(chunks, query, top_k=8)hybrid     = fuse_with_rrf(vec_hits, bm25_hits, top_k=6)final_docs = rerank(query, hybrid, top_k=3)assert len(final_docs) == 3, \"Should return exactly 3 results after reranking.\"print(f\"✅ Task 5 passed — cross-encoder reranking working\")for i, doc in enumerate(final_docs, 1):    print(f\"  [{i}] {doc['text'][:100]}...\")",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#task-6-evaluation-full-pipeline-10-min",
    "href": "L03/Day3_Exercise.html#task-6-evaluation-full-pipeline-10-min",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Part A: Implement evaluate_precision() — a simplified version of RAGAS Context Precision.\nContext Precision = fraction of retrieved chunks whose cosine similarity with the query exceeds a threshold.\nPart B: Wire everything together in run_pipeline() and run it on three queries.\n\nHint for Part A: embed_model.encode(text, normalize_embeddings=True) returns a unit vector. Cosine similarity between two unit vectors = np.dot(vec_a, vec_b).\n\n\nembed_model = SentenceTransformer(\"all-MiniLM-L6-v2\")def evaluate_precision(query, retrieved_docs, threshold=0.30):    \"\"\"    Compute context precision: fraction of retrieved chunks    with cosine similarity &gt; threshold relative to the query.    Returns: float between 0.0 and 1.0    \"\"\"    query_emb = embed_model.encode(query, normalize_embeddings=True)    # TODO: for each doc in retrieved_docs, encode its text and compute cosine similarity    scores = []    for doc in retrieved_docs:        doc_emb = ???        similarity = ???        scores.append(similarity)    # TODO: compute the fraction of scores above the threshold    precision = ???    return round(precision, 3)def run_pipeline(query, top_k_retrieve=8, top_k_rerank=3):    \"\"\"    Full RAG pipeline: query → hybrid retrieval → rerank → prompt → evaluation.    \"\"\"    print(f\"\\nQUERY: {query}\")    print(\"-\" * 60)    # Stage 1: Vector search    vec_raw  = collection.query(query_texts=[query], n_results=top_k_retrieve)    vec_hits = [{\"id\": i, \"document\": d}                for i, d in zip(vec_raw[\"ids\"][0], vec_raw[\"documents\"][0])]    # Stage 2: BM25 keyword search    bm25_hits = keyword_search(chunks, query, top_k=top_k_retrieve)    # Stage 3: RRF fusion    hybrid = fuse_with_rrf(vec_hits, bm25_hits, top_k=top_k_retrieve)    # Stage 4: Cross-encoder rerank    final_docs = rerank(query, hybrid, top_k=top_k_rerank)    # Stage 5: Build RAG prompt (in production you would call an LLM here)    context = \"\\n\\n\".join(f\"[{i+1}] {d['text']}\" for i, d in enumerate(final_docs))    prompt  = (        f\"Answer based strictly on the context below.\\n\\n\"        f\"CONTEXT:\\n{context}\\n\\n\"        f\"QUESTION: {query}\\nANSWER:\"    )    # Stage 6: Evaluate    precision = evaluate_precision(query, final_docs)    print(f\"Top {top_k_rerank} chunks after reranking:\")    for i, doc in enumerate(final_docs, 1):        print(f\"  [{i}] {doc['text'][:90]}...\")    print(f\"\\nContext Precision: {precision}\")    if precision &lt; 0.5:        print(\"⚠️  Low precision — try adjusting chunk size or threshold.\")    else:        print(\"✅  Good retrieval quality.\")    print(f\"\\nPrompt (first 300 chars):\\n{prompt[:300]}...\")    return final_docs, precision\n\n\n# ── TEST 6 ── run the full pipeline on three queriesTEST_QUERIES = [    \"How does Kafka guarantee message ordering?\",    \"What is the difference between tumbling and sliding windows?\",    \"How does cross-encoder reranking improve RAG accuracy?\",]all_passed = Truefor q in TEST_QUERIES:    docs, prec = run_pipeline(q)    if len(docs) != 3 or not (0.0 &lt;= prec &lt;= 1.0):        all_passed = False        print(f\"❌ Issue with query: {q}\")if all_passed:    print(\"\\n\" + \"=\"*60)    print(\"✅ ALL TASKS COMPLETE — Full RAG pipeline working!\")    print(\"=\"*60)",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L03/Day3_Exercise.html#bonus-challenge-if-you-finish-early",
    "href": "L03/Day3_Exercise.html#bonus-challenge-if-you-finish-early",
    "title": "Exercise: Build a RAG Pipeline from Scratch",
    "section": "",
    "text": "Pick one of the following:\nA — Chunk size experiment Run run_pipeline() with chunk_size=1 and chunk_size=3 and compare the precision scores. Which produces better results for these queries, and why?\nB — Threshold sensitivity Change the threshold in evaluate_precision() from 0.30 to 0.50. How many of your retrieved chunks survive at the higher threshold? What does this tell you about retrieval quality?\nC — Remove the reranker Comment out the rerank step in run_pipeline() and pass hybrid directly to evaluate_precision(). Does precision go up or down? Explain why.",
    "crumbs": [
      "Home",
      "Day 3 — Vector Databases and Advanced RAG Engineering",
      "Exercise: Build a RAG Pipeline from Scratch"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html",
    "href": "L04/Day4_Lab.html",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "We are using the UCI Online Retail Dataset — 541,909 real transactions from a UK-based online retailer (2010–2011). This dataset has genuine quality problems: 25% of CustomerIDs are missing, cancelled orders have negative quantities, and there are formatting inconsistencies.\nYour pipeline will: 1. Download the dataset from Kaggle 2. Profile the raw data to discover quality issues 3. Validate records against a Pydantic v2 data contract 4. Run all 6 DAMA quality dimensions on the clean subset 5. Route data to production or quarantine based on quality verdict 6. Run the same checks again through a real Great Expectations checkpoint 7. Emit an OpenLineage-style audit trail, and also real openlineage-python events\n\n\n\n\n!pip install kagglehub pandas loguru \"pydantic&gt;=2.0\" pyarrow great_expectations openlineage-python\n\n\nRequirement already satisfied: kagglehub in /usr/local/lib/python3.12/dist-packages (1.0.2)\n\nRequirement already satisfied: pandas in /usr/local/lib/python3.12/dist-packages (2.2.2)\n\nCollecting loguru\n\n  Downloading loguru-0.7.3-py3-none-any.whl.metadata (22 kB)\n\nRequirement already satisfied: pydantic&gt;=2.0 in /usr/local/lib/python3.12/dist-packages (2.13.4)\n\nRequirement already satisfied: pyarrow in /usr/local/lib/python3.12/dist-packages (18.1.0)\n\nRequirement already satisfied: kagglesdk&lt;1.0,&gt;=0.1.22 in /usr/local/lib/python3.12/dist-packages (from kagglehub) (0.1.23)\n\nRequirement already satisfied: packaging in /usr/local/lib/python3.12/dist-packages (from kagglehub) (26.2)\n\nRequirement already satisfied: pyyaml in /usr/local/lib/python3.12/dist-packages (from kagglehub) (6.0.3)\n\nRequirement already satisfied: requests in /usr/local/lib/python3.12/dist-packages (from kagglehub) (2.32.4)\n\nRequirement already satisfied: tqdm in /usr/local/lib/python3.12/dist-packages (from kagglehub) (4.67.3)\n\nRequirement already satisfied: numpy&gt;=1.26.0 in /usr/local/lib/python3.12/dist-packages (from pandas) (2.0.2)\n\nRequirement already satisfied: python-dateutil&gt;=2.8.2 in /usr/local/lib/python3.12/dist-packages (from pandas) (2.9.0.post0)\n\nRequirement already satisfied: pytz&gt;=2020.1 in /usr/local/lib/python3.12/dist-packages (from pandas) (2025.2)\n\nRequirement already satisfied: tzdata&gt;=2022.7 in /usr/local/lib/python3.12/dist-packages (from pandas) (2026.2)\n\nRequirement already satisfied: annotated-types&gt;=0.6.0 in /usr/local/lib/python3.12/dist-packages (from pydantic&gt;=2.0) (0.7.0)\n\nRequirement already satisfied: pydantic-core==2.46.4 in /usr/local/lib/python3.12/dist-packages (from pydantic&gt;=2.0) (2.46.4)\n\nRequirement already satisfied: typing-extensions&gt;=4.14.1 in /usr/local/lib/python3.12/dist-packages (from pydantic&gt;=2.0) (4.15.0)\n\nRequirement already satisfied: typing-inspection&gt;=0.4.2 in /usr/local/lib/python3.12/dist-packages (from pydantic&gt;=2.0) (0.4.2)\n\nRequirement already satisfied: protobuf in /usr/local/lib/python3.12/dist-packages (from kagglesdk&lt;1.0,&gt;=0.1.22-&gt;kagglehub) (5.29.6)\n\nRequirement already satisfied: six&gt;=1.5 in /usr/local/lib/python3.12/dist-packages (from python-dateutil&gt;=2.8.2-&gt;pandas) (1.17.0)\n\nRequirement already satisfied: charset_normalizer&lt;4,&gt;=2 in /usr/local/lib/python3.12/dist-packages (from requests-&gt;kagglehub) (3.4.7)\n\nRequirement already satisfied: idna&lt;4,&gt;=2.5 in /usr/local/lib/python3.12/dist-packages (from requests-&gt;kagglehub) (3.18)\n\nRequirement already satisfied: urllib3&lt;3,&gt;=1.21.1 in /usr/local/lib/python3.12/dist-packages (from requests-&gt;kagglehub) (2.5.0)\n\nRequirement already satisfied: certifi&gt;=2017.4.17 in /usr/local/lib/python3.12/dist-packages (from requests-&gt;kagglehub) (2026.6.17)\n\nDownloading loguru-0.7.3-py3-none-any.whl (61 kB)\n\n   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 61.6/61.6 kB 1.7 MB/s eta 0:00:00\n\nInstalling collected packages: loguru\n\nSuccessfully installed loguru-0.7.3\n\n\n\n\n\n\n\nYou need a Kaggle account and API key.\nOption A — Colab Secrets (recommended): 1. Go to kaggle.com → Account → API → Create New Token → downloads kaggle.json 2. In Colab: click the 🔑 icon (left sidebar) → add two secrets: - KAGGLE_USERNAME = your Kaggle username - KAGGLE_KEY = the key value from kaggle.json\nOption B — Upload kaggle.json directly: Run the upload cell below instead.\n\n# ── Option A: Colab Secrets ──────────────────────────────────────────\nimport os\n\ntry:\n    from google.colab import userdata\n    os.environ[\"KAGGLE_USERNAME\"] = userdata.get(\"KAGGLE_USERNAME\")\n    os.environ[\"KAGGLE_KEY\"]      = userdata.get(\"KAGGLE_KEY\")\n    print(\"✅ Kaggle credentials loaded from Colab Secrets.\")\nexcept Exception:\n    print(\"⚠️  Colab Secrets not available — run Option B below.\")\n\n✅ Kaggle credentials loaded from Colab Secrets.\n\n\n\n# ── Option B: Upload kaggle.json manually (skip if Option A worked) ──\n# from google.colab import files\n# uploaded = files.upload()          # select your kaggle.json\n# import os, shutil\n# os.makedirs(\"/root/.kaggle\", exist_ok=True)\n# shutil.move(\"kaggle.json\", \"/root/.kaggle/kaggle.json\")\n# os.chmod(\"/root/.kaggle/kaggle.json\", 0o600)\n# print(\"✅ kaggle.json installed.\")\n\n\n\n\n\nimport kagglehub, glob\n\n# UCI Online Retail Dataset — 541,909 real transactions, ~45 MB\npath = kagglehub.dataset_download(\"carrie1/ecommerce-data\")\nprint(f\"Downloaded to: {path}\")\n\ncsv_files = glob.glob(f\"{path}/**/*.csv\", recursive=True)\nprint(f\"CSV files found: {csv_files}\")\nRAW_CSV = csv_files[0]\nprint(f\"Using: {RAW_CSV}\")\n\nDownloading to /root/.cache/kagglehub/datasets/carrie1/ecommerce-data/1.archive...\n\n\n100%|██████████| 7.20M/7.20M [00:00&lt;00:00, 115MB/s]\n\n\nExtracting files...\n\n\n\n\n\nDownloaded to: /root/.cache/kagglehub/datasets/carrie1/ecommerce-data/versions/1\nCSV files found: ['/root/.cache/kagglehub/datasets/carrie1/ecommerce-data/versions/1/data.csv']\nUsing: /root/.cache/kagglehub/datasets/carrie1/ecommerce-data/versions/1/data.csv\n\n\n\n\n\nBefore running any pipeline, always profile the data to understand what you are dealing with. This is the discovery phase — you are looking for quality issues before writing any validation rules.\n\nimport pandas as pd\nfrom datetime import datetime\n\ndf_raw = pd.read_csv(RAW_CSV, encoding=\"ISO-8859-1\", dtype=str)\nprint(f\"Shape: {df_raw.shape[0]:,} rows × {df_raw.shape[1]} columns\")\nprint(f\"Columns: {list(df_raw.columns)}\")\ndf_raw.head(5)\n\nShape: 541,909 rows × 8 columns\nColumns: ['InvoiceNo', 'StockCode', 'Description', 'Quantity', 'InvoiceDate', 'UnitPrice', 'CustomerID', 'Country']\n\n\n\n    \n\n\n\n\n\n\nInvoiceNo\nStockCode\nDescription\nQuantity\nInvoiceDate\nUnitPrice\nCustomerID\nCountry\n\n\n\n\n0\n536365\n85123A\nWHITE HANGING HEART T-LIGHT HOLDER\n6\n12/1/2010 8:26\n2.55\n17850\nUnited Kingdom\n\n\n1\n536365\n71053\nWHITE METAL LANTERN\n6\n12/1/2010 8:26\n3.39\n17850\nUnited Kingdom\n\n\n2\n536365\n84406B\nCREAM CUPID HEARTS COAT HANGER\n8\n12/1/2010 8:26\n2.75\n17850\nUnited Kingdom\n\n\n3\n536365\n84029G\nKNITTED UNION FLAG HOT WATER BOTTLE\n6\n12/1/2010 8:26\n3.39\n17850\nUnited Kingdom\n\n\n4\n536365\n84029E\nRED WOOLLY HOTTIE WHITE HEART.\n6\n12/1/2010 8:26\n3.39\n17850\nUnited Kingdom\n\n\n\n\n\n    \n\n  \n    \n\n  \n    \n  \n    \n\n  \n\n    \n  \n\n\n    \n  \n\n\n\n# ── Data profiling ───────────────────────────────────────────────────\nprint(\"=\" * 55)\nprint(\"  RAW DATA QUALITY PROFILE\")\nprint(\"=\" * 55)\n\n# Null counts per column\nnulls = df_raw.isnull().sum()\nprint(\"\\nNull counts per column:\")\nfor col, n in nulls.items():\n    pct = 100 * n / len(df_raw)\n    bar = \"█\" * int(pct / 5)\n    print(f\"  {col:&lt;15} {n:&gt;7,}  ({pct:5.1f}%)  {bar}\")\n\n# Basic type conversion to inspect value ranges\ndf_raw[\"Quantity\"]  = pd.to_numeric(df_raw[\"Quantity\"],  errors=\"coerce\")\ndf_raw[\"UnitPrice\"] = pd.to_numeric(df_raw[\"UnitPrice\"], errors=\"coerce\")\n\nprint(f\"\\nQuantity range  : {df_raw['Quantity'].min():,.0f}  →  {df_raw['Quantity'].max():,.0f}\")\nprint(f\"  Negative (cancellations): {(df_raw['Quantity'] &lt; 0).sum():,} rows\")\nprint(f\"  Zero quantity           : {(df_raw['Quantity'] == 0).sum():,} rows\")\n\nprint(f\"\\nUnitPrice range : {df_raw['UnitPrice'].min():.2f}  →  {df_raw['UnitPrice'].max():.2f}\")\nprint(f\"  Negative price          : {(df_raw['UnitPrice'] &lt; 0).sum():,} rows\")\nprint(f\"  Zero price              : {(df_raw['UnitPrice'] == 0).sum():,} rows\")\n\ndupes = df_raw.duplicated().sum()\nprint(f\"\\nDuplicate rows  : {dupes:,}\")\n\ncountries = df_raw[\"Country\"].nunique()\nprint(f\"\\nUnique countries: {countries}\")\nprint(f\"  Sample: {list(df_raw['Country'].unique()[:8])}\")\n\nprint(\"=\" * 55)\nprint(\"\\n💡 Key findings:\")\nprint(f\"   • CustomerID is NULL in {nulls.get('CustomerID', 0):,} rows ({100*nulls.get('CustomerID',0)/len(df_raw):.1f}%)\")\nprint(f\"   • {(df_raw['Quantity'] &lt; 0).sum():,} rows have negative Quantity (cancelled orders)\")\nprint(f\"   • {(df_raw['UnitPrice'] == 0).sum():,} rows have UnitPrice = 0 (no-charge items)\")\nprint(f\"   • {dupes:,} exact duplicate rows detected\")\n\n=======================================================\n  RAW DATA QUALITY PROFILE\n=======================================================\n\nNull counts per column:\n  InvoiceNo             0  (  0.0%)  \n  StockCode             0  (  0.0%)  \n  Description       1,454  (  0.3%)  \n  Quantity              0  (  0.0%)  \n  InvoiceDate           0  (  0.0%)  \n  UnitPrice             0  (  0.0%)  \n  CustomerID      135,080  ( 24.9%)  ████\n  Country               0  (  0.0%)  \n\nQuantity range  : -80,995  →  80,995\n  Negative (cancellations): 10,624 rows\n  Zero quantity           : 0 rows\n\nUnitPrice range : -11062.06  →  38970.00\n  Negative price          : 2 rows\n  Zero price              : 2,515 rows\n\nDuplicate rows  : 5,268\n\nUnique countries: 38\n  Sample: ['United Kingdom', 'France', 'Australia', 'Netherlands', 'Germany', 'Norway', 'EIRE', 'Switzerland']\n=======================================================\n\n💡 Key findings:\n   • CustomerID is NULL in 135,080 rows (24.9%)\n   • 10,624 rows have negative Quantity (cancelled orders)\n   • 2,515 rows have UnitPrice = 0 (no-charge items)\n   • 5,268 exact duplicate rows detected\n\n\n\n\n\nNow that we know the quality issues, we define a contract at the pipeline boundary. Any record that violates the contract is rejected immediately — before it enters the pipeline.\n\nimport re\nfrom pydantic import BaseModel, field_validator, ConfigDict\n\nclass RetailTransactionContract(BaseModel):\n    \"\"\"\n    Machine-enforceable schema for the Online Retail dataset.\n    strict=True means no silent type coercion — '123' stays a string.\n    \"\"\"\n    model_config = ConfigDict(strict=False)   # lenient for CSV strings\n\n    InvoiceNo:   str\n    StockCode:   str\n    Quantity:    float\n    UnitPrice:   float\n    CustomerID:  str\n    Country:     str\n\n    @field_validator(\"CustomerID\")\n    @classmethod\n    def customer_id_required(cls, v: str) -&gt; str:\n        if not v or v.strip() in (\"\", \"nan\", \"None\"):\n            raise ValueError(\"CustomerID is required — cannot be null\")\n        return v.strip()\n\n    @field_validator(\"Quantity\")\n    @classmethod\n    def positive_quantity(cls, v: float) -&gt; float:\n        if v &lt;= 0:\n            raise ValueError(f\"Quantity must be &gt; 0 (got {v} — likely a cancellation)\")\n        return v\n\n    @field_validator(\"UnitPrice\")\n    @classmethod\n    def positive_price(cls, v: float) -&gt; float:\n        if v &lt;= 0:\n            raise ValueError(f\"UnitPrice must be &gt; 0 (got {v})\")\n        return v\n\n    @field_validator(\"InvoiceNo\")\n    @classmethod\n    def valid_invoice(cls, v: str) -&gt; str:\n        if not re.match(r\"^[A-Z]?\\d{5,6}$\", v.strip()):\n            raise ValueError(f\"InvoiceNo format invalid: '{v}'\")\n        return v.strip()\n\nprint(\"✅ RetailTransactionContract defined.\")\n\n✅ RetailTransactionContract defined.\n\n\n\nimport pandas as pd\n# ── Apply contract validation ──────────────────────────────────────────────\nfrom loguru import logger\nlogger.add(\"pipeline_execution.log\", format=\"{time:YYYY-MM-DD HH:mm:ss} | {level} | {message}\")\n\nprint(\"Running contract validation on all rows (this may take ~30 seconds)...\")\n\nvalid_rows, rejected_rows = [], []\n\nfor _, row in df_raw.iterrows():\n    try:\n        RetailTransactionContract(\n            InvoiceNo  = str(row.get(\"InvoiceNo\",  \"\") or \"\"),\n            StockCode  = str(row.get(\"StockCode\",  \"\") or \"\"),\n            Quantity   = row.get(\"Quantity\",  0),\n            UnitPrice  = row.get(\"UnitPrice\", 0),\n            CustomerID = str(row.get(\"CustomerID\", \"\") or \"\"),\n            Country    = str(row.get(\"Country\",    \"\") or \"\"),\n        )\n        valid_rows.append(row)\n    except Exception as exc:\n        row_copy = dict(row)\n        row_copy[\"rejection_reason\"] = str(exc)\n        rejected_rows.append(row_copy)\n\ndf_valid    = pd.DataFrame(valid_rows)\ndf_rejected = pd.DataFrame(rejected_rows)\n\ntotal = len(df_raw)\nprint(f\"\\n{'='*55}\")\nprint(f\"  CONTRACT VALIDATION RESULTS\")\nprint(f\"{'='*55}\")\nprint(f\"  Total rows    : {total:&gt;10,}\")\nprint(f\"  ✅ Valid       : {len(df_valid):&gt;10,}  ({100*len(df_valid)/total:.1f}%)\")\nprint(f\"  ❌ Rejected    : {len(df_rejected):&gt;10,}  ({100*len(df_rejected)/total:.1f}%)\")\nprint(f\"{'='*55}\")\n\nif not df_rejected.empty:\n    reasons = pd.DataFrame(df_rejected)[\"rejection_reason\"]\n    print(\"\\nTop rejection reasons:\")\n    print(reasons.str.split(\":\").str[0].value_counts().head(5).to_string())\n\nRunning contract validation on all rows (this may take ~30 seconds)...\n\n=======================================================\n  CONTRACT VALIDATION RESULTS\n=======================================================\n  Total rows    :    541,909\n  ✅ Valid       :    397,884  (73.4%)\n  ❌ Rejected    :    144,025  (26.6%)\n=======================================================\n\nTop rejection reasons:\nrejection_reason\n1 validation error for RetailTransactionContract\\nCustomerID\\n  Value error, CustomerID is required — cannot be null [type=value_error, input_value='nan', input_type=str]\\n    For further information visit https               132220\n1 validation error for RetailTransactionContract\\nQuantity\\n  Value error, Quantity must be &gt; 0 (got -1.0 — likely a cancellation) [type=value_error, input_value=-1, input_type=int]\\n    For further information visit https      3848\n1 validation error for RetailTransactionContract\\nQuantity\\n  Value error, Quantity must be &gt; 0 (got -2.0 — likely a cancellation) [type=value_error, input_value=-2, input_type=int]\\n    For further information visit https      1329\n2 validation errors for RetailTransactionContract\\nUnitPrice\\n  Value error, UnitPrice must be &gt; 0 (got 0.0) [type=value_error, input_value=0.0, input_type=float]\\n    For further information visit https                         1139\n1 validation error for RetailTransactionContract\\nQuantity\\n  Value error, Quantity must be &gt; 0 (got -3.0 — likely a cancellation) [type=value_error, input_value=-3, input_type=int]\\n    For further information visit https       577\n\n\n\n\n\nWe run all six DAMA quality dimensions on the contract-validated subset. These are not planted anomalies — they are real patterns in the data.\n\nimport json, uuid\nfrom datetime import timedelta, datetime, UTC\n\nclass DataQualityEngine:\n    \"\"\"\n    Evaluates 6 DAMA dimensions on the Online Retail data:\n      1. Completeness  — no nulls in critical fields\n      2. Accuracy      — prices and quantities in valid ranges\n      3. Consistency   — same InvoiceNo maps to same Country\n      4. Timeliness    — all invoices within expected date range\n      5. Uniqueness    — no full row duplicates\n      6. Validity      — StockCode matches expected format\n    \"\"\"\n    EXPECTED_START = pd.Timestamp(\"2010-01-01\")\n    EXPECTED_END   = pd.Timestamp(\"2012-01-01\")\n\n    def __init__(self, df: pd.DataFrame):\n        self.df  = df.copy()\n        self.log = {}\n\n    def run(self) -&gt; dict:\n        logger.info(\"Starting DAMA 6-dimension scan on real retail data...\")\n\n        # 1. Completeness — Description field (optional but check anyway)\n        null_desc = self.df[\"Description\"].isnull().sum() if \"Description\" in self.df.columns else 0\n        self._record(\"1_completeness_description\",\n                     null_desc == 0,\n                     f\"{null_desc:,} rows missing Description\")\n\n        # 2. Accuracy — UnitPrice and Quantity in business range\n        high_price = (self.df[\"UnitPrice\"] &gt; 10_000).sum()\n        self._record(\"2_accuracy_unit_price\",\n                     high_price == 0,\n                     f\"{high_price:,} rows with UnitPrice &gt; £10,000 (likely data entry error)\")\n\n        # 3. Consistency — same InvoiceNo should map to same Country\n        inconsistent = (\n            self.df.groupby(\"InvoiceNo\")[\"Country\"].nunique().max() &gt; 1\n        )\n        self._record(\"3_consistency_invoice_country\",\n                     not inconsistent,\n                     \"Some InvoiceNos have transactions from multiple countries\")\n\n        # 4. Timeliness — InvoiceDate within expected range\n        if \"InvoiceDate\" in self.df.columns:\n            try:\n                dates = pd.to_datetime(self.df[\"InvoiceDate\"], errors=\"coerce\")\n                out_of_range = ((dates &lt; self.EXPECTED_START) | (dates &gt; self.EXPECTED_END)).sum()\n                self._record(\"4_timeliness_invoice_date\",\n                             out_of_range == 0,\n                             f\"{out_of_range:,} invoices outside 2010–2012 range\")\n            except Exception:\n                self._record(\"4_timeliness_invoice_date\", False, \"Could not parse InvoiceDate\")\n\n        # 5. Uniqueness — no fully duplicated rows\n        dupes = self.df.duplicated().sum()\n        self._record(\"5_uniqueness_rows\",\n                     dupes == 0,\n                     f\"{dupes:,} exact duplicate rows\")\n\n        # 6. Validity — StockCode should be 5 digits or 5 digits + letter\n        invalid_sc = (~self.df[\"StockCode\"].astype(str).str.match(\n            r\"^\\d{5}[A-Z]?$\", na=False\n        )).sum()\n        self._record(\"6_validity_stock_code\",\n                     invalid_sc == 0,\n                     f\"{invalid_sc:,} StockCodes don't match 5-digit format\")\n\n        passed = all(v[\"passed\"] for v in self.log.values())\n        return {\"passed\": passed, \"dimensions\": self.log,\n                \"timestamp\": datetime.now(UTC).isoformat(), \"row_count\": len(self.df)}\n\n    def _record(self, name, passed, detail):\n        self.log[name] = {\"passed\": passed,\n                          \"status\": \"PASSED\" if passed else \"FAILED\",\n                          \"detail\": detail}\n        if passed:\n            logger.success(f\"  [PASSED] {name}: {detail}\")\n        else:\n            logger.warning(f\"  [FAILED] {name}: {detail}\")\n\n# Run the engine\ndq = DataQualityEngine(df_valid)\nreport = dq.run()\n\nprint(\"\\n\" + \"=\"*55)\nprint(\"  DAMA DATA QUALITY REPORT\")\nprint(\"=\"*55)\nfor dim, result in report[\"dimensions\"].items():\n    icon = \"✅\" if result[\"passed\"] else \"❌\"\n    print(f\"  {icon}  {dim}\")\n    print(f\"       {result['detail']}\")\nprint(\"=\"*55)\nprint(f\"\\nOverall verdict: {'PASSED ✅' if report['passed'] else 'FAILED ❌'}\")\nprint(f\"Rows evaluated : {report['row_count']:,}\")\n\n\n2026-07-01 11:01:02.194 | INFO     | __main__:run:22 - Starting DAMA 6-dimension scan on real retail data...\n\n2026-07-01 11:01:02.218 | SUCCESS  | __main__:_record:78 -   [PASSED] 1_completeness_description: 0 rows missing Description\n\n2026-07-01 11:01:02.222 | SUCCESS  | __main__:_record:78 -   [PASSED] 2_accuracy_unit_price: 0 rows with UnitPrice &gt; £10,000 (likely data entry error)\n\n2026-07-01 11:01:02.299 | SUCCESS  | __main__:_record:78 -   [PASSED] 3_consistency_invoice_country: Some InvoiceNos have transactions from multiple countries\n\n2026-07-01 11:01:02.416 | SUCCESS  | __main__:_record:78 -   [PASSED] 4_timeliness_invoice_date: 0 invoices outside 2010–2012 range\n\n2026-07-01 11:01:02.695 | WARNING  | __main__:_record:80 -   [FAILED] 5_uniqueness_rows: 5,192 exact duplicate rows\n\n2026-07-01 11:01:02.886 | WARNING  | __main__:_record:80 -   [FAILED] 6_validity_stock_code: 1,838 StockCodes don't match 5-digit format\n\n\n\n\n\n=======================================================\n  DAMA DATA QUALITY REPORT\n=======================================================\n  ✅  1_completeness_description\n       0 rows missing Description\n  ✅  2_accuracy_unit_price\n       0 rows with UnitPrice &gt; £10,000 (likely data entry error)\n  ✅  3_consistency_invoice_country\n       Some InvoiceNos have transactions from multiple countries\n  ✅  4_timeliness_invoice_date\n       0 invoices outside 2010–2012 range\n  ❌  5_uniqueness_rows\n       5,192 exact duplicate rows\n  ❌  6_validity_stock_code\n       1,838 StockCodes don't match 5-digit format\n=======================================================\n\nOverall verdict: FAILED ❌\nRows evaluated : 397,884\n\n\n\n\n\nThe DAMA scan above is a hand-rolled implementation of the same six dimensions. Here we run the core checks again through a real Great Expectations 1.x checkpoint — the actual library the capstone rubric names — so you see both the concept and the real tool.\n\nimport great_expectations as gx\nimport great_expectations.expectations as gxe\n\ndef run_great_expectations_checkpoint(df):\n    \"\"\"Runs core quality rules as a real GX 1.x fluent-API checkpoint.\"\"\"\n    context = gx.get_context(mode=\"ephemeral\")\n    data_source = context.data_sources.add_pandas(\"pandas_dq_lab\")\n    data_asset = data_source.add_dataframe_asset(name=\"retail_transactions\")\n    batch_definition = data_asset.add_batch_definition_whole_dataframe(\"whole_df\")\n\n    suite = context.suites.add(gx.ExpectationSuite(name=\"retail_quality_suite\"))\n    suite.add_expectation(gxe.ExpectColumnValuesToNotBeNull(column=\"CustomerID\"))\n    suite.add_expectation(gxe.ExpectColumnValuesToBeBetween(column=\"Quantity\", min_value=0.0001))\n    suite.add_expectation(gxe.ExpectColumnValuesToBeBetween(column=\"UnitPrice\", min_value=0.0001))\n    suite.add_expectation(gxe.ExpectColumnValuesToMatchRegex(column=\"InvoiceNo\", regex=r\"^[A-Z]?\\d{5,6}$\"))\n\n    validation_definition = context.validation_definitions.add(\n        gx.ValidationDefinition(\n            name=\"retail_quality_validation\",\n            data=batch_definition,\n            suite=suite,\n        )\n    )\n    checkpoint = context.checkpoints.add(\n        gx.Checkpoint(\n            name=\"retail_quality_checkpoint\",\n            validation_definitions=[validation_definition],\n        )\n    )\n    result = checkpoint.run(batch_parameters={\"dataframe\": df})\n\n    print(f\"[GX] Real Great Expectations checkpoint success={result.success}\")\n    for run_result in result.run_results.values():\n        for r in run_result[\"results\"]:\n            status = \"PASSED\" if r[\"success\"] else \"FAILED\"\n            print(f\"  [GX] {status} {r['expectation_config']['type']}\")\n    return result.success\n\n\ngx_passed = run_great_expectations_checkpoint(df_valid)\nprint(f\"\\n[GX] Real Great Expectations checkpoint agrees: success={gx_passed}\")\n\n[GX] Real Great Expectations checkpoint success=True\n  [GX] PASSED expect_column_values_to_not_be_null\n  [GX] PASSED expect_column_values_to_be_between\n  [GX] PASSED expect_column_values_to_be_between\n  [GX] PASSED expect_column_values_to_match_regex\n\n[GX] Real Great Expectations checkpoint agrees: success=True\n\n\n\n\n\nBased on the quality verdict, we either promote the data or quarantine it. Regardless of the verdict, we always write the rejected rows to quarantine so producers can fix them.\n\nimport os\nfrom datetime import datetime, UTC\n\nos.makedirs(\"production_warehouse\", exist_ok=True)\nos.makedirs(\"quarantine_zone\",      exist_ok=True)\n\n# Always quarantine contract-rejected rows\nif not df_rejected.empty:\n    q_path = f\"quarantine_zone/contract_violations_{int(datetime.now(UTC).timestamp())}.csv\"\n    pd.DataFrame(df_rejected).to_csv(q_path, index=False)\n    logger.error(f\"{len(df_rejected):,} contract violations → {q_path}\")\n\n# Route based on DAMA verdict\nif report[\"passed\"]:\n    out = \"production_warehouse/online_retail_clean.parquet\"\n    df_valid.to_parquet(out, index=False)\n    logger.success(f\"Quality gate PASSED — {len(df_valid):,} rows → {out}\")\n    print(f\"\\n✅ Production write: {out}\")\n    print(f\"   {len(df_valid):,} clean rows ready for downstream analytics\")\nelse:\n    q_path = f\"quarantine_zone/dq_failure_{int(datetime.now(UTC).timestamp())}.csv\"\n    df_valid.to_csv(q_path, index=False)\n    logger.error(f\"Quality gate FAILED — {len(df_valid):,} rows → {q_path}\")\n    print(f\"\\n❌ Quality gate failed — data quarantined to {q_path}\")\n    print(\"   Fix the flagged dimensions and re-run the pipeline.\")\n\n\n2026-07-01 11:01:05.556 | ERROR    | __main__:&lt;cell line: 0&gt;:11 - 144,025 contract violations → quarantine_zone/contract_violations_1782903663.csv\n\n2026-07-01 11:01:07.441 | ERROR    | __main__:&lt;cell line: 0&gt;:23 - Quality gate FAILED — 397,884 rows → quarantine_zone/dq_failure_1782903665.csv\n\n\n\n\n\n❌ Quality gate failed — data quarantined to quarantine_zone/dq_failure_1782903665.csv\n   Fix the flagged dimensions and re-run the pipeline.\n\n\n\n\n\nEvery pipeline run emits structured lineage events. In production these would be sent to a Marquez server via HTTP POST. Here we write them locally for inspection.\n\nimport json, uuid, os\nfrom datetime import datetime, UTC\n\nclass LineageEmitter:\n    def __init__(self, job_name):\n        self.job_name = job_name\n        self.run_id   = str(uuid.uuid4())\n        self.events   = []\n\n    def emit(self, event_type, input_ds, output_ds, facets=None):\n        self.events.append({\n            \"eventType\": event_type,\n            \"eventTime\": datetime.now(UTC).isoformat(),\n            \"run\":    {\"runId\": self.run_id},\n            \"job\":    {\"namespace\": \"day4_lab\", \"name\": self.job_name},\n            \"inputs\":  [{\"namespace\": \"kaggle\", \"name\": input_ds}],\n            \"outputs\": [{\"namespace\": \"lab\",    \"name\": output_ds,\n                         \"facets\": facets or {}}],\n        })\n        print(f\"[LINEAGE] {event_type:10s} | {input_ds} → {output_ds}\")\n\n    def write(self, path=\"lineage_events/run.json\"):\n        os.makedirs(os.path.dirname(path), exist_ok=True)\n        with open(path, \"w\") as f:\n            json.dump(self.events, f, indent=2)\n        print(f\"\\n📋 {len(self.events)} lineage events → {path}\")\n\n\nlineage = LineageEmitter(\"retail_quality_pipeline\")\n\nlineage.emit(\"START\",    \"online_retail_raw.csv\", \"landing_zone\",\n             {\"row_count\": len(df_raw)})\n\nlineage.emit(\"COMPLETE\" if df_rejected.empty else \"FAIL\",\n             \"landing_zone\", \"quarantine_zone/contract_violations\",\n             {\"rejected_rows\": len(df_rejected),\n              \"rejection_rate_pct\": round(100 * len(df_rejected) / len(df_raw), 2)})\n\nif report[\"passed\"]:\n    lineage.emit(\"COMPLETE\", \"landing_zone\", \"production_warehouse/online_retail_clean\",\n                 {\"row_count\": len(df_valid),\n                  \"dama_dimensions_passed\": sum(1 for v in report[\"dimensions\"].values() if v[\"passed\"]),\n                  \"dama_dimensions_total\":  len(report[\"dimensions\"])})\nelse:\n    failed_dims = [k for k, v in report[\"dimensions\"].items() if not v[\"passed\"]]\n    lineage.emit(\"FAIL\", \"landing_zone\", \"quarantine_zone/dq_failure\",\n                 {\"row_count\": len(df_valid), \"failed_dimensions\": failed_dims})\n\nlineage.write()\n\n[LINEAGE] START      | online_retail_raw.csv → landing_zone\n[LINEAGE] FAIL       | landing_zone → quarantine_zone/contract_violations\n[LINEAGE] FAIL       | landing_zone → quarantine_zone/dq_failure\n\n📋 3 lineage events → lineage_events/run.json\n\n\n\n\n\nLineageEmitter above hand-builds JSON matching the OpenLineage event shape. Here we emit real openlineage-python START/COMPLETE (or FAIL) events to a local file transport — no Marquez server required — as the real-library counterpart.\n\nfrom openlineage.client import OpenLineageClient\nfrom openlineage.client.transport.file import FileConfig, FileTransport\nfrom openlineage.client.event_v2 import RunEvent, RunState, Run, Job\nfrom openlineage.client.uuid import generate_new_uuid\n\ndef emit_real_openlineage_events(job_name, row_count, passed):\n    \"\"\"Emits real OpenLineage START/COMPLETE (or FAIL) events to a local file transport.\"\"\"\n    os.makedirs(\"lineage_events\", exist_ok=True)\n    transport = FileTransport(FileConfig(log_file_path=\"lineage_events/openlineage_run.log\"))\n    client = OpenLineageClient(transport=transport)\n\n    run_id = str(generate_new_uuid())\n    job = Job(namespace=\"day4_lab\", name=job_name)\n    run = Run(runId=run_id)\n    now = datetime.now(UTC).isoformat()\n\n    client.emit(RunEvent(\n        eventType=RunState.START, eventTime=now, run=run, job=job,\n        producer=\"https://github.com/sdaia/modern-data-engineering-lab\",\n    ))\n    client.emit(RunEvent(\n        eventType=RunState.COMPLETE if passed else RunState.FAIL,\n        eventTime=now, run=run, job=job,\n        producer=\"https://github.com/sdaia/modern-data-engineering-lab\",\n    ))\n    print(f\"[OpenLineage] real START/{'COMPLETE' if passed else 'FAIL'} events \"\n          f\"({row_count} rows) -&gt; lineage_events/openlineage_run.log*\")\n\n\nemit_real_openlineage_events(\"retail_quality_pipeline\", len(df_valid), report[\"passed\"])\n\n[OpenLineage] real START/FAIL events (397884 rows) -&gt; lineage_events/openlineage_run.log*\n\n\n\n\n\nNow let’s actually look at what passed the quality gates and draw some insights.\n\nif report[\"passed\"] and os.path.exists(\"production_warehouse/online_retail_clean.parquet\"):\n    df_clean = pd.read_parquet(\"production_warehouse/online_retail_clean.parquet\")\nelse:\n    df_clean = df_valid.copy()\n\ndf_clean[\"Revenue\"] = df_clean[\"Quantity\"].astype(float) * df_clean[\"UnitPrice\"].astype(float)\n\nprint(\"📊 Top 10 countries by revenue (clean data only):\")\ntop_countries = (\n    df_clean.groupby(\"Country\")[\"Revenue\"]\n    .sum()\n    .sort_values(ascending=False)\n    .head(10)\n)\nprint(top_countries.apply(lambda x: f\"  £{x:&gt;12,.2f}\").to_string())\n\nprint(f\"\\n📦 Unique products  : {df_clean['StockCode'].nunique():,}\")\nprint(f\"👥 Unique customers : {df_clean['CustomerID'].nunique():,}\")\nprint(f\"🧾 Unique invoices  : {df_clean['InvoiceNo'].nunique():,}\")\nprint(f\"💷 Total revenue    : £{df_clean['Revenue'].sum():,.2f}\")\n\nprint(\"\\n✅ Pipeline complete.\")\nprint(f\"   Started with {len(df_raw):,} raw rows.\")\nprint(f\"   {len(df_rejected):,} rejected by contract ({100*len(df_rejected)/len(df_raw):.1f}%).\")\nprint(f\"   {len(df_clean):,} rows in production warehouse ready for analytics.\")\n\n📊 Top 10 countries by revenue (clean data only):\nCountry\nUnited Kingdom      £7,308,391.55\nNetherlands         £  285,446.34\nEIRE                £  265,545.90\nGermany             £  228,867.14\nFrance              £  209,024.05\nAustralia           £  138,521.31\nSpain               £   61,577.11\nSwitzerland         £   56,443.95\nBelgium             £   41,196.34\nSweden              £   38,378.33\n\n📦 Unique products  : 3,665\n👥 Unique customers : 4,338\n🧾 Unique invoices  : 18,532\n💷 Total revenue    : £8,911,407.90\n\n✅ Pipeline complete.\n   Started with 541,909 raw rows.\n   144,025 rejected by contract (26.6%).\n   397,884 rows in production warehouse ready for analytics.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#using-real-world-e-commerce-data-kaggle",
    "href": "L04/Day4_Lab.html#using-real-world-e-commerce-data-kaggle",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "We are using the UCI Online Retail Dataset — 541,909 real transactions from a UK-based online retailer (2010–2011). This dataset has genuine quality problems: 25% of CustomerIDs are missing, cancelled orders have negative quantities, and there are formatting inconsistencies.\nYour pipeline will: 1. Download the dataset from Kaggle 2. Profile the raw data to discover quality issues 3. Validate records against a Pydantic v2 data contract 4. Run all 6 DAMA quality dimensions on the clean subset 5. Route data to production or quarantine based on quality verdict 6. Run the same checks again through a real Great Expectations checkpoint 7. Emit an OpenLineage-style audit trail, and also real openlineage-python events",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-1-install-dependencies",
    "href": "L04/Day4_Lab.html#step-1-install-dependencies",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "!pip install kagglehub pandas loguru \"pydantic&gt;=2.0\" pyarrow great_expectations openlineage-python\n\n\nRequirement already satisfied: kagglehub in /usr/local/lib/python3.12/dist-packages (1.0.2)\n\nRequirement already satisfied: pandas in /usr/local/lib/python3.12/dist-packages (2.2.2)\n\nCollecting loguru\n\n  Downloading loguru-0.7.3-py3-none-any.whl.metadata (22 kB)\n\nRequirement already satisfied: pydantic&gt;=2.0 in /usr/local/lib/python3.12/dist-packages (2.13.4)\n\nRequirement already satisfied: pyarrow in /usr/local/lib/python3.12/dist-packages (18.1.0)\n\nRequirement already satisfied: kagglesdk&lt;1.0,&gt;=0.1.22 in /usr/local/lib/python3.12/dist-packages (from kagglehub) (0.1.23)\n\nRequirement already satisfied: packaging in /usr/local/lib/python3.12/dist-packages (from kagglehub) (26.2)\n\nRequirement already satisfied: pyyaml in /usr/local/lib/python3.12/dist-packages (from kagglehub) (6.0.3)\n\nRequirement already satisfied: requests in /usr/local/lib/python3.12/dist-packages (from kagglehub) (2.32.4)\n\nRequirement already satisfied: tqdm in /usr/local/lib/python3.12/dist-packages (from kagglehub) (4.67.3)\n\nRequirement already satisfied: numpy&gt;=1.26.0 in /usr/local/lib/python3.12/dist-packages (from pandas) (2.0.2)\n\nRequirement already satisfied: python-dateutil&gt;=2.8.2 in /usr/local/lib/python3.12/dist-packages (from pandas) (2.9.0.post0)\n\nRequirement already satisfied: pytz&gt;=2020.1 in /usr/local/lib/python3.12/dist-packages (from pandas) (2025.2)\n\nRequirement already satisfied: tzdata&gt;=2022.7 in /usr/local/lib/python3.12/dist-packages (from pandas) (2026.2)\n\nRequirement already satisfied: annotated-types&gt;=0.6.0 in /usr/local/lib/python3.12/dist-packages (from pydantic&gt;=2.0) (0.7.0)\n\nRequirement already satisfied: pydantic-core==2.46.4 in /usr/local/lib/python3.12/dist-packages (from pydantic&gt;=2.0) (2.46.4)\n\nRequirement already satisfied: typing-extensions&gt;=4.14.1 in /usr/local/lib/python3.12/dist-packages (from pydantic&gt;=2.0) (4.15.0)\n\nRequirement already satisfied: typing-inspection&gt;=0.4.2 in /usr/local/lib/python3.12/dist-packages (from pydantic&gt;=2.0) (0.4.2)\n\nRequirement already satisfied: protobuf in /usr/local/lib/python3.12/dist-packages (from kagglesdk&lt;1.0,&gt;=0.1.22-&gt;kagglehub) (5.29.6)\n\nRequirement already satisfied: six&gt;=1.5 in /usr/local/lib/python3.12/dist-packages (from python-dateutil&gt;=2.8.2-&gt;pandas) (1.17.0)\n\nRequirement already satisfied: charset_normalizer&lt;4,&gt;=2 in /usr/local/lib/python3.12/dist-packages (from requests-&gt;kagglehub) (3.4.7)\n\nRequirement already satisfied: idna&lt;4,&gt;=2.5 in /usr/local/lib/python3.12/dist-packages (from requests-&gt;kagglehub) (3.18)\n\nRequirement already satisfied: urllib3&lt;3,&gt;=1.21.1 in /usr/local/lib/python3.12/dist-packages (from requests-&gt;kagglehub) (2.5.0)\n\nRequirement already satisfied: certifi&gt;=2017.4.17 in /usr/local/lib/python3.12/dist-packages (from requests-&gt;kagglehub) (2026.6.17)\n\nDownloading loguru-0.7.3-py3-none-any.whl (61 kB)\n\n   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 61.6/61.6 kB 1.7 MB/s eta 0:00:00\n\nInstalling collected packages: loguru\n\nSuccessfully installed loguru-0.7.3",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-2-kaggle-authentication",
    "href": "L04/Day4_Lab.html#step-2-kaggle-authentication",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "You need a Kaggle account and API key.\nOption A — Colab Secrets (recommended): 1. Go to kaggle.com → Account → API → Create New Token → downloads kaggle.json 2. In Colab: click the 🔑 icon (left sidebar) → add two secrets: - KAGGLE_USERNAME = your Kaggle username - KAGGLE_KEY = the key value from kaggle.json\nOption B — Upload kaggle.json directly: Run the upload cell below instead.\n\n# ── Option A: Colab Secrets ──────────────────────────────────────────\nimport os\n\ntry:\n    from google.colab import userdata\n    os.environ[\"KAGGLE_USERNAME\"] = userdata.get(\"KAGGLE_USERNAME\")\n    os.environ[\"KAGGLE_KEY\"]      = userdata.get(\"KAGGLE_KEY\")\n    print(\"✅ Kaggle credentials loaded from Colab Secrets.\")\nexcept Exception:\n    print(\"⚠️  Colab Secrets not available — run Option B below.\")\n\n✅ Kaggle credentials loaded from Colab Secrets.\n\n\n\n# ── Option B: Upload kaggle.json manually (skip if Option A worked) ──\n# from google.colab import files\n# uploaded = files.upload()          # select your kaggle.json\n# import os, shutil\n# os.makedirs(\"/root/.kaggle\", exist_ok=True)\n# shutil.move(\"kaggle.json\", \"/root/.kaggle/kaggle.json\")\n# os.chmod(\"/root/.kaggle/kaggle.json\", 0o600)\n# print(\"✅ kaggle.json installed.\")",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-3-download-the-dataset",
    "href": "L04/Day4_Lab.html#step-3-download-the-dataset",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "import kagglehub, glob\n\n# UCI Online Retail Dataset — 541,909 real transactions, ~45 MB\npath = kagglehub.dataset_download(\"carrie1/ecommerce-data\")\nprint(f\"Downloaded to: {path}\")\n\ncsv_files = glob.glob(f\"{path}/**/*.csv\", recursive=True)\nprint(f\"CSV files found: {csv_files}\")\nRAW_CSV = csv_files[0]\nprint(f\"Using: {RAW_CSV}\")\n\nDownloading to /root/.cache/kagglehub/datasets/carrie1/ecommerce-data/1.archive...\n\n\n100%|██████████| 7.20M/7.20M [00:00&lt;00:00, 115MB/s]\n\n\nExtracting files...\n\n\n\n\n\nDownloaded to: /root/.cache/kagglehub/datasets/carrie1/ecommerce-data/versions/1\nCSV files found: ['/root/.cache/kagglehub/datasets/carrie1/ecommerce-data/versions/1/data.csv']\nUsing: /root/.cache/kagglehub/datasets/carrie1/ecommerce-data/versions/1/data.csv",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-4-load-and-profile-the-raw-data",
    "href": "L04/Day4_Lab.html#step-4-load-and-profile-the-raw-data",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "Before running any pipeline, always profile the data to understand what you are dealing with. This is the discovery phase — you are looking for quality issues before writing any validation rules.\n\nimport pandas as pd\nfrom datetime import datetime\n\ndf_raw = pd.read_csv(RAW_CSV, encoding=\"ISO-8859-1\", dtype=str)\nprint(f\"Shape: {df_raw.shape[0]:,} rows × {df_raw.shape[1]} columns\")\nprint(f\"Columns: {list(df_raw.columns)}\")\ndf_raw.head(5)\n\nShape: 541,909 rows × 8 columns\nColumns: ['InvoiceNo', 'StockCode', 'Description', 'Quantity', 'InvoiceDate', 'UnitPrice', 'CustomerID', 'Country']\n\n\n\n    \n\n\n\n\n\n\nInvoiceNo\nStockCode\nDescription\nQuantity\nInvoiceDate\nUnitPrice\nCustomerID\nCountry\n\n\n\n\n0\n536365\n85123A\nWHITE HANGING HEART T-LIGHT HOLDER\n6\n12/1/2010 8:26\n2.55\n17850\nUnited Kingdom\n\n\n1\n536365\n71053\nWHITE METAL LANTERN\n6\n12/1/2010 8:26\n3.39\n17850\nUnited Kingdom\n\n\n2\n536365\n84406B\nCREAM CUPID HEARTS COAT HANGER\n8\n12/1/2010 8:26\n2.75\n17850\nUnited Kingdom\n\n\n3\n536365\n84029G\nKNITTED UNION FLAG HOT WATER BOTTLE\n6\n12/1/2010 8:26\n3.39\n17850\nUnited Kingdom\n\n\n4\n536365\n84029E\nRED WOOLLY HOTTIE WHITE HEART.\n6\n12/1/2010 8:26\n3.39\n17850\nUnited Kingdom\n\n\n\n\n\n    \n\n  \n    \n\n  \n    \n  \n    \n\n  \n\n    \n  \n\n\n    \n  \n\n\n\n# ── Data profiling ───────────────────────────────────────────────────\nprint(\"=\" * 55)\nprint(\"  RAW DATA QUALITY PROFILE\")\nprint(\"=\" * 55)\n\n# Null counts per column\nnulls = df_raw.isnull().sum()\nprint(\"\\nNull counts per column:\")\nfor col, n in nulls.items():\n    pct = 100 * n / len(df_raw)\n    bar = \"█\" * int(pct / 5)\n    print(f\"  {col:&lt;15} {n:&gt;7,}  ({pct:5.1f}%)  {bar}\")\n\n# Basic type conversion to inspect value ranges\ndf_raw[\"Quantity\"]  = pd.to_numeric(df_raw[\"Quantity\"],  errors=\"coerce\")\ndf_raw[\"UnitPrice\"] = pd.to_numeric(df_raw[\"UnitPrice\"], errors=\"coerce\")\n\nprint(f\"\\nQuantity range  : {df_raw['Quantity'].min():,.0f}  →  {df_raw['Quantity'].max():,.0f}\")\nprint(f\"  Negative (cancellations): {(df_raw['Quantity'] &lt; 0).sum():,} rows\")\nprint(f\"  Zero quantity           : {(df_raw['Quantity'] == 0).sum():,} rows\")\n\nprint(f\"\\nUnitPrice range : {df_raw['UnitPrice'].min():.2f}  →  {df_raw['UnitPrice'].max():.2f}\")\nprint(f\"  Negative price          : {(df_raw['UnitPrice'] &lt; 0).sum():,} rows\")\nprint(f\"  Zero price              : {(df_raw['UnitPrice'] == 0).sum():,} rows\")\n\ndupes = df_raw.duplicated().sum()\nprint(f\"\\nDuplicate rows  : {dupes:,}\")\n\ncountries = df_raw[\"Country\"].nunique()\nprint(f\"\\nUnique countries: {countries}\")\nprint(f\"  Sample: {list(df_raw['Country'].unique()[:8])}\")\n\nprint(\"=\" * 55)\nprint(\"\\n💡 Key findings:\")\nprint(f\"   • CustomerID is NULL in {nulls.get('CustomerID', 0):,} rows ({100*nulls.get('CustomerID',0)/len(df_raw):.1f}%)\")\nprint(f\"   • {(df_raw['Quantity'] &lt; 0).sum():,} rows have negative Quantity (cancelled orders)\")\nprint(f\"   • {(df_raw['UnitPrice'] == 0).sum():,} rows have UnitPrice = 0 (no-charge items)\")\nprint(f\"   • {dupes:,} exact duplicate rows detected\")\n\n=======================================================\n  RAW DATA QUALITY PROFILE\n=======================================================\n\nNull counts per column:\n  InvoiceNo             0  (  0.0%)  \n  StockCode             0  (  0.0%)  \n  Description       1,454  (  0.3%)  \n  Quantity              0  (  0.0%)  \n  InvoiceDate           0  (  0.0%)  \n  UnitPrice             0  (  0.0%)  \n  CustomerID      135,080  ( 24.9%)  ████\n  Country               0  (  0.0%)  \n\nQuantity range  : -80,995  →  80,995\n  Negative (cancellations): 10,624 rows\n  Zero quantity           : 0 rows\n\nUnitPrice range : -11062.06  →  38970.00\n  Negative price          : 2 rows\n  Zero price              : 2,515 rows\n\nDuplicate rows  : 5,268\n\nUnique countries: 38\n  Sample: ['United Kingdom', 'France', 'Australia', 'Netherlands', 'Germany', 'Norway', 'EIRE', 'Switzerland']\n=======================================================\n\n💡 Key findings:\n   • CustomerID is NULL in 135,080 rows (24.9%)\n   • 10,624 rows have negative Quantity (cancelled orders)\n   • 2,515 rows have UnitPrice = 0 (no-charge items)\n   • 5,268 exact duplicate rows detected",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-5-data-contract-pydantic-v2",
    "href": "L04/Day4_Lab.html#step-5-data-contract-pydantic-v2",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "Now that we know the quality issues, we define a contract at the pipeline boundary. Any record that violates the contract is rejected immediately — before it enters the pipeline.\n\nimport re\nfrom pydantic import BaseModel, field_validator, ConfigDict\n\nclass RetailTransactionContract(BaseModel):\n    \"\"\"\n    Machine-enforceable schema for the Online Retail dataset.\n    strict=True means no silent type coercion — '123' stays a string.\n    \"\"\"\n    model_config = ConfigDict(strict=False)   # lenient for CSV strings\n\n    InvoiceNo:   str\n    StockCode:   str\n    Quantity:    float\n    UnitPrice:   float\n    CustomerID:  str\n    Country:     str\n\n    @field_validator(\"CustomerID\")\n    @classmethod\n    def customer_id_required(cls, v: str) -&gt; str:\n        if not v or v.strip() in (\"\", \"nan\", \"None\"):\n            raise ValueError(\"CustomerID is required — cannot be null\")\n        return v.strip()\n\n    @field_validator(\"Quantity\")\n    @classmethod\n    def positive_quantity(cls, v: float) -&gt; float:\n        if v &lt;= 0:\n            raise ValueError(f\"Quantity must be &gt; 0 (got {v} — likely a cancellation)\")\n        return v\n\n    @field_validator(\"UnitPrice\")\n    @classmethod\n    def positive_price(cls, v: float) -&gt; float:\n        if v &lt;= 0:\n            raise ValueError(f\"UnitPrice must be &gt; 0 (got {v})\")\n        return v\n\n    @field_validator(\"InvoiceNo\")\n    @classmethod\n    def valid_invoice(cls, v: str) -&gt; str:\n        if not re.match(r\"^[A-Z]?\\d{5,6}$\", v.strip()):\n            raise ValueError(f\"InvoiceNo format invalid: '{v}'\")\n        return v.strip()\n\nprint(\"✅ RetailTransactionContract defined.\")\n\n✅ RetailTransactionContract defined.\n\n\n\nimport pandas as pd\n# ── Apply contract validation ──────────────────────────────────────────────\nfrom loguru import logger\nlogger.add(\"pipeline_execution.log\", format=\"{time:YYYY-MM-DD HH:mm:ss} | {level} | {message}\")\n\nprint(\"Running contract validation on all rows (this may take ~30 seconds)...\")\n\nvalid_rows, rejected_rows = [], []\n\nfor _, row in df_raw.iterrows():\n    try:\n        RetailTransactionContract(\n            InvoiceNo  = str(row.get(\"InvoiceNo\",  \"\") or \"\"),\n            StockCode  = str(row.get(\"StockCode\",  \"\") or \"\"),\n            Quantity   = row.get(\"Quantity\",  0),\n            UnitPrice  = row.get(\"UnitPrice\", 0),\n            CustomerID = str(row.get(\"CustomerID\", \"\") or \"\"),\n            Country    = str(row.get(\"Country\",    \"\") or \"\"),\n        )\n        valid_rows.append(row)\n    except Exception as exc:\n        row_copy = dict(row)\n        row_copy[\"rejection_reason\"] = str(exc)\n        rejected_rows.append(row_copy)\n\ndf_valid    = pd.DataFrame(valid_rows)\ndf_rejected = pd.DataFrame(rejected_rows)\n\ntotal = len(df_raw)\nprint(f\"\\n{'='*55}\")\nprint(f\"  CONTRACT VALIDATION RESULTS\")\nprint(f\"{'='*55}\")\nprint(f\"  Total rows    : {total:&gt;10,}\")\nprint(f\"  ✅ Valid       : {len(df_valid):&gt;10,}  ({100*len(df_valid)/total:.1f}%)\")\nprint(f\"  ❌ Rejected    : {len(df_rejected):&gt;10,}  ({100*len(df_rejected)/total:.1f}%)\")\nprint(f\"{'='*55}\")\n\nif not df_rejected.empty:\n    reasons = pd.DataFrame(df_rejected)[\"rejection_reason\"]\n    print(\"\\nTop rejection reasons:\")\n    print(reasons.str.split(\":\").str[0].value_counts().head(5).to_string())\n\nRunning contract validation on all rows (this may take ~30 seconds)...\n\n=======================================================\n  CONTRACT VALIDATION RESULTS\n=======================================================\n  Total rows    :    541,909\n  ✅ Valid       :    397,884  (73.4%)\n  ❌ Rejected    :    144,025  (26.6%)\n=======================================================\n\nTop rejection reasons:\nrejection_reason\n1 validation error for RetailTransactionContract\\nCustomerID\\n  Value error, CustomerID is required — cannot be null [type=value_error, input_value='nan', input_type=str]\\n    For further information visit https               132220\n1 validation error for RetailTransactionContract\\nQuantity\\n  Value error, Quantity must be &gt; 0 (got -1.0 — likely a cancellation) [type=value_error, input_value=-1, input_type=int]\\n    For further information visit https      3848\n1 validation error for RetailTransactionContract\\nQuantity\\n  Value error, Quantity must be &gt; 0 (got -2.0 — likely a cancellation) [type=value_error, input_value=-2, input_type=int]\\n    For further information visit https      1329\n2 validation errors for RetailTransactionContract\\nUnitPrice\\n  Value error, UnitPrice must be &gt; 0 (got 0.0) [type=value_error, input_value=0.0, input_type=float]\\n    For further information visit https                         1139\n1 validation error for RetailTransactionContract\\nQuantity\\n  Value error, Quantity must be &gt; 0 (got -3.0 — likely a cancellation) [type=value_error, input_value=-3, input_type=int]\\n    For further information visit https       577",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-6-dama-6-dimension-quality-engine",
    "href": "L04/Day4_Lab.html#step-6-dama-6-dimension-quality-engine",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "We run all six DAMA quality dimensions on the contract-validated subset. These are not planted anomalies — they are real patterns in the data.\n\nimport json, uuid\nfrom datetime import timedelta, datetime, UTC\n\nclass DataQualityEngine:\n    \"\"\"\n    Evaluates 6 DAMA dimensions on the Online Retail data:\n      1. Completeness  — no nulls in critical fields\n      2. Accuracy      — prices and quantities in valid ranges\n      3. Consistency   — same InvoiceNo maps to same Country\n      4. Timeliness    — all invoices within expected date range\n      5. Uniqueness    — no full row duplicates\n      6. Validity      — StockCode matches expected format\n    \"\"\"\n    EXPECTED_START = pd.Timestamp(\"2010-01-01\")\n    EXPECTED_END   = pd.Timestamp(\"2012-01-01\")\n\n    def __init__(self, df: pd.DataFrame):\n        self.df  = df.copy()\n        self.log = {}\n\n    def run(self) -&gt; dict:\n        logger.info(\"Starting DAMA 6-dimension scan on real retail data...\")\n\n        # 1. Completeness — Description field (optional but check anyway)\n        null_desc = self.df[\"Description\"].isnull().sum() if \"Description\" in self.df.columns else 0\n        self._record(\"1_completeness_description\",\n                     null_desc == 0,\n                     f\"{null_desc:,} rows missing Description\")\n\n        # 2. Accuracy — UnitPrice and Quantity in business range\n        high_price = (self.df[\"UnitPrice\"] &gt; 10_000).sum()\n        self._record(\"2_accuracy_unit_price\",\n                     high_price == 0,\n                     f\"{high_price:,} rows with UnitPrice &gt; £10,000 (likely data entry error)\")\n\n        # 3. Consistency — same InvoiceNo should map to same Country\n        inconsistent = (\n            self.df.groupby(\"InvoiceNo\")[\"Country\"].nunique().max() &gt; 1\n        )\n        self._record(\"3_consistency_invoice_country\",\n                     not inconsistent,\n                     \"Some InvoiceNos have transactions from multiple countries\")\n\n        # 4. Timeliness — InvoiceDate within expected range\n        if \"InvoiceDate\" in self.df.columns:\n            try:\n                dates = pd.to_datetime(self.df[\"InvoiceDate\"], errors=\"coerce\")\n                out_of_range = ((dates &lt; self.EXPECTED_START) | (dates &gt; self.EXPECTED_END)).sum()\n                self._record(\"4_timeliness_invoice_date\",\n                             out_of_range == 0,\n                             f\"{out_of_range:,} invoices outside 2010–2012 range\")\n            except Exception:\n                self._record(\"4_timeliness_invoice_date\", False, \"Could not parse InvoiceDate\")\n\n        # 5. Uniqueness — no fully duplicated rows\n        dupes = self.df.duplicated().sum()\n        self._record(\"5_uniqueness_rows\",\n                     dupes == 0,\n                     f\"{dupes:,} exact duplicate rows\")\n\n        # 6. Validity — StockCode should be 5 digits or 5 digits + letter\n        invalid_sc = (~self.df[\"StockCode\"].astype(str).str.match(\n            r\"^\\d{5}[A-Z]?$\", na=False\n        )).sum()\n        self._record(\"6_validity_stock_code\",\n                     invalid_sc == 0,\n                     f\"{invalid_sc:,} StockCodes don't match 5-digit format\")\n\n        passed = all(v[\"passed\"] for v in self.log.values())\n        return {\"passed\": passed, \"dimensions\": self.log,\n                \"timestamp\": datetime.now(UTC).isoformat(), \"row_count\": len(self.df)}\n\n    def _record(self, name, passed, detail):\n        self.log[name] = {\"passed\": passed,\n                          \"status\": \"PASSED\" if passed else \"FAILED\",\n                          \"detail\": detail}\n        if passed:\n            logger.success(f\"  [PASSED] {name}: {detail}\")\n        else:\n            logger.warning(f\"  [FAILED] {name}: {detail}\")\n\n# Run the engine\ndq = DataQualityEngine(df_valid)\nreport = dq.run()\n\nprint(\"\\n\" + \"=\"*55)\nprint(\"  DAMA DATA QUALITY REPORT\")\nprint(\"=\"*55)\nfor dim, result in report[\"dimensions\"].items():\n    icon = \"✅\" if result[\"passed\"] else \"❌\"\n    print(f\"  {icon}  {dim}\")\n    print(f\"       {result['detail']}\")\nprint(\"=\"*55)\nprint(f\"\\nOverall verdict: {'PASSED ✅' if report['passed'] else 'FAILED ❌'}\")\nprint(f\"Rows evaluated : {report['row_count']:,}\")\n\n\n2026-07-01 11:01:02.194 | INFO     | __main__:run:22 - Starting DAMA 6-dimension scan on real retail data...\n\n2026-07-01 11:01:02.218 | SUCCESS  | __main__:_record:78 -   [PASSED] 1_completeness_description: 0 rows missing Description\n\n2026-07-01 11:01:02.222 | SUCCESS  | __main__:_record:78 -   [PASSED] 2_accuracy_unit_price: 0 rows with UnitPrice &gt; £10,000 (likely data entry error)\n\n2026-07-01 11:01:02.299 | SUCCESS  | __main__:_record:78 -   [PASSED] 3_consistency_invoice_country: Some InvoiceNos have transactions from multiple countries\n\n2026-07-01 11:01:02.416 | SUCCESS  | __main__:_record:78 -   [PASSED] 4_timeliness_invoice_date: 0 invoices outside 2010–2012 range\n\n2026-07-01 11:01:02.695 | WARNING  | __main__:_record:80 -   [FAILED] 5_uniqueness_rows: 5,192 exact duplicate rows\n\n2026-07-01 11:01:02.886 | WARNING  | __main__:_record:80 -   [FAILED] 6_validity_stock_code: 1,838 StockCodes don't match 5-digit format\n\n\n\n\n\n=======================================================\n  DAMA DATA QUALITY REPORT\n=======================================================\n  ✅  1_completeness_description\n       0 rows missing Description\n  ✅  2_accuracy_unit_price\n       0 rows with UnitPrice &gt; £10,000 (likely data entry error)\n  ✅  3_consistency_invoice_country\n       Some InvoiceNos have transactions from multiple countries\n  ✅  4_timeliness_invoice_date\n       0 invoices outside 2010–2012 range\n  ❌  5_uniqueness_rows\n       5,192 exact duplicate rows\n  ❌  6_validity_stock_code\n       1,838 StockCodes don't match 5-digit format\n=======================================================\n\nOverall verdict: FAILED ❌\nRows evaluated : 397,884",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-6b-real-great-expectations-checkpoint",
    "href": "L04/Day4_Lab.html#step-6b-real-great-expectations-checkpoint",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "The DAMA scan above is a hand-rolled implementation of the same six dimensions. Here we run the core checks again through a real Great Expectations 1.x checkpoint — the actual library the capstone rubric names — so you see both the concept and the real tool.\n\nimport great_expectations as gx\nimport great_expectations.expectations as gxe\n\ndef run_great_expectations_checkpoint(df):\n    \"\"\"Runs core quality rules as a real GX 1.x fluent-API checkpoint.\"\"\"\n    context = gx.get_context(mode=\"ephemeral\")\n    data_source = context.data_sources.add_pandas(\"pandas_dq_lab\")\n    data_asset = data_source.add_dataframe_asset(name=\"retail_transactions\")\n    batch_definition = data_asset.add_batch_definition_whole_dataframe(\"whole_df\")\n\n    suite = context.suites.add(gx.ExpectationSuite(name=\"retail_quality_suite\"))\n    suite.add_expectation(gxe.ExpectColumnValuesToNotBeNull(column=\"CustomerID\"))\n    suite.add_expectation(gxe.ExpectColumnValuesToBeBetween(column=\"Quantity\", min_value=0.0001))\n    suite.add_expectation(gxe.ExpectColumnValuesToBeBetween(column=\"UnitPrice\", min_value=0.0001))\n    suite.add_expectation(gxe.ExpectColumnValuesToMatchRegex(column=\"InvoiceNo\", regex=r\"^[A-Z]?\\d{5,6}$\"))\n\n    validation_definition = context.validation_definitions.add(\n        gx.ValidationDefinition(\n            name=\"retail_quality_validation\",\n            data=batch_definition,\n            suite=suite,\n        )\n    )\n    checkpoint = context.checkpoints.add(\n        gx.Checkpoint(\n            name=\"retail_quality_checkpoint\",\n            validation_definitions=[validation_definition],\n        )\n    )\n    result = checkpoint.run(batch_parameters={\"dataframe\": df})\n\n    print(f\"[GX] Real Great Expectations checkpoint success={result.success}\")\n    for run_result in result.run_results.values():\n        for r in run_result[\"results\"]:\n            status = \"PASSED\" if r[\"success\"] else \"FAILED\"\n            print(f\"  [GX] {status} {r['expectation_config']['type']}\")\n    return result.success\n\n\ngx_passed = run_great_expectations_checkpoint(df_valid)\nprint(f\"\\n[GX] Real Great Expectations checkpoint agrees: success={gx_passed}\")\n\n[GX] Real Great Expectations checkpoint success=True\n  [GX] PASSED expect_column_values_to_not_be_null\n  [GX] PASSED expect_column_values_to_be_between\n  [GX] PASSED expect_column_values_to_be_between\n  [GX] PASSED expect_column_values_to_match_regex\n\n[GX] Real Great Expectations checkpoint agrees: success=True",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-7-route-production-warehouse-or-quarantine",
    "href": "L04/Day4_Lab.html#step-7-route-production-warehouse-or-quarantine",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "Based on the quality verdict, we either promote the data or quarantine it. Regardless of the verdict, we always write the rejected rows to quarantine so producers can fix them.\n\nimport os\nfrom datetime import datetime, UTC\n\nos.makedirs(\"production_warehouse\", exist_ok=True)\nos.makedirs(\"quarantine_zone\",      exist_ok=True)\n\n# Always quarantine contract-rejected rows\nif not df_rejected.empty:\n    q_path = f\"quarantine_zone/contract_violations_{int(datetime.now(UTC).timestamp())}.csv\"\n    pd.DataFrame(df_rejected).to_csv(q_path, index=False)\n    logger.error(f\"{len(df_rejected):,} contract violations → {q_path}\")\n\n# Route based on DAMA verdict\nif report[\"passed\"]:\n    out = \"production_warehouse/online_retail_clean.parquet\"\n    df_valid.to_parquet(out, index=False)\n    logger.success(f\"Quality gate PASSED — {len(df_valid):,} rows → {out}\")\n    print(f\"\\n✅ Production write: {out}\")\n    print(f\"   {len(df_valid):,} clean rows ready for downstream analytics\")\nelse:\n    q_path = f\"quarantine_zone/dq_failure_{int(datetime.now(UTC).timestamp())}.csv\"\n    df_valid.to_csv(q_path, index=False)\n    logger.error(f\"Quality gate FAILED — {len(df_valid):,} rows → {q_path}\")\n    print(f\"\\n❌ Quality gate failed — data quarantined to {q_path}\")\n    print(\"   Fix the flagged dimensions and re-run the pipeline.\")\n\n\n2026-07-01 11:01:05.556 | ERROR    | __main__:&lt;cell line: 0&gt;:11 - 144,025 contract violations → quarantine_zone/contract_violations_1782903663.csv\n\n2026-07-01 11:01:07.441 | ERROR    | __main__:&lt;cell line: 0&gt;:23 - Quality gate FAILED — 397,884 rows → quarantine_zone/dq_failure_1782903665.csv\n\n\n\n\n\n❌ Quality gate failed — data quarantined to quarantine_zone/dq_failure_1782903665.csv\n   Fix the flagged dimensions and re-run the pipeline.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-8-openlineage-audit-trail",
    "href": "L04/Day4_Lab.html#step-8-openlineage-audit-trail",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "Every pipeline run emits structured lineage events. In production these would be sent to a Marquez server via HTTP POST. Here we write them locally for inspection.\n\nimport json, uuid, os\nfrom datetime import datetime, UTC\n\nclass LineageEmitter:\n    def __init__(self, job_name):\n        self.job_name = job_name\n        self.run_id   = str(uuid.uuid4())\n        self.events   = []\n\n    def emit(self, event_type, input_ds, output_ds, facets=None):\n        self.events.append({\n            \"eventType\": event_type,\n            \"eventTime\": datetime.now(UTC).isoformat(),\n            \"run\":    {\"runId\": self.run_id},\n            \"job\":    {\"namespace\": \"day4_lab\", \"name\": self.job_name},\n            \"inputs\":  [{\"namespace\": \"kaggle\", \"name\": input_ds}],\n            \"outputs\": [{\"namespace\": \"lab\",    \"name\": output_ds,\n                         \"facets\": facets or {}}],\n        })\n        print(f\"[LINEAGE] {event_type:10s} | {input_ds} → {output_ds}\")\n\n    def write(self, path=\"lineage_events/run.json\"):\n        os.makedirs(os.path.dirname(path), exist_ok=True)\n        with open(path, \"w\") as f:\n            json.dump(self.events, f, indent=2)\n        print(f\"\\n📋 {len(self.events)} lineage events → {path}\")\n\n\nlineage = LineageEmitter(\"retail_quality_pipeline\")\n\nlineage.emit(\"START\",    \"online_retail_raw.csv\", \"landing_zone\",\n             {\"row_count\": len(df_raw)})\n\nlineage.emit(\"COMPLETE\" if df_rejected.empty else \"FAIL\",\n             \"landing_zone\", \"quarantine_zone/contract_violations\",\n             {\"rejected_rows\": len(df_rejected),\n              \"rejection_rate_pct\": round(100 * len(df_rejected) / len(df_raw), 2)})\n\nif report[\"passed\"]:\n    lineage.emit(\"COMPLETE\", \"landing_zone\", \"production_warehouse/online_retail_clean\",\n                 {\"row_count\": len(df_valid),\n                  \"dama_dimensions_passed\": sum(1 for v in report[\"dimensions\"].values() if v[\"passed\"]),\n                  \"dama_dimensions_total\":  len(report[\"dimensions\"])})\nelse:\n    failed_dims = [k for k, v in report[\"dimensions\"].items() if not v[\"passed\"]]\n    lineage.emit(\"FAIL\", \"landing_zone\", \"quarantine_zone/dq_failure\",\n                 {\"row_count\": len(df_valid), \"failed_dimensions\": failed_dims})\n\nlineage.write()\n\n[LINEAGE] START      | online_retail_raw.csv → landing_zone\n[LINEAGE] FAIL       | landing_zone → quarantine_zone/contract_violations\n[LINEAGE] FAIL       | landing_zone → quarantine_zone/dq_failure\n\n📋 3 lineage events → lineage_events/run.json",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-8b-real-openlineage-emitter",
    "href": "L04/Day4_Lab.html#step-8b-real-openlineage-emitter",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "LineageEmitter above hand-builds JSON matching the OpenLineage event shape. Here we emit real openlineage-python START/COMPLETE (or FAIL) events to a local file transport — no Marquez server required — as the real-library counterpart.\n\nfrom openlineage.client import OpenLineageClient\nfrom openlineage.client.transport.file import FileConfig, FileTransport\nfrom openlineage.client.event_v2 import RunEvent, RunState, Run, Job\nfrom openlineage.client.uuid import generate_new_uuid\n\ndef emit_real_openlineage_events(job_name, row_count, passed):\n    \"\"\"Emits real OpenLineage START/COMPLETE (or FAIL) events to a local file transport.\"\"\"\n    os.makedirs(\"lineage_events\", exist_ok=True)\n    transport = FileTransport(FileConfig(log_file_path=\"lineage_events/openlineage_run.log\"))\n    client = OpenLineageClient(transport=transport)\n\n    run_id = str(generate_new_uuid())\n    job = Job(namespace=\"day4_lab\", name=job_name)\n    run = Run(runId=run_id)\n    now = datetime.now(UTC).isoformat()\n\n    client.emit(RunEvent(\n        eventType=RunState.START, eventTime=now, run=run, job=job,\n        producer=\"https://github.com/sdaia/modern-data-engineering-lab\",\n    ))\n    client.emit(RunEvent(\n        eventType=RunState.COMPLETE if passed else RunState.FAIL,\n        eventTime=now, run=run, job=job,\n        producer=\"https://github.com/sdaia/modern-data-engineering-lab\",\n    ))\n    print(f\"[OpenLineage] real START/{'COMPLETE' if passed else 'FAIL'} events \"\n          f\"({row_count} rows) -&gt; lineage_events/openlineage_run.log*\")\n\n\nemit_real_openlineage_events(\"retail_quality_pipeline\", len(df_valid), report[\"passed\"])\n\n[OpenLineage] real START/FAIL events (397884 rows) -&gt; lineage_events/openlineage_run.log*",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  },
  {
    "objectID": "L04/Day4_Lab.html#step-9-explore-the-clean-data",
    "href": "L04/Day4_Lab.html#step-9-explore-the-clean-data",
    "title": "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data",
    "section": "",
    "text": "Now let’s actually look at what passed the quality gates and draw some insights.\n\nif report[\"passed\"] and os.path.exists(\"production_warehouse/online_retail_clean.parquet\"):\n    df_clean = pd.read_parquet(\"production_warehouse/online_retail_clean.parquet\")\nelse:\n    df_clean = df_valid.copy()\n\ndf_clean[\"Revenue\"] = df_clean[\"Quantity\"].astype(float) * df_clean[\"UnitPrice\"].astype(float)\n\nprint(\"📊 Top 10 countries by revenue (clean data only):\")\ntop_countries = (\n    df_clean.groupby(\"Country\")[\"Revenue\"]\n    .sum()\n    .sort_values(ascending=False)\n    .head(10)\n)\nprint(top_countries.apply(lambda x: f\"  £{x:&gt;12,.2f}\").to_string())\n\nprint(f\"\\n📦 Unique products  : {df_clean['StockCode'].nunique():,}\")\nprint(f\"👥 Unique customers : {df_clean['CustomerID'].nunique():,}\")\nprint(f\"🧾 Unique invoices  : {df_clean['InvoiceNo'].nunique():,}\")\nprint(f\"💷 Total revenue    : £{df_clean['Revenue'].sum():,.2f}\")\n\nprint(\"\\n✅ Pipeline complete.\")\nprint(f\"   Started with {len(df_raw):,} raw rows.\")\nprint(f\"   {len(df_rejected):,} rejected by contract ({100*len(df_rejected)/len(df_raw):.1f}%).\")\nprint(f\"   {len(df_clean):,} rows in production warehouse ready for analytics.\")\n\n📊 Top 10 countries by revenue (clean data only):\nCountry\nUnited Kingdom      £7,308,391.55\nNetherlands         £  285,446.34\nEIRE                £  265,545.90\nGermany             £  228,867.14\nFrance              £  209,024.05\nAustralia           £  138,521.31\nSpain               £   61,577.11\nSwitzerland         £   56,443.95\nBelgium             £   41,196.34\nSweden              £   38,378.33\n\n📦 Unique products  : 3,665\n👥 Unique customers : 4,338\n🧾 Unique invoices  : 18,532\n💷 Total revenue    : £8,911,407.90\n\n✅ Pipeline complete.\n   Started with 541,909 raw rows.\n   144,025 rejected by contract (26.6%).\n   397,884 rows in production warehouse ready for analytics.",
    "crumbs": [
      "Home",
      "Day 4 — Data Quality, Governance, and Lineage",
      "Lab: Data Quality Gates, Great Expectations, and OpenLineage on Real Retail Data"
    ]
  }
]